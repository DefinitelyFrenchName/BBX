# gates/static.txt — BBX gates that need the lineage trees (BBX_BBH_HOME set), no instrument (bbh's static tier)
# One gate per line, relative to gates/. Complete both ways (BBX-9) — enforced by bin/bbx selftest.
census_recount
fidelity_bbh
