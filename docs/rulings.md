# Rulings — the open queue

Shape: queue. The maintainer rules; the contributor recommends. Each entry
carries the recommendation, the alternatives declined, and a slot for the
answer with its date. An answered ruling moves to `DECISIONS.md` (in force)
and its story to `DECISIONS_HISTORY.md`; the entry here keeps the answer line
so the queue is a complete record. **An entry lives under the heading of its
state:** an answered entry is moved under an `## Answered …` heading in the
same edit that answers it, never left under `## Open` with a note (the
maintainer, bbx-2: "typically the kind of dark pattern that leads to silent
issues" — gotcha G14). `gates/rulings_shape.sh` fails on an `Answer` line
that is not `(open)` under an Open heading, on `(open)` under an Answered
heading, and on an answered `R<n>` with no row in `DECISIONS.md`. Nothing below is a tool; no tool is
written until the ruling its slice needs is answered (CLAUDE.md §6, §9).

## Answered

### R6 — Where the BBX repository root is
- **Recommendation:** `git init` in `BBX/`, the directory holding `CLAUDE.md` (matches bbh and VampireSaved).
- **Declined:** the parent `generalized-blackbox-harness/`; no init this session.
- **Context measured:** `git rev-parse --show-toplevel` from BBX resolved to `/Users/koneko` — the HOME directory is an accidental repository (3 commits of unrelated design documents). Left alone; see `docs/gotchas.md` G1.
- **Answer:** BBX directory. — maintainer, 2026-09-09.

### R7 — Whether session 1 ends in a commit
- **Recommendation:** one commit: memory files, census, plan documents, rulings. No remote, no push.
- **Declined:** leave uncommitted for review first.
- **Answer:** commit at session end. — maintainer, 2026-09-09.

### R8 — Which bbh state is the fidelity baseline
- **Recommendation:** HEAD `f675710`; the 4 uncommitted modifications recorded as a finding, never touched by BBX.
- **Declined:** the working tree as-is (baseline not a commit hash); waiting for the maintainer to commit in bbh first.
- **Answer:** HEAD f675710, dirty files noted. — maintainer, 2026-09-09.

## Answered 2026-09-09 (batch 1–3) — carried from CLAUDE.md §8

### R0 — Name
- **Recommendation:** working name BLACKBOX, rule prefix `BBX`, repository `BBX`.
- **Declined:** keeping it inside bbh as a major version — bbh must stay the frame-driven lineage this project is proved against.
- **Answer:** BBX = **Black Box harness eXpanded**; short name and rule prefix `BBX`; repository directory `BBX`. — maintainer, 2026-09-09.

### R1 — Licence
- **Recommendation:** GPL-3, matching bbh (`~/Developer/blackbox-harness/LICENSE`), so the lineage chain has one licence. No `LICENSE` file is written until ruled; commit one carries none.
- **Declined:** MIT/BSD (a weaker licence downstream of a GPL lineage complicates the fidelity fixtures, which are bbh's files run in place, not copied).
- **Answer:** GPL-3, matching bbh. `LICENSE` added in the next commit. — maintainer, 2026-09-09.

### R2 — The second and third subject kinds
- **Recommendation:** a document set with re-derivable claims (no executable) and a command-line tool with deterministic output (executable, not frame-driven) — `docs/generality.md`.
- **Declined:** an FPGA fit/timing flow (needs a toolchain no fixture can stand in for); a data pipeline (a cli tool over files is its minimal case — add it as a fourth kind if the third proves too close to the second); a model evaluation (non-deterministic by default, would enter under R5 from day one).
- **Answer:** Document set + command-line tool, as recommended, **with two additions ruled as R14 and R15**: (1) the self-validation clause — BBX must be able to validate itself using itself without degrading its rules ("Quis custodiet ipsos custodes": without it, only an external tool could say whether the checker has rotted); (2) BBX is architected so that external testing tools and their frameworks can be interfaced or integrated as drivers — for legacy software, and so that agents working in any codebase can bring their tooling of choice or build ad hoc, with BBX as the guardrail wherever the tests considered are deterministic. — maintainer, 2026-09-09.

### R3 — Language and portability floor
- **Recommendation:** POSIX sh + Python 3, as bbh; macOS bash 3.2 and Linux; every platform guard has a gate on both platforms. The BBX classifier is bbh's `lib/sh/classify.sh` contract, reproduced not imported (fidelity diffs the two).
- **Declined:** Python only (bbh's gate contract is exit-status-first and shell-native; the demand-after-trap incident `[BBH-17]` is a shell fact that must stay testable); a compiled language (no runtime dependency beyond sh + python3 is a lineage ruling, bbh conventions row 1).
- **Answer:** POSIX sh + Python 3 as the floor, and **fully portable, OS-agnostic**: macOS, Linux, and Windows through Ubuntu emulation (WSL) — treated as Linux for agentic development. Variance and fragmentation are held in check by BBX validating BBX (R14) on each platform. — maintainer, 2026-09-09.

### R4 — The relationship to bbh's skill
- **Recommendation:** BBX carries its own skill, generated by the same H10 machinery (`bbh skill-guide` shape), citing `[BBH-N]` rules rather than copying them; `[BBH-` is *not* a forbidden token in BBX's skill, every other lineage prefix is.
- **Declined:** copying the 87 rules under new numbers (two copies drift — the reason VS moved its classes out of CLAUDE.md); extending bbh's skill in place (bbh is not modified).
- **Answer:** Own skill, generated by the H10 machinery, citing `[BBH-N]` by ID. — maintainer, 2026-09-09.

### R5 — What "any project" excludes
- **Recommendation:** nothing by kind; a subject that cannot be driven deterministically is admitted with BBX-14 reported *unmet* in every readout.
- **Declined:** excluding non-deterministic subjects outright (would exclude the model-evaluation kind before it is tried); admitting them silently (a readout that hides an unmet rule is the lie of omission BBX-30 forbids).
- **Answer:** Nothing excluded by kind; a non-deterministic subject is admitted with BBX-14 reported unmet in every readout. — maintainer, 2026-09-09.

## Answered 2026-09-09 (batch 2–3) — raised by session 1

### R9 — The census recount gate
- **Recommendation:** the first gate BBX ever runs (slice S1) is a recount: it re-runs every command in `docs/census/*.md` at the recorded HEADs and fails on any count that moved, and its must-fire is being pointed at a wrong HEAD. Not written this session (no tool before the STOP); the census files carry the commands so the recount is possible by hand today.
- **Declined:** writing the recount script this session (it is a tool); never automating it (the census would rot the way VampireSaved's own three counters did — `docs/census/vampiresaved.md`, the stale-counter rows).
- **Answer:** The recount gate is the first gate of slice S1, and the first instance of BBX validating its own documents (R14). — maintainer, 2026-09-09.

### R10 — Must-fire controls become a machine-readable contract (abstraction G3)
- **Recommendation:** a gate declares its control(s) in one header grammar; a controls registry is complete both ways; a declared control that does not fire refuses the verdict; the readout counts fired / declared.
- **Declined:** keeping the marker as prose (VampireSaved: 74 of 311 gates, 15+ spellings, no reader; the rot taxonomy's only silent class is exactly this); a separate registry file hand-maintained (BBX-9).
- **Cost of being wrong:** every bbh selftest gains a header line; bbh itself is not modified, so bbh's selftests are read under an *absent-declaration is not asserting* rule during fidelity, reported as such.
- **Answer:** Adopt the machine-readable must-fire contract (abstraction G3). — maintainer, 2026-09-09.

### R11 — One closed provenance vocabulary
- **Recommendation:** for expectations, ranked per CLAUDE.md §3.3: `reference` (independent of us: hardware, a specification, an upstream artifact, a reference implementation) > `corroborator` (a lineage-independent second implementation) > `self` (our own build: locks currency, never correctness) > `derived` (computed by a tool from something above; as good as the reader) > `hash-lock` > `registry` (a ledger of decisions) > `fixture` (synthesized by us with known truth; never evidence about a real subject) > `testimony` (a filed count, a witness — never reads green). For defaults: `principled` / `reference-calibrated` / `arbitrary` (BBX-24). Every register row names exactly one class; riders go in a notes column, never in the class.
- **Declined:** keeping bbh's six free-text classes (VampireSaved's register shows 13 distinct strings over 6 base classes — riders became classes); three vocabularies (VS today: expectations, rule-5 values, decisions).
- **Answer:** Adopt the closed vocabulary as stated. — maintainer, 2026-09-09.

### R12 — Corrections to CLAUDE.md, document first (BBX-19)
- **Finding 1:** BBX-5 cites `MFI-52`, which does not exist (MFI is 1..46; `git -C ~/Developer/Vampire_Saved/VampireSaved grep -ohE 'MFI-[0-9]+' | sort -uV`). The paragraph it describes — "separate the AUTHOR from the VERDICT … prove an instrument FIRES on a known positive, FAILS on a known negative, and name its IMPLAUSIBLE value before its first real use" — is **MJC-52**, `.claude/skills/mister-jtframe-core/SKILL.md:85`, anchored at `docs/project/gotchas.md:2913-2922`. A prefix typo.
- **Finding 2 (corrected 2026-09-09 before the edit):** bbh's README line 6 carries "304 gates, 4,000 frozen expectations" (measured today: 311 gates; 4,808 tracked expectation files, 3,814 verdict-bearing). The first draft of this finding said CLAUDE.md repeats them; a grep before editing showed **it does not** (`grep -n '304\|4,000' CLAUDE.md` → nothing). The constitution carries no lineage count; the filed counts stay labelled in `docs/census/`. Gotcha G8.
- **Recommendation:** correct BBX-5's citation to `MJC-52`, one commit, before any check is written against the wording. (The count-labelling half of the recommendation had no target once Finding 2 was corrected.)
- **Declined:** silently editing (BBX-22 requires the sweep and the shown empty result); leaving it (a constitution with a dangling citation teaches every reader that citations are decoration).
- **Answer:** The contributor edits CLAUDE.md once, now: `MFI-52` → `MJC-52` (done, commit "CLAUDE.md: BBX-5 cites MJC-52"); the count half of the edit had no target in CLAUDE.md (Finding 2 corrected). **From now on any edit to CLAUDE.md requires maintainer approval** (ruled as R16). — maintainer, 2026-09-09.

### R13 — SETUP-FAIL as a reported sub-outcome (abstraction G5)
- **Recommendation:** a gate whose fixture failed to stage prints `SETUP-FAIL:` and exits non-zero; the classifier still reads FAIL (byte-for-byte fidelity with bbh is untouched), and the readout reports the sub-outcome separately so a staging failure is never read as a rule failure (SMS `tools/test_clash_air.lua:39-42`, trap 28).
- **Declined:** a fifth verdict word (would change the classifier bbh fidelity diffs); ignoring the distinction (SMS paid for it: "a test that reports 'the rule is broken' when it merely failed to stage is the exact false verdict this project keeps paying for").
- **Answer:** Adopt SETUP-FAIL as a reported sub-outcome of FAIL; the four verdict words stay bbh's. — maintainer, 2026-09-09.

## Answered — created by the maintainer's answers (2026-09-09)

### R14 — The self-validation clause (the maintainer's "Juvenal Escape Clause")
- **Ruling (maintainer, 2026-09-09):** BBX must be able to validate itself using itself without degrading its rules. Without it, only an external tool could say whether the checker has rotted — *quis custodiet ipsos custodes*.
- **How it is met, by construction (contributor's reading, to be proved not asserted):** BBX's own tree is a SUBJECT of its own kinds — its documents through the document-set kind (every count in `docs/census/*.md` and every number in a readout re-derived: the recount gate R9 is the first instance), its gates and runners through the command-line kind, its registries through the registry contract (complete both ways). Self-consistency alone cannot catch a convention error shared with the checker (BBX-15), so the clause is paired with the two custodians BBX already has: the must-fire controls (a checker proven to fail, R10) and the fidelity rows against bbh (a lineage-independent third party, `docs/fidelity.md`). A self-validation gate whose controls are dead or whose fidelity row is red does not count. On every platform of R3.
- **Declined:** none — the maintainer added this to R2.

### R15 — External test frameworks as drivers (the adapter contract)
- **Ruling (maintainer, 2026-09-09):** BBX is architected so that existing testing tools and frameworks can be interfaced or integrated — for legacy software, and so that agents working in any codebase can bring their tooling of choice or build ad hoc, with BBX as the guardrail wherever the tests considered are deterministic.
- **How it is met (contributor's reading):** abstraction D7 — a test framework is a DRIVER under the four-argument contract: an ADAPTER runs the framework's own suite as the scenario, maps its results into the observation grammar (one point per test case, tokens = its verdict and any hashable output), refuses what it cannot honour, and reports non-determinism when the framework's own run differs between two invocations. The framework's verdict is an observation, never a BBX verdict: the comparator and the readout stay BBX's. The command-line kind (R2) is the minimal adapter; a framework adapter is one more driver of that kind, and BBX-25 applies (a generic adapter needs two frameworks).
- **Declined:** none — added by the maintainer to R2.

### R16 — Edits to CLAUDE.md require maintainer approval
- **Ruling (maintainer, 2026-09-09):** the R12 edit is approved once; from now on any edit to `CLAUDE.md` requires maintainer approval before it is made. A contributor who finds a defect in the constitution files it in `docs/rulings.md` with the proposed wording and waits.

### R17 — The session ritual and the session key
- **Ruling (maintainer, 2026-09-09):** the close ritual proposed at the bbx-1 close is adopted as written in `HANDOFF.md` ("The ritual"); sessions are keyed `bbx-N`, one key per sitting, never renamed.
- **Adapted from:** VampireSaved VSP-17 (begin by reading STATE, end by updating it; the rollover rule), VSP-18 (every measurement a rerunnable case before the session ends), VSP-13 (grep for the claim), VSP-162 (the key is a lookup index, never re-based), the CLOSE row that quotes the measured tallies.
- **Declined:** a separate NEXT_SESSION file (HANDOFF carries the orientation; one fewer page to rot); the three-group STATE window (the constitution wants STATE lean, so the history twin holds one paragraph per sitting); the ROM audit and freeze steps (consumer-specific).
- **To become a gate:** steps 8 and 9 (the sweeps, the lineage check) in S1 step 4.

## Answered 2026-09-10 (after the bbx-2 close) — R22, the remote

### R22 — A GitHub remote and pushing (supersedes R7's "no remote, no push")
- **Context:** R7 (session 1) ruled one commit per sitting, no remote, no push. Every commit since has been local; the R21 procedure carried BBX to another host as a bundle for that reason.
- **Answer (maintainer, 2026-09-10):** "You have my blessing to create a BBX repo on my github and push." Applied: `github.com/DefinitelyFrenchName/BBX` created from this tree (private — visibility was not stated, and private is the reversible choice; one command makes it public), `origin` set, `main` pushed after the bbx-2 close. R7's one-commit-per-sitting close stands; "no push" is replaced by: the close commit is pushed, and any post-close correction with it. R21's procedure becomes `git clone` of the remote.
- **Declined:** none asked.

## Answered 2026-09-09 (bbx-2) — R18 raised at the open, R19 by the maintainer, R20 by R18's answer

### R18 — What the census recount measures when a lineage repository moves
- **Context measured (bbx-2 open):** between the bbx-1 close and the bbx-2 open, VampireSaved received two commits (`5df1d8be` → `0cdd9726`: its `STATE.md` +3/−1 at line 42, one re-frozen expectation). `bin/bbx selftest` opened red on `census_recount` (`verdict=HEAD-MOVED rows=127`), so the sitting's first task was the re-measure: one count moved (A77, `wc -l < STATE.md`, 1509 → 1511), thirteen §B citations into `STATE.md` shifted by two (ten hand-read rows re-pointed by hand after diffing old line against new line; three regenerated by G1), three header numbers (commits, untracked). The 563 rule rows, the bins and every other recountable count were unchanged. Gotcha G12.
- **The question:** the census is a document keyed by (repository, HEAD) — BBX-29 — and the lineage repositories are live projects that commit daily. As ruled (R9, R17), a moved HEAD refuses every row and the sitting opens red until the census is re-measured at the tip. That is correct as a rot detector for the census file, but it makes the first task of most sittings clerical, and it conflates two findings: "the census no longer describes any tree" (the file rotted) and "the lineage moved on" (a fact about the lineage, not about BBX).
- **Recommendation:** recount at the recorded HEAD, and report drift at the tip separately. The recount tool obtains the recorded commit read-only — `git clone --shared --no-checkout <lineage> <scratch>` then `git -C <scratch> checkout <recorded HEAD>`, in `TMPDIR`, nothing written under the lineage's `.git` — and runs every §A command there; a mismatch there is the census file rotting and stays FATAL. The same commands are then run at the lineage's tip and the rows that differ are printed as a NOTE-class number (`lineage=vampiresaved recorded=5df1d8be tip=0cdd9726 ahead=2 rows_moved=1 (A77)`) in the selftest's NOTE block, never fatal. A re-measure of the census (moving its recorded HEAD) becomes a deliberate step a slice takes when it needs current lineage facts, recorded as a dated line in the census as done at bbx-2. Hand-read line citations (§B `read` rows) stay a known rot surface and are listed in the NOTE with the files whose line counts moved.
- **Declined:** the status quo (every lineage commit turns BBX's own self-validation red; the readout cannot tell the maintainer whether BBX rotted or VampireSaved worked); dropping the HEAD check (a count at an unknown HEAD is a filed count, CLAUDE.md §1); `git worktree add` inside the lineage repository (it writes under the lineage's `.git`; the lineage is read-only from here); `git archive` of the recorded commit (loses `.git`, and the census grammar's own rule 6 requires `git grep` / `git ls-files`).
- **Cost of being wrong:** a shared clone of VampireSaved (7497 tracked files) is written to `TMPDIR` per run and the VampireSaved rows run twice (recorded HEAD and tip); the gate's runtime, quoted in its header (D2), is re-measured and the header updated. A shared clone borrows the lineage's object store: if the lineage ever prunes objects, the recount says so (exit 2, "could not measure"), never a count.
- **Answer (maintainer, 2026-09-09, bbx-2):** "one thing that census and tests should do is either work on a clone or, if impossible, make very explicit that no change to the tree should be done"; clones also allow multiple instances on clones of the same commit for heavy tasks; and, on a follow-up: "clones are not mandatory but they are safe and we value safe. any similar implementation is fundamentally acceptable". Applied: the recount runs every command on a shared clone of the recorded commit under `TMPDIR` (nothing written under the lineage; the lineage's tip and porcelain are reported; a moved tip is a NOTE-class drift line, never fatal); a census command that names the repository's absolute path is REFUSED (the one way a clone could leak); the fidelity gate, which runs in bbh's tree in place, now declares READ-ONLY in its header and proves it (tracked porcelain before = after, or FAIL). Measured before ruling: a shared clone of VampireSaved takes 1.8 s and 363 MB, and the bbx-1 census recounts on it at `5df1d8be` 100/100 in 19.8 s while the lineage's tip is `0cdd9726` — and during this sitting VampireSaved's porcelain moved 373 → 377 under another session's hands, none of it BBX's. Wider than R18 asked: the maintainer's wording covers the tests too; the two tools that touch a lineage tree are covered (recount: clone; fidelity: declared and proved read-only), and R20 asks whether fidelity should move to a clone as well.

### R19 — Parallel work as a pull queue: N workers over N clones (raised by the maintainer, bbx-2)
- **The maintainer's direction (2026-09-09):** "batching is good, a pull system with a queue and N workers, kanban-style is even better"; clones allow multiple instances of a heavy task on clones of one commit.
- **Measured precedent:** VampireSaved `tests/run_all_emulator.sh` at `0cdd9726` (14z-144, lines 383–420): the barrier (`_running` up to N then `wait` for all) was replaced by a FIFO token semaphore where the token is the slot number; a worker blocks reading a token, runs, writes it back; the slot binds the scratch clone `<base>-slotN`. Measured there on the M17 sweep: barrier 1.65× at `--jobs 4` against a queue's 3.48×; at 8, 2.47× against 6.05×. bbh's `bbh-run-sweep` (and BBX's lift, `bin/bbx-run-sweep:366-368`) still carry the barrier; bash 3.2 has no `wait -n`, which is why.
- **Recommendation:** lift 14z-144's queue into `bin/bbx-run-sweep` as the `--jobs N` mechanism (slot = token = clone), keep the prereq lane serial, keep row order in `results.tsv` non-deterministic and keyed by name; fidelity F14 is unaffected at `--jobs 1` (bbh's default) and a new pair at `--jobs 2` over bbh's example diffs the sorted results, never the order. Generalize the scratch binding from `[sweep].scratch_lanes` to a kind-blind `[sweep].clone_per_slot = true|false` (D-row) whose clone is a shared clone of the consumer's own repository at HEAD, so any kind's heavy gates run N-wide on N pinned trees.
- **Declined:** `xargs -P` / GNU parallel (not in the R3 floor); Python workers (the runner is sh so its verdict contract stays shell-native, R3); a global BBX job server (BBX-25: one consumer).
- **Cost of being wrong:** one FIFO and N clones per run; a gate that is not clone-safe (writes outside its slot) is detected by the tree check, which each clone gets.
- **Answer (maintainer, 2026-09-09):** agreed — as a default implementation and recommendation. The principle must hold (parallel work is pulled from a queue by N workers, each on its own pinned tree); the exact implementation matters little. Applied when lifted: the FIFO token queue is BBX's default `--jobs` mechanism, documented as one implementation of the principle; a consumer or adapter may bring another as long as workers pull and trees are pinned per worker, and the readout says which. **Lifted the same sitting:** `bin/bbx-run-sweep` `--jobs N` is the FIFO token queue (slot = token, the loop in the main shell); `[sweep].clone_per_slot` (D21) gives every slot a plain clone of HEAD; `gates/sweep_runner.sh` sections 15–16 measure both from the gates' own stamps and trees, with `serial-order` and `no-clone-dirties` as the known negatives; F14 against bbh unchanged.

### R20 — Whether the fidelity gate runs on a clone of bbh at f675710 (raised bbx-2)
- **Context:** `gates/fidelity_bbh.sh` runs bbh's own runners and example in bbh's working tree in place; R8 fixed the baseline as the commit `f675710` while the tree carries 4 uncommitted modifications (`docs/config.md`, `example/consumers/bbh.vampire.toml`, `lib/py/bbh/config.py`, `selftest/test_fidelity_vampire.sh`). Every F13–F15 row so far was measured on the dirty tree.
- **Recommendation:** move it to a shared clone at `f675710` (R18's mechanism) and re-measure F13–F15 there; if any row's text differs from the in-place run, the difference is a finding about the 4 files, recorded as a dated line in `docs/rebaselines.md`, and the clone becomes the baseline (it is what R8 says the baseline is). Until ruled, the gate stays in place, declared and proved read-only.
- **Declined:** keeping the dirty tree as the instrument forever (a baseline that is not a commit cannot be re-created on another machine, R3).
- **Cost and risk, measured (bbx-2, 2026-09-09, on the maintainer's question):**

  | | shared clone (`--shared`) | plain local clone (hardlinked objects) | in place (today) |
  |---|---|---|---|
  | clone of bbh at `f675710` | 0.14 s, 1.7 MB | 0.32 s, 2.3 MB | — |
  | clone of VampireSaved (for F20 later) | 1.76 s, 363 MB | 3.40 s, 490 MB as `du` counts it (the 949 MB pack is a hardlink, 2 links: no extra disk) | — |
  | fidelity gate, whole run | 44.4 s (PASS) | not run separately (same tree) | 38.7 s (PASS) |
  | output diff, clone vs in place | empty except `porcelain=4` → `porcelain=0` (two lines) | same | — |
  | what the 4 dirty files change | nothing in F13–F15: they re-point VampireSaved build directories (`m3b_merged25` → `26`, `don_m21` → `22`, `hui55` → `56`, `pyron40` → `41`, `m5_stock16` → `17`), which matter to F20 only | | they are part of the measured input, undeclared |
  | read-only proof | complete: a clone starts with zero untracked AND zero ignored files, so `status --ignored` after the run must be empty — measured empty | same | blind to ignored paths (`example/build/`, `.DS_Store`) by construction; measured not written today, unprovable in general |
  | dependence on bbh's `.git` during the run | an `alternates` file points at bbh's object store: a `git gc --prune` in bbh during the ~45 s window would break the clone loudly (git errors → FAIL), never silently | none: objects are hardlinked at clone time; a repack in bbh leaves the inode | the tree itself; bbh's HEAD moving turns the gate red until re-baselined |
  | when bbh's HEAD moves | the gate keeps measuring `f675710` (the ruled baseline) and reports drift as a NOTE, as the census does | same | red |
  | portability | `git` only (R3 floor); hardlinks need the same filesystem, else git copies (bbh: 2.3 MB, trivial) | same | — |

  Risks that survive either clone: a fixture that depends on an ignored or untracked file in bbh's tree would pass in place and fail on a clone — that is the finding wanted, not a risk to avoid (measured: none today, F13–F15 identical); a consumer config with absolute paths into another lineage (bbh's `bbh.vampire.toml` → VampireSaved) is unaffected because the clone is only of bbh.
- **Recommendation, revised by the measurement:** a PLAIN local clone (not `--shared`) at `f675710` under `TMPDIR` — 0.2 s more than shared, and no live dependence on bbh's object store; the same choice is worth making in the recount (D17) for the same reason at +1.6 s on VampireSaved. The read-only proof on the clone then includes ignored files (`status --porcelain --ignored` empty after the run). The in-place path stays as `BBX_BBH_HOME` pointing at a tree, declared and proved as today, for a verifier who wants the dirty tree on purpose.
- **Answer (maintainer, 2026-09-09):** plain clone — "I'm happy not that my intuition was correct but that we measured and even you arrived at the conclusion that the risk/reward was not worth it". Applied: the fidelity gate runs bbh from a plain local clone of `f675710` under `TMPDIR`; the recount's clone becomes plain too (D17 re-measured); the read-only proof on a clone includes ignored files.

## Answered 2026-09-10 (bbx-3) — the S2 plan's rulings, `docs/plans/S2.md`

### R23 — Where a comparator FAMILY and its VIEW are declared (abstraction C2; raised bbx-3)
- **Context measured (2026-09-10, bbh at `f675710` on a plain clone):** bbh's spec line is `<class> <baseset> <args>` in a `.masked` file, dispatched by `lib/sh/masked_compare.sh` (130 lines, 11 verdict-text lines); the example carries 10 `.masked` specs over 5 classes. F16 diffs every one of them through both dispatchers, so the line cannot change without a translation layer — a second implementation of the vocabulary, the thing the dispatcher's own "WHY ONE COPY" paragraph (bbh `lib/sh/masked_compare.sh`) and abstraction C2 exist to forbid.
- **Recommendation:** the family and the view are declared by the expectation KIND (the file extension, registered once — E1), never inside the spec line. `.masked` = the temporal family over the checksum-log view, its spec line bbh's verbatim. A new family is a new kind (`.set`, `.schema`, `.numeric` in S3–S4), each with its own `<class> <baseset> <args>` line and its own dispatcher function under one `compare.sh`; the kinds table lives in the kind profile (`lib/py/bbx/config.py`) and nowhere else, the frame-driven profile carrying bbh's five kinds so `enumerate` reproduces bbh's `name|kind|disposition` lines byte for byte.
- **Declined:** a family prefix inside the spec line (breaks F16 or needs a translator); one universal grammar for every family (a set inventory and a frame window do not share arguments; a grammar wide enough for both asserts nothing about either); keeping the kinds in a shell `case` as bbh does (a second kind profile would need a second `case`).
- **Cost of being wrong:** a kind that turns out to need two families (a structured log compared both as a schema and as a temporal stream) needs two files, one per kind — reported as a limit of the design, not worked around.
- **Answer:** Agreed: the family and the view are declared by the expectation kind, never inside the spec line; a new family is a new kind in the profile's table. — maintainer, 2026-09-10.

### R25 — What a LOOSENED threshold needs before a comparator accepts it (abstraction E6, BBX-13; raised bbx-3)
- **Context measured (2026-09-10):** bbh's three thresholds (`flicker_max` 2, `reconverge` 60, `flicker_max_total` 8; `[thresholds]`, origin "config (policy)") are read once (`thresholds.py`, 45 lines) and any consumer value is accepted. BBX-13 says a class may be tightened freely but loosened only with a measured mechanism named and a maintainer ruling; the slice's controls list "a loosened class with no ruling row refused". Looser is per key: LARGER for `flicker_max` and `flicker_max_total`, SMALLER for `reconverge`.
- **Recommendation:** a consumer value looser than its kind profile's needs a `[thresholds].rulings` entry naming the key and a ruling id (`flicker_max = "R31"`), else every comparator, the proposer and the suite REFUSE to run with one line naming the key, the profile value, the consumer value and the missing ruling (exit 3, the driver's refusal convention). Tighter values need nothing. The direction of looseness per key is a defaults-register row of class `principled`.
- **Declined:** accepting any value as bbh does (a knob); refusing every override (a consumer that ratified a tighter value would be blocked for no reason); a free-text justification (a justification is prose, a ruling id is a registry key the shape gate can check).
- **Cost of being wrong:** a consumer whose looser value predates BBX writes one ruling row citing its own decision record; the thresholds selftest proves the override reaches every tool (bbh `[BBH-48]`).
- **Answer:** Agreed: a value looser than the kind profile's needs a ruling id in the config or every comparator, the proposer and the suite refuse; tighter needs nothing. — maintainer, 2026-09-10.

### R26 — Where a bare driver NAME resolves, and the fake driver for F12 (raised bbx-3)
- **Context measured (2026-09-10):** bbh resolves `[suite].driver = "fake"` to `$BBH_HOME/drivers/fake.sh`; BBX has no `drivers/` directory and, under BBX-25, gets none until two kinds have drivers (S3, S4). F12 needs bbh's fake driver and its fake machine (`example/fakesys/`), both bbh's, unmodified.
- **Recommendation:** a kind profile key, `[suite].driver_home`: kind-blind default `$BBX_HOME/drivers` (absent until S3 — a bare name then FAILS naming the directory), frame-driven `$BBX_BBH_HOME/drivers` so a bbh consumer's config resolves in BBX to the driver it resolves to in bbh; a path from the consumer root always wins, `--driver` overrides both. bbh stays the frame-driven consumer's driver home (CLAUDE.md §2); BBX never copies a driver out of it.
- **Declined:** copying `drivers/fake.sh` and `fakesys/` into BBX (a second copy of the fixture subject, and its `--check` generator would have to travel too); requiring `--driver <path>` always (the example's `bbh.toml` would not resolve, and F12 must read it unchanged).
- **Cost of being wrong:** `BBX_BBH_HOME` unset makes the frame-driven kind's suite FAIL at the driver line, named; the fidelity gate already SKIPs on the same condition.
- **Answer:** Agreed: a per-kind driver home; the frame-driven kind's is bbh's own `drivers/` through `BBX_BBH_HOME`; nothing is copied out of bbh. — maintainer, 2026-09-10.

### R24 — The expectation register's format under the closed vocabulary (abstraction E3, R11; raised bbx-3)
- **Context measured (2026-09-10):** bbh's `provenance.py` (140 lines) reads a markdown table, matches the `rests on` cell by SUBSTRING against a consumer-configured list (`[provenance].evidence_classes`, 4 strings in the example, 7 in the lineage); the example page has 3 rows for 3 files in scope. R11 closed the vocabulary to eight classes and moved riders to a notes column; a substring match over a consumer list is the mechanism that let VampireSaved's 6 classes become 13 strings.
- **Recommendation:** the register is a TSV beside the expectation tree, `<expected_dir>/PROVENANCE.tsv`: `file <TAB> describes <TAB> class <TAB> refreeze <TAB> notes`; `class` must EQUAL one of R11's eight, complete both ways over the files in scope (E3), an expectation of class `testimony` never reads green (BBX-3), and a `fixture`-class expectation is counted on the screen as "not evidence about a real subject". The screen's "expectations relied upon" line is the histogram of the classes the run's expectation set carries. bbh's markdown page and its consumer list stay bbh's: reading bbh's example page under R11 is F19's question (slice S6), and the frame-driven profile maps bbh's four example strings to R11 classes there (`registry`→`registry`, `self-frozen`→`self`, `measured (two runs compared)`→`self`, `authored`→`derived`), not here.
- **Declined:** keeping the markdown table with an exact-match column (a page is prose; a TSV is a registry — BBX-9); letting a consumer extend the vocabulary (R11 closed it); a register per kind.
- **Cost of being wrong:** a consumer with a markdown-only workflow writes the TSV by hand; the completeness check is what makes that safe.
- **Objection (maintainer, 2026-09-10):** agrees with the principle; wary of TSV even more than CSV — wants a format that cannot dramatically break from mishandling a spacer-type character.
- **Revised recommendation (measured 2026-09-10 against `lib/py/bbx/toml_subset.py`, the reader BBX already trusts for every config):** `PROVENANCE.toml` beside the expectation tree, one bare-key table per row with the FILE AS A VALUE and every field NAMED — `[e1]` / `file = "build-a/01_idle.masked"` / `describes` / `class` / `refreeze` / `notes`. Measured: the subset parses it; a tab or a pipe inside a string changes nothing; a table declared twice is REFUSED by the parser (`table [e1] declared twice`); the file name as a key (`["build-a/01_idle.masked"]`, or a quoted key in a `[rows]` table) is refused as malformed, which is why the file is a value. The tool then refuses a missing field by name, a `file` value named twice (a duplicate is hand-editing, BBX-17), and a `class` not equal to one of R11's eight. No positional column exists to shift (BBX-12: by field name, never by position). Everything else in the original recommendation stands (complete both ways; `testimony` never green; `fixture` counted as not evidence about a real subject; bbh's markdown page stays bbh's until F19).
- **Declined at the revision:** the TSV (an invisible separator; the lineage's `retractions.tsv`, `registry.tsv` and `sweep.tsv` keep theirs, each read by a strict column count, but a hand-written prose register is where a tab in a sentence is most likely); a markdown table (a visible separator, but prose — and bbh's substring-matched page is the mechanism R11 retired); JSON (no comments, and a second parser in a tree that has one).
- **Answer:** Agreed, as revised: a TOML register (`PROVENANCE.toml`, one bare table per row, the file a value, every field named, the class equal to one of R11's eight). — maintainer, 2026-09-10 (recorded after the bbx-3 close: the answer was in the maintainer's message before it and was missed by the contributor).

### R27 — The close ritual reviews the sitting's incidents for learnings (raised and ruled at the bbx-3 close)
- **Context measured (2026-09-10):** bbx-3 filed two incidents (G17, G18) both caught by the harness; asked whether they carried a learning, the contributor measured rather than assumed — one yielded a mechanism (the kept run recording untracked entries), the other an authoring trap. The ritual had no step that asks the question; it was asked by the maintainer.
- **Recommendation (the maintainer's, recorded by the contributor):** a close step, before the close commit: every incident of the sitting — caught by a gate or discovered after the fact — is re-read for a learning that would improve the harness; a learning that is a mechanism becomes the next sitting's first small fix (or a ruling if it changes a contract), a learning that is a trap becomes a HANDOFF hazard, and "no learning" is written in the gotcha entry so the question is seen to have been asked.
- **Declined:** leaving it to the gotcha entry's author (G17's learning was found only because the maintainer asked); a separate learnings ledger (the gotchas already carry the price and the rule re-anchored; the learning belongs beside them).
- **Answer:** Adopt: the close ritual's step 10, the commit becoming step 11. — maintainer, 2026-09-10.

### R28 — Re-baseline bbh's fidelity commit to `10a82d2` (raised bbx-4, 2026-09-10)
- **Context measured (2026-09-10, bbx-4 open):** bbh's tip moved one commit past the baseline (`10a82d2`, "config: the M18 re-point that sat dirty since 14z-144; the example lib's root one level too high; the extraction counts dated"): 7 files, 13 insertions, 9 deletions; porcelain now 0 (the four dirty files of R8 are committed). Of the lifted or fixture files it touches `lib/py/bbh/config.py` (the `[sweep].placeholders` and `[inp].build` literals re-pointed M18: `m3b_merged25`→`26`, `don_m21`→`22`, `hui55`→`56`, `pyron40`→`41`, `m5_stock16`→`17`) and `example/tests/lib/needs_fake.sh` (G11 fixed in bbh: the lib's root two levels up, not three). Both fidelity gates run against the tip by the D20 override (`BBX_BBH_BASELINE=10a82d2`) and PASS: F13, F14, F14f, F12, F16, F17 identical — the example config overrides the re-pointed literals, so they are not exercised by any pair.
- **Recommendation:** re-baseline to `10a82d2` at the next close, as one commit (bbh `[BBH-83]`: loud, dated, both sides): D20's value; D12's row (the frame-driven profile is "bbh's DEFAULTS verbatim" — the five `[sweep].placeholders` follow the re-point, and the profile carries no `[inp]` table, so nothing else moves); a dated line in `docs/rebaselines.md`; the census `docs/census/bbh.md` re-measured on the new tip (`bin/bbx recount`, row A2 moved); HANDOFF's hazard about bbh's four dirty files retired; G11's "bbh's to fix" closed as fixed in bbh at `10a82d2`.
- **Declined:** staying at `f675710` indefinitely (the baseline is a commit, not a contract: a baseline behind a clean tip that fixes a finding this project filed is a stale reference, BBX-10 class 4); re-baselining silently inside a step (the loud rule).
- **Cost of being wrong:** one commit to revert; every fidelity row is re-run on the clone in the same sitting.
- **Answer:** Validated — and reclassified: aligning BBX's baseline with a moved bbh is not a ruling on BBX but a PROCEDURE under R8 and R20 (a dated line in `docs/rebaselines.md`, D20, D12 by its own definition, the census re-measured); it needed no queue entry and future re-baselines get none. — maintainer, 2026-09-10 (bbx-5).

### R30 — The header is the leading comment block, not "line 2 to the first bare `#`" (raised by the maintainer from bbh's R10 work, bbx-5, 2026-09-10)
- **Context (the maintainer's, measured in bbh):** 264 of bbh's 315 gates have a bare `#` separator within their first five lines, so under "line 2 to the first bare `#`" a MUST-FIRE line would have to be jammed under the title on every gate, colliding with bbh's own header API (the gate index takes a script's sentence from the top of the header; 78 gates would carry a control line where their purpose sits). bbh's two header readers already define the header as the leading comment block — every `#` line after the shebang up to the first non-comment line. Measured here (2026-09-10): BBX has TWO readers of that block (`controls.py` `header_lines`, `readout.py` `header_block`) where bbh `[BBH-60]` says one; no BBX gate has a bare `#` in its first five lines; under the new definition the 19 gates declare the same 55 controls (both readers run over every gate); a declaration after the first code line is not read (the new control `body-is-not-header`).
- **Recommendation:** adopt: `header_lines()` reads the leading comment block, `readout.py` imports it (one reader), the contract page's sentence changes, `gates/controls.sh` proves a declaration after a bare `#` is read and one after the first code line is not; a courtesy convention, not a rule: group the MUST-FIRE and NOT-ASSERTED lines at the end of the header.
- **Declined:** keeping bbh's [BBH-18] extent (the collision above, measured in bbh); a placement rule (the maintainer: a declaration can sit anywhere in the header, and this tree needs no convention).
- **Answer:** Change the definition. — maintainer, 2026-09-10 (bbx-5; built the same sitting).

### R29 — Executable controls: the runner drives each declared control on the gate's real path and requires the gate's own FAIL (raised by the maintainer from bbh's R10 work, bbx-5, 2026-09-10)
- **Context (the maintainer's, from bbh; measured there):** R10's `CONTROL FIRED` is the gate's self-report — the control ran its perturbation on a copy and its own check failed. A control can satisfy that while testing nothing: the 14z-144 case wrote a value, asserted the value was not something else, and would have printed FIRED. The executable form adds one observation: the runner invokes the gate with `CONTROL=<name>`, the gate applies that control's perturbation to its REAL input and runs to its verdict, and the runner requires the gate's own FAIL. A gate that stays green under its own perturbation has a control that lies, whatever it printed. Strictly stronger and compatible: the R10 lines stay; the runner does one more thing. Measured here: 19 gates, 55 declared controls; the two static S2 gates run ~60–70 s each, so one run per control there is ~5–6 min more per battery.
- **Recommendation:** adopt the contract now on the page (`docs/controls.md`: a declared control is also a MODE — `CONTROL=<name>` makes the gate perturb its real input and run to its verdict; the gate's exit must be non-zero and its log carry its own FAIL; a gate that is green under `CONTROL=<name>` is reported `CONTROL LIES: <name>` and the run is NOT GREEN; a gate that does not honour the mode prints `REFUSED: CONTROL=<name> is not a mode of this gate`, exit 3, read as a dead control), and BUILD it in slice S6 (the controls registry and the rot classes), where the runner already gains the registry. Cadence: every battery for a gate whose quoted runtime is under the portable tier's budget, with the gate free to short-circuit to the affected section under the mode; the static tier's controls under `--strict`/release only, until measured. Each gate's modes are added as the gate is next touched, its header saying which controls are executable; the report gains a column (`executable=<n>`).
- **Declined:** the executable form replacing the printed one (the printed line is what a reader finds in a log; the mode is what the runner proves); every control executable at every battery from day one (the static tier's cost, unmeasured); deciding the cadence in the contract (the maintainer's point: it does not have to).
- **Cost of being wrong:** a gate whose perturbation cannot be applied on its real path (a fixture-generating gate) declares its controls `perturbed-copy` as today and marks them not executable; the report says how many are.
- **Answer:** Agreed with the recommendation: the contract on the page now, built in slice S6 with the controls registry, the cadence per tier. — maintainer, 2026-09-10 (after the bbx-5 close).

## Answered 2026-09-10 (after the bbx-6 close) — the S3 plan's rulings, `docs/plans/S3.md`

### R31 — The OBSERVATION POINT of a non-frame kind is an integer index into the scenario's own inventory (abstraction O1, O3; raised bbx-6, S3 plan §3)
- **Context measured (2026-09-10, BBX at `415c88b`):** `lib/py/bbx/logfmt.py` is lifted VERBATIM from bbh (fidelity F17 diffs every comparator reading it); its `frames()` reads `(int(f[0]), f[1])` — the point must be an integer, the token is the second field, a third field is ignored. Abstraction O1 says the point "generalizes the frame: a frame index, a claim id, an output line or field name", and O3 says one reader, by field name. A claim id is `(document, line, form)` — three fields, not an integer.
- **Recommendation:** the point stays an INTEGER and logfmt stays bbh's: a document-set observation is `<index> <token>` per claim then `END <n>`, where the index is the claim's position in the deterministic extraction order (document order in the claim set, then line, then form) and the claim set is the map from index to `(document, line, form)`; the token is one space-free field `<status>:<sha1-of-quoted>:<sha1-of-derived>` split by `docset.py` in one place. The same rule serves S4 (an output line number, a field's position in a declared field list).
- **Declined:** extending `logfmt` to string points (a change to a lifted file, F17 red or a translation layer — R23's argument); a second reader for non-frame logs (O3: one reader; two grammars drift); a comment line in the log carrying the map (bbh's grammar has none, and a consumer's log reader would have to skip it).
- **Cost of being wrong:** a kind whose points are unordered by nature (S4's unordered listing) uses the `set` family over rows, not the point-indexed log — reported as the family's job, not the grammar's.
- **Answer:** Recommendation validated. — maintainer, 2026-09-10 (after the bbx-6 close).


### R32 — The SCENARIO contract per kind: the scenario extension is a kind-profile key, and a scenario may carry more than one expectation kind (abstraction D2, S1; raised bbx-6, S3 plan §3)
- **Context measured (2026-09-10, BBX at `415c88b`):** `bin/bbx-run-suite` line 170 loops `for rpl in "$BBX_ROOT/$REPLAYS_DIR"/*.rpl` — bbh's literal — and lines 188–190 decide ONE kind per scenario by file existence (`.masked` > `.diverge` > `.sha1`). A claim set is not a replay, and a document-set scenario freezes a truth, an inventory and a covered set — three kinds of one scenario — plus a schema per artifact.
- **Recommendation:** (a) `[suite].scenario_ext`, a kind-profile key: kind-blind `rpl` (bbh's literal, ARBITRARY class, kept so a bbh config resolves unchanged), `document-set` `claims`; the loop reads it. (b) Under a profile whose kinds table carries more than one EVAL kind, every expectation file of a scenario is its own PAIRING with its own verdict line and its own kept-run row keyed `(scenario, kind)` (D30's `results.tsv` already has the column); under the `frame-driven` profile the loop keeps bbh's precedence verbatim — one kind per scenario — so F12 diffs empty, and `gates/fidelity_bbh_s2.sh` green after the change is the control that nothing moved for bbh. (c) The kind's driver-variable family (D2) is the profile's `hermetic_unset` list, scrubbed before any driver runs.
- **Declined:** a hard-coded second extension beside `.rpl` (the third kind would need a third); a kind-neutral extension such as `.scn` for every kind (hides the kind, and bbh's example must read unchanged); one file per scenario bundling every kind (a new grammar, and R23 said one file per kind with its own spec line); a separate suite runner per kind (CLAUDE.md §2: contracts, never structure).
- **Cost of being wrong:** a consumer whose scenarios carry two kinds under the frame-driven profile keeps bbh's precedence and is told so by the enumeration (bbh's own behaviour today).
- **Answer:** Recommendation validated. — maintainer, 2026-09-10 (after the bbx-6 close).


### R33 — The coverage contract of a document set: the denominator, the binding-status vocabulary, the shrink-only covered set, and PARAPHRASE (BBX-7, BBX-17, BBX-18, abstraction E5; raised bbx-6, S3 plan §3)
- **Context measured (2026-09-10, on the plain clones):** SMS `checkdocs.py` at `ecc5481` prints coverage as a sentence ("428 distinct ROM addresses …, N re-derived", `docaddrs.report`, line 490), never gates it, lists what could not be pinned and why, and keeps a quoted instruction outside the encoder's subset as `UNENCODABLE` — printed, never dropped; "describing an absence is fine and stays unchecked" (`docs/game/README.md:82-113`). VampireSaved `checkdocs_rom.py` at `0cdd9726` declares a summarising quote `PARAPHRASE` per check (14 mentions), checks it against the literal fact, prints the tag (never skips); freezes the covered set as a TSV (31 rows) compared as a MULTISET — a lost row is `MISMATCH … no longer covers`, growth is printed and re-freezes, a duplicate fails (line 671); prints `NOTE: checkdocs_rom.coverage <n>/346` where 346 is a consumer RULING (V-D7) and the tool prints a NOTE when the census returns another number. BBX's screen reads `NOTE: coverage …` already (readout.py:42).
- **Recommendation:** (1) the binding-status vocabulary is CLOSED: `BOUND` (quoted == derived), `PARAPHRASE` (declared in the claim set; bound to the literal fact it summarises; PASSes on the literal; never counted as `BOUND`), `UNBINDABLE` (declared in the claim set with a reason from `absence | no-source | no-form`; counted, never bound; an undeclared one is the driver's REFUSAL — an absence nobody listed is not silently dropped), `STALE` (the quoted sentence is no longer in the document), `MISMATCH` (quoted != derived). (2) The DENOMINATOR is the log's `END <n>`: every claim the declared forms bound plus every declared unbindable — never a ruled constant (a consumer that wants a constant writes it into its claim set as unbindable rows with a reason). (3) Coverage is `NOTE: coverage <BOUND+PARAPHRASE>/<n>` at column 0, NEVER fatal; the `covered` kind freezes the covered multiset SHRINK-ONLY: a frozen row not reached FAILS naming it, a duplicate row FAILS as hand-editing (BBX-17), growth PASSes with `NOTE: coverage grew <n>` and `--freeze` rewrites it. (4) `PARAPHRASE` and `UNBINDABLE` counts are notes on the screen.
- **Declined:** coverage fatal (a number that stops the run is a number someone will stop showing — SMS's reason it is a note); growth fatal (VampireSaved measured growth as the expected direction); a ruled denominator inside the harness (V-D7 is one consumer's); a looser regex to bind a near-miss (C6: not expressible means root-cause); PARAPHRASE green as BOUND (VampireSaved: "a word compare would report correct documentation as stale, which is how a checker trains its reader to ignore it" — the tag is the point).
- **Cost of being wrong:** a consumer whose forms bind nothing reads `coverage 0/n` on every screen — loud, and the honest floor.
- **Answer:** Recommendation validated; to be reworked if and only if experience raises cases that prove the contract insufficient. — maintainer, 2026-09-10 (after the bbx-6 close).


### R34 — The SCHEMA family's spec and the SET family's modes; verdict text frozen by the family's own gate (abstraction C1, C2, C4; raised bbx-6, S3 plan §3)
- **Context measured (2026-09-10):** R23 fixed WHERE a family plugs in (a new kind in the profile's table, its own spec line, its own function under one `compare.sh`) and left the families' grammars to S3–S4. BBX's `lib/sh/compare.sh` at `415c88b` has one family branch (`temporal`); `lib/py/bbx/toml_subset.py` is the one config reader and accepts no floats. The schema family's first consumer is a TSV artifact of 4 columns; its second is S4's JSON output.
- **Recommendation:** `schema <baseset> tsv` followed by `column <name> <type>` lines, in order, with `<type>` from the closed set `int | hex | str | nonempty`, and one `rows <op> <n>` line (`>=` or `=`); the shape is checked BEFORE any value kind of the same scenario is evaluated, and on a schema FAIL the value kinds' kept-run rows read `NOT-EVALUATED (schema failed)` (finding `pending`, E2) — never silently passed. `multiset <baseset> inventory` compares the frozen rows against the run's both ways, naming the first row and its direction; `multiset <baseset> shrink-only` is R33's covered set. `exact <baseset>` compares the truth log by INDEX and names the first differing index (FAIL-SHORT when the run log ends first, C3). Every verdict line of the three families is BBX's own text, frozen from its first green by the family's gate with a shadow-tool control (one string changed → the gate FAILS): with no ancestor, the gate is the freeze (C4). A fifth schema type, a JSON schema line and a tolerant-numeric field are S4's rulings.
- **Declined:** a schema language (JSON Schema, a TOML schema table: a third parser in a tree with one, and R24's argument); inferring the schema from the artifact at freeze time and comparing later (§3.4: an expectation derived from the subject's own metadata); evaluating values when the schema failed and reporting both (plausible garbage, BBX-16: reading in the wrong view yields plausible values, not an error).
- **Cost of being wrong:** a consumer whose artifact has a type the four cannot express declares the column `nonempty` and reads that limit on the screen (the schema comparator prints the types it checked), and the fifth type is a ruling when the second such consumer exists (BBX-25).
- **Revision on the caveat (contributor, 2026-09-10, recorded with the answer; the S3 plan's §3 follows it):** the caveat lands on ONE place in the plan — the `claims` and `covered` expectation files were designed to carry their frozen rows tab-separated after the spec line, a hand-written register of exactly the shape R24 moved to TOML. Revised: those two kinds (and the `schema` kind's column lines) are TOML-subset files read by the parser BBX already trusts — a `[spec]` table with `class`, `baseset` and `mode` (or `format`) named, then one bare table per row with every field named (`document`, `line`, `form`, `status`; `name`, `type`), so no positional column exists to shift (BBX-12) and a tab inside a value changes nothing; the parser refuses a duplicate table. The fixture's ARTIFACT stays a TSV on purpose: it is the SUBJECT, a consumer's format is whatever it is, and R34's schema-before-values is precisely the guard against a TSV that broke silently — a row whose column count is wrong (a tab inside a value) is a schema FAIL naming the row, which becomes a fifteenth control. The kept run's `results.tsv` (D30) stays: machine-written, read by column count, the lineage's shape; noted as the remaining TSV.
- **Answer:** Recommendation validated, with the caveat that TSV is still considered dangerous: it can break silently very easily. — maintainer, 2026-09-10 (after the bbx-6 close).
- **Answer on the revision:** the TOML shape validated — "R34 with TOML is better in my experience." — maintainer, 2026-09-10 (bbx-7 open).


## Answered 2026-09-10 (after the bbx-12 close) — the S4 plan's rulings, `docs/plans/S4.md`

### R35 — The command-line scenario and observation contract (raised bbx-12, `docs/plans/S4.md` §3)
- **Context measured (2026-09-10):** bbh's fixture selects its features by 5 environment knobs and 1 argv option (`git show 10a82d2:example/fakesys/fakesys.py`); a command-line tool's contract is its command line, so the cli fixture's features are OPTIONS. SMS `tools/cliguard.py` (108 lines, 12 self-test cases at `ecc5481`) is the incident behind refusing an option a tool does not define. `docs/generality.md`'s kind-B row says `--crash-at n` → "exit 1, discarded"; abstraction D4 and bins B-X19 say exit 2 with the observation as the bug report.
- **Recommendation:** the scenario is `<name>.cli`, a TOML-subset file — `[scenario]` `args` (one string per argv element), `stdin` / `stdin_file`, `cwd` (relative to the sandbox), `emits` (files the tool must produce), `bands` (named fields judged by the band kind), `fields = "json"` (the structured reader); `[env]` the variables the tool may see — and an unknown key is REFUSED (exit 3, cliguard's rule on the scenario grammar). The observation: `0 exit:<n>` first, then `<i> line:<sha1>` per stdout line or `<i> field:<name>:<sha1>` per sorted JSON key (a band field's token the constant `field:<name>:band`), `<i> err:<sha1>` per stderr line, `<i> file:<name>:<sha1>` per declared emitted file, `END <n>`; `<out>.bands` the band view (`<i> <name>=<value>`, O4). A non-zero exit of the TOOL is an observation, never a driver failure; death by a signal is the guard — exit 2, `END-CRASH <n>`, the partial output kept (D4, B-X19) — and `docs/generality.md`'s row is corrected in its own commit with a retraction row when this is answered (BBX-19).
- **Declined:** a shell line for `args` (a quoting layer between the scenario and the tool: the observation would depend on the shell); the exit status inside `END` (it would not be a point, and a tool's refusal could not be frozen as a scenario); a crash as exit 1 (the partial observation is the bug report, and D4 already says so); the band value inside the main log's token (the exact family would fail on every legitimate move — the band view is another file in the same grammar, BBX-16).
- **Answer:** Recommendation validated. — maintainer, 2026-09-10 (after the bbx-12 close, answered one by one in the session). `docs/generality.md`'s crash row corrected in its own commit before this record (X13, BBX-19).

### R36 — The tolerant-numeric contract (raised bbx-12, `docs/plans/S4.md` §2, §3 E1)
- **Context measured (2026-09-10):** VampireSaved at `0cdd9726` keeps the MiSTer sim anchor's band at `+/- 30` through two re-centres (`tests/test_mister_sim_anchor.sh` 58, 74, 89: "root-cause rather than widen", "retracted, not widened"; `docs/platform/mister_history.md:99` "re-frozen at 2609/463, band unchanged"); 0 of its tools take a tolerance as an option. BBX's R25 mechanism (`lib/py/bbx/thresholds.py` 64–73) refuses a looser consumer threshold with no ruling id.
- **Recommendation:** an expectation KIND `band`, family `tolerant-numeric`: `[spec] class = "band", baseset, measured = "<how the band was measured>", rulings = { <field> = "R<n>" }`; `[b<i>] field, min, max` inclusive. The comparator reads the band view, PASSes a value inside, FAILs naming the field, the value and the band, prints `NOTE: band-fields <n>` (BBX-13's inventory watch on the screen). `--freeze` REFUSES to write a band WIDER than the frozen one for a field with no `rulings` entry (exit 3, the file unchanged); narrower is written freely. The frozen band carries the provenance class of what it was measured on (`fixture` for the fixture; `reference` or `self` for a real tool, R11).
- **Declined:** a tolerance option on the comparator or the suite (bins B-P6: "none is a tolerance"; a knob is what VampireSaved never allowed); a band measured at run time from the two runs of BBX-14 (the two runs must be identical: any difference is a failure before any class); a percentage band (a band is what was measured, in the field's own unit).
- **Answer:** Recommendation validated. — maintainer, 2026-09-10 (after the bbx-12 close, answered one by one in the session).

### R37 — The two adapter frameworks (raised bbx-12, `docs/plans/S4.md` §2 "The host", §3 D7)
- **Context measured (2026-09-10, this host):** `pytest`, `bats`, `shellcheck` absent (`command -v`); `node` present through fnm but off R3's floor; Python 3.9.6 with `unittest` in the stdlib; `python3 -m unittest -v` prints one `<case> (<module.Class>) ... <verdict>` line per case and `Ran <n> tests`. SMS and VampireSaved use neither unittest nor pytest (0 files each at their census commits).
- **Recommendation:** Python's `unittest` (`drivers/unittest.sh`) and BBX's own gate battery through `bin/bbx run-static --log` (`drivers/gates.sh`, R14) — both on R3's floor, both zero-dependency, both over the one `bbx.cli` core (three consumers of it with the cli driver). The framework's verdict is an OBSERVATION token (`case:<verdict>:<sha1-of-name>`, `gate:<verdict>:<sha1-of-name>`), never a BBX verdict; `Ran 0 tests` is DISCARDED (exit 1, BBX-7).
- **Declined:** pytest (absent; a consumer that has it adds the third adapter under the same core); bats (absent); node's `--test` runner (off the floor: R3 is sh + python3); a single adapter parameterised by framework (one driver with two modes is one consumer wearing two hats — BBX-25 wants two drivers over one core).
- **Answer:** Recommendation validated. — maintainer, 2026-09-10 (after the bbx-12 close, answered one by one in the session).

### R38 — The self subject's identity (raised bbx-12, `docs/plans/S4.md` §3 S2, R14)
- **Context measured (2026-09-10):** `lib/py/bbx/fingerprint.py` already carries a `command` kind (`program_command` / `wholeset_command`, lines 197–201; `config.py` 93–94) with no consumer. Every BBX commit moves `HEAD`, but the harness's behaviour moves only when `bin`, `lib` or `drivers` move.
- **Recommendation:** the self subject (`fixture/selfgates/`, BBX's runners driven through the gates adapter) is identified by the `command` kind: program key = the tree hash of `bin`, `lib` and `drivers` at HEAD (`git rev-parse HEAD:bin HEAD:lib HEAD:drivers`, hashed), whole-set key the same plus `gates`. A moved key is "unregistered build fingerprint" (exit 1) until the row is re-frozen — a REVIEWED step of the close (the registry row is a build decision, `[BBH-67]`), never a step of the runner. The cost stated: a sitting that touches the kernel re-freezes the self suite's row at its close, after reading the verdicts.
- **Declined:** `HEAD` itself (every commit would unregister a harness that did not change); no identity for the self subject (a suite that compares against a set nobody registered is the false green BBX-3 names); an automatic refreeze when the key moves (a registry row written by the runner is an expectation derived from the subject's own metadata, §3.4).
- **Answer:** Recommendation validated. — maintainer, 2026-09-10 (after the bbx-12 close, answered one by one in the session).

### R39 — The file census as a gate (raised bbx-12, `docs/plans/S4.md` §3, §5, §6; `docs/census/bbx_files.md` §C)
- **Context measured (2026-09-10):** the bbx-11 census was measured once by a scratchpad instrument run by hand (§C: `filecensus.sh`, `fc_analyze.py`), 23 gates run once each in a shadow tree, about 9 minutes; its §A rows A5–A17 are derived from §B, not recountable by a command, so `gates/census_recount.sh` cannot read it. The instrument's first version ended every header (G25). `gates/sweep.tsv` is empty; the sweep runner's release scope and cadence exist (D15) with no row.
- **Recommendation:** the instrument lifted into `lib/py/bbx/file_census.py` and `bin/bbx file-census [--check]`, which REGENERATES `docs/census/bbx_files.md` §A/§B from a run and, under `--check`, diffs the tree's document (BBX-21: the census is generated, never hand-edited); a frozen kind-set per file (`expected/file_census.toml`, class `self`: currency, not correctness) compared SHRINK-ONLY — a file reached by no gate FAILs always, a file that lost a kind FAILs naming it, a gained kind is a NOTE and re-freezes; the gate `gates/file_census.sh` static, the first row of `gates/sweep.tsv` at release cadence (it runs the whole battery in a shadow); plus `gates/file_census_tool.sh`, portable, the instrument proven on a synthetic tree every battery, with G25's shebang insertion as a must-fire mode.
- **Declined:** the census as a hand-measured document forever (the honest floor was for one sitting; a number that is not gated rots — BBX-10); a portable gate that re-runs the battery every battery (about 9 minutes doubled on every close); an instrument through `sh -x` or `BASH_ENV` (neither reaches a sourced library under `/bin/sh` on this host; the shadow's inserted line is the measured mechanism); the frozen kind-sets as `derived` (they are BBX's measurement of BBX: `self` is the true class, and the design's shared-file list in the plan is what the readout compares them against in prose).
- **Answer:** Recommendation validated. — maintainer, 2026-09-10 (after the bbx-12 close, answered one by one in the session).

### R40 — The JSON schema format and its type vocabulary (raised bbx-12, `docs/plans/S4.md` §3 E1; R34's second consumer)
- **Context measured (2026-09-10):** `lib/py/bbx/compare_schema.py` (123 lines) accepts `format = "tsv"` only (line 58) with D41's four types; the fixture's `show --emit-json` object carries five keys of four JSON kinds (`str`, `int`, `int`, `int`, `list`) by design.
- **Recommendation:** `[spec] class = "schema", format = "json"`; `[k<i>] name, type` with keys in SORTED order and the type from `int | number | str | bool | null | list | object`; `items <op> <n>` (`>=` or `=`) for a list-valued key — the smallest vocabulary that names every JSON value kind once (R34's argument for the TSV four); a type outside it is `FAIL unknown schema type`; a nested object is one `object` key in S4 (a key path `a.b` is a consumer's question, stated NOT-ASSERTED). The TSV format and D41 stay as they are.
- **Declined:** reusing D41's four types for JSON (`hex` has no JSON meaning and `bool`/`null`/`list`/`object` have no TSV meaning: two formats, two vocabularies, one comparator); a JSON Schema document as the spec (a second language for one check; the TOML-subset reader BBX already trusts, R24); a recursive schema (no consumer asks for it).
- **Answer:** Recommendation validated, with a caveat in the maintainer's words: "no caveat on R40 but a caveat on the use of TSV elsewhere" — maintainer, 2026-09-10 (after the bbx-12 close, answered one by one in the session). The caveat applied: every file S4 introduces is a TOML-subset file (the `.cli` scenario, the `unordered`, `schema`, `band` expectations, `expected/file_census.toml`); the kernel's pre-existing TSVs (`results.tsv` D30, `notes.tsv` D44, `registry.tsv`, `gates/sweep.tsv`) are not multiplied, and the gates adapter reads `results.tsv` by column name only, never the printed rows.

## Answered 2026-09-11 (bbx-17) — R41 ruled at the open; R42 raised by the maintainer's crash reports and ruled the same sitting

### R41 — One writer of the `band-fields` NOTE key: the log summary (D43) or the comparator (R36) (raised bbx-16, S4 step 4)
- **Context measured (2026-09-10):** the first suite run over `fixture/fakecli/` printed `NOTE: band-fields 1` TWICE under `04_band` — once from `compare_band.py` after its PASS (R36's text: "the comparator … prints `NOTE: band-fields <n>`") and once from `bbx.cli summary` through `[suite].log_summary` (D43's command-line value, joined at S4 step 2). `notes.tsv` keeps both rows and the readout's screen prints `note: 04_band: band-fields 1` twice (`gates/cli_suite.sh` freezes both as printed). Under a band FAIL only the summary's row survives (the comparator prints no NOTE on FAIL — the gate's `band-through-the-loop` control). Both texts are the contributor's; the maintainer never chose between them because no plan section showed them together (`docs/plans/S4.md` §3 S1 gives the key to the summary, §3 E1's band row and R36 to the comparator).
- **Recommendation:** ONE writer — the summary (the observation side: it counts the band tokens the log holds, survives a band FAIL, and reports a band field nobody froze — BBX-13's watch where it is most useful); the comparator's NOTE retired, its PASS line alone. R36's letter is amended by this ruling; `gates/band.sh`, `gates/cli_suite.sh`'s frozen text, `compare_band.py`'s docstring and the plan's band row are corrected with a retraction row when it is answered.
- **Declined:** the comparator as the one writer (no count on a FAIL, none where no `.band` is frozen); two keys, one per writer (two names for one number on the screen); the suite de-duplicating identical NOTE rows (hides the day the two disagree); leaving both (the screen shows one number twice — today's state, frozen as printed until this is answered).
- **Cost of being wrong:** one frozen line in two gates and one docstring; nothing measured changes either way.
- **Measured again before the answer (2026-09-11, bbx-17):** `drivers/cli.sh` run over `fixture/fakecli/scenarios/04_band.cli`, then each writer run over what it produced. On a PASS both print `NOTE: band-fields 1`. With the frozen band narrowed to `[236, 240]` against the measured value 265 the comparator prints `FAIL band size: 265 outside [236, 240]` and NO NOTE at all, while the summary still prints `NOTE: band-fields 1`. Over `01_list`, which declares no band, no `<log>.bands` is written, the comparator never runs, and the summary prints `NOTE: band-fields 0`. One fact the raising session did not record: **the two counts cannot disagree under this driver** — the summary counts the log's `field:<name>:band` tokens and the comparator counts the frozen bands after proving the inventory matches the view both ways, and `lib/py/bbx/cli.py` writes the log and the band view from one `sc.bands` list in one run of `run()`. The duplicate is one number by two routes, not a cross-check. NOT gated: that the two routes agree for a driver that writes them apart — the residual blind spot, now `gates/cli_suite.sh`'s NOT-ASSERTED line.
- **Answer:** **Both writers kept, as printed today — the recommendation declined.** — maintainer, 2026-09-11 (bbx-17, ruled at the open against the two measured screens). The duplicate under `04_band` is deliberate: the gates freeze it as an intended screen, not as a pending state. Nothing printed changes — `gates/cli_suite.sh`'s 43 frozen lines, its `notes.tsv` count of 28 and its two `note: 04_band: band-fields 1` screen lines stand, and `gates/band.sh`'s two NOTE checks stay on the comparator. What changes is wording: the provisional phrasing comes off `gates/cli_suite.sh`, `docs/plans/S4.md` and `docs/slices.md`, `compare_band.py`'s docstring gains the reason the second writer exists so nobody retires it as a bug, and the gate's blind-spot line becomes the residual one above. R36's letter stands unamended; D43's key keeps its second writer. **No retraction row** (BBX-22 asked): no wording BBX stated as a current fact was corrected — a recommendation was declined, which is the queue working as designed.

### R42 — The fixture's deliberate crash leaves a macOS crash report on the host (raised and answered bbx-17, over S4 step 2's control)
- **Witness (2026-09-11):** the maintainer reported seeing Python crashes "for a couple of days", had already traced their origin to BBX sessions, and asked how bad or innocuous it was. A witness, not an instrument (BBX-28): converted into a reproducible case below before any theory was built on it (BBX-23's archaeology first — what changed, and when the crashes began).
- **Context measured (2026-09-11, bbx-17):** `fixture/fakecli/subject/fakecli.py --crash-at <n>` calls `os.abort()` and dies by SIGABRT; macOS's crash reporter writes a ~10 KB `.ips` report per death into `~/Library/Logs/DiagnosticReports/`. Two must-fire controls fire it on every battery — `gates/cli_driver.sh`'s `crash-vs-exit` and `gates/cli_suite.sh`'s `crash-vs-refusal` — and the driver's self-test does NOT (its `crash_log(6, …)` is a pure text function that spawns nothing). Counted on this host: 23 reports on 2026-09-10 and 2 on 2026-09-11, against **zero** before 19:53 local on 2026-09-10 despite about forty kept battery runs earlier that day; the first report falls in the sitting that built `--crash-at` (bbx-14). The report itself reads `EXC_CRASH` / `SIGABRT`, `abort() called`, the process dead 80 ms after launch with a Python parent — the driver spawning the tool.
- **The control measured both ways (the positive and the negative, BBX-5):** the tool's own `--crash-at 1` left exactly ONE new `.ips`; the same process killed with SIGKILL left NONE. Then the whole driver path on a COPY of the fixture under TMPDIR, the tree untouched: with `os.abort()` the driver exits 2 and writes `CRASH signal:6:SIGABRT` / `END-CRASH 3` with the three lines before the death kept; with the death changed to SIGKILL the driver exits 2 and writes `CRASH signal:9:SIGKILL` / `END-CRASH 3` with the same three lines. The two logs differ by ONE line, and the SIGKILL run left no report.
- **The gap this exposed:** `drivers/cli.sh` promises its sandbox is removed after the run, and its NOT-ASSERTED lines name the undeclared artifacts a tool may leave — a file outside `emits`, a socket, the clock, the terminal. The crash report is written by the OPERATING SYSTEM, outside the sandbox, into the maintainer's home, and outlives the run; no line of the contract declared it. Nothing is broken and the control is correct: the harm is that a genuine Python crash now hides among deliberate ones, and the maintainer is trained to ignore the reports (BBX-6's concern in mirror image — not a dead control, but a live one indistinguishable from a defect).
- **Recommendation:** the fixture dies by SIGKILL instead of `os.abort()`. The driver cannot see the difference beyond the signal's name, the naming of signal 6 stays under test in `bbx.cli`'s self-test (which spawns nothing), and the host artifact disappears. The cost is one frozen line in each of two gates, the generator and its regenerated tool, the registry's whole-set key, and a retraction row.
- **Declined:** keeping SIGABRT and only declaring the side effect (the reports keep arriving at two a battery, and burying a real crash is the harm itself); SIGKILL every battery with the abort kept at release cadence in `gates/sweep.tsv` (full fidelity, but a second crash option, a second control and a sweep row to preserve a difference the driver cannot observe); deleting the reports the gates cause (BBX writing outside its own tree into the maintainer's diagnostic store, where one matching slip destroys evidence of a real crash); disabling macOS crash reporting on this host (it hides every real crash, and it is a host change, not a harness one).
- **Answer:** Recommendation validated — the fixture dies by SIGKILL. — maintainer, 2026-09-11 (bbx-17, ruled from the measured table).

## Open

### R21 — How the platform runs of R3 are obtained (raised bbx-2)
- **Context measured (2026-09-09):** this host is macOS only — `docker`, `podman`, `limactl`, `multipass`, `orb` and `wsl.exe` are all absent (`command -v` each), so no Linux or WSL run of `bin/bbx selftest` can be produced from here. R3 rules every platform guard gated on each platform and BBX validating BBX per platform; today every green is macOS-only and every readout says so.
- **Recommendation:** the maintainer runs `BBX_BBH_HOME=<bbh> bin/bbx selftest --log build/selftest_<platform>_<stamp>` on a Linux or WSL host with the lineage cloned beside it, and brings back the kept run directory (results.tsv, run.txt, controls.txt, the logs); `bin/bbx readout` on that directory is the platform's screen, committed under `docs/platforms/<platform>/` with the run's HEAD. Until a run directory exists, the platform row in the readout reads *not run*, never *passed*. A run brought back by hand is a filed count until its directory is in the tree (CLAUDE.md §1); once in the tree it is a kept run keyed by version (BBX-29). A second recommendation, cheaper per sitting: a container runtime on this host (any of the six above), after which a `platform_linux` gate can run the battery inside it and the row becomes a measurement, not a delivery.
- **Declined:** marking the platform row green from the POSIX-ness of the scripts (a claim, not a run); a CI service (no remote exists, R7; a run nobody can re-derive locally is a filed count).
- **Procedure (the maintainer asked "can I just clone the repo and run the selftest?", 2026-09-10 — yes, with three facts measured here):** (1) `git clone https://github.com/DefinitelyFrenchName/BBX.git` (R22; before it, a bundle was the way); (2) bbh's baseline is `10a82d2` since the R28 re-baseline of 2026-09-10, and it is on `origin/main` of github.com/DefinitelyFrenchName/BBH-frame-based (`git branch -r --contains 10a82d2` → `origin/main`, measured 2026-09-11; bbh's remote was RENAMED from `blackbox-harness` on 2026-09-10 and the old URL redirects), so `git clone https://github.com/DefinitelyFrenchName/BBH-frame-based.git blackbox-harness` beside BBX is enough for the fidelity gates (they clone `10a82d2` from that tree, R20) — note the explicit directory name, without which the clone lands in `BBH-frame-based/` and the `BBX_BBH_HOME` below points at nothing; `gates/suite.sh` clones `f675710` from the same tree until R43 is answered, also on `origin/main` (measured the same way, 2026-09-11); (3) the census gate reads each census file's recorded absolute path (`/Users/koneko/…`) and SKIPs when it is absent — on the Linux host it will SKIP, asserting nothing, and the screen says so; a full census run there would need the three lineage trees at those exact paths, which is not asked. Then, needing only `sh`, `python3`, `git` and coreutils `timeout`: `cd BBX && BBX_BBH_HOME=../blackbox-harness bin/bbx selftest --log build/selftest_linux_$(date -u +%Y%m%dT%H%M%SZ)`, twice (BBX-14), and bring back `build/selftest_linux_*` (tar it); `bin/bbx readout <second> --against <first>` is the platform's screen. Expected: 28 registered gates (24 portable + 4 static, measured 2026-09-11 from `gates/portable.txt` and `gates/static.txt`), with `census_recount` the one SKIP a Linux host is known to produce (the three lineage paths it records are macOS-local); nothing else is known to SKIP off macOS, which is what the run is for. Anything red is the finding the run is for (BBX-26 applies to it on the next sitting, not to this one).
- **Status:** the maintainer will produce the run "in the coming days" (2026-09-10); it blocks nothing else. The procedure above was CORRECTED at bbx-18 (2026-09-11, BBX-19, retractions X26 and X27): it had named the pre-R28 baseline, the pre-rename remote and a gate count from ten gates ago.
- **Answer:** (open)

### R43 — Which bbh commit `gates/suite.sh` clones, and where the one baseline default lives (raised bbx-18)
- **Context measured (2026-09-11, at the bbx-18 open):** three gates read the environment variable `BBX_BBH_BASELINE`. `gates/fidelity_bbh.sh:37` and `gates/fidelity_bbh_s2.sh:45` default to `10a82d2` and cite `D20`; `gates/suite.sh:28` defaults to `f675710`, the value `D20` carried until the R28 re-baseline of 2026-09-10. The re-baseline moved two of the three and the register row; the third was found six sittings later by reading this queue, not by a gate (gotcha G32). The gate's own header says it copies bbh's `example/` "from a plain clone at the baseline (D20)", which its code has not done since that date. Measured both ways, `BBX_BBH_HOME=~/Developer/blackbox-harness gates/suite.sh` at its own default and with `BBX_BBH_BASELINE=10a82d2`: exit 0 both, 77 printed lines both, `diff` EMPTY. The two commits are not identical where it matters in principle — `10a82d2` touched 7 files, three under `example/` and `selftest/`, one of them `example/tests/lib/needs_fake.sh`, whose root fallback bbh fixed there after BBX reported it (G11) — so the gate copies an example tree carrying that defect, and the empty diff locks currency, never correctness (§3.3).
- **Recommendation:** ONE value for one variable, read from ONE place. The baseline stops being a literal in three gate files and becomes a single definition the three of them source (a `lib/sh/` one-liner beside the other shared shell, or a `[fidelity]` key in the kind-blind config layer read by `bin/bbx config`), with `D20`'s "where it lives" column naming every reader and the value staying `10a82d2`. A re-baseline then edits one line, which is what R28's procedure assumed it already did. The three gates' headers are corrected in the same commit, and each is re-run through the classifier; the expected result is the same 77 lines from `gates/suite.sh`, measured above.
- **Declined:** (a) editing `gates/suite.sh`'s literal to `10a82d2` and leaving three copies — it fixes today's value and keeps the mechanism that produced G32, which BBX-9 names ("a hand-maintained list is a smaller thing to forget to update"); (b) keeping this gate pinned to `f675710` with a register row of its own, on the argument that sections 1–8 were lifted from bbh's `selftest/test_suite_dispatch.sh` at that commit — defensible for the lifted TEXT, but the gate does not run bbh's test, it runs BBX's runner over bbh's `example/`, and pinning the fixture tree to a commit whose known defect bbh has already fixed buys nothing; it also leaves two values for one variable, so a `BBX_BBH_BASELINE=<tip>` set once for a battery would still move all three gates while only two are documented to move; (c) leaving it as found and recording it as a hazard — declined because the gate's header states the baseline it no longer clones, so the next reader is misled by the file itself (BBX-30).
- **Not blocked by this:** S4 step 5. The gate is green at both commits, measured; nothing waits on the answer.
- **Answer:** (open)

### R44 — What the self subject's moving identity costs at every close, and whether the whole-set key keeps `gates` (raised bbx-18, from R38 made concrete)
- **Context measured (2026-09-11, before `fixture/selfgates/` was written):** R38 rules the self subject's identity to be the `command` fingerprint — program key the tree hash of `bin`, `lib`, `drivers` at HEAD, whole-set key the same plus `gates` — and the whole-set key is the one the registry maps to an expectation set, so it decides whether the suite runs at all ("unregistered build fingerprint" before any scenario, `[BBH-67]`). Measured over BBX's own history: **22 of 53 commits** touch `bin`, `lib`, `drivers` or `gates`, and **5 of the last 10** do. So the registry row needs a reviewed refreeze about every other commit, not about every slice. Two more measured facts shape the answer: the key is of the COMMIT, so an uncommitted edit does not move it (the gate can be built and run green while the tree is dirty, and the kept run's `porcelain` line is what sees the dirt); and a refreeze commit that touches only `fixture/selfgates/expected/registry.tsv` does NOT move the key again, so the sequence "land the code, refreeze, battery" terminates.
- **Recommendation:** keep R38's key exactly as ruled, `gates` included, and make the refreeze an explicit, printed step of the close rather than a thing someone remembers: the gate prints the identity it computed and the row it matched, a mismatch is RED with the new key on the screen (so a forgotten refreeze is loud, never silent), and the close's registry edit is one line quoted in the commit message beside the tally. The cost is one line per sitting that touches the kernel, paid in exchange for the one subject that notices the harness moved (R14).
- **Declined:** (a) dropping `gates` from the whole-set key — cheaper, and the key would then move only on kernel changes, but the subject whose whole purpose is to notice that BBX moved would stop noticing the half of BBX that changes most often; (b) computing the identity from the WORKING TREE instead of HEAD — it would move on every edit, turning the gate red for the duration of every build and making "green first" impossible mid-sitting; (c) letting the runner refreeze automatically when the key moves — that is a self-frozen expectation, which answers only "did this change" (R11, BBX-3, §3.4), and it would erase exactly the signal R14 asks for; (d) keying the expectation set by something stable (a version string in the config) — a number we write about ourselves, §3.4's self-reference.
- **Answer:** (open)

### R45 — How BBX-9's orphan direction gets a VERDICT, when the runner that reports it is the lineage's (raised bbx-18, from G34)
- **Context measured (2026-09-11):** BBX-9 rules that every registry is complete both ways and that an unregistered item and a dead row BOTH fail. Measured: a dead row fails (it becomes a `MISSING` row, `rc=1`), and an orphan does not — `bbx tier <config> --unregistered` exits 0 whether the registry is complete, holds three orphans or holds a dead row, and an orphan planted in BBX's own `gates/` was named on the screen while `gates/tier.sh` PASSed and the runner's `rc` never read the list (it is computed from `n_fail`, `n_miss`, `--strict`'s `n_skip` and the controls block). BBX's own registry is complete today, so nothing is red; what is missing is the ability to go red. The runner cannot be the place it is fixed: its printed text and its verdict are bbh's, and F13's pairs diff both runners over a synthetic repo that CONTAINS an orphan, so a new failure there would break the fidelity obligation that this repository exists to keep (§2, §7.2).
- **Recommendation:** one new portable gate — `gates/registry_complete.sh` — whose positive is BBX's OWN config (`bbx tier bbx.toml --unregistered` must print the `ok: every instrument-free gate is registered` line, and anything else is FAIL naming the files) and whose MUST-FIRE control is a synthetic consumer under `TMPDIR` with one orphan planted, where the same reader must report non-ok. The verdict is BBX's, the lineage's runner is untouched, and F12/F13 stay byte-identical. It belongs beside the other registry gates and costs about a second.
- **Declined:** (a) making `bin/bbx-run-static` fail on an orphan — it is the lineage's runner, F13's pairs would diverge, and "reproduce" in this project always means that obligation (§2); (b) leaving the orphan direction as a REPORT — BBX-9's own words are that it fails, and a check that cannot fail where it should is not evidence (§1); (c) a `--strict-registry` flag on the runner — still the lineage's runner, and a flag nobody sets is a control that never fires (BBX-6); (d) folding the assertion into `gates/tier.sh` — that gate's subject is the tier CLASSIFIER on a synthetic tree, and pointing it at BBX's own tree would mix a unit with a self-check and hide which one failed.
- **Answer:** (open)

### R46 — Where the harness's own identity key is COMPUTED, now that two writers of it exist (raised bbx-19, from G42)
- **Context measured (2026-09-12):** R38 ruled the self subject's identity to be the tree hash of `bin`, `lib`, `drivers` (and `gates` for the whole-set key) hashed into one. Until this sitting one file computed it: `fixture/selfgates/idkey.sh`, a fixture's script. S4 step 6 needed the same key for a second subject — the file census, which must be keyed by what it DESCRIBES rather than by the tree that stores it (G42: keying it by `HEAD` made `--check` fail on its own output, because the commit that writes the document moves `HEAD`) — so `lib/py/bbx/file_census.py` computes it too. There are now TWO writers of one key, in two languages, and `gates/file_census_tool.sh` proves they agree on BBX's own tree on every battery. Measured: the two keys are equal; the census's copy takes a `--root`, the fixture's reads `$BBX_HOME` only.
- **Recommendation:** ONE definition, in the harness, with the fixture reading it — `python3 -m bbx.fingerprint` already carries the `command` kind (R38's mechanism) and is where a tree-hash identity belongs; `idkey.sh` becomes a three-line shim that calls it, and the cross-check in the portable gate stays as the proof that the shim did not drift. The argument is D47's and D57's, made twice in this tree already: one writer of a token, one resolver of a view.
- **Declined:** (a) leaving two independent implementations with the cross-check as the only guard — it is a real guard, and it is exactly what R41 ACCEPTED for `band-fields` after measuring that the two routes could not disagree; but these two CAN disagree (different languages, different roots, one of them free to change what it hashes) and the cross-check runs on one tree only, so it proves agreement about BBX and nothing about a consumer; (b) moving the definition into the fixture and having the harness call the fixture's script — a harness tool that depends on a fixture inverts the direction the whole tree is built on; (c) deleting the census's copy and having it shell out to `idkey.sh` — the census must key an arbitrary `--root` (its portable gate's synthetic tree), and `idkey.sh` reads `$BBX_HOME` by deliberate design (R38, measured at bbx-18).
- **Not blocked by this:** S4 step 7 and the slice's close. Both writers are green and proven equal today.
- **Answer:** (open)

### R47 — What detects a harness file that no gate executes BETWEEN releases, now that the census is release-scoped (raised bbx-19, from R39's own cadence)
- **Context measured (2026-09-12):** R39 put the census at release cadence for a good reason — it runs BBX's whole battery inside a shadow, about 20 minutes, and "a portable gate that re-runs the battery every battery" was declined. So the assertion "no harness file is reached by NO gate" (rot class 1, the orphan direction of the file census) is now made at release and not otherwise, while `gates/file_census_tool.sh` proves only that the INSTRUMENT can detect one, on a synthetic tree. Between releases a new module that nothing runs reads green. Measured today: the universe is 44 files, `expected/file_census.toml` carries 44 frozen rows, and `git ls-files lib bin drivers | grep -vc '\.md$'` costs milliseconds.
- **Recommendation:** a cheap row in the PORTABLE tier that asserts the register is complete both ways against the universe — every tracked file under `bin/`, `lib/` and `drivers/` has a frozen row, and every frozen row names a file that exists — with no shadow, no battery and no trace. It does not prove a file is reached (only the release run can), and it must say so; what it catches is the likeliest rot by far: a file added and never brought under any gate, which today would sit unnoticed until the next release. It is BBX-9's completeness check applied to the census's own register, and it costs about a second. The new file then forces a deliberate act — either a gate reaches it, or a release run re-freezes the row with its measured kinds.
- **Declined:** (a) nothing, on the argument that the release run catches it — true, and the gap is every sitting in between; this project's own evidence is that a number which is not gated rots (BBX-10) and that a file census measured once was stale within one step; (b) moving the whole census into the portable tier — R39 declined it on the measured cost and the answer has not changed; (c) a partial census every battery (a subset of gates) — a trace from a subset UNDER-reports every file the missing gates reach, which is the same defect as the SKIP this gate already refuses to take (`BBX_BBH_HOME` unset), and an under-report that looks like a measurement is worse than no measurement; (d) folding it into `gates/file_census_tool.sh` — that gate's subject is a synthetic tree and pointing it at BBX's own register would mix an instrument's ground truth with a self-check, which is the argument R45 made for not folding the orphan verdict into `gates/tier.sh`.
- **Answer:** (open)

(A new ruling is added here with its recommendation, the alternatives declined, and `- **Answer:** (open)` until the maintainer answers, then moved.)
