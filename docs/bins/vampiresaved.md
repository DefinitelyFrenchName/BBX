# Bins — VampireSaved (census docs/census/vampiresaved.md @ 5df1d8be) — 2026-09-09

Shape: table, one row per census B item, ids identical to the census. Bins per CLAUDE.md §5. Every `drop` carries a reason. Totals at the end are produced by the command shown.

| id | item (≤ 80 chars) | bin | becomes / reason |
|---|---|---|---|
| V-R1 | [CPE-1] → lifted to level 0 as [MFI-1] (`mame-fbneo-instruments`); the anchor… | consumer | redirect -> MFI-1; content binned at that row |
| V-R2 | [CPE-2] → lifted to level 0 as [MFI-2] (`mame-fbneo-instruments`); the anchor… | consumer | redirect -> MFI-2; content binned at that row |
| V-R3 | [CPE-3] → lifted to level 0 as [MFI-3] (`mame-fbneo-instruments`); the anchor… | consumer | redirect -> MFI-3; content binned at that row |
| V-R4 | [CPE-4] → lifted to level 0 as [MFI-4] (`mame-fbneo-instruments`); the anchor… | consumer | redirect -> MFI-4; content binned at that row |
| V-R5 | [CPE-5] → lifted to level 0 as [MFI-5] (`mame-fbneo-instruments`); the anchor… | consumer | redirect -> MFI-5; content binned at that row |
| V-R6 | [CPE-6] → lifted to level 0 as [MFI-6] (`mame-fbneo-instruments`); the anchor… | consumer | redirect -> MFI-6; content binned at that row |
| V-R7 | [CPE-7] → lifted to level 0 as [MFI-7] (`mame-fbneo-instruments`); the anchor… | consumer | redirect -> MFI-7; content binned at that row |
| V-R8 | [CPE-8] → lifted to level 0 as [MFI-8] (`mame-fbneo-instruments`); the anchor… | consumer | redirect -> MFI-8; content binned at that row |
| V-R9 | [CPE-9] → lifted to level 0 as [MFI-9] (`mame-fbneo-instruments`); the anchor… | consumer | redirect -> MFI-9; content binned at that row |
| V-R10 | [CPE-10] → lifted to level 0 as [MFI-10] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-10; content binned at that row |
| V-R11 | [CPE-11] → lifted to level 0 as [MFI-11] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-11; content binned at that row |
| V-R12 | [CPE-12] → lifted to level 0 as [MFI-12] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-12; content binned at that row |
| V-R13 | [CPE-13] → lifted to level 0 as [MFI-13] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-13; content binned at that row |
| V-R14 | [CPE-14] → lifted to level 0 as [MFI-14] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-14; content binned at that row |
| V-R15 | [CPE-15] → lifted to level 0 as [MFI-15] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-15; content binned at that row |
| V-R16 | [CPE-16] → lifted to level 0 as [MFI-16] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-16; content binned at that row |
| V-R17 | [CPE-17] → lifted to level 0 as [MFI-17] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-17; content binned at that row |
| V-R18 | [CPE-18] → lifted to level 0 as [MFI-18] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-18; content binned at that row |
| V-R19 | [CPE-19] → lifted to level 0 as [MFI-19] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-19; content binned at that row |
| V-R20 | [CPE-20] → lifted to level 0 as [MFI-20] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-20; content binned at that row |
| V-R21 | [CPE-21] → lifted to level 0 as [MFI-21] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-21; content binned at that row |
| V-R22 | [CPE-22] → lifted to level 0 as [MFI-22] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-22; content binned at that row |
| V-R23 | [CPE-23] → lifted to level 0 as [MFI-23] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-23; content binned at that row |
| V-R24 | [CPE-24] → lifted to level 0 as [MFI-24] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-24; content binned at that row |
| V-R25 | [CPE-25] → lifted to level 0 as [MFI-25] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-25; content binned at that row |
| V-R26 | [CPE-26] → lifted to level 0 as [MFI-26] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-26; content binned at that row |
| V-R27 | [CPE-27] → lifted to level 0 as [MFI-27] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-27; content binned at that row |
| V-R28 | [CPE-28] → lifted to level 0 as [MFI-28] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-28; content binned at that row |
| V-R29 | [CPE-29] → lifted to level 0 as [MFI-29] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-29; content binned at that row |
| V-R30 | [CPE-30] → lifted to level 0 as [MFI-30] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-30; content binned at that row |
| V-R31 | [CPE-31] → lifted to level 0 as [MFI-31] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-31; content binned at that row |
| V-R32 | [CPE-32] → lifted to level 0 as [MFI-32] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-32; content binned at that row |
| V-R33 | [CPE-33] → lifted to level 0 as [MFI-33] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-33; content binned at that row |
| V-R34 | [CPE-34] → lifted to level 0 as [MFI-34] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-34; content binned at that row |
| V-R35 | [CPE-35] → lifted to level 0 as [MFI-35] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-35; content binned at that row |
| V-R36 | [CPE-36] → lifted to level 0 as [MFI-36] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-36; content binned at that row |
| V-R37 | [CPE-37] → lifted to level 0 as [MFI-37] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-37; content binned at that row |
| V-R38 | [CPE-38] → lifted to level 0 as [MFI-38] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-38; content binned at that row |
| V-R39 | [CPE-39] → lifted to level 0 as [MFI-39] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-39; content binned at that row |
| V-R40 | [CPE-40] → lifted to level 0 as [MFI-40] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-40; content binned at that row |
| V-R41 | [CPE-41] → lifted to level 0 as [MFI-41] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-41; content binned at that row |
| V-R42 | [CPE-42] → lifted to level 0 as [MFI-42] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-42; content binned at that row |
| V-R43 | [CPH-1] → lifted to level 0 as [MFI-43] (`mame-fbneo-instruments`); the ancho… | consumer | redirect -> MFI-43; content binned at that row |
| V-R44 | [CPH-2] The program zips store CODE encrypted; only DATA reads bypass the cry… | consumer | board (CPS-2) |
| V-R45 | [CPH-3] PC-relative operand reads are PROGRAM-space and DECRYPTED; `(An)`-bas… | generalize | -> S4 VIEWS: the view is a property of the ACCESS MODE (BBX-16) |
| V-R46 | [CPH-4] Anything fetched through the opcode space must be written as CODE whe… | consumer | board (CPS-2) |
| V-R47 | [CPH-5] When a table's view is in doubt, DECODE BOTH VIEWS and keep the one w… | generalize | -> BBX-16: a wrong view yields plausible garbage; decide the view by evidence |
| V-R48 | [CPH-6] Relocating a pc-relative DISPATCHER: carry a copy of its table INSIDE… | consumer | board (CPS-2) |
| V-R49 | [CPH-7] The encrypted range is INCLUSIVE of its upper word — the limit test i… | consumer | board (CPS-2) |
| V-R50 | [CPH-8] Code above the encryption window is stored RAW, automatically and by… | consumer | board (CPS-2) |
| V-R51 | [CPH-9] Gfx simms are NOT tile-contiguous: a tile's 32 bytes are sixteen two-… | consumer | board (CPS-2) |
| V-R52 | [CPH-10] Compose a tile address the way the hardware does — `code` | consumer | board (CPS-2) |
| V-R53 | [CPH-11] Within each 8-pixel half of an OBJ tile row, plane bit `i` is pixel… | consumer | board (CPS-2) |
| V-R54 | [CPH-12] → lifted to level 0 as [MFI-44] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-44; content binned at that row |
| V-R55 | [CPH-13] → lifted to level 0 as [MFI-45] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-45; content binned at that row |
| V-R56 | [CPH-14] OBJ y-word bit 15 is the sprite-list TERMINATOR, not a spare bit — s… | consumer | board (CPS-2) |
| V-R57 | [CPH-15] The CPS-2 Turbo rule is the precedent for a 19th tile-address bit: p… | consumer | board (CPS-2) |
| V-R58 | [CPH-16] OBJ RAM is double-buffered and a dump spans BOTH pages plus several… | consumer | board (CPS-2) |
| V-R59 | [CPH-17] Walk a structure the way the hardware walks it — terminators, bounds… | generalize | -> O1/O2: an extractor reads the artifact the way its consumer walks it |
| V-R60 | [CPH-18] → lifted to level 0 as [MFI-46] (`mame-fbneo-instruments`); the anch… | consumer | redirect -> MFI-46; content binned at that row |
| V-R61 | [CPH-19] A sample window must live in ONE HALF of its 64K bank — the DSP comp… | consumer | board (CPS-2) |
| V-R62 | [CPH-20] A copied sample window must keep the SOURCE offset's BYTE PARITY — t… | consumer | board (CPS-2) |
| V-R63 | [CPH-21] A sample record's `end` offset PLAYS — copy the INCLUSIVE window, pr… | consumer | board (CPS-2) |
| V-R64 | [CPH-22] A "pure synthetic beep" IS a tight-loop sample (a few dozen saturate… | consumer | board (CPS-2) |
| V-R65 | [CPH-23] The loaders' shape rules bind the ROMSET, and each belongs to one im… | consumer | board (CPS-2) |
| V-R66 | [CPH-24] The WIDE profile is a NAMED, VERSIONED hardware shape — program 6 MB… | generalize | -> S1: a KIND PROFILE is a named, versioned shape |
| V-R67 | [CPH-25] `$400000-$40000F` is reserved and never allocated: the three impleme… | consumer | board (CPS-2) |
| V-R68 | [CPH-26] Content authored into the extension is written RAW but laid out in F… | consumer | board (CPS-2) |
| V-R69 | [CPH-27] Before widening anything, MEASURE that the stock game never touches… | generalize | -> BBX-7: measure the absence with a positive control before relying on it |
| V-R70 | [CPH-28] Inertness is not functionality. Proving a widened path HARMLESS on s… | generalize | -> BBX-30/RO2: harmless is not working; a green states what it does not assert |
| V-R71 | [CPH-29] Every emulator change under the profile is governed: bounded and dec… | generalize | -> S1: instrument changes bounded, declarative, gated by the KIND PROFILE |
| V-R72 | [CPH-30] The change budget is MEASURED, and an undercount is a retraction-cla… | keep | 1 / BBX-22 the change budget is measured; an undercount is retraction-class |
| V-R73 | [MFI-1] Driver `logerror` lines need `-log`, not `-verbose` — they land in `e…` | consumer | MAME/FBNeo instrument |
| V-R74 | [MFI-2] `-debug` perturbs multi-CPU timing: a debug run diverges from the ide… | generalize | -> O2 BASIS: the instrument perturbs the subject; a debug run is another scenario |
| V-R75 | [MFI-3] Every `-debug` watch configuration is its own TIMELINE — two debug tr… | generalize | -> O2: two observations under different bases are not comparable |
| V-R76 | [MFI-4] Breakpoint logging is a SAMPLER, never an inventory: the Lua pump dro… | generalize | -> BBX-7: a sampler cannot answer an inventory question |
| V-R77 | [MFI-5] A stopped CPU keeps emitting frames: `frame_done` fires while the deb… | consumer | MAME/FBNeo instrument |
| V-R78 | [MFI-6] Condition a breakpoint that drives replay input so it fires a HANDFUL… | consumer | MAME/FBNeo instrument |
| V-R79 | [MFI-7] An ARMED breakpoint delays input application by a beat whenever it st… | consumer | MAME/FBNeo instrument |
| V-R80 | [MFI-8] A `wpset` watchpoint is SILENTLY BLIND to every pc-relative read (ser… | consumer | MAME/FBNeo instrument |
| V-R81 | [MFI-9] Watchpoint LENGTH is parsed as HEX (`10` = sixteen). A length a harne… | consumer | MAME/FBNeo instrument |
| V-R82 | [MFI-10] A boot-time RAM test writes EVERY byte of work RAM, so a bare write… | consumer | MAME/FBNeo instrument |
| V-R83 | [MFI-11] A MAME watchpoint logs REGISTERS, not the value written; reading "th… | consumer | MAME/FBNeo instrument |
| V-R84 | [MFI-12] MAME Lua write taps are silently DROPPED when the driver re-installs… | consumer | MAME/FBNeo instrument |
| V-R85 | [MFI-13] Write taps must be WORD-aligned on a 16-bit bus (tap the containing… | consumer | MAME/FBNeo instrument |
| V-R86 | [MFI-14] Lua READ taps observe nothing where memory is direct-mapped or serve… | consumer | MAME/FBNeo instrument |
| V-R87 | [MFI-15] `-video none` STILL creates a window that can take focus, and host k… | consumer | MAME/FBNeo instrument |
| V-R88 | [MFI-16] `-aviwrite` is headless-capable but uncompressed (gigabytes in minut… | consumer | MAME/FBNeo instrument |
| V-R89 | [MFI-17] Cross-driver framebuffer CHECKSUMS are NOT comparable — thousands of… | generalize | -> O2/D6: two implementations compare only on MAPPED state, never raw hashes |
| V-R90 | [MFI-18] A chained rompath makes MAME a LIAR about member identity: a stale m… | generalize | -> S2/O5: a search path lies about identity; report what was actually opened |
| V-R91 | [MFI-19] Palette RAM takes Lua pokes for READBACK but not for RENDERING — a p… | consumer | MAME/FBNeo instrument |
| V-R92 | [MFI-20] MAME audits the whole board: per-set key files and any SHARED device… | consumer | MAME/FBNeo instrument |
| V-R93 | [MFI-21] `git submodule add` stages the DEFAULT BRANCH, not the tag you check… | keep | BBX-10 stale reference: a pinned instrument version is asserted, never assumed |
| V-R94 | [MFI-22] GENie cannot handle a SPACE anywhere in the source path, and a symli… | consumer | MAME/FBNeo instrument |
| V-R95 | [MFI-23] The OSD is found ONLY through pkg-config (`REGENIE=1` after installi… | consumer | MAME/FBNeo instrument |
| V-R96 | [MFI-24] Parity BEFORE the patch. Swapping a binary changes the INSTRUMENT, n… | keep | BBX-5: parity before the patch - swapping a binary changes the INSTRUMENT |
| V-R97 | [MFI-25] `git apply` inside another repository's working tree SILENTLY SKIPS… | keep | BBX-1: a tool that exits 0 having silently done nothing is the false-green class |
| V-R98 | [MFI-26] `make sdl2 SKIPDEPEND=1` is mandatory on a fresh clone — and it hide… | consumer | MAME/FBNeo instrument |
| V-R99 | [MFI-27] The shared EEPROM breaks run-to-run determinism — `$HOME` overrides… | generalize | -> D5 sandbox: the driver isolates every mutable state or determinism dies |
| V-R100 | [MFI-28] FBNeo matches a member by CRC FIRST, then by name; when NEITHER matc… | consumer | MAME/FBNeo instrument |
| V-R101 | [MFI-29] The SDL frontend has NO `-rompath`; the flag is silently ignored and… | consumer | MAME/FBNeo instrument |
| V-R102 | [MFI-30] Without the framebuffer knob the sprite path never runs (`pBurnDraw`… | consumer | MAME/FBNeo instrument |
| V-R103 | [MFI-31] A reference binary must differ from the build under test by EXACTLY… | keep | BBX-5: a reference leg differs from the test leg by exactly one thing |
| V-R104 | [MFI-32] Instrument extensions to a frontend are frontend-only and opt-in: sc… | generalize | -> D1/BBX-5: the instrument layer is opt-in and sits above the subject |
| V-R105 | [MFI-33] Same inputs are NOT the same content across emulators: a boot-phase… | generalize | -> D6/O1: same inputs are not the same content across implementations |
| V-R106 | [MFI-34] Frame indices and object SLOTS do not transfer between emulators — t… | generalize | -> D6: indices do not transfer; anchor each side on what it finds itself |
| V-R107 | [MFI-35] A RAM-checksum gate is structurally BLIND to the video path — a rend… | generalize | -> RO2/S4: a one-view gate is structurally blind to the other views |
| V-R108 | [MFI-36] Sound is invisible to every RAM and pixel gate — it lives in a ring… | generalize | -> RO2/O4: a channel no basis reads needs its own observation |
| V-R109 | [MFI-37] A value fed by allocation, RNG or sound state cannot be correlated A… | generalize | -> BBX-14: state fed by allocation or RNG is not correlatable across runs |
| V-R110 | [MFI-38] A canary changes exactly ONE thing or it answers nothing. When a ROM… | keep | BBX-5: a canary changes exactly one thing or it answers nothing |
| V-R111 | [MFI-39] A relocation test with no negative control proves nothing — "I moved… | keep | BBX-5 / BBX-8: a relocation test with no negative control proves nothing |
| V-R112 | [MFI-40] "Unknown system" is an EMULATOR problem, not a ROM problem — renamin… | consumer | MAME/FBNeo instrument |
| V-R113 | [MFI-41] What a machine migration puts at risk is only the frozen MAME expect… | generalize | -> E4: absolute frozen expectations are instrument-bound; a live A/B survives |
| V-R114 | [MFI-42] Two builds can share a PROGRAM fingerprint and differ in every graph… | generalize | -> S2: IDENTITY must cover the whole SUBJECT SET, not one member |
| V-R115 | [MFI-43] ROM FILES in a word-swapped region are little-endian word pairs; IMA… | consumer | MAME/FBNeo instrument |
| V-R116 | [MFI-44] Both emulators resolve a ROM member by HASH before NAME, so a member… | consumer | MAME/FBNeo instrument |
| V-R117 | [MFI-45] Descriptor CRCs: FIXED-content members carry their real CRC; VARIABL… | consumer | MAME/FBNeo instrument |
| V-R118 | [MFI-46] A member's REGION layout is not its FILE layout, and a sound CPU's o… | generalize | -> S4 VIEWS: one artifact, three coordinate systems; name the one you read |
| V-R119 | [MSC-1] → lifted to level 0 as [MJC-1] (`mister-jtframe-core`); the anchored… | consumer | redirect -> MJC-1; content binned at that row |
| V-R120 | [MSC-2] → lifted to level 0 as [MJC-2] (`mister-jtframe-core`); the anchored… | consumer | redirect -> MJC-2; content binned at that row |
| V-R121 | [MSC-3] → lifted to level 0 as [MJC-3] (`mister-jtframe-core`); the anchored… | consumer | redirect -> MJC-3; content binned at that row |
| V-R122 | [MSC-4] → lifted to level 0 as [MJC-4] (`mister-jtframe-core`); the anchored… | consumer | redirect -> MJC-4; content binned at that row |
| V-R123 | [MSC-5] → lifted to level 0 as [MJC-5] (`mister-jtframe-core`); the anchored… | consumer | redirect -> MJC-5; content binned at that row |
| V-R124 | [MSC-6] → lifted to level 0 as [MJC-6] (`mister-jtframe-core`); the anchored… | consumer | redirect -> MJC-6; content binned at that row |
| V-R125 | [MSC-7] → lifted to level 0 as [MJC-7] (`mister-jtframe-core`); the anchored… | consumer | redirect -> MJC-7; content binned at that row |
| V-R126 | [MSC-8] → lifted to level 0 as [MJC-8] (`mister-jtframe-core`); the anchored… | consumer | redirect -> MJC-8; content binned at that row |
| V-R127 | [MSC-9] → lifted to level 0 as [MJC-9] (`mister-jtframe-core`); the anchored… | consumer | redirect -> MJC-9; content binned at that row |
| V-R128 | [MSC-10] → lifted to level 0 as [MJC-10] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-10; content binned at that row |
| V-R129 | [MSC-11] → lifted to level 0 as [MJC-11] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-11; content binned at that row |
| V-R130 | [MSC-12] → lifted to level 0 as [MJC-12] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-12; content binned at that row |
| V-R131 | [MSC-13] → lifted to level 0 as [MJC-13] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-13; content binned at that row |
| V-R132 | [MSC-14] → lifted to level 0 as [MJC-14] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-14; content binned at that row |
| V-R133 | [MSC-15] → lifted to level 0 as [MJC-15] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-15; content binned at that row |
| V-R134 | [MSC-16] → lifted to level 0 as [MJC-16] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-16; content binned at that row |
| V-R135 | [MSC-17] → lifted to level 0 as [MJC-17] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-17; content binned at that row |
| V-R136 | [MSC-18] → lifted to level 0 as [MJC-18] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-18; content binned at that row |
| V-R137 | [MSC-19] → lifted to level 0 as [MJC-19] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-19; content binned at that row |
| V-R138 | [MSC-20] → lifted to level 0 as [MJC-20] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-20; content binned at that row |
| V-R139 | [MSC-21] A CPS-2 tile code IS its SDRAM address: the download scramble undoes… | consumer | board (CPS-2 on MiSTer) |
| V-R140 | [MSC-22] → lifted to level 0 as [MJC-22] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-22; content binned at that row |
| V-R141 | [MSC-23] → lifted to level 0 as [MJC-23] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-23; content binned at that row |
| V-R142 | [MSC-24] → lifted to level 0 as [MJC-24] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-24; content binned at that row |
| V-R143 | [MSC-25] GFX ROM CONTENT changes object TIMING — the object pipeline skips it… | consumer | board (CPS-2 on MiSTer) |
| V-R144 | [MSC-26] → lifted to level 0 as [MJC-26] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-26; content binned at that row |
| V-R145 | [MSC-27] Widening SDRAM does not widen the core. The caps are FORMAT: a 16-bi… | consumer | board (CPS-2 on MiSTer) |
| V-R146 | [MSC-28] Promote AFTER the terminator test. y-word bit 15 is the sprite-list… | consumer | board (CPS-2 on MiSTer) |
| V-R147 | [MSC-29] → lifted to level 0 as [MJC-29] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-29; content binned at that row |
| V-R148 | [MSC-30] → lifted to level 0 as [MJC-30] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-30; content binned at that row |
| V-R149 | [MSC-31] → lifted to level 0 as [MJC-31] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-31; content binned at that row |
| V-R150 | [MSC-32] The CPS-2 key's encrypted-opcode RANGE word is stored COMPLEMENTED;… | consumer | board (CPS-2 on MiSTer) |
| V-R151 | [MSC-33] → lifted to level 0 as [MJC-33] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-33; content binned at that row |
| V-R152 | [MSC-34] "Data read from above the window" is not "code executed from above t… | generalize | -> BBX-8: a control must exercise the claim it certifies, not an adjacent one |
| V-R153 | [MSC-35] Rule 1 v2 has a MiSTer form for every clause: bounded = the enumerat… | consumer | board (CPS-2 on MiSTer) |
| V-R154 | [MSC-36] → lifted to level 0 as [MJC-36] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-36; content binned at that row |
| V-R155 | [MSC-37] → lifted to level 0 as [MJC-37] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-37; content binned at that row |
| V-R156 | [MSC-38] On CPS-2 pass `-load` on EVERY run and drop `-setname`. The transfer… | consumer | board (CPS-2 on MiSTer) |
| V-R157 | [MSC-39] → lifted to level 0 as [MJC-39] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-39; content binned at that row |
| V-R158 | [MSC-40] → lifted to level 0 as [MJC-40] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-40; content binned at that row |
| V-R159 | [MSC-41] → lifted to level 0 as [MJC-41] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-41; content binned at that row |
| V-R160 | [MSC-42] → lifted to level 0 as [MJC-42] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-42; content binned at that row |
| V-R161 | [MSC-43] → lifted to level 0 as [MJC-43] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-43; content binned at that row |
| V-R162 | [MSC-44] → lifted to level 0 as [MJC-44] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-44; content binned at that row |
| V-R163 | [MSC-45] → lifted to level 0 as [MJC-45] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-45; content binned at that row |
| V-R164 | [MSC-46] → lifted to level 0 as [MJC-46] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-46; content binned at that row |
| V-R165 | [MSC-47] → lifted to level 0 as [MJC-47] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-47; content binned at that row |
| V-R166 | [MSC-48] → lifted to level 0 as [MJC-48] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-48; content binned at that row |
| V-R167 | [MSC-49] → lifted to level 0 as [MJC-49] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-49; content binned at that row |
| V-R168 | [MSC-50] → lifted to level 0 as [MJC-50] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-50; content binned at that row |
| V-R169 | [MSC-51] → lifted to level 0 as [MJC-51] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-51; content binned at that row |
| V-R170 | [MSC-52] → lifted to level 0 as [MJC-52] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-52; content binned at that row |
| V-R171 | [MSC-53] → lifted to level 0 as [MJC-53] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-53; content binned at that row |
| V-R172 | [MSC-54] → lifted to level 0 as [MJC-54] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-54; content binned at that row |
| V-R173 | [MSC-55] → lifted to level 0 as [MJC-55] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-55; content binned at that row |
| V-R174 | [MSC-56] → lifted to level 0 as [MJC-56] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-56; content binned at that row |
| V-R175 | [MSC-57] → lifted to level 0 as [MJC-57] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-57; content binned at that row |
| V-R176 | [MSC-58] → lifted to level 0 as [MJC-58] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-58; content binned at that row |
| V-R177 | [MSC-59] → lifted to level 0 as [MJC-59] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-59; content binned at that row |
| V-R178 | [MSC-60] → lifted to level 0 as [MJC-60] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-60; content binned at that row |
| V-R179 | [MSC-61] → lifted to level 0 as [MJC-61] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-61; content binned at that row |
| V-R180 | [MSC-62] → lifted to level 0 as [MJC-62] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-62; content binned at that row |
| V-R181 | [MSC-63] → lifted to level 0 as [MJC-63] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-63; content binned at that row |
| V-R182 | [MSC-64] → lifted to level 0 as [MJC-64] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-64; content binned at that row |
| V-R183 | [MSC-65] → lifted to level 0 as [MJC-65] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-65; content binned at that row |
| V-R184 | [MSC-66] → lifted to level 0 as [MJC-66] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-66; content binned at that row |
| V-R185 | [MSC-67] → lifted to level 0 as [MJC-67] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-67; content binned at that row |
| V-R186 | [MSC-68] → lifted to level 0 as [MJC-68] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-68; content binned at that row |
| V-R187 | [MSC-69] → lifted to level 0 as [MJC-69] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-69; content binned at that row |
| V-R188 | [MSC-70] Pixels have never been compared by an instrument between the core an… | generalize | -> RO2: the readout declares the blind spot (pixels and audio never measured) |
| V-R189 | [MSC-71] Real silicon's decryption window is INFERRED, never measured — both… | generalize | -> 3.3 / BBX-15: a shared-ancestry corroborator is not lineage-independent |
| V-R190 | [MSC-72] → lifted to level 0 as [MJC-72] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-72; content binned at that row |
| V-R191 | [MSC-73] → lifted to level 0 as [MJC-73] (`mister-jtframe-core`); the anchore… | consumer | redirect -> MJC-73; content binned at that row |
| V-R192 | [MJC-1] An extended core is a SEPARATE core directory, `cores/<x>`, pulling s… | consumer | MiSTer / jtframe |
| V-R193 | [MJC-2] `jtframe files` deduplicates by FULL PATH. Overriding a shared file m… | consumer | MiSTer / jtframe |
| V-R194 | [MJC-3] A new jtframe module is pulled from the CORE's `game.yaml` (`- from:…` | consumer | MiSTer / jtframe |
| V-R195 | [MJC-4] The fork's delta is mirrored in-tree as a PATCH SERIES, one file per… | generalize | -> E5/R1: the instrument delta is a registered, byte-compared inventory |
| V-R196 | [MJC-5] An extended profile is NOT a macro. The new core's `macros.def` diffe… | consumer | MiSTer / jtframe |
| V-R197 | [MJC-6] A new core missing a `$readmemh` file the reference core carries (a p… | consumer | MiSTer / jtframe |
| V-R198 | [MJC-7] A pin is a tree; a grep proves a fact about the tree you grepped. "Fe… | keep | BBX-7: a grep proves a fact about the tree you grepped; scope the absence claim |
| V-R199 | [MJC-8] Profile selection is a RUNTIME bit in the MRA header, decoded by one… | consumer | MiSTer / jtframe |
| V-R200 | [MJC-9] Find the free header byte by reading the CONSUMER's decoder — the byt… | generalize | -> 3.4: decide a format question from the CONSUMER decoder, not from intent |
| V-R201 | [MJC-10] `[header] fill=0xff` FORCES active-low polarity. The fill must mean… | consumer | MiSTer / jtframe |
| V-R202 | [MJC-11] Scope the header row with `setname=` (`RawData` embeds `Selectable`)… | consumer | MiSTer / jtframe |
| V-R203 | [MJC-12] It is a STATIC configuration bit: written only while the ROM streams… | consumer | MiSTer / jtframe |
| V-R204 | [MJC-13] `[parse] sourcefile` is a SECOND profile gate, in the mapping tool.… | consumer | MiSTer / jtframe |
| V-R205 | [MJC-14] Gate at BOTH ends — source and destination. When the destination sel… | consumer | MiSTer / jtframe |
| V-R206 | [MJC-15] An elaboration-time parameter cannot be gated (`SLOTn_OFFSET`, port… | consumer | MiSTer / jtframe |
| V-R207 | [MJC-16] At jtcores v1.7.3, 64 MB is PHYSICAL, not a setting: the bank-core t… | consumer | MiSTer / jtframe |
| V-R208 | [MJC-17] `JTFRAME_SDRAM_XL` (128 MB) is upstream-only, two chips on one modul… | consumer | MiSTer / jtframe |
| V-R209 | [MJC-18] On a DE10-Nano the dual-SDRAM pin set and the ANALOG I/O board are m… | consumer | MiSTer / jtframe |
| V-R210 | [MJC-19] A jtframe 8-bit SDRAM slot caps at `SDRAMW`, and past it the BUILD F… | consumer | MiSTer / jtframe |
| V-R211 | [MJC-20] Offsets are ADDs (`sdram_addr = offset + …`), elaboration-time, word… | consumer | MiSTer / jtframe |
| V-R212 | [MJC-22] THE SAME ART HAS THREE SIZES — live bytes < address footprint < decl… | consumer | MiSTer / jtframe |
| V-R213 | [MJC-23] Bank arbitration is strict `ba0 > ba1 > ba2 > ba3` (`BAPRIO=1`), so… | consumer | MiSTer / jtframe |
| V-R214 | [MJC-24] Measure bank headroom BEFORE choosing a placement — accesses/frame,… | consumer | MiSTer / jtframe |
| V-R215 | [MJC-26] A download-side `?:` chain has a fall-through arm more regions reach… | consumer | MiSTer / jtframe |
| V-R216 | [MJC-29] A widened bus is only as wide as its NARROWEST port, and Verilog say… | consumer | MiSTer / jtframe |
| V-R217 | [MJC-30] Validate a bit's MEANING against a second implementation, never agai… | generalize | -> 3.3/E4: validate a meaning against a second implementation, not a comment |
| V-R218 | [MJC-31] A read-only decode extension must be checked against EVERY other dec… | consumer | MiSTer / jtframe |
| V-R219 | [MJC-33] Keep a LEDGER of every gated site (expression, file, slice) and a ga… | keep | R1/R2: a ledger of every gated site plus a gate that re-reads each one verbatim |
| V-R220 | [MJC-36] What breaks a finished core, in order of likelihood: growth of a dec… | consumer | MiSTer / jtframe |
| V-R221 | [MJC-37] Simulate in a SCRATCH CLONE outside the repo, never inside the pinne… | generalize | -> D5 sandbox: simulate in a scratch clone, never inside the pinned subject |
| V-R222 | [MJC-39] The download CONSUMES input lines (`sim_inputs.next()` fires from t=… | generalize | -> O5 input integrity: account for the pre-roll; assert what the subject received |
| V-R223 | [MJC-40] A macro named for what you want is not evidence that it does it — re… | keep | 1: a name is not evidence; read the module that consumes it |
| V-R224 | [MJC-41] A dump hook addresses SDRAM, not the CPU bus — a memory-map change I… | generalize | -> rot class 4/6: an observation point tied to a layout dies with the layout |
| V-R225 | [MJC-42] Check NON-CONSTANCY before anything else on a new dump path: an all-… | keep | BBX-5: check NON-CONSTANCY first - an all-zero buffer agreed 99.2% of the time |
| V-R226 | [MJC-43] Assert the DUMP SET is complete — every frame of the window, exact l… | keep | BBX-4 / O1: assert the observation set is complete before comparing anything |
| V-R227 | [MJC-44] A harness that DRIVES a port asserts every bit of it, modelled or no… | generalize | -> D2/O5: the driver asserts every variable it drives, modelled or not |
| V-R228 | [MJC-45] Derive an input bit map from the port's bit ORDER, then confirm it a… | generalize | -> BBX-15 chirality: symmetric content cannot distinguish a swap from a match |
| V-R229 | [MJC-46] Frame output OFF for any state oracle. The harness forks a child per… | consumer | MiSTer / jtframe |
| V-R230 | [MJC-47] Anything that parses a jtsim log de-duplicates by the reporter's own… | keep | BBX-5 / BBX-12: name the implausible value BEFORE the run; parse by field name |
| V-R231 | [MJC-48] An SDRAM read probe has slots in PAIRS on purpose: some arm the wind… | generalize | -> G3: probes come in pairs - windows that must fire beside windows under test |
| V-R232 | [MJC-49] The control shape is two legs differing by ONE BYTE — the profile by… | keep | BBX-5: the control is two legs differing by one byte, built even for a counter |
| V-R233 | [MJC-50] The census asks what is IN memory; the probe asks what was READ. `te…` | generalize | -> O1: presence and use are two different observation questions |
| V-R234 | [MJC-51] A simulation model can be a DIFFERENT PART from the one the design t… | generalize | -> S2/BBX-5: the instrument own identity is a measured fact, not a label |
| V-R235 | [MJC-52] THE INSTRUMENT PROTOCOL: (1) separate the AUTHOR from the VERDICT —… | keep | BBX-5 cites it: THE INSTRUMENT PROTOCOL (author != verdict, prove it fires, etc.) |
| V-R236 | [MJC-53] VRAM is NOT a cross-implementation video oracle — two unrelated impl… | generalize | -> D6: internal state is not a cross-implementation oracle; map at anchors |
| V-R237 | [MJC-54] Never edit a running shell script — `sh` reads by byte offset and a… | keep | portable-floor POSIX fact: sh reads a running script by byte offset |
| V-R238 | [MJC-55] `pgrep -f` waiters match their own command line and never exit. Wait… | keep | portable-floor POSIX fact: a self-matching waiter never exits |
| V-R239 | [MJC-56] A fixture whose meaning depends on the build is a CLAIM about the bu… | keep | BBX-3/S3: a fixture whose meaning depends on the subject version is a CLAIM |
| V-R240 | [MJC-57] Quartus in Docker: keep `--network host` (without it `quartus_map` s… | consumer | MiSTer / jtframe |
| V-R241 | [MJC-58] Fit and timing are SEPARATE verdicts. Never report "closes timing" f… | generalize | -> BBX-14/RO1: a stochastic instrument reports a distribution, not one draw |
| V-R242 | [MJC-59] `xjtcore.sh` calls `jtseed`, which retries `--seed $RANDOM` and BREA… | generalize | -> BBX-14: a retry-until-success runner turns a green into a selection artifact |
| V-R243 | [MJC-60] Failing paths that RESHUFFLE between seeds mean a marginal CONE, not… | consumer | MiSTer / jtframe |
| V-R244 | [MJC-61] A jtcores bitstream carries a `%y%m%d` build datestamp: the same see… | generalize | -> S2/S3: the hash identifies the ARTIFACT, the recipe identifies the RESULT |
| V-R245 | [MJC-62] Release policy: a shipped bitstream is built from a NAMED seed (`jtc…` | generalize | -> S3/BBX-29: a released artifact records its exact reproduction recipe |
| V-R246 | [MJC-63] A set must exist in `$JTROOT/doc/mame.xml` — jtframe's own REDUCED,… | consumer | MiSTer / jtframe |
| V-R247 | [MJC-64] `mra2rom` locates every zip member by CRC32 and by NOTHING ELSE (`na…` | consumer | MiSTer / jtframe |
| V-R248 | [MJC-65] The zip search path is a HARD-CODED `$HOME/.mame/roms/<name>.zip` wi… | generalize | -> D5 sandbox: stage a private HOME for a tool with a hard-coded path |
| V-R249 | [MJC-66] `parts=` puts every part of a `width>8` region inside ONE `<interlea…` | consumer | MiSTer / jtframe |
| V-R250 | [MJC-67] Region-start arithmetic has three silent traps: the generator's `pos…` | consumer | MiSTer / jtframe |
| V-R251 | [MJC-68] `jtframe mra -n` is the ROM-free mode: no zips opened, `md5="None"`,… | generalize | -> R2 tiers: a subject-free tier whose output is a pure function of the catalogue |
| V-R252 | [MJC-69] An MRA part that does not resolve is `0xFF`-FILLED, never refused, s… | generalize | -> D3/D4: a tool that fills instead of refusing manufactures a false green |
| V-R253 | [MJC-72] The 128 MB module's chip-select polarity is inferred from jtframe's… | generalize | -> 3.3: an inferred fact is filed as weaker evidence and names its confirmation |
| V-R254 | [MJC-73] Timing closure is a SEED LOTTERY: a shipped `.rbf` is a passing DRAW… | generalize | -> BBX-14: a green drawn from a lottery is a sample, and the readout says so |
| V-R255 | [MSV-1] The demand figures are merged-m6's — quote them WITH their freeze. PR… | generalize | -> BBX-29: a number is keyed to the subject version it was measured on |
| V-R256 | [MSV-2] The roster's art is 13x every blank tile in vanilla's 32 MB put toget… | consumer | MiSTer build/freeze |
| V-R257 | [MSV-3] Two counts of "group-C live codes" circulate and are different quanti… | generalize | -> 1/BBX-24: two numbers under one name; label the quantity and its provenance |
| V-R258 | [MSV-4] The frozen extents are what a moved extent has to be re-frozen AGAINS… | consumer | MiSTer build/freeze |
| V-R259 | [MSV-5] The map, in bytes: bank 0 — PRG 6 MB at 0, VRAM `0x600000`, ORAM `0x6…` | consumer | MiSTer build/freeze |
| V-R260 | [MSV-6] QSound is SPLIT across two banks on `pcm_addr[23]` — stock DSP banks… | consumer | MiSTer build/freeze |
| V-R261 | [MSV-7] Group C is one obj bank per SDRAM bank, and the assignment is deliber… | consumer | MiSTer build/freeze |
| V-R262 | [MSV-8] The QSound trim is MANDATORY, not an optimisation: mapped verbatim th… | consumer | MiSTer build/freeze |
| V-R263 | [MSV-9] Bank 0 needs SEVEN streams, and upstream's `ram1_Nslots` family stops… | consumer | MiSTer build/freeze |
| V-R264 | [MSV-10] The load is MEASURED on the WIDE image and it is GO — with the cavea… | consumer | MiSTer build/freeze |
| V-R265 | [MSV-11] What cannot grow: the group-C ROMSET REGION. A fifth group-C member… | consumer | MiSTer build/freeze |
| V-R266 | [MSV-12] On `cps2w` work RAM is SDRAM bank 0 byte `0x648000`; `0x600000` — th… | consumer | MiSTer build/freeze |
| V-R267 | [MSV-13] The `$400000-$40000F` reservation is load-bearing THREE ways: ROM on… | consumer | MiSTer build/freeze |
| V-R268 | [MSV-14] The synthesis cannot go stale silently: `tools/mk_mister_page.py --c…` | generalize | -> BBX-18: a document numbers are re-derived and compared, never restated |
| V-R269 | [MSV-15] The `vsavjw` catalogue entry is `vsavj`'s VERBATIM except `sourcefil…` | consumer | MiSTer build/freeze |
| V-R270 | [MSV-16] EVERY romset re-freeze has a MiSTer TAIL, and it is triggered from O… | consumer | MiSTer build/freeze |
| V-R271 | [MSV-17] The QSound extension is its OWN region `qsoundw`, `{ name="qsoundw",…` | consumer | MiSTer build/freeze |
| V-R272 | [MSV-18] ONE-ZIP PACKAGING since 14z-112: the four patched group-A members `v…` | consumer | MiSTer build/freeze |
| V-R273 | [MSV-19] `tools/mister_mra.sh` is the only way to make MRAs and `.rom`s: a sc… | consumer | MiSTer build/freeze |
| V-R274 | [MSV-20] The MiSTer release layer is `release/<name>/mister/` (`release_forma…` | consumer | MiSTer build/freeze |
| V-R275 | [MSV-21] Every MRA part must resolve against the exact zips the card carries:… | consumer | MiSTer build/freeze |
| V-R276 | [MSV-22] The `[STOCK CONTROL]` MRA is the superset invariant ON SILICON — sto… | consumer | MiSTer build/freeze |
| V-R277 | [MSV-23] The frozen anchors: stock `05_timeout_idle` MAME 2146 / sim 2609 / s… | consumer | MiSTer build/freeze |
| V-R278 | [MSV-24] The CPU opponent is the SOUND-STATE-FED LOTTERY (`atlas/ram.md:99`)… | consumer | MiSTer build/freeze |
| V-R279 | [MSV-25] The fetch demonstration (`test_mister_gfxc_fetch`, one-byte control)… | consumer | MiSTer build/freeze |
| V-R280 | [MSV-26] The QSound extension is FETCHED (`test_mister_qsound_ext`): 210,180… | consumer | MiSTer build/freeze |
| V-R281 | [MSV-27] `test_mister_wide_inert` is THE inertness instrument: `cps2` and `cp…` | consumer | MiSTer build/freeze |
| V-R282 | [MSV-28] To reach a tenant use `36_pick_tenant_cell` (Donovan `0x13`), `37_pi…` | consumer | MiSTer build/freeze |
| V-R283 | [MSV-29] The OBJ-list oracle (`test_mister_obj_oracle`): the promoted subset… | consumer | MiSTer build/freeze |
| V-R284 | [MSV-30] Know the gate inventory and its cost before promising a re-run: ci_p… | keep | 6/BBX-27: know the gate inventory and its cost before promising a re-run |
| V-R285 | [MSV-31] Hardware answers what simulation cannot — pixels seen, voices heard,… | keep | BBX-30 cites MSV-31: "verified on hardware" meant "judged by eye", and it said so |
| V-R286 | [MSV-32] Never field-test without the control: `[STOCK CONTROL]` on the same… | generalize | -> BBX-5: a field trial carries its control leg on the same instrument |
| V-R287 | [MSV-33] Report the failure MODE, and time it: no sync vs black vs RAM-test l… | keep | BBX-11/BBX-4: report the failure MODE and time it - the mode is the finding |
| V-R288 | [MSV-34] FIELD REPORTS ARE RECORDINGS (CLAUDE.md §4, ruled 14z-111): a board… | keep | BBX-28 cites MSV-34: a field report is captured as a reproducible recording first |
| V-R289 | [MSV-35] The board is a WITNESS, not the instrument, and a board crash is not… | keep | BBX-28 cites MSV-35: the board is a WITNESS, not an instrument |
| V-R290 | [MSV-36] The field bundles live OUTSIDE the repo (rule 7) and their READMEs g… | generalize | -> BBX-22: the retraction sweep covers every artifact that has left the tree |
| V-R291 | [VSP-1] THE SUPERSET INVARIANT (CLAUDE.md §1) — every path not involving the… | generalize | -> acceptance contract (3.1): the untouched subset compares EXACT vs reference |
| V-R292 | [VSP-2] Rule 1 v2 (§2.1) — emulator cores are never modified outside the rati… | generalize | -> S1: instrument changes only under a ratified, gated KIND PROFILE |
| V-R293 | [VSP-3] Rule 2 (§2.2) — no untested change survives a session; "it should be… | keep | BBX-26 cites VSP-3: no untested change |
| V-R294 | [VSP-4] Rule 3 (§2.3) — no hand-edited binaries. Output bytes come from `buil…` | generalize | -> S2/S3: no hand-edited artifacts; output comes from the pipeline |
| V-R295 | [VSP-5] Rule 4 (§2.4) — every byte range is tagged VSAV / VS2 / VH2 / GEN / N… | generalize | -> E3: every region tagged with its provenance, in the same commit |
| V-R296 | [VSP-6] Rule 5 (§2.5) — anything that defines how a ported character plays li… | generalize | -> L2 fact tables: behaviour in declared tables, not baked in code |
| V-R297 | [VSP-7] Rule 6 (§2.6) — a diverging regression suite halts forward work; fixi… | keep | BBX-26 cites VSP-7: a diverging suite halts feature work |
| V-R298 | [VSP-8] Rule 7 (§2.7) — no ROM bytes in the repo or any artifact, in any form… | consumer | game / licence (rule 7) |
| V-R299 | [VSP-9] The reference sets (§3) — `vsavj` (Japan 970519, DECIDED, do not reop… | generalize | -> S2: the reference SUBJECT SET lives outside the tree and is hash-locked |
| V-R300 | [VSP-10] Gameplay decisions are not yours (§5) — anything a player could feel… | keep | 3.1 / 8: decisions the maintainer owns go to the rulings queue with options |
| V-R301 | [VSP-11] Address notation (§5) — `PRG:0x0F1234`, `CPU:$0F1234`, `GFX:tile 0x1…` | generalize | -> S4/BBX-16: every address carries the namespace of the view it is read in |
| V-R302 | [VSP-12] File by the FACT (§5) — `docs/game/` (true if the hack were abandone… | generalize | -> the outlives-the-project question decides a document home (5 bins) |
| V-R303 | [VSP-13] RETRACTION DISCIPLINE (§5, standing order 14z-71) — when a claim cha… | keep | BBX-22 cites VSP-13: retraction discipline, grep the wording everywhere |
| V-R304 | [VSP-14] BUG ARCHAEOLOGY FIRST (§5, standing order 14z-75) — before theorisin… | keep | BBX-23 cites VSP-14: archaeology before theory |
| V-R305 | [VSP-15] Anti-hyperfocus checkpoint (§5) — at a confirmed finding, a green su… | keep | BBX-27 cites VSP-15: anti-hyperfocus checkpoint at a fixed cadence |
| V-R306 | [VSP-16] Build conventions (§5) — builders are Python taking `(src, out)` so… | generalize | -> D1: a fixed argument contract makes tools chain; every tunable is a flag |
| V-R307 | [VSP-162] The `14z-N` session tag is the archive's LOOKUP KEY (§5) — one SESS… | keep | BBX-29 / 6: the session tag is the archive lookup key |
| V-R308 | [VSP-17] The session ritual (§5) — begin by reading STATE (after CLAUDE.md, t… | keep | 6: the session ritual, read-then-update |
| V-R309 | [VSP-18] THE PERSISTENT SUITE DOCTRINE (§4) — every in-emulator measurement,… | keep | 6.4: every in-emulator measurement becomes a scripted case |
| V-R310 | [VSP-19] Verdict logic is itself tested (§4) — a classifier's verdicts are va… | keep | BBX-2 cites VSP-19: verdict logic validated in both directions |
| V-R311 | [VSP-166] Where the classifier LOOKS is validated too, and never from our own… | keep | 3.4 cites VSP-166: where the classifier looks is validated, never from our generator |
| V-R312 | [VSP-20] FIELD REPORTS ARE RECORDINGS (§4, maintainer-ruled 14z-111) — every… | keep | BBX-28: a field report is a recording before it is a theory |
| V-R313 | [VSP-21] STANDING PRINCIPLE: vanilla wins ties (maintainer, 2026-08-05) — whe… | consumer | game design ruling |
| V-R314 | [VSP-22] THE DEADNESS REGISTER's rule for a row — a claim of the form "legacy… | keep | BBX-7 cites VSP-22: an absence claim needs a control, a guard and a fallback |
| V-R315 | [VSP-23] The register is the FIRST place to look for any unexplained regressi… | keep | BBX-23: the absence register is the first place archaeology looks |
| V-R316 | [VSP-24] Which emulator runs which oracle (§4, corrected GitHub #78) — MAME i… | consumer | MAME/FBNeo instrument assignment |
| V-R317 | [VSP-25] Dual-track inertness means bit-identical UP TO SELECT ENTRY (ratifie… | consumer | build/freeze |
| V-R318 | [VSP-26] The two FBNeo-only phase classes are FROZEN INVENTORIES, not windows… | generalize | -> C1/E5: a frozen INVENTORY class instead of a widened window |
| V-R319 | [VSP-27] Hooked-build legacy comparison is live RAM under exactly the named m… | generalize | -> O2: the comparison is defined by its declared mask (BASIS) |
| V-R320 | [VSP-28] The bounded re-convergent window (`oracle_classes.md` v3) needs all… | generalize | -> C1: every non-exact class is a measured mechanism, frozen per scenario |
| V-R321 | [VSP-29] Composite (`oracle_classes.md` v4) is the strict CONJUNCTION of flic… | generalize | -> C1: composite is a conjunction of mechanisms and adds NO tolerance |
| V-R322 | [VSP-30] The ≥60-frame rule is INTRA-MECHANISM (`oracle_classes.md` v5, maint… | generalize | -> C1: a threshold scope is ruled, not assumed |
| V-R323 | [VSP-31] THE STANDING WATCH (maintainer, 2026-07-27) — flickers growing beyon… | keep | BBX-13: a growing tolerance inventory means stop and root-cause |
| V-R324 | [VSP-32] Dual-emulator agreement for tenant content (§4) — patched FBNeo and… | generalize | -> D6/3.3: a second implementation corroborates on MAPPED fields only |
| V-R325 | [VSP-33] Test-matrix growth and the edge-case bias (§4) — every new capabilit… | generalize | -> BBX-18: the SCENARIO matrix is designed and its bias is stated |
| V-R326 | [VSP-34] A masked window is a BASIS, not a flag — `replay.lua` skips masked b… | generalize | -> O2: the BASIS is part of the observation identity; changing it re-bases it |
| V-R327 | [VSP-35] Palette CONTENT in a fade's row set is CYCLE-relevant — data-only is… | consumer | game engine |
| V-R328 | [VSP-36] A self-frozen `.sha1` cannot see a legacy regression — it answers "d… | keep | E4: a self-frozen expectation answers only "did this change since I froze it" |
| V-R329 | [VSP-37] A comparator's own ground-truth test can RATIFY a deviation from the… | keep | BBX-15 / 3.4: artifacts agreeing with each other cannot ratify a deviation |
| V-R330 | [VSP-38] Moving a frozen ONSET frame can silently cross a verification-class… | generalize | -> E6/C1: a re-freeze can cross a class boundary; re-derive the class at freeze |
| V-R331 | [VSP-39] A hook on a hot shared path can flip a frame-boundary parity PERMANE… | consumer | game engine |
| V-R332 | [VSP-40] Build in one step, measure in another — never in one command — a cha… | keep | BBX-5: build in one step, measure in another - never one command |
| V-R333 | [VSP-41] Engine hooks break whole-RAM comparison BY CONSTRUCTION — a hook on… | generalize | -> C1: a structural perturbation is a class question, never a tolerance |
| V-R334 | [VSP-42] GFX and coordinate data are INVISIBLE to every RAM-basis gate — tile… | generalize | -> RO2/S4: what a basis cannot see is declared |
| V-R335 | [VSP-43] The RAM gate cannot see NEW-CHARACTER visual wrongness — masked gate… | generalize | -> RO2/S4: what a basis cannot see is declared |
| V-R336 | [VSP-44] Sound and anything leaving the 68k address space needs its own detec… | generalize | -> O4: a channel outside the basis needs its own observation |
| V-R337 | [VSP-45] Sibling-coincident engine refs are INVISIBLE to the diff oracle, and… | consumer | game / port RE |
| V-R338 | [VSP-46] Bare-long "pointers" in 68k code are usually OPERAND PAIRS — sibling… | consumer | game / port RE |
| V-R339 | [VSP-47] PC-relative word tables are DATA — record every discovered table's f… | consumer | game / port RE |
| V-R340 | [VSP-48] Blind long relocation over data blobs corrupts streams — for a MIXED… | consumer | game / port RE |
| V-R341 | [VSP-49] Never write an unverified gap — a gap between known tables is not a… | consumer | game / port RE |
| V-R342 | [VSP-50] Disabling a heuristic CLASS wholesale reverts load-bearing writes —… | consumer | game / port RE |
| V-R343 | [VSP-51] Early generic rows can masquerade over later-understood structures —… | consumer | game / port RE |
| V-R344 | [VSP-52] Sibling twins can differ by ONE hoisted instruction — when a source… | consumer | game / port RE |
| V-R345 | [VSP-53] Fuzzy code-similarity reconciliation collapses near-identical helper… | consumer | game / port RE |
| V-R346 | [VSP-54] Twin sites are paired through the DISPATCH TABLE, never by byte patt… | consumer | game / port RE |
| V-R347 | [VSP-55] A single-shift sibling scan dies at the newcomer window's PIECEWISE… | consumer | game / port RE |
| V-R348 | [VSP-56] A window constant is a census, not a fact — the newcomer-code window… | generalize | -> 1/BBX-24: a census is not a law; a derived constant carries its provenance |
| V-R349 | [VSP-57] CODE-region ref fields are word-aligned by ISA; an idempotence latch… | consumer | game / port RE |
| V-R350 | [VSP-58] A SET-NAME MISMATCH IS A FALSE GREEN — when a build's pack flips to… | generalize | -> S2/O5: a subject-name mismatch is a false green; report what was opened |
| V-R351 | [VSP-59] PC-relative escapes in engine-style regions are INVISIBLE to the sib… | consumer | game / port RE |
| V-R352 | [VSP-60] A tenant porting SHARED regions inherits every region-scoped mechani… | consumer | game / port RE |
| V-R353 | [VSP-61] A `port_patch` on a shared engine-family region fixes ONE tenant's c… | consumer | game / port RE |
| V-R354 | [VSP-62] The four checks before rooting a code region — bound by the sibling… | consumer | game / port RE |
| V-R355 | [VSP-63] Crypt placement: the opcode view executes, the data view is garbage… | consumer | game / port RE |
| V-R356 | [VSP-64] Put an execution breakpoint on code before attributing a symptom to… | keep | BBX-23: co-location is not causation; probe before attributing |
| V-R357 | [VSP-65] A ported region must CONTAIN its constants and its tables — pc-relat… | consumer | game / port RE |
| V-R358 | [VSP-66] A dead-filler classifier comparing the OPCODE view is blind to DATA… | generalize | -> BBX-16: a classifier declares its view; the other view is its blind spot |
| V-R359 | [VSP-67] `placements.json` is a LINEAR map but the extractor auto-discovers s… | consumer | game / port RE |
| V-R360 | [VSP-68] A built-image walk VERIFIES the source's structure; it never re-deri… | generalize | -> 3.4/E4: a walk over the built artifact verifies structure, never re-derives it |
| V-R361 | [VSP-69] Every TYPE/CLASS byte in a ported record indexes some engine dispatc… | consumer | game / port RE |
| V-R362 | [VSP-70] "Entry N" past the end of a word-displacement jump table is the NEXT… | consumer | game / port RE |
| V-R363 | [VSP-71] "16 + the same 16 repeated" is a silent tenant trap — an aliasing gu… | consumer | game / port RE |
| V-R364 | [VSP-72] Hole "a" is inside the crypt range; thunks with EMBEDDED DATA go to… | consumer | game / port RE |
| V-R365 | [VSP-73] A slot id or a PLACED ADDRESS baked into hand-authored `thunk_hex` t… | consumer | game / port RE |
| V-R366 | [VSP-74] The manifest subset: no dotted table names, no arrays — `_minitoml`… | generalize | -> O3: two parsers of one format are diffed, or the format is narrowed |
| V-R367 | [VSP-75] Descriptor CRCs for variable-content members are SENTINELS — a real… | consumer | game / port RE |
| V-R368 | [VSP-76] An output dir that different modes populate differently is cleaned p… | keep | D5 hygiene: the output tree is cleaned before the run |
| V-R369 | [VSP-77] Exactly one writer per WORD — `patch_prg.py` hard-fails on overlap n… | generalize | -> R1/E5: exactly one owner per slot; an overlap hard-fails naming both |
| V-R370 | [VSP-78] The tile-placement pool is block-aware first-fit — carving cells out… | consumer | game / port RE |
| V-R371 | [VSP-79] "Inside the placed band window" is NOT "overwritten" — the placement… | consumer | game / port RE |
| V-R372 | [VSP-80] In-place substitution has invisible slot dependencies — every per-sl… | consumer | game / port RE |
| V-R373 | [VSP-81] A record's BANK is a property of the DRAWING OBJECT, not of the reco… | consumer | game / port RE |
| V-R374 | [VSP-82] The A5-relative work-var displacements differ between engines — a po… | consumer | game / port RE |
| V-R375 | [VSP-83] A ported region whose consumers index NEGATIVELY needs headroom belo… | consumer | game / port RE |
| V-R376 | [VSP-84] Route objects to a ported machine on facts baked at BUILD time — dis… | consumer | game / port RE |
| V-R377 | [VSP-85] The discriminator for a ported machine is PER-EFFECT, not per-hit or… | consumer | game / port RE |
| V-R378 | [VSP-86] An "owner-gated" thunk on a shared routine is not scoped by owner al… | consumer | game / port RE |
| V-R379 | [VSP-87] Per-char dispatch on a COMMON seq state needs the target flow's FULL… | consumer | game / port RE |
| V-R380 | [VSP-88] A stamp census must enumerate WRITE FORMS — the `move.b #imm,(d16,An…` | generalize | -> BBX-9: a census enumerates every FORM, or it is a sample |
| V-R381 | [VSP-89] Sampled uniformity is not uniformity — disassemble ALL N sibling cas… | keep | 1 / BBX-7: sampled uniformity is not uniformity |
| V-R382 | [VSP-90] "Run once at match start" is a TIMING TRAP — char-init, the first pe… | consumer | game / port RE |
| V-R383 | [VSP-91] A 0x7xx sfx id's faithfulness is a property of its CONTENT, not its… | consumer | game / port RE |
| V-R384 | [VSP-92] The tenant manifest's slot-row vocabulary is how a row states its te… | consumer | game / port RE |
| V-R385 | [VSP-93] The hardening register is maintained in the SAME commit as any chang… | generalize | -> E3/R1: the register moves in the same commit as the change it describes |
| V-R386 | [VSP-94] Every frozen build is git-tagged `freeze/<name>` (annotated, fingerp… | keep | E3/R3: every freeze is tagged with its fingerprint and reproduction, plus a row |
| V-R387 | [VSP-95] A profile bump invalidates every frozen build as a REFERENCE (not as… | keep | rot class 4: a profile bump invalidates references; a re-freeze re-points gates |
| V-R388 | [VSP-96] Build-dir policy: keep CURRENT + ONE BACK per track, and before dele… | generalize | -> BBX-22/R1: sweep every reference before deleting a referenced artifact |
| V-R389 | [VSP-97] "Tracked" build dirs are only PARTLY tracked — a gate whose fixture… | generalize | -> G5/rot class 4: a fixture reproducibility is a registered property |
| V-R390 | [VSP-98] Recordings: tracked the moment they have a consumer, deleted the mom… | keep | BBX-9: tracked when it has a consumer, deleted when it has none - both ways |
| V-R391 | [VSP-99] `patch_index.md` is the REGISTRY, updated in the same commit as any… | keep | R1: the registry is updated in the same commit as the change |
| V-R392 | [VSP-100] A release is ONE DIRECTORY PER PLATFORM, each self-sufficient, with… | consumer | release layout |
| V-R393 | [VSP-101] THE PRE-COMMIT COMMAND is `tests/run_all_static.sh`, not a filename… | keep | R2/G4: the entry point is the tier, not a filename |
| V-R394 | [VSP-164] THE EMULATOR-TIER COMMAND is `tests/run_all_emulator.sh`, and `test…` | generalize | -> R2: the sweep registry is where a release scope is DECLARED |
| V-R395 | [VSP-165] A FROZEN EXPECTATION MUST SAY WHERE ITS NUMBERS CAME FROM, and a ga… | keep | E3 + G2: a frozen expectation says where its numbers came from |
| V-R396 | [VSP-176] A `${VAR:?}` demand after an armed EXIT trap EXITS 0 on macOS bash… | keep | G1/BBX-1: a gate that never ran must not read as PASS (bash 3.2 floor, R3) |
| V-R397 | [VSP-177] A comment never follows a line-continuation backslash — `cmd` | keep | portable-floor sh fact + the control that provisions from nothing and RUNS it |
| V-R398 | [VSP-178] A frozen expectation FOLLOWS whatever moves it, whatever the gate's… | keep | E3/rot class 4: a frozen expectation follows whatever moves it |
| V-R399 | [VSP-102] Close stdin when looping a gate list (`</dev/null`) — a gate that r… | keep | runner hygiene: close stdin when looping a gate list |
| V-R400 | [VSP-103] A gate that is not in the battery can sit FAILING for sessions — wh… | keep | BBX-9 rot class 1: a gate outside the battery can sit failing for sessions |
| V-R401 | [VSP-104] A self-test's deliberate FAIL must never write into the shared evid… | keep | G3: a deliberate control failure never writes into the shared evidence path |
| V-R402 | [VSP-105] Propagate a builder's status: redirect, never pipe through `tail`;… | generalize | -> D4: never swallow the driver status; assert the SUBJECT SET exists first |
| V-R403 | [VSP-106] A rompath is a SEARCH PATH — name the set you actually built — `bui…` | generalize | -> S2: a subject set is a SEARCH PATH; name the subject you actually built |
| V-R404 | [VSP-107] `BUILD=... tests/<gate>.sh` is SILENTLY IGNORED by positional-arg g… | keep | D3: a variable the gate cannot honour must refuse, never be ignored |
| V-R405 | [VSP-108] Canonicalise every path argument at the top and print the resolved… | keep | O5/S2: canonicalise every path and print the fingerprint of what was opened |
| V-R406 | [VSP-109] A gate must not depend on the caller's environment — a relative `RO…` | keep | D5/BBH-35: a gate must not depend on the caller environment |
| V-R407 | [VSP-110] The shell traps that fail silently and leave a plausible result — b… | keep | portable-floor: the shell traps that fail silently and leave a plausible result |
| V-R408 | [VSP-111] awk compares hex fields as NUMBERS when they look numeric, and macO… | keep | R3 portability floor: a platform guard needs a gate on both platforms |
| V-R409 | [VSP-112] `grep -c ...` | keep | BBX-4: ABSENT, ZEROS and a value are three outcomes, never two |
| V-R410 | [VSP-113] A worktree branches from a STALE `origin/main` — `git reset --hard…` | keep | project-blind VCS fact: a worktree branches from a stale remote ref |
| V-R411 | [VSP-114] A verdict line may state only what its OWN branch measured — a PASS… | keep | G2/RO3: a verdict line states only what its own branch measured |
| V-R412 | [VSP-115] A two-leg A/B that bails on leg 1 leaves leg 2's health an ASSUMPTI… | keep | BBX-5/BBX-4: a two-leg A/B that bails leaves the other leg an assumption |
| V-R413 | [VSP-116] A pre-armed attribution is a hypothesis — comments and headers may… | keep | 1/RO1: a pre-armed attribution is a hypothesis; the verdict prints measurements |
| V-R414 | [VSP-117] The masked-basis canary corrupted the basis it verified — an explic… | generalize | -> D5/G3: a control must not write into the basis it verifies |
| V-R415 | [VSP-118] `_PRG_RE` must match the WIDE extension members — two builds differ… | generalize | -> S2: the identity function must cover every member of the subject set |
| V-R416 | [VSP-119] RECORD YOUR SESSION — `WIDE_RECORD=<name> tools/run_wide.sh <build>…` | consumer | rig / MAME-Lua instrument |
| V-R417 | [VSP-120] Forced-pick pokes on `$FF8782`/`$FF8B82` end by ~frame 1500 — the e… | consumer | rig / MAME-Lua instrument |
| V-R418 | [VSP-121] The forced pick does not populate the HUD index field — in-match HU… | consumer | rig / MAME-Lua instrument |
| V-R419 | [VSP-122] The forced pick holds through SELECT only — on a vs-CPU rig P2's lo… | consumer | rig / MAME-Lua instrument |
| V-R420 | [VSP-123] The native leg is reachable for ANY tenant screen with the same pok… | consumer | rig / MAME-Lua instrument |
| V-R421 | [VSP-124] Rig grammar from the #103 hunt — an opponent-class poke overlapping… | consumer | rig / MAME-Lua instrument |
| V-R422 | [VSP-125] Kill/heal pokes write BOTH HP words — the 2-byte shape leaves white… | consumer | rig / MAME-Lua instrument |
| V-R423 | [VSP-126] Probe ENTRY addresses, never the crash-line PC — a group-0 exceptio… | consumer | rig / MAME-Lua instrument |
| V-R424 | [VSP-127] One dump directory per rig iteration, one LOG directory per leg — `…` | generalize | -> D5: one output tree per run; legs never share artifacts |
| V-R425 | [VSP-128] `DUMPS` windows are `;`-separated — a comma-joined spec exits rc=3… | consumer | rig / MAME-Lua instrument |
| V-R426 | [VSP-129] Cross-build A/B dumps run PROBE-FREE — debugger overhead lands at d… | generalize | -> O2: the probe changes the timeline; A/B legs must share one basis |
| V-R427 | [VSP-130] `-debug` fire counts do not transfer to checksum timelines — on col… | generalize | -> O2: counts taken under one basis do not transfer to another |
| V-R428 | [VSP-131] A scripted motion input is NOT the move you named — the buffer fold… | generalize | -> O5 input integrity: assert what the subject received, not what was scripted |
| V-R429 | [VSP-132] A 1P-arcade rig is silently pinned to the ARCADE DRAW — any timing… | consumer | rig / MAME-Lua instrument |
| V-R430 | [VSP-133] A fixed-frame rig verified on ONE character whiffs on another — rea… | consumer | rig / MAME-Lua instrument |
| V-R431 | [VSP-134] An entry-set A/B needs a QUIET-FRAME presence profile — an entry pr… | generalize | -> BBX-5: a difference control needs a quiet-baseline profile |
| V-R432 | [VSP-135] Cross-emulator POSITION A/B compares RELATIVE offset, never absolut… | generalize | -> D6: compare relative anchors across implementations, never absolutes |
| V-R433 | [VSP-136] Cross-game pixel A/B aligns by DISPLAYED RECORD, not frame — the en… | generalize | -> D6/O1: align two implementations at anchors each finds on its own |
| V-R434 | [VSP-137] A soak must assert the mechanism it exists to exercise fired — scri… | keep | G3/BBX-6: a soak asserts the mechanism it exists to exercise actually fired |
| V-R435 | [VSP-138] Validate a fix against the reporter's EXACT input and A/B fix-on/fi… | keep | BBX-28/BBX-2: validate against the reporter exact input, fix-on/fix-off |
| V-R436 | [VSP-139] Verify at the RENDER layer before believing the RAM layer — `tests/…` | generalize | -> S4/BBX-15: verify at the layer the claim is about |
| V-R437 | [VSP-140] The masked checksum samples EARLIER in the frame than dumps do, and… | generalize | -> O2: two observations at different points of the cycle are different bases |
| V-R438 | [VSP-141] A RENDER verdict from a gfx-free build is VOID — the program-over-z… | generalize | -> S2/RO2: the subject set must contain what the claim is about |
| V-R439 | [VSP-142] Resolve `placements.json` regions PER TENANT — a lookup hardcoding… | consumer | rig / MAME-Lua instrument |
| V-R440 | [VSP-143] A cited address in a session log is a CLAIM — grep the manifest row… | keep | 1: a cited number in a log is a filed claim; re-derive before building on it |
| V-R441 | [VSP-144] A pool measurement is a claim about ONE pool — decode the dispatch… | generalize | -> 1: the scope of a measurement is part of the number |
| V-R442 | [VSP-145] A0-at-write is post-increment — subtract the movem batch size from… | consumer | rig / MAME-Lua instrument |
| V-R443 | [VSP-146] A same-slot "vanilla control" controls nothing — to test "is X per-… | keep | BBX-5: a control must vary the variable under test, not a neighbouring one |
| V-R444 | [VSP-147] The `-debug` write-trace never disagreed with the non-debug dumps —… | consumer | rig / MAME-Lua instrument |
| V-R445 | [VSP-148] A measurement returning a clean null is a bug report about the meas… | keep | BBX-5: a measurement returning a clean null is a bug report about the measurement |
| V-R446 | [VSP-149] Half the Lua instruments stage inputs one frame off `replay.lua` —… | consumer | rig / MAME-Lua instrument |
| V-R447 | [VSP-150] THE OUT-OF-RANGE INDEX TOOLKIT: read the CONSEQUENCE before valuing… | generalize | -> BBX-7: prove the instrument fires before trusting quiet; value by consequence |
| V-R448 | [VSP-151] The DF palette-seq block census is MEASURED, never derived from the… | generalize | -> 3.4: derive from behaviour, never from the table that describes it |
| V-R449 | [VSP-152] The M2a debug environment on `run_replay_guarded.sh` — `GUARD_PROBE…` | consumer | rig / MAME-Lua instrument |
| V-R450 | [VSP-167] A ported character's reference is the SOURCE GAME, and a second leg… | keep | 3.3: our own second leg is an INSTRUMENT CHECK, never a baseline |
| V-R451 | [VSP-168] Check the mechanism can PRODUCE the observable, and exclude the one… | keep | BBX-5/BBX-7: check the mechanism can produce the observable; exclude those that cannot |
| V-R452 | [VSP-169] Compare the ORDERED sequence of states, not sets; assert structure… | generalize | -> C1: an ordered-sequence comparator; a set comparison is blind to order |
| V-R453 | [VSP-170] Refuse to judge a leg that did not produce the event, and assert an… | keep | D4/BBX-1: refuse to judge a leg that did not produce the event |
| V-R454 | [VSP-171] Measure the cost of widening before arguing about the sample, and r… | keep | E6/BBX-13: widening is a measurement; re-check every constant it froze |
| V-R455 | [VSP-172] Diff a strengthened gate against what it REPLACED — widening silent… | keep | BBX-6/rot: diff a strengthened gate against what it replaced |
| V-R456 | [VSP-173] Capture the picture BEFORE writing the sentence about it — a correc… | keep | 1: capture the evidence before writing the sentence about it |
| V-R457 | [VSP-174] A "divergence" appearing exactly on the rows with a special resolut… | generalize | -> BBX-23: a divergence that matches a known mechanism is the instrument |
| V-R458 | [VSP-175] A gate is SOLO-SPECIFIC only if a single-tenant build is the SUBJEC… | generalize | -> R2: a gate tier is decided by the SUBJECT of its assertion |
| V-R459 | [VSP-179] When the defect class is an OMISSION, ENUMERATE — targeted testing… | keep | BBX-9/BBX-18: an omission class is reached by enumeration, never by targeting |
| V-R460 | [VSP-180] ANCHOR, THEN ENUMERATE — pay the expensive measurement once to prov… | keep | BBX-5: prove the cheap measurement predicts the expensive one, then enumerate |
| V-R461 | [VSP-153] A symptom grouping is a HYPOTHESIS — test it member by member, chea… | keep | BBX-23: a symptom grouping is a hypothesis; test member by member |
| V-R462 | [VSP-154] Cross-build A/B is the cheapest attribution and is routinely skippe… | keep | BBX-23: the cheapest attribution first |
| V-R463 | [VSP-155] PRIOR ART FIRST — before porting a per-character subsystem for tena… | keep | BBX-23: prior art before new measurement |
| V-R464 | [VSP-156] A REPLAY'S NAME IS A CLAIM ABOUT THE BUILD — an artifact whose mean… | generalize | -> S3/BBX-29: a scenario name is a claim about the subject version; assert it |
| V-R465 | [VSP-157] Root-causing a stochastic crash: determinism first, vector+address… | keep | BBX-14/BBX-23: determinism first, then vector, then history |
| V-R466 | [VSP-158] Never chain "the fix" from a poke-contaminated mechanism when a fie… | generalize | -> 3.3/BBX-28: a witness recording outranks a poke-contaminated rig |
| V-R467 | [VSP-163] A tenant replay is a claim about the build it was authored for, and… | generalize | -> S3/BBX-15: a confound check is not an identity check |
| V-R468 | [VSP-159] The four questions of any ported effect, in order — does the host h… | consumer | game / port RE |
| V-R469 | [VSP-160] Strip placement: inventories are SPANS, not samples — the `--strip-…` | generalize | -> E5: inventories are SPANS, not samples |
| V-R470 | [VSP-161] The order of work and the check at each step, ending with LOOKING —… | keep | 6.3: small verified steps, ending with looking at the result |
| V-R471 | [VSE-1] vsav, vsav2 and vhunt2 are three official builds of ONE engine, and t… | consumer | game engine |
| V-R472 | [VSE-2] THE DEAD-ROW CLASS is the most common defect shape: vsav ships table… | consumer | game engine |
| V-R473 | [VSE-3] Sibling-COINCIDENT values are invisible to the diff, and the coincide… | consumer | game engine |
| V-R474 | [VSE-4] The A5 work-variable layouts differ between generations by a uniform… | consumer | game engine |
| V-R475 | [VSE-5] vsavj keeps byte-identical COPIES of engine code inside per-character… | consumer | game engine |
| V-R476 | [VSE-6] Engine-generation TUNING drifts inside structurally identical twins:… | consumer | game engine |
| V-R477 | [VSE-7] vs2 WIDENED index consumers vsav left narrow, and every such widening… | consumer | game engine |
| V-R478 | [VSE-8] A PC on one leg does not name the same routine on the other — and SOM… | generalize | -> D6: cross-implementation identity needs mapped anchors; coincidence is the trap |
| V-R479 | [VSE-9] The per-character bank is a contiguous run of 32-entry tables, stride… | consumer | game engine |
| V-R480 | [VSE-10] The character id is 5-bit everywhere it is stored; the variant half… | consumer | game engine |
| V-R481 | [VSE-11] A register-dataflow walk cannot see a mask applied straight to a mem… | generalize | -> BBX-16/RO2: an extractor blind class is a property of its method; scan for it |
| V-R482 | [VSE-12] Count a table's rows from the DATA view and the indexer's arithmetic… | generalize | -> 3.4: count from the consumer arithmetic, never from where bytes stop looking familiar |
| V-R483 | [VSE-13] Reserved variant ids: `0x12` (the Gallon-variant path writes it as a… | consumer | game engine |
| V-R484 | [VSE-14] No legacy gameplay path in the corpus writes a variant-half id (unio… | consumer | game engine |
| V-R485 | [VSE-15] "Selectable" is not "fightable": the CPU opponent, the attract assig… | consumer | game engine |
| V-R486 | [VSE-16] Per-character table entries are PAIRS more often than they look: a 1… | consumer | game engine |
| V-R487 | [VSE-17] Match-init id normalisation folds `0x0B`/`0x1B` (the Shadow/Marionet… | consumer | game engine |
| V-R488 | [VSE-18] Every secondary object is ticked by a per-frame POOL WALKER that dis… | consumer | game engine |
| V-R489 | [VSE-19] "Never dispatched in the corpus" is not "free": the corpus-observed… | generalize | -> BBX-7/BBX-18: an absence over a corpus is a coverage artefact until swept |
| V-R490 | [VSE-20] Row 8 of the big type table is the SHARED companion machine, and vs2… | consumer | game engine |
| V-R491 | [VSE-21] Type stamps have TWO forms — a header long `move.l #$01xxTTss,(A4)`… | generalize | -> BBX-9: a census that scans one form undercounts; enumerate every form |
| V-R492 | [VSE-22] vanilla never seeds the secondary-object pools during a normal match… | consumer | game engine |
| V-R493 | [VSE-23] The two generations' allocators differ in semantics: a RECYCLED slot… | consumer | game engine |
| V-R494 | [VSE-24] A shared engine routine may be reached by a ported character through… | consumer | game engine |
| V-R495 | [VSE-25] Anim nodes are `0x18` bytes: duration, flags (bit 7 = follow the LIN… | consumer | game engine |
| V-R496 | [VSE-26] Anim NUMBERS: facing adds `0x300`; set-anim QUEUES a (number, params… | consumer | game engine |
| V-R497 | [VSE-27] The drawer: object `+0x1C` → node → node+4 → the SPRITE LIST; the li… | consumer | game engine |
| V-R498 | [VSE-28] Types 4, 6 and 8 bias every emitted tile code by a GAME-SPECIFIC con… | consumer | game engine |
| V-R499 | [VSE-29] Bank attribution is per-RECORD and per-LIST-TYPE, never per-characte… | consumer | game engine |
| V-R500 | [VSE-30] Every list handler debits its declared budget from the frame's share… | consumer | game engine |
| V-R501 | [VSE-31] OBJ record formats differ in ENTRY STRIDE (format 2: 4-byte `(tile,…` | consumer | game engine |
| V-R502 | [VSE-32] Record walks that follow POINTERS miss OFFSET-COMPUTED records (aux… | generalize | -> BBX-9: an enumerator following one reference form misses the computed ones |
| V-R503 | [VSE-33] A multi-tile sprite walks `tile = base + row*0x10 + ((base + col) &…` | consumer | game engine |
| V-R504 | [VSE-34] The per-character OBJ bank word is NOT display-only — changing a row… | generalize | -> BBX-7: "display-only" is a claim; measure the state it is assumed not to touch |
| V-R505 | [VSE-35] A character's band is NOT free once the character is replaced — syst… | consumer | game engine |
| V-R506 | [VSE-36] OBJ RAM diffing traps: entries relocate between frames ("who writes… | consumer | game engine |
| V-R507 | [VSE-37] To identify what an effect draws, diff the OBJ list before vs during… | generalize | -> D6: join two legs by CONTENT, never by index |
| V-R508 | [VSE-38] THE ANCHOR METHOD for "this effect does not draw": start from the da… | generalize | -> BBX-7: prove an absence from the data the mechanism is forced to read |
| V-R509 | [VSE-39] Grouping by SYMPTOM sends the search after a single root that may no… | keep | BBX-23: grouping by symptom presumes a single root that may not exist |
| V-R510 | [VSE-40] Two parallel damage APPLIERS (fighter-hit stager, object-hit applier… | consumer | game engine |
| V-R511 | [VSE-41] THE ROUND JUDGE KILLS ON THE SIGN OF WHITE HP (`+0x52`), never `+0x5…` | consumer | game engine |
| V-R512 | [VSE-42] The victim's reaction class byte (`+0x54`, from the hit record) inde… | consumer | game engine |
| V-R513 | [VSE-43] In the hit-spark spawner `a1` is the VICTIM and `a6` the attacker; a… | consumer | game engine |
| V-R514 | [VSE-44] Capture: ONE anim node of the ATTACKER supplies both the victim's po… | consumer | game engine |
| V-R515 | [VSE-45] Throw arcs: the launch row comes from `table2[map1[2*subidx + d0] *…` | consumer | game engine |
| V-R516 | [VSE-46] The command-input system: per-character eval handlers call MOTION HE… | consumer | game engine |
| V-R517 | [VSE-47] A scripted motion input is NOT the move you named — the input buffer… | consumer | game engine |
| V-R518 | [VSE-48] Cell index IS character id: the navigation routine commits the TABLE… | consumer | game engine |
| V-R519 | [VSE-49] TABLE B is 32 rows and the index is unmasked, so variant-half cells… | consumer | game engine |
| V-R520 | [VSE-50] The hovered cell's UI pieces come from per-piece RECORD-POINTER arra… | consumer | game engine |
| V-R521 | [VSE-51] Venue-asset numerology lies: palette index ≠ character id (the 3×3 p… | generalize | -> 1: identify by measurement, never by numerology |
| V-R522 | [VSE-52] There is no such thing as a free palette row on a venue screen: venu… | consumer | game engine |
| V-R523 | [VSE-53] Palette rows `0x10+` belong to the P2 CHARACTER: attribute a row wit… | generalize | -> BBX-5: attribute with a control that varies the variable under test |
| V-R524 | [VSE-54] "Slot-indexed cell" does not mean "slot-exclusive data": cell pokes… | consumer | game engine |
| V-R525 | [VSE-55] The per-character strip zone INTERLEAVES the shared music-sequence p… | consumer | game engine |
| V-R526 | [VSE-56] The attract INTRO CUTSCENE is Jedah: his per-character display sites… | consumer | game engine |
| V-R527 | [VSE-57] Per-slot presentation assets split three ways (`venue_assets.md`): s… | consumer | game engine |
| V-R528 | [VSE-58] The win screen draws from THREE independent per-winner tables — posi… | consumer | game engine |
| V-R529 | [VSE-59] The arcade ladder is a PAIR per rung — "fight this class, at this st… | consumer | game engine |
| V-R530 | [VSE-60] The stage-banner family is addressed from an ANCHOR that is the fami… | consumer | game engine |
| V-R531 | [VSE-61] The 68k side is id-only: game code queues 16-byte entries into a rin… | consumer | game engine |
| V-R532 | [VSE-62] `+0x382` is the character id ONLY at select/commit — in match it is… | generalize | -> BBX-16: a field meaning is phase-dependent; the view is part of the claim |
| V-R533 | [VSE-63] A SECOND voice family lives in the sound KERNEL: four per-class word… | consumer | game engine |
| V-R534 | [VSE-64] The `+0x300` id alias is the FACING bit and natively selects a per-f… | consumer | game engine |
| V-R535 | [VSE-65] The Z80 driver is NOT encrypted, and its 24-bit logical addresses ar… | consumer | game engine |
| V-R536 | [VSE-66] Stream grammar: bytes `< 0x20` dispatch through a 32-entry pointer t… | consumer | game engine |
| V-R537 | [VSE-67] For the shared sfx library the two games use IDENTICAL ids keying th… | consumer | game engine |
| V-R538 | [VSE-68] DARK FORCE COSTS ONE BANKED STOCK (`+0x109`); with an empty meter th… | consumer | game engine |
| V-R539 | [VSE-69] The two engines run DIFFERENT Dark Force systems: both write `seq 0x…` | consumer | game engine |
| V-R540 | [VSE-70] Which DF palette-seq ids belong to whom cannot be derived from the p… | consumer | game engine |
| V-R541 | [VSE-71] Shadow/reflection SERVANTS (a class-`0x0C` trio per player) mirror t… | consumer | game engine |
| V-R542 | [VSE-72] THE LEAPING PURSUIT is vsav's own (U + any button on a knocked-down… | consumer | game engine |
| V-R543 | [VSE-73] The game installs REAL handlers on every 68k exception vector: each… | consumer | game engine |
| V-R544 | [VSE-74] The object-script state dispatchers (three siblings, 80-entry tables… | consumer | game engine |
| V-R545 | [VSE-75] CPU AI: four per-class tables of script starts, 32 longs each — 16 c… | consumer | game engine |
| V-R546 | [VSE-76] A MODE-gated symptom needs the MODE PROVEN ENTERED — assert the stat… | keep | O5/BBX-5: assert the state was entered, never the input that should have entered it |
| V-R547 | [VSE-77] Sample a state-gated effect at the moment it gates on — DF afterimag… | generalize | -> O1: the observation point is chosen where the property is observable |
| V-R548 | [VSE-78] A WATCHDOG REBOOT masquerades as a clean "nothing happened": guard c… | generalize | -> BBX-4/D4: "nothing happened" and "the run restarted" are different findings |
| V-R549 | [VSE-79] A 1P-arcade rig is silently pinned to the ARCADE DRAW: any timing ch… | consumer | game engine |
| V-R550 | [VSE-80] Post-match surfaces: coarse sampling after a round end lands on the… | consumer | game engine |
| V-R551 | [VSE-81] Per-character timing is per-character: reach, knockdown arcs, down w… | consumer | game engine |
| V-R552 | [VSE-82] Player-struct bytes `$42-$45` are SHARED SCRATCH across phases (sele… | consumer | game engine |
| V-R553 | [VSE-83] The attract demo runs REAL characters (a Jedah vs Victor demo from ~… | consumer | game engine |
| V-R554 | [VSE-84] A duration in VIDEO FRAMES is not comparable between the sibling gam… | generalize | -> O1: the observation unit must be the same on both sides or the comparison is void |
| V-R555 | [VSE-85] Demonstrate saturation, never assume it — a mash that presses one fr… | keep | BBX-5/BBX-7: demonstrate saturation, never assume it |
| V-R556 | VSP — `vampire-saved-port`, "level 2, this project only", 180 rules; anchors… | generalize | -> R2 skills registry row: level = liftability rank, anchors, forbid list (R4) |
| V-R557 | VSE — `vampire-savior-engine`, "level 1 for the GAME: true with no port in mi… | generalize | -> R2 skills registry row: level = liftability rank, anchors, forbid list (R4) |
| V-R558 | MSC — `mister-cps2-wide-core`, "level 1, game-independent", 73 rules; forbids… | generalize | -> R2 skills registry row: level = liftability rank, anchors, forbid list (R4) |
| V-R559 | MJC — `mister-jtframe-core`, "level 0, board-agnostic", 63 rules; lifts MSC-N… | generalize | -> R2 skills registry row: level = liftability rank, anchors, forbid list (R4) |
| V-R560 | MFI — `mame-fbneo-instruments`, "level 0, board-agnostic", 46 rules; lifts CP… | generalize | -> R2 skills registry row: level = liftability rank, anchors, forbid list (R4) |
| V-R561 | CPE — `cps2-emulation`, "level 1, game-independent", 42 rules, ALL of them RE… | generalize | -> R2 skills registry row: level = liftability rank, anchors, forbid list (R4) |
| V-R562 | MSV — `mister-vampire-saved`, "level 2, dies with the project", 36 rules | generalize | -> R2 skills registry row: level = liftability rank, anchors, forbid list (R4) |
| V-R563 | CPH — `cps2-hardware`, "level 1, game-independent", 30 rules | generalize | -> R2 skills registry row: level = liftability rank, anchors, forbid list (R4) |
| V-C1 | "THE SUPERSET INVARIANT (never violate, never weaken): Any match, menu path,… | generalize | superset invariant -> acceptance contract (3.1): the untouched subset compares EXACT vs the reference |
| V-C2 | "THE ONE BOUNDED EXCEPTION" — emulator cores never modified outside the ratif… | generalize | -> S1 KIND PROFILE: an instrument change is bounded, declarative, profile-gated |
| V-C3 | "No hand-edited binaries. All ROM modifications are produced by the build pip… | generalize | -> S2/S3: the SUBJECT ARTIFACT is built from source, reproducible at any version |
| V-C4 | "Provenance is tracked per region." — every byte range tagged VSAV/VS2/VH2/GE… | generalize | -> E3 provenance register, written in the same commit, complete both ways (R1) |
| V-C5 | "Behavioral values live in documented tables, not in code." — rule 5, the obl… | generalize | -> L2 fact tables: behaviour lives in declared tables with provenance (BBX-18) |
| V-C6 | "No copyrighted ROM content in the repo or in any distributed artifact." — ru… | consumer | game / licence |
| V-C7 | Gate registry completeness BOTH WAYS: "a new tests/*.sh without a row here fa… | keep | R1 registry complete both ways, re-derived every run |
| V-C8 | Sweep registry completeness both ways — `ci_emulator.tsv` enumerates the set,… | keep | R1 registry complete both ways, re-derived every run |
| V-C9 | Expectation provenance completeness both ways: "a file with no row fails, a r… | keep | E3 + R1: expectation provenance complete both ways |
| V-C10 | ONE verdict classifier, sourced by all three runners; `vs_classify <exit> <lo…` | keep | G1 one classifier, sourced by every runner |
| V-C11 | "EXIT STATUS DECIDES FIRST. A gate that prints `SKIP:` AND exits non-zero is… | keep | G1 / BBX-1 exit status decides before any text |
| V-C12 | "THE THREE EXCEPTIONS, each written for a false green that was paid for" — ex… | generalize | -> G1: timeout + crash-line exceptions keep; the MAME segv line is a KIND PROFILE addition |
| V-C13 | Suite dispatch order: `.skip` → `.pending` (FAIL) → `.masked` → `.diverge` →… | keep | E1 expectation-kind dispatch, registered once |
| V-C14 | "`.pending`: the shape has been MEASURED but its comparison class is not rati… | keep | E2 / BBX-3 an unratified expectation is a FAILURE |
| V-C15 | `NO-EXPECTATION (freeze after review, as a STATE.md decision)` — an unfrozen… | keep | E2: an unfrozen scenario never reads green |
| V-C16 | A gate's HEADER must state the default its CODE actually uses; `--fix` rewrit… | keep | G2 the header names the default the code uses |
| V-C17 | The gate index is GENERATED from each script's own header sentence + `gate_in…` | keep | G4 / BBX-21 the index is generated from headers + registries |
| V-C18 | Skill lock: every rule defined once, anchored exactly once in a declared doc,… | keep | R2 skills registry, complete both ways |
| V-C19 | "a skill quotes a number only from a LOG" — every numeric token in a SKILL mu… | keep | BBX-21 every number in a skill appears in a log |
| V-C20 | Liftability enforcement: a forbidden token in a level-1/level-0 skill fails,… | generalize | -> the extraction question mechanized: forbidden-token levels (R4) |
| V-C21 | A `[VSP-N]` anchor may sit in `STATE.md` ONLY in `## STANDING PRINCIPLE` / `#…` | generalize | -> R2 documents registry: anchors only in declared, non-rolling homes |
| V-C22 | No anchor may live in a `_history` twin — a history file is a LOG, not an anc… | keep | BBX-20 a history twin is a LOG, never an anchor home |
| V-C23 | Every hand-written document declares its SHAPE (class, history twin, requires… | keep | BBX-20 / R2 every document declares its shape and is checked against it |
| V-C24 | Load-bearing numbers shared across documents are locked by label + canonical… | keep | BBX-18 cross-document number locks |
| V-C25 | L1 routing: README COMPLETENESS (every declared doc listed) and TWO-WAY TWINS… | keep | BBX-20 routing completeness + two-way twins |
| V-C26 | Every registry row also has an annotated git tag `freeze/<expectation-set>` a… | generalize | -> E3 "how to re-freeze" + S3: the freeze is marked in the version control of record |
| V-C27 | "THE JOB MUST FAIL ON SKIP. A gate that SKIPs for want of a fixture is not a… | keep | BBX-1 SKIP asserts nothing; a job that skips is not a passing job |
| V-C28 | The fidelity gate SKIPs when the harness is absent — "a clean checkout has no… | keep | F12 fidelity gate: absence is a SKIP in the tier that expects it |
| V-C29 | L3 check contract: QUOTE the claim from the document (`says()`, whitespace-co… | generalize | -> document-set kind driver: QUOTE the claim, DERIVE it, COMPARE (BBX-18) |
| V-C30 | "a reworded document is a STALE verdict, never a silent pass" — the L3 anti-d… | keep | BBX-18/19 a reworded document is a STALE verdict, never a silent pass |
| V-C31 | Frozen covered-set compared as a MULTISET so a hand-added duplicate fails | keep | E5 / BBX-17 inventories compared as multisets |
| V-C32 | The persistent suite doctrine: every in-emulator test becomes a scripted reru… | keep | 6.4 every in-session measurement becomes a rerunnable case |
| V-C33 | Auto-detecting runner: the suite FINGERPRINTS the build under test and select… | generalize | -> S2 SUBJECT IDENTITY selects the expectation set through a registry row |
| V-C34 | Recording capture law: every reproducible crash captured FIRST as a hand-play… | generalize | -> BBX-28 a witness becomes a reproducible SCENARIO before any theory |
| V-K1 | Oracle v1/v2 — "the masked basis, and the classes exact / flicker-tolerated /… | generalize | -> C1 temporal family (exact / flicker / frozen first divergence) |
| V-K2 | Oracle v3 — "the bounded re-convergent window (approved 2026-08-05)" | generalize | -> C1 temporal family (bounded re-convergent window) |
| V-K3 | Oracle v4 — "composite (ratified 2026-08-06)" | generalize | -> C1 temporal family (composite) |
| V-K4 | Oracle v5 — "the ≥60 rule is INTRA-MECHANISM (ruled 2026-08-16)" | generalize | -> C1: a threshold carries its SCOPE, and the scope is ruled |
| V-K5 | Oracle v6 — "the dual-track FROZEN OFFSETS (ruled 2026-09-05)" | generalize | -> C1: dual-track frozen offsets |
| V-K6 | "Checkers and their ground truth" — the table binding each class to its check… | keep | C1 every class names its checker and the evidence that ratified it |
| V-K7 | Spec keyword `window` — 1322 of 2311 `.masked` specs | generalize | -> C1 temporal family member (window) |
| V-K8 | Spec keyword `composite` — 596 specs | generalize | -> C1 temporal family member (composite) |
| V-K9 | Spec keyword `exact` — 192 specs | keep | C1 exact family |
| V-K10 | Spec keyword `diverge` — 43 specs (no `.diverge` FILES are frozen; the class… | generalize | -> C1 temporal family member (diverge) |
| V-K11 | Spec keyword `flicker` — 42 specs | generalize | -> C1 temporal family member (flicker) |
| V-K12 | `masked_check` — the shared masked comparison entry point sourced by the suit… | keep | C2 one dispatcher for the whole class vocabulary |
| V-K13 | `compare_flicker.py` — the flicker-tolerated class checker | generalize | -> temporal-family checker over point-indexed OBSERVATION LOGS |
| V-K14 | `compare_window.py` — the bounded re-convergent window checker | generalize | -> temporal-family checker over point-indexed OBSERVATION LOGS |
| V-K15 | `compare_composite.py` — the composite class checker | generalize | -> temporal-family checker over point-indexed OBSERVATION LOGS |
| V-K16 | `describe_masked_shape.py` — proposes a spec line meant to drop into a `.mask…` | keep | C5 proposer = enforcer; the proposed line drops into a spec verbatim |
| V-K17 | `compare_fields.py` / `check_wram_dumps.py` — the field comparator and dump c… | generalize | -> schema / tolerant-numeric comparator families (C1) |
| V-K18 | The MASK basis file, one per expectation set (55 sets carry one) — the masked… | generalize | -> O2 BASIS: one basis file per expectation set |
| V-T1 | The MUST-FIRE control convention: 74 of 311 gate scripts assert their own con… | generalize | -> G3 controls registry, one grammar, machine-read, complete both ways |
| V-T2 | "the claim must be measured with a POSITIVE CONTROL on the same instrument an… | keep | BBX-5 / BBX-7 a positive control on the same instrument and leg |
| V-T3 | `CPS2_WIDE_CANARY` — the positive control gated into the ratified WIDE profil… | consumer | board / profile canary |
| V-T4 | The emulator superset invariant — the patched binary running stock `vsavj` re… | generalize | -> fidelity control: the profile-gated instrument reproduces the reference verdicts |
| V-T5 | The deadness register's guard column — every "legacy never reaches this" clai… | keep | BBX-7 every absence claim names the guard that would catch it being wrong |
| V-T6 | `runs_per_replay` double-run → `NONDETERMINISTIC (first divergent frame below…` | keep | O6 / BBX-14 run twice; any difference is a failure before any class |
| V-T7 | The fidelity gate's own control: a config copy rooted here must resolve to th… | keep | BBX-8 point the validator at the wrong thing and require it to fail |
| V-T8 | The rule-7 entropy tripwire on untracked files, threshold 6.0 bits/byte | consumer | game / licence tripwire |
| V-T9 | The CI gate-count FLOOR — "Raise it when the list grows; never lower it to ma… | keep | BBX-6 raise a floor when the list grows, never lower it |
| V-T10 | `test_checkskills.sh` fixture prefixes `XX`/`YY`/`ZZ` + nine perturbed-copy m… | generalize | -> G3 control SHAPE "perturbed copy" + a FIXTURE SUBJECT for the skills-lock kind |
| V-T11 | `checkdocs_rom` — 15 checks, 12 table controls, five must-fire controls, four… | generalize | -> document-set kind: the driver and its declared controls (G3) |
| V-T12 | The saturation control — "RAISE IT and show the result does not move, and kee… | keep | BBX-5 / BBX-7 a live control against assertions going vacuous |
| V-T13 | Rot class 3's cure: "gates check their own controls and refuse a verdict when… | keep | BBX-6 gates check their own controls and refuse a verdict on a dead one |
| V-D1 | `FLICKER_MAX = 2` — a divergent run this short or shorter is a FLICKER frame… | generalize | -> KIND PROFILE threshold, declared once, provenance-classed (BBX-24) |
| V-D2 | `RECONVERGE = 60` — identical frames required after the last divergence; v5 r… | generalize | -> KIND PROFILE threshold, declared once, provenance-classed (BBX-24) |
| V-D3 | "These are RATIFIED values. Changing one is a §4 amendment and a maintainer d… | keep | BBX-13 / E6 a ratified class loosens only by measured mechanism + ruling |
| V-D4 | Entropy threshold 6.0 — "MEASURED, not the 3.0 the issue suggested"; measured… | generalize | -> R2 defaults register: a MEASURED default carrying its spread (BBX-24) |
| V-D5 | CI gate floor 60 (61 registered at 14z-124, 15 at 14z-93) | generalize | -> R2 defaults register: a registry floor (BBX-24) |
| V-D6 | `$BBH_HOME` default: beside this tree, else beside its PARENT; SKIP when abse… | generalize | -> F12: lineage-harness discovery default, registered (BBX-24) |
| V-D7 | L3 coverage denominator 346 (473 atlas addresses minus the 127 carried only b… | generalize | -> BBX-18: the coverage denominator is a ruling, registered (BBX-24) |
| V-D8 | The NOTE class — a measured number reported by the static runner's advisory b… | keep | G1 / BBX-1 an advisory NOTE is never a fifth verdict word |
| V-D9 | `test_build_ref_rot` reports CURRENCY and never fails on it — "a superseded r… | keep | 3.3 a measurement on our own build locks CURRENCY, never correctness |
| V-D10 | The eight harness defaults, each RULED/DECIDED with the maintainer's verbatim… | keep | BBX-24 / 3.1 every default RULED, dated, with a stated veto path |
| V-D11 | Per-gate default BUILD dirs — re-pointed at every freeze by the sweep; the cl… | generalize | -> rot class 4: default SUBJECT SET pointers re-pointed at every freeze |
| V-G1 | `tests/ci_portable.txt` — the ROM-free tier registry (68 rows); also records… | keep | R2 gates by tier (portable), exclusions recorded with their reason |
| V-G2 | `tests/ci_static.txt` — the static tier registry (74 rows); needs ROMDIR / bu… | keep | R2 gates by tier (static) |
| V-G3 | `tests/ci_emulator.tsv` — THE SWEEP REGISTRY (165 rows), read at `run_all_emu…` | keep | R2 sweep registry, complete both ways |
| V-G4 | `tests/gate_index.tsv` — family assignment, "the one hand-maintained input" (… | keep | R2 gate registry; the hand-maintained input BBX-9 warns about |
| V-G5 | `docs/project/gate_index.md` — GENERATED gate index, 311 rows, currency gated… | keep | G4 / BBX-21 generated index with a currency gate |
| V-G6 | `tests/expected/registry.tsv` — build fingerprint (program-image SHA-1) → exp… | keep | S2 / R3 identity -> expectation set, rows written at freeze time |
| V-G7 | `tests/expected/PROVENANCE.md` — "Frozen expectations — WHERE EACH NUMBER CAM… | keep | E3 the expectation provenance register |
| V-G8 | `tests/expected/doc_anchor_census.tsv` — every skill anchor's FILE and SECTIO… | keep | R2 skills registry: anchor file+section frozen; a changed row is a MOVED anchor |
| V-G9 | `docs/doc_shape.tsv` — 74 declared document shapes + `doc_shape_allow.tsv` ex… | keep | R2 documents registry (declared shapes) |
| V-G10 | `docs/doc_locks.tsv` — 19 cross-document number locks | keep | BBX-18 cross-document number locks |
| V-G11 | `docs/GOTCHAS.md` — GENERATED index, one line per bucket entry; "do not edit;… | keep | BBX-21 generated index, never hand-edited |
| V-G12 | `docs/annotations.md` — GENERATED address → carrier stream, 3023 lines; "An i… | generalize | -> a generated index over the subject claim space (BBX-18 coverage) |
| V-G13 | `docs/project/hardening_register.md` — the crash-candidate inventory of the m… | consumer | build/freeze inventory |
| V-G14 | `docs/project/tables/rule5_ledger.md` — one row per session of BAKED→IN-TABLE… | generalize | -> a workstream ledger whose numbers come from the tool output (1, BBX-21) |
| V-G15 | `tests/expected/checkdocs_rom_covered.tsv` — 31 rows, the frozen L3 covered s… | generalize | -> the frozen COVERED SET of a document-set subject (BBX-18, E5) |
| V-G16 | `docs/checksums.txt` — reference romset SHA-1s; "If checksums mismatch, stop." | generalize | -> S2: reference-artifact hash lock; an unknown identity stops the run |
| V-G17 | `tools/checkskills.py` `SKILLS = {...}` — THE SKILL LOCK (8 rows: path, docs,… | keep | R2 skills registry (the lock itself) |
| V-G18 | `docs/project/patch_index.md` — the registry of romsets and patch ops | consumer | game / build patch registry |
| V-F1 | F1 classifier — a synthetic ~25-stub fake repo, every case of both runner tes… | keep | F12+: classifier fidelity over a synthetic fixture repo |
| V-F2 | F2 static tier, live — `ci_portable.txt` then `ci_static.txt`, verdict column… | keep | F12+: static-tier fidelity - verdict column, tally, anti-orphan, exit status |
| V-F3 | F3 tier classifier — `tests/*.sh` (304 at the time); instrument set == sweep… | keep | F12+: tier classifier; instrument set == registry rows exactly |
| V-F4 | F4 sweep registry — `run_all_emulator.sh --list/--dry-run` vs `bbh-run-sweep`… | keep | F12+: sweep registry fidelity (--list / --dry-run) |
| V-F5 | F5 comparators corpus-wide — every `.masked` spec paired with a DIFFERENT set… | keep | F12+: comparator fidelity corpus-wide, cross-paired, verdicts diffed byte for byte |
| V-F6 | F6 fingerprint — `$ROMDIR` and every present `build/*/rompath`, all flags, pl… | generalize | -> F12+: SUBJECT IDENTITY fidelity over the subject set + the registry NOTE |
| V-F7 | F7 suite dispatch — real expectation trees, a stub driver copying a sibling s… | keep | F12+: suite dispatch fidelity driven by a stub driver |
| V-F8 | F8 the replay engine — real emulators, `cmp`-identical logs, every artifact i… | consumer | MAME/FBNeo instruments; BBX F12 runs bbh fakesys instead |
| V-F9 | F9 hygiene tools — provenance, header defaults, ref rot, demand-after-trap, `…` | keep | F12+: hygiene-tool fidelity, generated index byte-identical |
| V-F10 | F10 fields + dumps — SYNTHETIC dump directories shaped like this tree's (a ho… | generalize | -> F12+: chiral synthetic fixtures for the schema / numeric comparators (BBX-15) |
| V-F11 | F11 the skills lock and the guide generator — 18 lines identical over the eig… | keep | F12+: skills lock and guide generator fidelity |
| V-F12 | The three fidelity rules: no verdict-string edit before H9; the generic class… | keep | 7: no verdict-string edit before the proof; the generic copy is the STRONGER one; TEXT is compared |
| V-I1 | Rot class 1 — "THE ORPHAN. A gate no runner calls." Cure: a registry with com… | keep | BBX-10 rot class 1 orphan -> R1 both ways |
| V-I2 | Rot class 2 — "THE SILENT DOWNGRADE. A verdict that is quietly softened: a SK… | keep | BBX-10 rot class 2 silent downgrade -> G1 |
| V-I3 | Rot class 3 — "THE DEAD CONTROL. A must-fire control that no longer fires. Th… | keep | BBX-6 / BBX-10 rot class 3 dead control -> G3 |
| V-I4 | Rot class 4 — "THE STALE REFERENCE. A default build dir, a reference binary,… | keep | BBX-10 rot class 4 stale reference |
| V-I5 | Rot class 5 — "THE OUTGROWN PARSER. A consumer that reads an instrument's out… | keep | BBX-12 / BBX-10 rot class 5 outgrown parser -> O3 |
| V-I6 | Rot class 6 — "THE DELETED MECHANISM. A gate that probes something a later de… | keep | BBX-10 rot class 6 deleted mechanism; re-target or drop is a coverage decision |
| V-I7 | Rot class 7 — "THE MISSING OPERAND. An instrument that needs operands describ… | keep | BBX-10 rot class 7 missing operand -> D3 refusal |
| V-I8 | "The diagnostic that beats all of them: compare a red gate's RUNTIME against… | keep | BBX-11 the red gate that failed faster than its own quoted runtime |
| V-I9 | The `#94` reference-rot class (issue numbering, distinct from the 1-7 taxonom… | generalize | -> rot class 4 workstream: a currency gate is extended, never relaxed |
| V-I10 | "The runner found three defects in ITSELF before it found any in the suite —… | keep | BBX-5 the instrument is suspect before the subject |
| V-I11 | "a lineage freeze rots the harness" — bbh's `config.py` carried this project'… | keep | BBX-24 the biased default; the second consumer is its detector |
| V-I12 | "`gfx_tiles.decode` had every 8-pixel half MIRRORED, and nothing noticed for… | generalize | -> BBX-15 chirality: verify synthesized content at the final (render) layer |
| V-I13 | "two traps from the #104 re-measurement — both produced a CONFIDENT WRONG ANS… | keep | 1 certainty is the failure signal - a working instrument gave a confident wrong answer |
| V-I14 | "`BUILD=... tests/<gate>.sh` is SILENTLY IGNORED by positional-arg gates — an… | keep | D3 a variable silently ignored is the false green gates exist to remove |
| V-I15 | "the deadness measurement was sound but its COVERAGE was four replays" — the… | keep | BBX-18 coverage is a number: a sound measurement over four cases is a coverage claim |
| V-P1 | "A RED GATE IS A QUESTION, NOT AN ANSWER, and the first thing the question ne… | keep | 3.2 / E3 a red gate is a question; which side rests on a measurement |
| V-P2 | "THE PRECEDENCE OF REFERENCES … ruling > vsavj > vs2. A build of OURS — solo… | keep | 3.3 provenance ranking; a build of ours is nowhere in the order |
| V-P3 | "*better no test than a bad one. Let's drop*" — the maintainer's rule for a g… | keep | 3.4 / BBX-6 no gate rather than one whose verdict control cannot be built |
| V-P4 | The extraction question — "*would this still be true if the thing under test… | generalize | -> the BBX extraction question (5): not frame-driven, not software, not executable |
| V-P5 | The documentation taxonomy question — "would this still be true if we abandon… | generalize | -> the document-filing question (R2 documents registry; the gotcha buckets) |
| V-P6 | "STANDING PRINCIPLE (maintainer, 2026-08-05): vanilla wins ties" — a port's c… | consumer | game design ruling |
| V-P7 | "*to know if we should fix the gate or what it caught, we must use data we ca… | keep | 1 the measurement rule |
| V-P8 | "An undocumented discovery is a discovery we will pay for twice." | keep | 6 nothing evaporates into prose |
| V-P9 | "a stale claim in a header is worse than no documentation: it is confidently… | keep | G2 / BBX-10 a stale header claim is confidently wrong |
| V-P10 | "The spec is NOT copied here on purpose — two copies drift, and that document… | keep | BBX-20 two copies drift; one page states what is true |
| V-P11 | "a gate's WHY lives in the gate" (14z-123 ruling) — which is why the gate ind… | keep | G2 the claim lives in the gate; the index is generated from it |
| V-P12 | "Where the synthesis and a log disagree, THE LOG WINS" | keep | BBX-21 the log wins over the synthesis |
| V-P13 | "Prefer designs where being wrong is *safe and loud* over designs that are me… | keep | BBX-7 prefer designs where being wrong is safe and loud |
| V-P14 | "Each is measured by ABSENCE, which is the weakest kind of evidence we accept… | keep | BBX-7 absence is the weakest evidence accepted |
| V-P15 | "The file is what a triage opens." — provenance is kept beside the frozen fil… | keep | E3 provenance is kept beside the frozen file |
| V-P16 | "The harness is this project's most valuable artifact (CLAUDE.md §4), and it… | keep | BBX-10 harness maintenance is a workstream, not an interruption |
| V-P17 | "'It should be equivalent' is not a test result." | keep | BBX-26 "it should be equivalent" is not a test result |
| V-P18 | "the fix is never to relax the control" | keep | BBX-6 the fix is never to relax the control |
| V-P19 | "archived entries are never rewritten (corrections are marked in place, as al… | keep | BBX-20 / 6 history is append-only; corrections marked in place |
| V-P20 | "A claim that cannot be found cannot be corrected" — the reason the retractio… | keep | BBX-22 a claim that cannot be found cannot be retracted |
| V-P21 | "So a tag is never renamed, renumbered or tidied: renaming one silently break… | keep | BBX-29 / 4 ids are stable and never renumbered |
| V-P22 | "Everything else here is a way the pipeline, a gate or a rig produced a confi… | keep | 0 / BBX-21 the skill is a ledger of confident wrong answers and their checks |
| V-P23 | "the eliminations usually stay valid even when the conclusion does not" — kee… | keep | BBX-22/23 keep the superseded analysis, marked RETRACTED |
| V-P24 | "a test's classification code must be validated against known ground-truth sc… | keep | BBX-2 verdict logic validated against ground truth before it is trusted |
| V-P25 | "the numbers are about an artifact nobody maintains" — the tell that separate… | keep | BBX-10 the tell that separates rot class 4 from a product defect |
| V-X1 | L1 routing enforcement (markdown) — README completeness + two-way twins + rou… | keep | BBX-20 routing and twin completeness are checks |
| V-X2 | L4 the rendered site — `mk_docs_site.py`, generated + never committed; landin… | generalize | -> the readout/site is GENERATED, never committed (BBX-21, RO3) |
| V-X3 | L2 fact tables with provenance — `audit_rule5.py`, the BAKED inventory frozen… | generalize | -> E5 a frozen inventory that may only SHRINK: a coverage ratchet |
| V-X4 | L3 ROM re-derivation — documents as SUBJECTS: quote the claim, derive the fac… | generalize | -> the document-set SUBJECT KIND (S1) with coverage reported (BBX-18) |
| V-X5 | Ruled slice order "L1 → L4 → L2 → L3" — enforcement first, then the site that… | generalize | -> slice ordering: enforcement before content (6 plan + STOP) |
| V-X6 | Provenance vocabulary #1 (rule-5 rows): "`measured 14z-N (rig)` · `derived (t…` | generalize | -> R11 closed provenance vocabulary (E3) |
| V-X7 | Provenance vocabulary #2 (frozen expectations): `in-emulator (reference)` / `…` | generalize | -> R11 closed provenance vocabulary (E3) |
| V-X8 | Provenance vocabulary #3 (rule-5 census classification): `IN-TABLE` / `DERIVE…` | generalize | -> R11 closed provenance vocabulary (E3) |
| V-X9 | The memory-file convention: STATE holds ~3 session groups + THE LEDGER + the… | keep | 6 STATE lean + history twin verbatim |
| V-X10 | The `_history.md` TWIN convention — docs stay lean, the complete LOG lives in… | keep | BBX-20 the living page / history twin convention |
| V-X11 | The eight harness_scope decisions (§7), each RULED/DECIDED with the maintaine… | keep | 3.1 / BBX-24 each default RULED with the maintainer words, a date and a veto path |
| V-X12 | The 19-red sweep — "155 gates, 136 PASS, 19 FAIL, ZERO SKIP … and NOT ONE red… | keep | BBX-10 the 19-red sweep; not one red was a product defect |
| V-X13 | The 19 reds mapped ONTO the rot classes: 1 orphan ×1, 3 dead control ×2, 4 st… | keep | BBX-10 the rot taxonomy is a standing checklist |
| V-X14 | THE DEADNESS REGISTER — every "legacy never reaches this, so we may reuse it"… | generalize | -> the absence-claim register with guard and fallback columns (BBX-7) |
| V-X15 | `GAME_TOKENS` (14) — `vsav vampire donovan huitzil phobos pyron tenant roster…` | consumer | game tokens |
| V-X16 | `BUILD_TOKENS` (8) — `0xEE73 0xFFDB 0x8E57F0 0x5FFF1E 32007911 build/ merged…` | consumer | build/freeze tokens |
| V-X17 | `BOARD_TOKENS` (5) — `cps qsound jtcps vsavjw wide_en`; forbidden at level 0… | consumer | board tokens |
| V-X18 | The lifting precedent: a lifted rule KEEPS ITS NUMBER, anchors in the SAME pa… | keep | 4 / BBX-21 a lifted rule keeps its number; the old id stays a redirect |
| V-X19 | The harness slices H1..H10 with landing commits (`803f372` … `e643219`); H8 (… | consumer | bbh extraction record (slices, landing commits) |
| V-X20 | The four extraction BINS — code / config / machine profile / stays — every pi… | generalize | -> the four BBX bins (5): keep / generalize / consumer / drop |
| V-X21 | "This tree never consumes it" — the extraction is proved by FIDELITY, not by… | keep | 2 / 7 the extraction is proved by FIDELITY, never by dependency |
| V-X22 | The three gotcha buckets, split by the outlives-the-project question; append… | generalize | -> the incident ledger bucketed by the extraction question (6 gotchas) |
| V-X23 | The `14z-N` session key: three namespaces never conflated (sessions / milesto… | keep | BBX-29 / 6 the session key; three namespaces never conflated |
| V-X24 | The gate-scoping method — 13 sections incl. "Widening is a measurement, not a… | keep | 6 / BBX-13 widening is a measurement; suspect the instrument |
| V-X25 | The skill levels 0/1/2 as a liftability ladder, declared in each SKILL.md's H… | generalize | -> the liftability ladder mechanized (R4: BBX carries its own generated skill) |
| V-X26 | The 6 `.pending` expectations still open — `donovan-m5` ×2, `huitzil-m13` ×3,… | consumer | build/freeze: the open .pending inventory |
| V-X27 | `docs/project/hardening_register.md`'s measured partition — rule-4 provenance… | consumer | build/ROM inventory |
| V-X28 | CI is DRAFTED-then-ENABLED as a policy decision, and `ci/README.md` names wha… | keep | 3.1 the maintainer rules; the readout names what is being DECIDED |
| V-X29 | The LIVE CROSS-REPO citation `[RH-N]` — the external `romhacking-methodology`… | generalize | -> R2 skills registry: a cross-repo citation to an external rule set (R4 cites BBH) |
| V-X30 | "`[RH-N]` is not checked (the RH skill lives outside the repo)" — a citation… | generalize | -> RO2/R1: a citation class the registry cannot close is declared UNCHECKED, not silent |
| V-X31 | The 4 SKILL.md headers that route to it by PLACEHOLDER (`[RH-NN]`), so no `[0…` | generalize | -> BBX-9/BBX-12: a placeholder form escapes every id census; enumerate by form |

## Totals

| bin | rows |
|---|---|
| keep | 190 |
| generalize | 192 |
| consumer | 358 |
| drop | 0 |
| total | 740 |

Row counts, re-derived in this session: census B rows **740** (`grep -c '^| V-' docs/census/vampiresaved.md`), bins rows **740** (`grep -c '^| V-' docs/bins/vampiresaved.md`), ids diffed identical and in order. The brief quoted 737; the census gained `V-X29`..`V-X31` (the external `[RH-N]` citation scheme) while this binning was in progress — 737 was measured at 2026-09-09 and is superseded by the 740 above, not carried forward.

`drop` is 0 rows: no census item met the §5 burden ("no meaning outside its origin AND not worth carrying even as a consumer item"). Every board, game, instrument and build row is still true and load-bearing for VampireSaved, so it is `consumer`, not `drop`.

Per-prefix split of the rule rows (keep/generalize/consumer/drop per prefix), produced by: `grep -E '^\| V-R' docs/bins/vampiresaved.md | awk -F'|' '{match($3,/\[[A-Z]+-/); p=substr($3,RSTART+1,RLENGTH-2); gsub(/ /,"",$4); print p, $4}' | sort | uniq -c`

```
   8  generalize
  42 CPE consumer
  22 CPH consumer
   7 CPH generalize
   1 CPH keep
  25 MFI consumer
  15 MFI generalize
   6 MFI keep
  31 MJC consumer
  21 MJC generalize
  11 MJC keep
  70 MSC consumer
   3 MSC generalize
  26 MSV consumer
   5 MSV generalize
   5 MSV keep
  67 VSE consumer
  15 VSE generalize
   3 VSE keep
  62 VSP consumer
  57 VSP generalize
  61 VSP keep
```

(The 8 rows with an empty prefix are `V-R556`..`V-R563`, the per-skill registry rows, which carry no `[PFX-N]` id in the census.)

Command: `grep -E '^\| V-' docs/bins/vampiresaved.md | awk -F'|' '{gsub(/ /,"",$4); print $4}' | sort | uniq -c`

```
 358 consumer
 192 generalize
 190 keep
```

Drop rows with an empty reason: `grep -E '^\| V-[^|]*\|[^|]*\| drop \| *\|' docs/bins/vampiresaved.md | wc -l` → must print 0 (actual output: `       0`).
