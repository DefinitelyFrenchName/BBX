# Defaults — the register (BBX-24, ruled R11 for the classes)

Shape: register, one row per default the harness introduces, with its
provenance class — `principled` (follows from a contract), `reference-calibrated`
(set by a measurement on a named reference, quoted), `arbitrary` (a choice
that could have been otherwise; the second consumer is its detector).
Changing a row is a ruling, dated in `DECISIONS_HISTORY.md`. A default with no
row is a default nobody can veto (`[BBH-84]`).

The lineage's biased default (bbh's `DEFAULTS` naming VampireSaved's build
directories, `docs/gotchas.md` G4) is the reason every row here names its
class.

The form (R61): one table, six cells per row, ids in order; a pipe inside a
cell is written `\|`; the class cell is `<class>[ (<qualifier>)]`, parts joined
by ` / `, each class one of the three words above. Every kind-blind `DEFAULTS`
key of `lib/py/bbx/config.py` is named as `[section].key` in the default cell
(the second) of the one row that registers it, and every `${BBX_*:-}` fallback
by its variable's name in a row.

| id | default | value | where it lives | class | why / measured on |
|---|---|---|---|---|---|
| D1 | seconds allowed per census command in the recount | 60 | `lib/py/bbx/recount.py` `--timeout`, env `BBX_CENSUS_TIMEOUT` | arbitrary | no census command has been measured above 5 s; 60 leaves room for a cold `python3 tools/checkskills.py -v` on VampireSaved without hiding a hang for a minute more |
| D2 | quoted runtime of `gates/census_recount.sh` | ~44 s (`real 43.50`; was 32 s with shared clones, 21 s in place) | the gate's header | reference-calibrated | `/usr/bin/time -p sh gates/census_recount.sh`, macOS Darwin 25.6, Apple Silicon, 300 rows, 3 shared clones + 4 shadow runs, 2026-09-09; re-measured when the census grows (BBX-11: a gate that fails far faster than its quoted runtime bailed before measuring) |
| D3 | where a census file's repository root comes from | line 2 of the census file (first backtick-quoted absolute path); `--root` overrides | `lib/py/bbx/recount.py` | principled | the census is the record of what was measured where; the recount reads the record, and an override is an explicit, visible choice |
| D4 | which rows a recount can re-run | count = plain integer or one quoted string; command = one leading backtick pipeline | `lib/py/bbx/recount.py` `classify()` | principled | anything else is reported NOT-RECOUNTABLE with its id; coverage is printed, never assumed (BBX-18) |
| D5 | the platform floor | POSIX sh + Python 3; macOS, Linux, Windows via WSL | ruled R3 | principled (ruled) | every platform guard has a gate on each platform |
| D6 | the environment every census command runs in | `PATH=/usr/bin:/bin:/usr/sbin:/sbin`, `LANG=LC_ALL=C.UTF-8`, `HOME`, `TMPDIR`, `PYTHONDONTWRITEBYTECODE=1`, nothing else | `lib/py/bbx/recount.py` `hermetic_env()` | principled | a count must not depend on which program answers to a name in the caller's shell (gotcha G9: ugrep vs BSD grep, six counts moved); bbh scrubs the replay family the same way `[BBH-35]` |
| D7 | where the gate finds the census files | `docs/census/` under the BBX root; env `BBX_CENSUS_DIR` overrides | `gates/census_recount.sh` | principled | the override exists so the SKIP path (an absent lineage tree) can be exercised on purpose without moving a repository |
| D8 | the kind-blind layout defaults (`[project].gates_dir` tests, `[project].gate_glob` *.sh, `[project].runner_prefixes` run_, `[project].manual_suffixes` _soak, `[registries].portable` `tests/ci_portable.txt` / `[registries].static` `tests/ci_static.txt`, `[tier].source_regex`, `[tier].source_depth` 2) | as listed | `lib/py/bbx/config.py` DEFAULTS | arbitrary | bbh's layout literals kept so a bbh config reads unchanged. The second consumer, BBX's own `bbx.toml` (kind `self`), names all eight and gives four of them another value (`gates_dir`, `portable`, `static`, `source_regex`); it sets `gate_glob`, `runner_prefixes`, `manual_suffixes` and `source_depth` to the kind-blind value, so no second consumer has differed on those four (measured bbx-29). A ninth, `[project].lib_dir` (`tests/lib`), was read by no line under `bin lib drivers gates`, nor by bbh's `bin` and `lib` at `10a82d2`, and was removed from BBX at bbx-29 (R66; bbh's side is bbh issue #2) |
| D9 | `[project].kind` when a config names none | `frame-driven` | `lib/py/bbx/config.py` DEFAULTS | arbitrary | bbh's configs carry no kind and must resolve to bbh's values (F13); a config for any other kind names it. Revisit when a third kind exists |
| D10 | `[controls].enforce` | `false` | `lib/py/bbx/config.py` DEFAULTS; `bbx.toml` sets `true` | principled | off is bbh's behaviour byte for byte (fidelity); the harness's own tree enforces (R10, R14) |
| D11 | the classifier's literals (`[classify].skip_regex` `^ *SKIP`, `[classify].shell_error_regex`, `[classify].timeout_exits` 124 137, `[classify].fail_tail` 4) | as listed | `lib/sh/classify.sh`, `config.py` DEFAULTS; the same four literals are the fallbacks of `BBX_CLASSIFY_SKIP_RE`, `BBX_CLASSIFY_SHELL_ERROR_RE` and `BBX_CLASSIFY_TIMEOUT_EXITS` in `lib/sh/classify.sh` and of `BBX_CLASSIFY_FAIL_TAIL` in `bin/bbx-run-static` (read at bbx-29) | principled | bbh's gate contract `[BBH-12]`–`[BBH-15]`, each paid for in the lineage; a consumer overrides through `[classify]` and gates/classify.sh proves the override reaches the classifier |
| D12 | the `frame-driven` kind profile (`instrument_word` emulator, `static_needs_env` ROMDIR, bbh's ten `tier.patterns`, the `[sweep]` literals) | bbh's DEFAULTS verbatim at the fidelity baseline (D20) | `config.py` KINDS | reference-calibrated | bbh `lib/py/bbh/config.py` at the baseline; F13 diffs a runner reading them against bbh. Followed the re-baseline to `10a82d2` on 2026-09-10 (the five `[sweep].placeholders` re-pointed M18: `m3b_merged26`, `don_m22`, `hui56`, `pyron41`, `m5_stock17`) — by this row's definition, not a new decision (R28) |
| D13 | the `self` kind profile (`gates_dir` gates, registries under gates/, `static_needs_env` BBX_BBH_HOME, controls enforced) | as listed | `config.py` KINDS; `bbx.toml` | principled | BBX reading its own tree (R14) |
| D14 | quoted runtime of `bin/bbx selftest` | ~222 s (`real 221.56`, 12 gates, 26 controls, kept with `--log`, the bbx-2 close; was 219, 223, 227, 156, 117, 47 s with 7 gates) | `HANDOFF.md` | reference-calibrated | `/usr/bin/time -p bin/bbx selftest` with BBX_BBH_HOME set, 9 gates (the sweep runner's ground truth carries three deliberately slow gates), macOS, 2026-09-09 |
| D15 | the kind-blind `[sweep]` defaults (`[sweep].lanes` one lane `prereq`, `[sweep].release_scope` `release`, `[sweep].cadences` `always`, `[sweep].default_timeout` 600, `[sweep].precondition` none, `[sweep].input_env` BBX_INPUT, `[sweep].log_dir_prefix` build/sweep_, `[sweep].placeholders` none, `[sweep].instruments` none, `[sweep].scratch_lanes`, `[sweep].scratch_env` and `[sweep].scratch_default` none; the other ten keys are D72's) | as listed | `lib/py/bbx/config.py` DEFAULTS | arbitrary | a sweep with no instrument named; the frame-driven profile carries bbh's literals (incl. VampireSaved's build dirs and MAME paths) so a bbh config resolves as in bbh (F14); `self` names `BBX_BBH_HOME` as its input |
| D16 | the kind-blind `[fingerprint]` defaults (`[fingerprint].kind` file-sha1, `[fingerprint].file_pattern` {set}.bin, `[fingerprint].parent_sets` none, `[fingerprint].region_rules` none, `[fingerprint].region_default` other; the other three keys are D73's) and `[suite].registry` tests/expected/registry.tsv, `[suite].default_set` "" | as listed | `config.py` DEFAULTS | principled | the most generic artifact is one file whose identity is its SHA-1 (abstraction S2); the frame-driven profile is bbh's zip-members literals verbatim (F14f) |
| D17 | where and how the recount obtains the tree it measures | a PLAIN local clone of the recorded commit under `${TMPDIR:-/tmp}` (`git clone --no-checkout` + `checkout <head>`; objects hardlinked on one filesystem, no `alternates`), removed after the run; `--in-place` runs in the working tree instead | `lib/py/bbx/recount.py` `make_clone` | principled | rulings R18 (the tree under measurement is never written) and R20 (plain, not `--shared`: a shared clone borrows the source's object store through an `alternates` file and a `git gc` there would break it mid-run). Measured 2026-09-09 on VampireSaved (`.git` 949 MB): plain clone + checkout 3.40 s, 490 MB as `du` counts a hardlinked pack (2 links, no extra disk); shared was 1.76 s, 363 MB. Never `git worktree add` (writes under the lineage's `.git`) |
| D18 | when the recount runs the rows a second time for drift | only when the repository's tip is not the recorded HEAD, and never under `--only` | `lib/py/bbx/recount.py` `main` | principled | drift is defined as tip ≠ recorded, so at the recorded HEAD there is nothing to measure; `--only` is the verifier's spot-check and a drift run there would double a partial count. Cost when it runs: VampireSaved's 127 rows again on a second clone — 42.8 s in all against 19.8 s without, measured 2026-09-09 |
| D19 | quoted runtime of `gates/fidelity_bbh.sh` | ~46 s (`real 46.10` on the plain clone; 37.49 in place; the header had said ~10 s + ~25 s) | the gate's header | reference-calibrated | `/usr/bin/time -p sh gates/fidelity_bbh.sh` with `BBX_BBH_HOME` set, F15 not run, macOS Darwin 25.6, Apple Silicon, 2026-09-09; re-measured whenever a pair is added (BBX-11) |
| D20 | the bbh commit the fidelity gates and the suite gate measure | `10a82d2` (was `f675710` until 2026-09-10 — `docs/rebaselines.md`); env `BBX_BBH_BASELINE` overrides; `BBX_FIDELITY_IN_PLACE=1` measures the working tree instead | **ONE definition since R43 (2026-09-12): `lib/sh/baseline.sh` (`BBX_BBH_BASELINE_DEFAULT`, read through `bbx_baseline`).** Its three readers, each sourcing that file: `gates/fidelity_bbh.sh`, `gates/fidelity_bbh_s2.sh`, `gates/suite.sh` | reference-calibrated | R8 set the baseline, R28's procedure moves it. Before R43 the value was a literal in three gate files, and the 2026-09-10 re-baseline moved two of them and this row while `gates/suite.sh` went on cloning `f675710` for six sittings with a header that said it cloned the baseline — found by reading the rulings queue, not by a gate (G32). Measured both ways before the consolidation: exit 0 and 77 identical printed lines at either commit, so it was currency and never correctness (§3.3). A config key was DECLINED at bbx-20 (R43, 2026-09-12): the baseline is a fact about BBX's relationship to the lineage, not a consumer value. The other reason recorded then — that fidelity pair F13e, which diffs bbh's and BBX's `config dump` of bbh's example, would stop being empty once a kind-blind section existed — was never measured and is false: a dump prints the consumer's file only, and F13e stayed identical when S5 step 1 added `[skills]` (measured bbx-26; G68, X55) |
| D21 | `[sweep].clone_per_slot` | `false`; a consumer sets `true` to give every `--jobs` slot (slot 0 included, the serial prereq lane too) its own plain clone of the consumer's HEAD under `${TMPDIR:-/tmp}/bbx-clones.XXXXXX/slotN`, removed at exit; the banner prints the commit and the working tree's porcelain | `lib/py/bbx/config.py` DEFAULTS, `bin/bbx-run-sweep` | arbitrary | ruling R19 (parallel work is a pull queue over pinned trees) — off by default because bbh's example and fidelity F14 run in place and a clone measures HEAD, not the working tree, which a consumer must choose knowingly; the second consumer that turns it on is its detector (BBX-24). Measured 2026-09-09: two slots on the gate's fixture, 2 clones, base untouched |
| D22 | where a kept run lives | nowhere unless `--log DIR` is given; the ritual uses `build/selftest_<stamp>` (gitignored) under the BBX root | `bin/bbx-run-static` `--log`, `HANDOFF.md` (the ritual) | arbitrary | a kept run is the readout's input and BBX-29's key (subject version + start time in `run.txt`); off by default so fidelity F13's printed output stays bbh's byte for byte, and `build/` because bbh's example logs there too (`log_dir_prefix`) |
| D23 | the temporal family's thresholds in the `frame-driven` kind profile | `flicker_max` 2, `reconverge` 60, `flicker_max_total` 8 | `lib/py/bbx/config.py` KINDS, read by `lib/py/bbx/thresholds.py` and nowhere else | reference-calibrated | bbh `lib/py/bbh/thresholds.py` and `[thresholds]` at f675710, verbatim (its lineage measured them in VampireSaved session 7: single-frame flickers, full re-convergence); F17 diffs every comparator reading them against bbh. A kind with no temporal family carries no `thresholds` table and a temporal comparator under it is REFUSED (S2 step 1, 2026-09-10) |
| D24 | which direction is LOOSER, per threshold key (R25) | `flicker_max` larger, `flicker_max_total` larger, `reconverge` smaller | `lib/py/bbx/thresholds.py` LOOSER_WHEN | principled | follows from what each number bounds: a longer run or a larger inventory tolerated is looser; fewer identical frames demanded after a divergence is looser. A looser consumer value needs a `[thresholds].rulings` entry or every importer refuses (exit 3, one line); tighter needs nothing (BBX-13) |
| D25 | the expectation KINDS table, `[expectations].kinds`, per kind profile | kind-blind `skip` / `sha1` (exact) / `pending`; frame-driven adds bbh's `masked` and `diverge` (temporal) | `lib/py/bbx/config.py` DEFAULTS and KINDS `[expectations].kinds`, read by `lib/py/bbx/expectations.py` and nowhere else | principled | E1: a kind is registered in one place so an unknown one is reported, never ignored `[BBH-50]`; R23 put the table in the profile so the family and the view come from the kind. The three kind-blind kinds are the ones with no comparator family or the exact one (S2 step 2, 2026-09-10) |
| D26 | `[suite].mask_default` and `[suite].mask_env` | kind-blind: mask_default EMPTY (a set without a mask file runs unmasked), mask_env `MASK_RANGES`; frame-driven: bbh's literal `043c-043d,4182-41a2,7f00-8000` | `lib/py/bbx/config.py`, read by `lib/sh/compare.sh` (`compare_mask_for`) through `bbx.expectations mask-default`, `BBX_MASK_DEFAULT` from a runner overriding | reference-calibrated (frame-driven) / principled (kind-blind) | bbh's literal is the lineage's board (a biased default, BBX-24) and stays under its profile's name; a mask is a frame-driven basis and no other kind has one yet, so the kind-blind value is none (2026-09-10) |
| D27 | `[suite].hash_cmd` | kind-blind `python3 -m bbx.sha1` (prints `<hex>  <file>`); frame-driven `shasum` | `lib/py/bbx/config.py`, read by `bin/bbx-run-suite` for the `.sha1` kind | principled (kind-blind) / reference-calibrated (frame-driven: bbh's) | R3's floor is sh + python3: `shasum` is not on every Linux, `sha1sum` not on macOS; the hex is the same whichever prints it, so F12's text is unchanged. R21's Linux run is the detector (S2 step 3, 2026-09-10) |
| D28 | `[suite].driver_home` — where a bare driver NAME resolves (R26) | kind-blind `$BBX_HOME/drivers` (absent until S3: a bare name FAILS naming the path); frame-driven `$BBX_BBH_HOME/drivers` | `lib/py/bbx/config.py`; `bin/bbx-run-suite` expands exactly those two tokens | principled | bbh stays the frame-driven consumer's driver home (CLAUDE.md §2), nothing is copied out of it; a path from the consumer root and `--driver` win; `BBX_BBH_HOME` unset under the frame-driven kind is a FAIL that names it |
| D29 | `[suite].runs_per_replay` | 2 (both layers) | `lib/py/bbx/config.py`; `bin/bbx-run-suite` | principled | BBX-14: every scenario runs more than once and any difference is NONDETERMINISTIC before any class is consulted; 2 is the smallest number that is more than once (bbh's value, `code (policy)` there) |
| D30 | the kept suite run's shape (`bbx-run-suite --log DIR`) | `results.tsv` — `scenario`, `kind`, `class`, `verdict` (the printed first line), `finding`; `run.txt` — subject, set, driver, HEAD, platform, counts, the suite's verdict; the finding vocabulary of `lib/py/bbx/finding.py` (`short` apart from `diverged`) | `bin/bbx-run-suite`, `lib/py/bbx/finding.py` | principled | BBX-4 and BBX-12: the printed text is bbh's and frozen (C4), so the distinction between a short observation and a divergence lives in the kept run, read by field name; `unclassified` for any line no rule names, never silently one of the others |
| D31 | `[provenance].register` and `[provenance].exclude` | `PROVENANCE.toml` inside `[suite].expected_dir`; excluded basenames `PROVENANCE.toml`, `README.md`, `mask`, `MASK` (the register's own name always) | `lib/py/bbx/config.py`, read by `lib/py/bbx/provenance.py` and the readout's suite screen | principled | R24: the register sits beside the files a triage opens, inside the tree it describes, so one path names both; the mask records are bases, not expectations, and the register cannot vouch for itself. A consumer's `[provenance].exclude` (bbh's example keeps bbh's) wins over the default, the register's own basename excepted (S2 step 4, 2026-09-10) |
| D32 | what the readout reads beyond a static run | a kept SUITE run (`run.txt` carries `expset=`) gets the suite screen: findings apart, the register's histogram for its set, the classes PASSed on a pairing whose row is not `fixture` or `testimony`; a static run's `untracked_before` / `untracked_after` fields (written by `bbx-run-static --log`, printing nothing) on the tree line | `lib/py/bbx/readout.py`, `bin/bbx-run-static` | principled | RO1 (the expectations relied upon, by class) and BBX-4 for the suite; G17's mechanism for the static run — the printed working-tree block is bbh's and tracked-only (F13), the kept run is BBX's (S2 step 4, 2026-09-10) |
| D33 | the `document-set` kind profile | `instrument_word` extractor; no `static_needs_env` (portable); no `tier.patterns`; `[suite]` `scenario_ext` claims, `driver` docset, `rompath_env` = `input_env` = `DOCSET_PATH`, `hermetic_unset` the driver family `DOCSET_FORMS` / `DOCSET_NONDET` / `DOCSET_VIEW`; `[fingerprint]` file-sha1 over `{set}.tsv` (the whole-set key `dir_sha1` over the artifact's directory = documents + artifact, NOT recursive); kinds: the kind-blind three + `truth` (exact) `claims` (set) `covered` (set) `schema` (schema); no `[thresholds]` | `lib/py/bbx/config.py` KINDS | principled | R14's third profile (docs/plans/S3.md §3; R31–R34); the identity is computed from the files, never from what a document says of itself (§3.4); a temporal comparator under it is REFUSED (D23). Limit stated: a nested document tree is not identified (S3 step 1, 2026-09-10) |
| D34 | `[suite].scenario_ext` — the scenario file extension per kind (R32) | kind-blind `rpl`; document-set `claims`; command-line `cli` | `lib/py/bbx/config.py`; read by `bin/bbx-run-suite` (the loop) and `lib/sh/expectation_kinds.sh` through `bbx.expectations scenario-ext` | arbitrary (kind-blind) / principled (document-set, command-line) | `rpl` is bbh's literal kept so a bbh config resolves unchanged (F12, F16c diff the loop and the enumeration); `claims` names what the scenario IS for the kind (S3 step 1, 2026-09-10); `cli` added for the command-line kind (R35; S4 step 1, 2026-09-10) |
| D35 | the fixture's three bindable SENTENCE FORMS and the claim-set grammar | `has`: `<Name> has <field> <value>.`; `of-is`: `The <field> of <Name> is <value>.`; `row`: `\| <Name> \| <field> \| <value> \|`; a claim set is a TOML-subset file — `[claims]` `documents` / `forms` / `view`, then `[p<n>]` paraphrase rows (`document`, `line`, `name`, `field`, `literal`) and `[u<n>]` unbindable rows (`document`, `line`, `reason` ∈ `absence` / `no-source` / `no-form`) | `fixture/docset/mkdocset.py` (writes them); `lib/py/bbx/docset.py` (binds them; S3 step 2) | arbitrary | the FIXTURE's forms (SMS `docs/game/README.md:82-113`: the prose form is a contract with the checker); a real consumer declares its own forms in its claim sets and the driver refuses the rest, so the first real consumer is the biased-default detector (BBX-24, BBX-25) (S3 step 1, 2026-09-10) |
| D36 | the fixture's CHIRALITY predicates (BBX-15) | names distinct; no name, id or weight equals its own mirror; no id's or weight's mirror is another id or weight; weights distinct; ranks a permutation that is neither its own inverse nor the reversal | `fixture/docset/mkdocset.py` `predicates()`, asserted on the design and on the tree's artifact under `--check`, each named when it fails | principled | a mirror, order or orientation error in a reader cannot hide if no value equals its mirror and no mirror of a value is another value (VampireSaved "mirrored decode", BBX-15); `gates/docset_fixture.sh` control `symmetric-fixture` (S3 step 1, 2026-09-10) |
| D37 | the binding-STATUS vocabulary and the observation token (R31, R33) | statuses `BOUND` / `PARAPHRASE` / `UNBINDABLE` / `STALE` / `MISMATCH`; the log line `<index> <status>:<sha1-of-quoted>:<sha1-of-derived>`, `END <n>` with n = claims seen; the inventory and covered rows carry `document`, `line`, `form`, `status` as named TOML fields (R34, the maintainer's TSV caveat) | `fixture/docset/mkdocset.py` (the expected statuses in the inventory); `lib/py/bbx/docset.py` (the token, S3 step 2) | principled (ruled) | R33: closed so a status nobody named cannot read green; R31: the point stays an integer and `logfmt` stays bbh's (S3 step 1, 2026-09-10) |
| D38 | the QUOTED and DERIVED strings behind D37's `<sha1-of-quoted>:<sha1-of-derived>` | quoted = the document's line verbatim (the `text` view); derived = `<name>.<field>=<value>` from the artifact, `<name>.<field>=<absent>` when the record or the column is missing, `unbindable:<reason>` for a listed unbindable; SHA-1 over UTF-8 | `lib/py/bbx/docset.py` `quoted_text` / `derived_text` / `derived_unbindable` / `token` / `split_token` (the one splitter) — imported by `fixture/docset/mkdocset.py` to write the truth from the design | principled | one definition, so the generator's truth (from the design) and the driver's log (from the binding) agree by construction and differ only where the binding differs from the design; the token compares as a token (O1) and a triage re-derives both sides from the index through `bbx.docset map` (S3 step 2, 2026-09-10) |
| D39 | the lexical classes of D35's forms, and the artifact's record key | `<Name>` and `<field>`: one token without spaces or bars; `<value>`: a decimal or hexadecimal numeral `[0-9A-Fa-f]+`; the record key is the artifact's FIRST column | `lib/py/bbx/docset.py` FORMS, `read_artifact` | arbitrary | the fixture's: a numeral value keeps a markdown table's header row (`\| name \| field \| value \|`) from being a claim, and the first column is where the fixture keeps its names; a real consumer's forms and key are its own (BBX-24: the first real consumer is the detector); a deleted record is therefore a lost reference (STALE), never a shift (G21) (S3 step 2, 2026-09-10) |
| D40 | the unlisted-claim GUARDS (BBX-7) | a line matching no declared form and not listed in the claim set is REFUSED (exit 3, naming `<document>:<line>`) when it states an absence (`^(Nothing\|None\|No <word>) (lists\|records\|gives\|states\|has\|carries\|mentions)`) or carries a digit; any other line is not a claim | `lib/py/bbx/docset.py` ABSENCE_GUARD, NUMBER_GUARD | arbitrary (the regexes) / principled (that an unlisted absence or number is loud) | an absence nobody listed and a number nobody bound are the two ways a claim silently leaves R33's denominator; the regexes are the fixture's vocabulary and the first real consumer will find them too narrow or too wide — widening one is a tightening (more is refused), narrowing one is a ruling (BBX-13's shape) (S3 step 2, 2026-09-10) |
| D41 | the schema family's TYPE vocabulary (R34) | `int` `^-?[0-9]+$`; `hex` `^[0-9A-Fa-f]+$`; `str` any cell, the empty one included; `nonempty` any cell but the empty one; the rows line's ops `>=` and `=` | `lib/py/bbx/compare_schema.py` TYPES, OPS | principled (ruled) | R34: the smallest set that distinguishes the fixture's four columns; a fifth type or a third op is a ruling, and a type outside the vocabulary in a frozen file is `FAIL unknown schema type` (never read as `str`). The one consumer is the fixture's TSV until S4's JSON (BBX-25 unmet, stated by `gates/set_schema.sh`) (S3 step 3, 2026-09-10) |
| D42 | the document-set kind's NOTE keys | `coverage <covered>/<END n>`, `paraphrase <n>`, `unbindable <n>`, `stale <n>`, `mismatch <n>` (printed by `bbx.docset summary` from a log); `covered-grew <k>` (printed by `compare_set.py` after a shrink-only PASS that grew, R33); `verdict-lines-frozen <n>` (printed by `gates/set_schema.sh`) | `lib/py/bbx/docset.py` `cmd_summary`, `lib/py/bbx/compare_set.py`, `gates/set_schema.sh` | principled | BBX-18 (coverage is a number on the screen: `coverage` is the readout's key, RO1), R33 (coverage never fatal; growth a note, never a verdict), `docs/controls.md` (a NOTE-class number is `NOTE: <key> <value>` at column 0; every key but `coverage` is listed as a note). The suite carries them onto the screen in S3 step 4 (S3 step 3, 2026-09-10) |
| D43 | `[suite].log_summary` — the command that prints a run log's OWN NOTE-class numbers | kind-blind EMPTY (nothing printed); document-set `python3 -m bbx.docset summary` (D42's keys: `coverage <n>/<m>`, `paraphrase`, `unbindable`, `stale`, `mismatch`); command-line `python3 -m bbx.cli summary` (`exit <n>` or `exit crash`, `band-fields <n>`, `emitted-files <n>`; joined S4 step 2, 2026-09-10) | `lib/py/bbx/config.py` DEFAULTS and KINDS; run by `bin/bbx-run-suite` after a scenario's pairings under the kinds loop, the lines printed at column 0 and kept (D44); skipped when the scenario's schema failed | principled | RO1 and BBX-18 (coverage is a number on the screen), BBX-25 (the suite names no kind's module: the profile does); the frame-driven profile prints none, so F12's text is bbh's; a summary that fails is a suite FAIL naming it (S3 step 4, 2026-09-10) EXTENDED 2026-09-11 (S4 step 5): two CONDITIONAL keys, `cases <n>` and `gates <n>`, printed only by a log that HOLDS such tokens — so the three lines a command-line log prints are unchanged (measured), and an adapter's run carries its own count onto the screen |
| D44 | the kept suite run's `notes.tsv` (beside D30's two files) | `scenario`, `key`, `value` — every `NOTE: <key> <value>` line the suite printed for a scenario: the log summary's (D43) and a comparator's (`covered-grew`, R33) | `bin/bbx-run-suite` `notes`; read by `lib/py/bbx/readout.py` — the suite screen's `coverage:` lines (one per scenario) and `note:` lines | principled | a number printed and not kept is a number the maintainer never saw (bbx-3's first fix); read by field name (BBX-12); the kept rows are PAIRINGS keyed (scenario, kind) since the kinds loop, and BBX-14 on the screen is met over pairings (S3 step 4, 2026-09-10) |
| D45 | the `command-line` kind profile | `instrument_word` tool; no `static_needs_env` (portable); no `tier.patterns`; `[suite]` `scenario_ext` cli, `driver` cli, `rompath_env` = `input_env` = `CLI_PATH`, `hermetic_unset` the driver family `CLI_NONDET` / `CLI_TIMEOUT` / `CLI_KEEP_ENV` (the search path is the suite's variable, D33's shape); `[fingerprint]` file-sha1 over `{set}.py` (the whole-set key `dir_sha1` over the tool's own directory, NOT recursive); kinds: the kind-blind three + `truth` (exact) `unordered` (set) `schema` (schema) `band` (tolerant-numeric); no `[thresholds]`; `log_summary` = `python3 -m bbx.cli summary` (joined with the driver at S4 step 2, as this row said it would; D43) | `lib/py/bbx/config.py` KINDS | principled | R14's fourth profile (docs/plans/S4.md §3 S1; R35–R40); the identity is computed from the tool's bytes, never from what it prints of itself (§3.4); a temporal comparator under it is REFUSED (D23). Limit stated: a tool that is a directory — a package, a script with siblings it imports — is identified by its one file only; such a consumer declares the `command` kind (S4 step 1, 2026-09-10) |
| D46 | the `.cli` scenario grammar (R35) | a TOML-subset file: `[scenario]` `args` (an array of strings, one per argv element — never a shell line), `stdin` (an array of lines, each newline-terminated) or `stdin_file` (relative to the scenario file's directory), `cwd` (relative to the sandbox; default `.`), `emits` (files the tool must produce in its working directory, each a `file` point), `bands` (the fields judged by the band kind), `fields = "json"` (stdout is ONE JSON object; absent: stdout is lines); `[env]` one key per variable the tool may see; nothing in it states an expected value; an unknown key is REFUSED by the driver (exit 3; S4 step 2) | `fixture/fakecli/mkfakecli.py` (writes them); `lib/py/bbx/cli.py` (reads them, S4 step 2) | principled (ruled) | R35: the scenario is an INVOCATION and nothing else (§3.4); SMS cliguard's rule applied to the scenario grammar; `stdin` as lines because the subset refuses escapes in a basic string (X19) (S4 step 1, 2026-09-10) |
| D47 | the command-line OBSERVATION token vocabulary (R31, R35) | `0 exit:<n>` first; `<i> line:<sha1>` per stdout line, or under `fields = "json"` `<i> field:<name>:<sha1>` per key in SORTED order (the hash over the value's CANONICAL JSON: sorted keys, no spaces, non-ASCII kept) and `field:<name>:band` for a band field (its value in the band view `<out>.bands`, `<i> <name>=<value>` / `END <n>`, written by the driver at step 2); `<i> err:<sha1>` per stderr line; `<i> file:<name>:<sha1-of-bytes>` per declared emitted file; `END <n>` with n = the last index; SHA-1 over UTF-8 without the newline; a name holds no space and no colon | `lib/py/bbx/cli.py` — the ONE writer of every token (the generator's truth logs now; the driver's logs at step 2, the S3 shape) | principled (ruled) | R31: the point stays an integer and `logfmt` stays bbh's; R35: the exit status is its own point, so a tool's refusal is a frozen scenario and a crash (exit 2, `END-CRASH`) is apart from it; the band value lives outside the log so the exact family never fails on a legitimate move (BBX-16) (S4 step 1, 2026-09-10) EXTENDED 2026-09-11 (S4 step 5, ruled R37 and the plan's §3 D7/§7, which listed all seven kinds from the start): `<i> case:<verdict>:<sha1 of the case NAME>` and `<i> gate:<verdict>:<sha1 of the gate NAME>`, each verdict from its closed vocabulary (D59) — the two adapters' tokens, written by the same module; nothing else in the row moved, and the command-line logs are byte-identical (measured by `gates/cli_driver.sh` and `gates/cli_suite.sh` after the change) |
| D48 | the `band` expectation's shape (R36) | `[spec]` `class = "band"`, `baseset`, `measured` (how the band was measured, in words), `rulings` (optional inline table `<field> = "R<n>"`); `[b<i>]` `field`, `min`, `max` — inclusive INTEGERS in the field's own unit (the subset refuses floats: a fractional band is a consumer's question, stated NOT-ASSERTED) | `fixture/fakecli/mkfakecli.py` (writes it: `size` over seeds 1..9 of the design, `[236, 270]`, class `fixture`); `lib/py/bbx/compare_band.py` (reads it, S4 step 3) | principled (ruled) | R36: a band is what was measured, never a knob (VampireSaved's `+/- 30` kept through two re-centres; 0 tools there take a tolerance option); the `rulings` table is R25's shape on the frozen file so `--freeze` can refuse to widen without a ruling id (S4 step 1, 2026-09-10) |
| D49 | the JSON schema spec's shape and TYPE vocabulary (R40; R34's second consumer) | `[spec]` `class = "schema"`, `baseset`, `format = "json"`; `[k<i>]` `name`, `type`, the keys in SORTED order and the type one of `int` `number` `str` `bool` `null` `list` `object`; a list-valued key adds `items_op` (`>=` or `=`) and `items_n`; a nested object is one `object` key | `fixture/fakecli/mkfakecli.py` (writes it: five keys, `str str int int list`); `lib/py/bbx/compare_schema.py` (reads it, S4 step 3; D41's TSV vocabulary untouched) | principled (ruled) | R40: the smallest set that names every JSON value kind once; two formats, two vocabularies, one comparator; a type outside it is `FAIL unknown schema type`, never read as `str` (S4 step 1, 2026-09-10) |
| D50 | the command-line fixture's CHIRALITY predicates (BBX-15) | D36's ten on the tool's table, plus: the rank order is neither the name order nor its reverse; no name, id or weight is shared with `fixture/docset`'s design (read from `mkdocset.py` through the harness root — measured, never assumed); the join scenario's stdin lines are in neither sorted order | `fixture/fakecli/mkfakecli.py` `predicates()`, asserted on the design and on the TREE's tool (its `RECORDS` literal read back by `ast`) under `--check`, each named when it fails | principled | an order error cannot hide behind a sorted listing, and a reader that confused the two fixtures could not read green on either; `gates/cli_fixture.sh` control `symmetric-fixture` (S4 step 1, 2026-09-10) |
| D51 | `CLI_TIMEOUT` — seconds the command-line driver lets the tool run before killing it | 60 | `drivers/cli.sh` (the variable; a non-number or 0 REFUSED, exit 3); `lib/py/bbx/cli.py` `DEFAULT_TIMEOUT`, `run --timeout`; elapsed: the tool killed, no log written, exit 1 DISCARDED | arbitrary | D1's value and reason: no fixture scenario runs above 1 s (the `--sleep` control uses 1 and 5), 60 leaves room for a real tool without hiding a hang for a minute more; the second consumer is the detector (BBX-24). Scrubbed by the suite (D45): a caller sets it through a wrapper driver, never the environment (S4 step 2, 2026-09-10) |
| D52 | the SUBJECT's environment under the command-line driver | exactly D6's set with the sandbox as `HOME` and `TMPDIR` (`PATH` pinned, `LANG`/`LC_ALL` `C.UTF-8`, `PYTHONDONTWRITEBYTECODE=1`) PLUS the scenario's `[env]` table and nothing from the caller; the sandbox is also the working directory (the scenario's `cwd` under it), a fresh `mktemp -d` removed after the run when omitted; a `<set>.py` runs under the harness's own python3 (`sys.executable`), an executable `<set>` as itself; `[env]` may not name one of D6's six; `CLI_KEEP_ENV` REFUSED | `lib/py/bbx/cli.py` `hermetic_env()`, `load_scenario()`, `run()`; `drivers/cli.sh` | principled | D6 (G9: a measurement must not depend on the caller's shell) applied to a subject, [BBH-35] and [BBH-36] (the sandbox is the machine's home); what was fed is RECORDED in the sandbox — `argv.txt`, `stdin.bin`, `env.txt` — so a gate reads it back against the scenario (O5); `gates/cli_driver.sh` control `env-scrub` measures the premise on the tool called directly before asserting the scrub (S4 step 2, 2026-09-10) |
| D53 | what the command-line driver writes beyond D47's log | the CRASH log (D4 exit 2): no `0 exit:` line (a tool killed by a signal has no status), the stdout then stderr points it produced, `CRASH signal:<n>:<NAME>`, `END-CRASH <last index>` — never compared (logfmt.frames does not read it); the band view `<out>.bands`: `<i> <name>=<canonical value>` per declared band field the object carries, sorted, i = 1..k, `END <k>` (k the inventory's size, BBX-13's watch), written only when stdout was one JSON object and `bands` is declared; under `fields = "json"` a stdout that is not ONE JSON object is observed as LINES (a subject divergence the truth names, never a discard); a non-UTF-8 output is exit 1 DISCARDED | `lib/py/bbx/cli.py` `crash_log()`, `band_view()`, `run()` | principled | R35 (a crash apart from a refusal by construction: bbh's guarded grammar ends `END-CRASH`, [BBH-33]); BBX-16 (the band value lives in its own view); BBX-4 (a wrong shape is a divergence, a killed tool is a discard, two reports); `gates/cli_driver.sh` control `crash-vs-exit` (S4 step 2, 2026-09-10) |
| D54 | the command-line driver's JSON VIEW `<out>.json` | the canonical JSON text of the ONE object stdout held (`bbx.cli.canon`: sorted keys, no spaces, non-ASCII kept — the text the `field:` hashes are over, D47) plus a newline; written whenever the observation was read as fields (`fields = "json"`, exit 0, stdout one object), so a scenario with band fields has both views; removed with the log and the band view before every run | `lib/py/bbx/cli.py` `run()`; `drivers/cli.sh` (removes it); read by `lib/py/bbx/compare_schema.py`'s json format as its artifact — passed by the caller (`gates/json_schema.sh`; the suite's hand-over is S4 step 4's) | principled | the schema is judged on the OBJECT (D49) and the kept run's log holds hashes only, so the shape must live where the comparator can read it: a view beside the log, D53's shape, never a re-derivation from hashes and never a second run of the tool (the plan's silence on where the object lives, decided here; docs/plans/S4.md §3 "E1"). The subject file the kinds loop hands the schema family today (`bbx.fingerprint --path`) is the wrong artifact for this format — measured by the gate, `FAIL schema: the artifact is not one JSON object` (S4 step 3, 2026-09-10) |
| D55 | `compare_band --freeze`: what a re-freeze of a band may do without a ruling | `--freeze <frozen.band> <proposed.band>`: the frozen file is replaced by the proposed BANDS (its class and baseset kept, the proposed `measured` and `rulings` taken) when no band widens on either side; a band that widens needs its field in the PROPOSED file's `[spec].rulings` or the whole freeze is REFUSED (exit 3, the file unchanged); narrowing is free; a field in one file and not the other is REFUSED (the inventory never moves through a freeze); the written text is the generator's shape byte for byte | `lib/py/bbx/compare_band.py` `freeze()`, `render()` | principled | R36 (a band is loosened only with a measured mechanism named and a ruling: VampireSaved's `+/- 30` kept through two re-centres); BBX-13 (a growing tolerance inventory means stop and root-cause: the inventory is edited by hand under the register, never by the freeze); the proposed file carries the measurement and the ruling together, R25's shape on the frozen file (D48). What the freeze cannot check — that the ruling id exists and names a mechanism — is `gates/band.sh`'s NOT-ASSERTED (S4 step 3, 2026-09-10) |
| D56 | the set family's LINE row shape (the `unordered` kind) | a frozen row is `[l<i>]` `line` (the text) and `sha1` (the line's, `bbx.cli.sha1_text`); the shape is told from the claim shape by the row's fields, a file that mixes them is malformed, a file with no rows reads as the claim shape; the run's rows are the log's `line:<sha1>` points (D47) as a multiset, compared by sha1 both ways; a frozen row whose sha1 is not its line's is hand-editing (FAIL); a frozen duplicate is hand-editing (FAIL); a run duplicate is a surplus copy named by the count; the shrink-only mode is refused under this shape; the artifact and the scenario-file arguments are accepted and not read | `lib/py/bbx/compare_set.py` `load_shape()`, `line_rows()`; written by `fixture/fakecli/mkfakecli.py` | principled | R34's caveat (named fields say the shape, so no `rows =` key is needed); BBX-17 (multisets both ways, a duplicate a signal of hand-editing); BBX-25 met for the set family by its second consumer with `lib/sh/compare.sh`'s set branch unchanged; a tool whose unordered output legitimately repeats a line cannot be frozen under this shape — stated NOT-ASSERTED by `gates/json_schema.sh`, a consumer's question (S4 step 3, 2026-09-10) |
| D57 | the VIEW column of the kinds table — what the kinds loop hands a family as its artifact | a fourth field per kinds row: `-` none (`skip`, `pending`), `log` the run log itself (`sha1`, `masked`, `diverge`, `truth`, `unordered`), `subject` the subject file the identity was computed from (`bbx.fingerprint --path`: the document-set's `claims`, `covered`, `schema`), `json` the driver's JSON view `<log>.json` (the command-line `schema`, D54), `bands` the band view `<log>.bands` (`band`, D53); resolved by `view_path` in `bin/bbx-run-suite` and nowhere else, passed as `compare_check`'s artifact argument (the tolerant-numeric branch hands it to `compare_band`, which derives no path); a view the loop cannot resolve is a FAIL at the entrance, after the identity and before any scenario runs; a row without its four fields is refused by `bbx.expectations` (one line, exit 1) | `lib/py/bbx/config.py` DEFAULTS and KINDS `[expectations].kinds`; `lib/py/bbx/expectations.py` `table()`, `kinds`, `view`; `bin/bbx-run-suite` `view_of`, `view_path`; `lib/sh/compare.sh` `compare_band` | principled | R23 (the family and the VIEW are declared by the kind, never inside a spec line) and BBX-16 (every comparator declares which view it reads): before this row the loop handed every family the subject file, the wrong artifact for the json format (measured at bbx-15 by `gates/json_schema.sh`: `FAIL schema: the artifact is not one JSON object`) and the band family derived its own `<log>.bands` — two resolvers, one of them implicit. Vocabulary closed; a fifth view is a row amendment and a `view_path` case, never a family's private derivation. The plan's zero-line target for the loop (docs/plans/S4.md §3 E1) did not survive: 29 lines added, 5 removed, measured (`git diff --numstat`); D25's shape and D45's kinds row amended by this row (S4 step 4, 2026-09-10) |
| D58 | the ADAPTERS' set: what `<set>` names for a driver of this kind that drives a framework | a DIRECTORY named `<set>` on `CLI_PATH` (the first component that holds one wins), where the command-line driver's `<set>` is a FILE — `bbx.adapters resolve`, the ONE resolver of the two adapters; a set that is no directory there is exit 1 DISCARDED naming the set and the path | `lib/py/bbx/adapters.py` `resolve_set`; `drivers/unittest.sh`, `drivers/gates.sh` | principled | R37's two frameworks are both directories of files (a unittest package, a consumer with its gates), so the resolver's shape follows the subject rather than the other way round; measured at bbx-18 over both fixtures (S4 step 5) |
| D59 | the adapters' CLOSED verdict vocabularies — a framework's own words mapped to a token | unittest: `ok`, `FAIL`, `ERROR`, `skip` (from `skipped '<reason>'`, the reason NOT hashed), `xfail` (`expected failure`), `xpass` (`unexpected success`); the static runner: `PASS`, `FAIL`, `SKIP`, `TIMEOUT`, `MISSING`. A tail or a column value outside the vocabulary is exit 1 DISCARDED, never mapped to the nearest word | `lib/py/bbx/cli.py` `CASE_VERDICTS` / `GATE_VERDICTS` and `split_token`; `lib/py/bbx/adapters.py` `CASE_TAILS` | principled | ruled R37 (the framework's verdict is an OBSERVATION, never a BBX verdict). The six unittest words were MEASURED on this host at bbx-18 from a probe module carrying one case of each; the five runner words are `bin/bbx-classify`'s four plus the runner's own `MISSING` (S4 step 5) |
| D60 | what a gates scenario may ask the driver to fill in, and where its program comes from | three placeholders in `args` — `{config}` the set's `bbx.toml`, `{log}` the kept run under the sandbox, `{set}` the set directory — and any other `{…}` is REFUSED (exit 3); the FIRST arg is the program, resolved under `$BBX_HOME/bin` against that directory's own listing and REFUSED (exit 3) if it is not an executable file there | `lib/py/bbx/adapters.py` `fill` / `resolve_program`; `drivers/gates.sh` | principled | D3's rule that a knob is refused rather than ignored, applied to the scenario: nothing is appended behind the scenario's back and nothing is looked up on a PATH. Measured both ways at bbx-18 by `gates/adapters.sh`'s `refusals` control (S4 step 5) |
| D61 | the one variable the unittest subject gets beyond D6's hermetic set | `PYTHONPATH = <the set directory>` — nothing else; the working directory stays the sandbox and `PYTHONDONTWRITEBYTECODE=1` is already in D6's set, so the fixture keeps no `__pycache__` (a gate control) | `lib/py/bbx/adapters.py` `run_unittest` | principled | a package must be importable to be run at all, and the alternative (running with the package as the working directory) would put the subject's writes inside the fixture; measured at bbx-18 that no `__pycache__` appears under `fixture/unittest` after 14 suite runs (S4 step 5) |
| D62 | the FILE CENSUS's subject identity and its frozen kind-sets (R39) | the census is keyed by R38's WHOLE-SET identity — the tree hash of `bin`, `lib`, `drivers` and `gates` at HEAD, hashed into one — and never by `HEAD`; the generated region of `docs/census/bbx_files.md` is delimited by a `GENERATED by \`bbx file-census\`: BEGIN` / `: END` comment pair and nothing outside it is written; `expected/file_census.toml` freezes one `[f<i>]` table per universe file with `file` and `kinds` (a subset of `KFDC`), class `self`, mode `shrink-only`: a lost kind FAILs naming the file and both sides, a file reached by NO gate FAILs ALWAYS (a fresh freeze cannot hide it), a frozen file outside the universe is a dead row, a file frozen twice is hand-editing, and a GAINED kind is `NOTE: file-census-grew` which re-freezes | `lib/py/bbx/file_census.py` `identity`, `BEGIN`/`END`, `render_frozen`, `compare_frozen`; read by `gates/file_census.sh` and proven on a synthetic tree by `gates/file_census_tool.sh` | principled | `HEAD` is the wrong key for a census that LIVES IN THE TREE it describes: the commit that writes the regenerated document moves `HEAD`, so `--check` would fail on its own output for ever after — measured at bbx-19 as the gate's own first red, and fixed by reusing the identity R38 already ruled (a docs-only commit leaves it unchanged; a commit touching the harness moves it, which is when the census IS stale). `self` is the true class because this is BBX's measurement of BBX: it locks CURRENCY and never correctness (§3.3, R11). The two writers of the key are proven to agree by the portable gate (S4 step 6, 2026-09-12)  AMENDED 2026-09-12 (R47): the register is ALSO read cheaply, with no shadow and no gate run, by `bbx file-census --check-register` — complete both ways against the universe (a universe file with no row and a row naming a file the universe does not hold are both FAIL), plus `NOTE: census-drift register=<a> tree=<b>` when the register's `measured_at` is not the tree's identity, which is LOUD and never fatal on R18's precedent that the register rotting and the harness moving are different findings. Held every battery by `gates/census_register.sh` (~1 s, 3 controls); the KINDS in a row are not read there at all  AMENDED 2026-09-13 (R46, bbx-23): the key is DEFINED once, in `lib/py/bbx/fingerprint.py` `harness_identity` (`python3 -m bbx.fingerprint --harness-identity program\|wholeset [--root DIR]`, the root defaulting to `$BBX_HOME`); `file_census.identity` delegates to it and `fixture/selfgates/idkey.sh` is a shim over it; `gates/fingerprint.sh` §4 checks both keys against R38's formula computed without the module, and `gates/file_census_tool.sh` §9 still proves the census and the shim read one key |
| D63 | the file census's CADENCE — why the measurement is not on every battery | `gates/file_census_tool.sh` is PORTABLE and runs the instrument on a synthetic tree every battery (~13 s measured); `gates/file_census.sh` is the first row of `gates/sweep.tsv` — lane `prereq`, scope `release`, cadence `always`, its own 7th-column timeout — and runs the instrument over BBX's own tree, which means the WHOLE battery inside a shadow. BBX's first `[tier].patterns` entry (`file-census --self`) is what classifies it INSTRUMENT, so the sweep row is a registration and not an orphan | `gates/sweep.tsv`; `bbx.toml` `[tier].patterns`; `lib/py/bbx/config.py` kind-blind `[sweep]` (lanes, release_scope, cadences, freeze_cadence, default_timeout 600) | principled | R39 declined "a portable gate that re-runs the battery every battery (about 9 minutes doubled on every close)" and asked for both gates instead, so the INSTRUMENT is under test continuously while the MEASUREMENT is release-scoped. Measured 2026-09-12: `lib/py/bbx/tier.py` reads `known = portable \| static` and never the sweep registry, so without the pattern the gate would be named by `--unregistered` while the battery stayed green (X36; G34's direction, R45) (S4 step 6, 2026-09-12) |
| D64 | the skills lock's settings, `[skills]` — `[skills].prefixes`, `[skills].history_regex`, `[skills].history_exempt`, `[skills].guided`, `[skills].guide_origin`, `[skills].guide_header` (S5 step 1) | `prefixes = []`, `history_regex = '_(history\|HISTORY)\.md$'`, `history_exempt = []`, `guided = []`, `guide_origin = ""`, `guide_header` = the lineage's opening lines — bbh `lib/py/bbh/config.py` at `10a82d2`, verbatim | `lib/py/bbx/config.py` kind-blind `DEFAULTS["skills"]`; read by `lib/py/bbx/checkskills.py` and `lib/py/bbx/gen_skill_guide.py`; a consumer's `[skills]` and `[skill_<PFX>]` tables override | principled (the four empty values: no skill, archive or origin is implied) / reference-calibrated (`history_regex` and `guide_header`: the lineage's twin naming and its exact header text) | bbh's values verbatim, so bbh's own skill and VampireSaved's eight skills read identically through both tools (F18, `gates/fidelity_bbh_s5.sh`, R56); bbh's `docs/config.md` documents these six settings beside five per-skill keys (measured bbx-26). `bbx config <file> dump` prints the consumer's file and never the defaults (`lib/py/bbx/config.py` `main`), and the F13e pair stayed identical with the new section: `F13e config dump of example/bbh.toml: identical (108 lines)`, `gates/fidelity_bbh.sh` PASS (measured bbx-26, S5 step 1, 2026-09-14; G68) |
| D65 | the lifted lock's and guide generator's code constants (S5 step 1) | a rule definition is `- [PFX-N]` opening a line and its anchor `**[PFX-N]**`; the frontmatter is `name: [a-z0-9-]+` then `description:`; six number patterns — `0x` hex of 3+, `$` hex of 4+, comma-grouped integers, decimals, integers of 4+ digits, lowercase hex of 8+ — with `20xx` years and `d.d` section numbers skipped; a forbid token matched case-insensitively as a substring of each body line; the guide quotes a run-on block as a `WINDOW` of 1800 characters around its anchor | `lib/py/bbx/checkskills.py` (`DEF_SKILL`, `ANCHOR_DOC`, `NUM_PATTERNS`, `numbers`, `check`), `lib/py/bbx/gen_skill_guide.py` (`WINDOW`) — bbh `10a82d2`, verbatim | reference-calibrated (the number patterns and the forbid matching: a ROM lineage's, G64) / arbitrary (`WINDOW`) | carried verbatim so F18 can diff them; measured at bbx-26 that the number patterns find 124 tokens in VampireSaved's skills and none in CLAUDE.md, and that a substring match makes short tokens hit ordinary words (`docs/plans/S5.md` §3.1, §4). R54 rules a per-skill number vocabulary and a one-line definition over these — built at S5 step 2 (D66) |
| D66 | the lock's two BBX deltas (S5 step 2, R54) | a definition followed by a continuation line FAILS naming the rule and the line — a continuation is an indented non-blank line, or an unindented one that opens no list item, heading, block quote, fence or thematic break; `[skill_<PFX>].numbers` is `lineage` (the default: D65's six patterns and bbh's substring test) or `integers`, which also reads every plain integer — not glued to a word character, not after `-` `.` `,` `$` `§`, not the head of a decimal or a comma group, not a heading or list ordinal, `20xx` years skipped — and finds it in the logs only as a whole integer token; any other value is refused naming the key | `lib/py/bbx/checkskills.py` (`continues`, `INTERRUPT`, `VOCABULARIES`, `INT_TOKEN`, `ORDINAL`, `integers`, `load_skills`, `check`, and its selftest); proven both ways by `gates/skills.sh` | principled (the one-line definition: the guide quotes one line, G63; what interrupts a list item: CommonMark's blocks; the token match: a test that can fail) | measured at bbx-26 before the build: 0 of the 642 definitions F18 reads continue onto a second line, so delta 5 is fidelity-neutral; over `docs/gotchas.md` and `docs/census/*.md` all 90 two-digit integers occur as substrings and 89 as tokens, and of 900 three-digit integers 662 as substrings and 355 as tokens — a substring test could never fail on a two-digit figure, and even the token test is weak for small figures over logs that broad (S5 step 2, 2026-09-14) |
| D67 | the ledger reader's grammar (S5 step 3, R53) | the rules are the `- [BBX-<n>]` lines under CLAUDE.md's `## 4.` heading, up to the next `## `; an entry is a `## G<n> — ` heading, the ids G1..GN in order, and any other `## ` heading or a `G<n>` heading at another level is an error; a list opens at a case-sensitive lead — `Re-anchors:`, `Re-anchors`, `Rule re-anchored in fact:`, `Rules re-anchored in fact:` — and is read inside its paragraph with the paragraph's lines joined, up to the end of its sentence (a `.` then a capital or `*`, or a final `.`), a colon or an em dash, with parentheses, quotations (straight and curly) and code spans (a backtick run closed only by a run of its own length) skipped; a lead inside any of them is prose, not a list; a paragraph holding a lead whose parentheses, quotations or code spans do not close is an error, never a guess; from G61 on the one lead is `Rules re-anchored in fact:`; an entry with no list is named on the ledger NOTE, never failed | `lib/py/bbx/reanchors.py` (`RULE_DEF`, `ENTRY`, `H2`, `G_HEADING`, `LEAD`, `LIST_END`, `ONE_WORDING`, `read_rules`, `read_entries`, `prose_mask`, `read_lists`); proven both ways by `gates/skills.sh` §7 | reference-calibrated (the leads and where a list ends: BBX's own ledger G1–G60, `docs/plans/S5.md` §3.2 and appendix A) / principled (the one wording from G61: R53; a code span's closing run: CommonMark) | measured at bbx-27 before the build: through G60 the reader reproduces the plan's §3.2 table rule by rule, 23 re-anchored and 7 inherited, and over the whole ledger reads 68 lists and 0 errors (both gated in `gates/skills.sh` §7); it names the same references as appendix A's prototype in all 69 entries, and of the ledger's 137 entry paragraphs none has an odd backtick count or an unpaired straight or curly quotation outside code (measured once, not gated; S5 step 3, 2026-09-14) |
| D68 | the skill generator's grammar and table keys (S5 step 4, R51, R52) | the rules section is the constitution's lines under `section` (`## 4.`) up to the next `## `; a bold-only line `**<group>**` opens a group; a rule is `- [PFX-N] `[tag]` body` and the indented lines under it, joined with single spaces; the `<cite>-N` ids its tag names (`cite = "BBH"`) are appended in order as ` [BBH-N]`, and a range (`..`) or a slash beside one is refused; SKILL.md is the frontmatter from `name` and `description`, `# <title>`, the `intro` lines, then `## <group>` and one line per rule; the page is the `page_header` lines, then `## <group>` and one paragraph per rule, `**[PFX-N]** RE-ANCHORED by <n> entr(y/ies) of <ledger> whose explicit list names it: <heading>; …` or `**[PFX-N]** INHERITED: … the constitution tags it <tag> — a candidate for dropping`; the ten keys `name`, `description`, `title`, `intro`, `constitution`, `section`, `ledger`, `page`, `page_header`, `cite` are required, `page` must be one of `docs`, and the rule-to-entries relation is `bbx reanchors`' (D67), never re-read | `lib/py/bbx/gen_skill.py` (`KEYS`, `GROUP`, `CONTINUATION`, `read_section`, `citations`, `render_skill`, `render_page`, `main`); `lib/py/bbx/reanchors.py` `registry`; proven both ways by `gates/skills.sh` §8 | principled (R51's skill, R52's page, R53's one reader) / reference-calibrated (the group and rule grammar: CLAUDE.md §4 as it stands) | measured at bbx-27 on the first run: 30 rules in 7 groups, the ids `bbx reanchors` reads, the six `[BBH-N]` ids the tags of BBX-1, BBX-14 and BBX-20 name; the lifted lock over the output reads ALL PASS with 2 numeric tokens quoted, and the lifted guide generator writes a guide `--check` reads current (S5 step 4, 2026-09-14) |
| D69 | BBX's own skill table, `skill/skills.toml` (S5 step 4, R54, R57) | name `bbx`, level 0 in its title; `path = "skill/bbx/SKILL.md"`, `docs = ["docs/rules.md"]`, `logs = ["docs/bins/vampiresaved.md", "DECISIONS.md"]`, `numbers = "integers"`; `forbid` = bbh's 33 tokens plus `vampiresaved`, `sailor moon`, `fbneo`, `[SMS-`, `[SSP-`, `[SNES-`; `guided = ["BBX"]`, `history_exempt = ["STATE_HISTORY.md", "DECISIONS_HISTORY.md"]`, BBX's own `guide_origin` and `guide_header`; the generator's keys of D68 | `skill/skills.toml` | principled (the name, the level, the shape of the forbid list and `integers`: R57 and R54; no `[BBH-` token and no `bbh`: R4) / reference-calibrated (the logs: the narrowest pages quoting the skill's two integers, `19` in `docs/bins/vampiresaved.md`'s V-X13 row and `0` in `DECISIONS.md`'s R57 row) | measured at bbx-27: the two logs together hold 40 of the 90 two-digit and 21 of the 900 three-digit integers as tokens, against 86 and 263 with `docs/census/vampiresaved.md` in place of the bins page; the bracket prefixes beyond bbh's list read at SMS `ecc5481` (`[SMS-` 122 occurrences, `[SSP-` 75, `[SNES-` 28) and at VampireSaved `0cdd9726` (none: its `[AW-` is Verilog, `addr[AW-1]`); the lock over the generated skill reads ALL PASS, so no token hits its body (S5 step 4, 2026-09-14) |
| D70 | the kind-blind `[project].root` and `[project].instrument_word` | `[project].root` `.`; `[project].instrument_word` `instrument` | `lib/py/bbx/config.py` DEFAULTS; `root` read by `config.py` `root_of`, joined to the config file's directory; `instrument_word` read by `bin/bbx-run-static`, `bin/bbx-run-sweep` and `lib/py/bbx/tier.py` | principled (`root`) / arbitrary (`instrument_word`) | `root`: paths in a config are relative to the config file's directory (`config.py`'s docstring), so `.` is that directory, and a consumer config may live outside the tree it describes by naming another (`lib/sh/config.sh`); bbh's value at `10a82d2`, unchanged. `instrument_word`: bbh's is `emulator`, which the frame-driven profile keeps (D12); the document-set and command-line profiles name `extractor` and `tool` (D33, D45) and the self profile repeats `instrument` (`config.py` KINDS); the kind-blind word arrived with S1 step 2 (`b212831`). Both values compared with bbh's `DEFAULTS` at `10a82d2`, the readers found by `git grep` over `bin lib drivers gates` (measured bbx-29) |
| D71 | the kind-blind `[registries].sweep`, `[registries].static_needs_env` and `[tier].patterns` | `[registries].sweep` `tests/ci_sweep.tsv`; `[registries].static_needs_env` empty; `[tier].patterns` empty | `lib/py/bbx/config.py` DEFAULTS; `sweep` read by `bin/bbx-run-sweep` and `lib/py/bbx/tier.py`; `static_needs_env` by `bin/bbx-run-static`, `lib/py/bbx/tier.py` and `lib/py/bbx/file_census.py`; `patterns` by `lib/py/bbx/tier.py` | arbitrary (`sweep`) / principled (`static_needs_env`, `patterns`) | the two empty values imply nothing, in DEFAULTS' own words: "the static tier always runs; a kind names its input variable" and "nothing reaches an instrument; a kind or a consumer says what does" — the frame-driven profile names bbh's `ROMDIR` and ten patterns (D12), the self profile `BBX_BBH_HOME` (D13), and `bbx.toml` its own pattern (D63). `tests/ci_sweep.tsv` keeps bbh's `tests/ci_` layout (D8) where bbh's value is `tests/ci_emulator.tsv`, which the frame-driven profile keeps; it arrived with S1 step 3 (`ee0a7bb`). Values compared with bbh's `DEFAULTS` at `10a82d2`, readers by `git grep` (measured bbx-29) |
| D72 | the rest of the kind-blind `[sweep]`: `[sweep].default_lanes`, `[sweep].prereq_lane`, `[sweep].freeze_cadence`, `[sweep].cadence_drop_note`, `[sweep].precondition_fail_text`, `[sweep].rompath_placeholder_suffix`, `[sweep].rompath_suffix`, `[sweep].build_sets`, `[sweep].env_defaults`, `[sweep].prereq_cite` | `[sweep].default_lanes` `prereq`; `[sweep].prereq_lane` `prereq`; `[sweep].freeze_cadence` `always`; `[sweep].cadence_drop_note` empty; `[sweep].precondition_fail_text` `precondition FAILED — stop`; `[sweep].rompath_placeholder_suffix` `_RP`; `[sweep].rompath_suffix` empty; `[sweep].build_sets` empty; `[sweep].env_defaults` empty; `[sweep].prereq_cite` empty | `lib/py/bbx/config.py` DEFAULTS; each read by `bin/bbx-run-sweep` and by nothing else under `bin lib drivers gates` | principled (`default_lanes`, `prereq_lane`, `freeze_cadence` and the five empty values) / arbitrary (`precondition_fail_text`, `rompath_placeholder_suffix`) | `default_lanes`, `prereq_lane` and `freeze_cadence` name D15's one lane and one cadence; the five empty values name no drop note, rompath suffix, build set, environment row or citation for a sweep with no instrument (D15). The failure text is a wording and `_RP` is bbh's literal. Eight of the ten differ from bbh's at `10a82d2` and the frame-driven profile carries bbh's for those eight (D12); `prereq_lane` and `rompath_placeholder_suffix` equal bbh's. The failure text arrived with S1 step 3 (`ee0a7bb`). Values compared with bbh's `DEFAULTS` at `10a82d2`, readers by `git grep` (measured bbx-29) |
| D73 | the kind-blind `[fingerprint].program_member_regex`, `[fingerprint].program_command` and `[fingerprint].wholeset_command` | `[fingerprint].program_member_regex` `\.(\d+)$`; `[fingerprint].program_command` empty; `[fingerprint].wholeset_command` empty | `lib/py/bbx/config.py` DEFAULTS; read by `lib/py/bbx/fingerprint.py` — the regex under the `zip-members` kind, the two commands under the `command` kind, which exits with `kind 'command' needs [fingerprint].program_command and wholeset_command` when the one it needs is empty | principled (the two empty commands) / arbitrary (`program_member_regex`) | a command that prints a key is the consumer's to write, and the two empty values are bbh's at `10a82d2`. The regex takes a member's numeric extension as its order (the module's docstring: "group 1 = the member's ORDER, an integer"); bbh's is VampireSaved's program-member pattern, which the frame-driven profile keeps (D16, F14f), and no kind profile but the frame-driven one selects `zip-members`. The regex arrived with S1 step 3 (`ee0a7bb`). Values compared with bbh's `DEFAULTS` at `10a82d2`, readers by reading `fingerprint.py` (measured bbx-29) |
| D74 | the kind-blind `[suite].replays_dir`, `[suite].expected_dir`, `[suite].driver`, `[suite].rompath_env`, `[suite].input_env` and `[suite].hermetic_unset`, and `BBX_REPLAYS_DIR` when unset | `[suite].replays_dir` `tests/replays`; `[suite].expected_dir` `tests/expected`; `[suite].driver` empty; `[suite].rompath_env` empty; `[suite].input_env` `BBX_INPUT`; `[suite].hermetic_unset` empty; `BBX_REPLAYS_DIR` unset: `tests/replays` | `lib/py/bbx/config.py` DEFAULTS, each read by `bin/bbx-run-suite` (`expected_dir` also by `lib/py/bbx/provenance.py` and `lib/py/bbx/readout.py`); the fallback in `lib/sh/expectation_kinds.sh` `enumerate_expectations`, after its third argument | arbitrary (`replays_dir`, `expected_dir`, the `BBX_REPLAYS_DIR` fallback, `input_env`) / principled (`driver`, `rompath_env`, `hermetic_unset`) | `replays_dir` and `expected_dir` are bbh's layout literals at `10a82d2`, kept so a bbh config reads unchanged (D8's reason), and the fallback repeats `tests/replays`; `input_env` is the name `[sweep].input_env` carries too (D15). The three empty values imply nothing, in DEFAULTS' own words: a consumer names its driver, "none = FAIL at the entrance"; "empty = the input directory is the search path"; and no variable is scrubbed until a profile names its driver family ("the frame-driven profile carries bbh's eight"; the document-set's and command-line's three in D33 and D45). Values compared with bbh's `DEFAULTS` at `10a82d2`, readers by `git grep` (measured bbx-29) |
| D75 | `BBX_HOME` when a program is started without it | the root of the tree the program sits in: `$(cd "$(dirname "$0")/.." && pwd)` in `bin/bbx-run-sweep`, `drivers/cli.sh`, `drivers/docset.sh`, `drivers/gates.sh` and `drivers/unittest.sh`; the root above the module in `lib/py/bbx/adapters.py` `bbx_home` and `lib/py/bbx/file_census.py` `harness_home`; the working directory for `file_census.py --self`; none for `python3 -m bbx.fingerprint --harness-identity`, which prints its usage and exits 2; `lib/sh/compare.sh` and `lib/sh/expectation_kinds.sh` leave `PYTHONPATH` as they found it | the files named | principled (the program's own tree) / arbitrary (the working directory of `file_census.py --self`) | `bin/bbx` sets `BBX_HOME` from its own path on every call (`bin/bbx:27`), so these apply to a program run by its own path; the harness is where the program is, never where the caller stands (`adapters.py`: "never from the caller's cwd (a consumer copied under TMPDIR has no harness beside it)"), with the census tool's `--self` the one reader that takes the working directory. Read at each site (measured bbx-29) |
| D76 | `BBX_BBH_HOME` when unset | empty: no lineage tree | `gates/fidelity_bbh.sh`, `gates/fidelity_bbh_s2.sh`, `gates/fidelity_bbh_s5.sh` and `gates/suite.sh` SKIP with `SKIP: BBX_BBH_HOME is not a bbh tree (unset)`; `gates/file_census.sh` SKIPs with its reason; `bin/bbx-run-suite` FAILs naming it when `[suite].driver_home` is `$BBX_BBH_HOME` (R26); it is the self profile's `[registries].static_needs_env` (D13), read by `bin/bbx-run-static` | principled | the lineage's place is a fact about the host and has no default value: unset, each reader SKIPs or FAILs with the variable named, never a guessed path. Read at each site (measured bbx-29) |
| D77 | `BBX_CONFIG` when unset | the shell side, `lib/sh/config.sh` `bbx_config_init`: the first argument, else `BBX_CONFIG`, else `bbx.toml` then `bbh.toml` in the working directory, else exit 2 naming the file; the Python side, `lib/py/bbx/config.py`, `fingerprint.py` and `thresholds.py`: the path passed, else `BBX_CONFIG`, else no consumer file — every key from the default kind's profile and the kind-blind values, and in `config.py` the working directory as the root | the files named | principled | a runner never runs on a config it did not find: the shell side exits naming what it looked for, and exports the absolute path for every Python reader it calls; `bbh.toml` is the name of bbh's example config, which the fidelity pairs run BBX's runners over (F13). Read at each site (measured bbx-29) |
| D78 | `BBX_FIDELITY_F15` when unset | F15 not run | `gates/fidelity_bbh.sh`: F15 runs only when the variable is `1`; otherwise the gate prints `== F15. not run (set BBX_FIDELITY_F15=1; …) ==` and its PASS line ends `F15 not run` | arbitrary | F15 runs bbh's whole selftest through both classifiers; off by default, and declared on the screen by the gate's `NOT-ASSERTED: F15 unless BBX_FIDELITY_F15=1 was set for the run`. It arrived with S1 step 2 (`b212831`). Read at the gate (measured bbx-29) |
| D79 | `BBX_FILE_CENSUS_OUT` when unset | `$T/run`, inside the gate's own `mktemp -d`, which its EXIT trap removes | `gates/file_census.sh` | principled | by default the release gate removes its run and leaves nothing on the host; set, the run's output goes to the named directory and outlives the gate. No header line names the variable, and before this row `git grep BBX_FILE_CENSUS_OUT` read its one assignment (measured bbx-29). It arrived with S4 step 6b (`4802d58`) |
| D80 | `BBX_FILE_CENSUS_SHADOW` when unset | empty: not inside an instrumented census shadow | set to `1` only by `lib/py/bbx/file_census.py`, in the shadow's environment; read by `gates/census_register.sh`, which SKIPs with its reason when it is set | principled | G46: inside the shadow the register's question cannot be answered (the universe holds the `sitecustomize.py` the instrument commits under `lib/py` there, a file the tree never holds), so the gate SKIPs there and is asked in the tree's battery; unset is the tree. Read at both sites (measured bbx-29) |
| D81 | the defaults check's form and its readers (`bbx defaults --check`, K3; R61, R66) | the register `docs/defaults.md`; the defaults read from `lib/py/bbx/config.py` as a syntax tree, never imported; fallbacks searched under `bin lib drivers gates`; readers searched under `bin lib drivers`, markdown files, lines starting `#` and the lines of the `DEFAULTS` and `KINDS` literals not counted; one declared run-time reader, `lib/py/bbx/fingerprint.py` `_KEYS` with the builder `"fingerprint." + k` | `lib/py/bbx/defaults.py` `REGISTER`, `CONFIG`, `READER_DIRS`, `FALLBACK_DIRS`, `RUNTIME_READERS`, `CLASS_CELL`, `READ_TOKEN` | principled (the form and both directions: R61; the full name and the declared reader list: R66) / reference-calibrated (the declared reader and the search scopes: BBX's tree at bbx-29) | measured at bbx-29 before the build: of the 75 keys then in `DEFAULTS`, nine had no full-name reader under `bin lib drivers` with comments and the literals set aside — `[project].lib_dir`, and the eight `[fingerprint]` keys `fingerprint.py` reads through `_KEYS` — and `[fingerprint].file_pattern`'s only full-name reader was under `gates`, which is why a test never counts as a reader. The check read red on the tree before R66's removal (`key-without-reader [project].lib_dir`, `keys=75 read=74`) and green after it (`keys=74 read=74 errors=0`) (S6 step 2, 2026-09-15) |
| D82 | the documents register's contract (`bbx documents --check`, K4; R62) | the register `docs/documents.toml`, one bare table per page with `file`, `shape`, `twin`, `routed_by`; the fifteen shapes `map`, `living`, `history`, `ledger`, `register`, `queue`, `proposal`, `reference`, `procedure`, `census`, `readout`, `generated`, `constitution`, `fixture`, `kept-run`; a page's `Shape:` line looked for in its first 12 lines and read by its first word; twins between a `living` and a `history` row both ways, or `none — <reason>`; one `root` row, a `map`; routers `map` and `census`, naming the page's path with no path character on either side | `lib/py/bbx/documents.py` `REGISTER`, `FIELDS`, `SHAPES`, `LEAD_LINES`, `SHAPE_LINE`, `ROUTERS`, `NONE_REASON` | principled (ruled: the fields, the vocabulary, the twins and the routing, R62) / arbitrary (`LEAD_LINES` 12, and the first word as the page's shape) | measured at bbx-29 before the build: 42 tracked pages, 29 of them with a `Shape:` line in their first 10 lines, 21 of those led by one of the fifteen words; the other 8 — `HANDOFF.md`, `docs/bins.md`, the three bins pages, `docs/readout.md`, `docs/rebaselines.md` and `docs/rules.md` (through its header in `skill/skills.toml`) — were reworded to lead with their shape, in their own commit before the check (BBX-19). `HANDOFF.md` names every other page by its path and never its own, hence one `root` (S6 step 2, 2026-09-15) |
| D83 | the trap lint's patterns and its two deltas (`bbx demand-after-trap`, K8; R63) | a trap is a line reading `trap` then `EXIT`, at the line's start or after `;`, `&` or `\|`; a demand is `${` then a name then `:?`; a heredoc body from `<<` to its terminator is skipped, and so is a `#` line; the lib subdir is `lib` unless `--lib` names one; BBX's deltas: a gates dir that is not a directory, or a `--lib` subdir named and absent, is refused with exit 2; a file without a `.sh` name is read when its first line is `#!`, an optional path and `env`, then `sh`, `bash`, `dash`, `ksh` or `zsh` | `lib/py/bbx/demand_after_trap.py` (`TRAP`, `DEMAND`, `HEREDOC`, `SHEBANG`, `shell_files`, `scan`, `main`) — bbh `10a82d2`, verbatim but for the two deltas | reference-calibrated (the patterns and the `lib` subdir: bbh's, whose docstring cites VampireSaved's M16 release run) / principled (the two deltas: BBX-8, R63) | carried verbatim so F19b can diff them; measured at bbx-30 before the lift (`docs/plans/S6_probes/probe_k8_bbx.sh`): bbh's lint reads 0 hits over BBX's `gates`, `drivers` and `lib/sh` and over the 5 shell files of `bin` copied under a `.sh` name, reports bbh's own case a, and finds no file without an extension in bbh's example (S6 step 3, 2026-09-15) |
| D84 | the gate index's kind-blind settings (`bbx gate-index`, K7; R63, R68) — `[gate_header].title_sep_regex`, `[gate_header].session_regex`, `[gate_header].duration_regex`, `[gate_header].index_out`, `[gate_header].families_tsv`, `[gate_header].families`, `[gate_header].kinds`, `[gate_header].needs_instruments`, `[gate_header].needs_build_regex`, `[gate_header].index_preamble`, `[gate_header].columns` | a dash run between the gate's name and its claim; a session as an ISO date; a runtime as `~` then a number and `min`, `s`, `sec`, `h` or `hours`; `docs/gate_index.md`; `tests/gate_index.tsv`; no family, no kind prefix, no instrument keyword; a build pattern that matches nothing; a preamble of one title line; no further column | `lib/py/bbx/config.py` kind-blind `DEFAULTS["gate_header"]`, read by `lib/py/bbx/gen_gate_index.py` (`Settings`); the `frame-driven` profile (D85) and a consumer's `[gate_header]` override | principled (the separator and the runtime: the header contract's own shapes; a date as a session; the empty values and the pattern that matches nothing: no family, kind, instrument, build directory or further column is implied, BBX-24, R68) / reference-calibrated (the two paths: bbh's example's, as D8's layout) | bbh's own values are its lineage's (`14z-` session tokens, MAME and Verilator, VampireSaved's ten families) and live in the `frame-driven` profile, D85; measured at bbx-30 (`docs/plans/S6_probes/probe_f19a_defaults.sh`): bbh's example index reproduces with sessions read as an ISO date only. BBX's own values are in `bbx.toml` (S6 step 3, 2026-09-15) |
| D85 | the `frame-driven` kind profile's gate header section (the gate index's session pattern, its path, the families, the kind prefixes, the instrument keywords, the build pattern and the preamble) | bbh's DEFAULTS verbatim at the fidelity baseline (D20) | `lib/py/bbx/config.py` KINDS, read by `lib/py/bbx/gen_gate_index.py` | reference-calibrated | bbh `lib/py/bbh/config.py` at `10a82d2`: the lineage's literals, so a bbh config, which names no kind (D9), resolves here to bbh's values and F19a can diff the index against bbh's (S6 step 3, 2026-09-15); held since bbx-31 by `gates/fidelity_bbh_s6.sh`'s D85 section, key by key against bbh's DEFAULTS on its clone, because F19's pairs read a default only through a key their configs leave unset: at bbh's tip `13ff687` `index_preamble`, which every F19a config sets, moved while the 19 pairs stayed identical and D85 read it (G96, G98) |
| D86 | the gate index tool's form (`bbx gate-index`, K7; R63, R68) | bbh's rendering: rows grouped by family in the configured order and sorted by gate; the claim the header's first paragraph, cut at 240 characters on a sentence end past character 120; `since` the first session match of the whole header, else `—`; `kind` `test` when no prefix matches. BBX's deltas: `--check` compares bytes; a family file whose name ends `.toml` is a TOML-subset register, one bare table per gate with `gate`, `family`, optional `needs` and `since`, where a gate named by two tables or a table naming no gate is a PROBLEM; the further columns `controls`, the declared must-fire controls counted by shape (`none` for a gate declaring it asserts nothing, `—` for one declaring nothing), and `blind spots`, the count of `NOT-ASSERTED:` lines | `lib/py/bbx/gen_gate_index.py` (`COLUMNS`, `Settings`, `claim`, `read_toml`, `controls_cell`, `blind_spots_cell`, `render`, `main`) — bbh `10a82d2` `gen_gate_index.py` and `gate_header.py`, verbatim but for the deltas, each header read through `bbx.controls` (R30) | reference-calibrated (the rendering and the claim's cut: bbh's, whose docstring cites VampireSaved's 14z-123) / principled (the deltas: BBX-8 for the bytes, R68 for the family file and the columns, BBX-17 for a gate named twice) | carried verbatim so F19a can diff them. bbh's header reader stopped at 120 lines where BBX's reads the whole leading block (R30); measured at bbx-30, the longest header is 7 lines among bbh's example gates and 74 among BBX's (`gates/skills.sh`), so no input of either reaches the difference. By hand at bbx-30, before the gate, 10 pairs of bbh's and BBX's `gate-index` read identical over bbh's example and bbh's own selftest root (S6 step 3, 2026-09-15) |
