# Defaults — the register (BBX-24, ruled R11 for the classes)

Shape: register, one row per default the harness introduces, with its
provenance class — `principled` (follows from a contract), `reference-calibrated`
(set by a measurement on a named reference, quoted), `arbitrary` (a choice
that could have been otherwise; the second consumer is its detector).
Changing a row is a ruling, dated in `DECISIONS_HISTORY.md`. A default with no
row is a default nobody can veto (`[BBH-84]`).

| id | default | value | where it lives | class | why / measured on |
|---|---|---|---|---|---|
| D1 | seconds allowed per census command in the recount | 60 | `lib/py/bbx/recount.py` `--timeout`, env `BBX_CENSUS_TIMEOUT` | arbitrary | no census command has been measured above 5 s; 60 leaves room for a cold `python3 tools/checkskills.py -v` on VampireSaved without hiding a hang for a minute more |
| D2 | quoted runtime of `gates/census_recount.sh` | ~44 s (`real 43.50`; was 32 s with shared clones, 21 s in place) | the gate's header | reference-calibrated | `/usr/bin/time -p sh gates/census_recount.sh`, macOS Darwin 25.6, Apple Silicon, 300 rows, 3 shared clones + 4 shadow runs, 2026-09-09; re-measured when the census grows (BBX-11: a gate that fails far faster than its quoted runtime bailed before measuring) |
| D3 | where a census file's repository root comes from | line 2 of the census file (first backtick-quoted absolute path); `--root` overrides | `lib/py/bbx/recount.py` | principled | the census is the record of what was measured where; the recount reads the record, and an override is an explicit, visible choice |
| D4 | which rows a recount can re-run | count = plain integer or one quoted string; command = one leading backtick pipeline | `lib/py/bbx/recount.py` `classify()` | principled | anything else is reported NOT-RECOUNTABLE with its id; coverage is printed, never assumed (BBX-18) |
| D8 | the kind-blind layout defaults (`gates_dir` tests, `gate_glob` *.sh, `lib_dir` tests/lib, `runner_prefixes` run_, `manual_suffixes` _soak, registries `tests/ci_portable.txt` / `tests/ci_static.txt`, `source_regex`, `source_depth` 2) | as listed | `lib/py/bbx/config.py` DEFAULTS | arbitrary | bbh's layout literals kept so a bbh config reads unchanged; the second consumer (the `self` kind) overrides every one of them, which is BBX-25's detector working |
| D9 | `[project].kind` when a config names none | `frame-driven` | `lib/py/bbx/config.py` DEFAULTS | arbitrary | bbh's configs carry no kind and must resolve to bbh's values (F13); a config for any other kind names it. Revisit when a third kind exists |
| D10 | `[controls].enforce` | `false` | `lib/py/bbx/config.py` DEFAULTS; `bbx.toml` sets `true` | principled | off is bbh's behaviour byte for byte (fidelity); the harness's own tree enforces (R10, R14) |
| D11 | the classifier's literals (`skip_regex` `^ *SKIP`, `shell_error_regex`, `timeout_exits` 124 137, `fail_tail` 4) | as listed | `lib/sh/classify.sh`, `config.py` DEFAULTS | principled | bbh's gate contract `[BBH-12]`–`[BBH-15]`, each paid for in the lineage; a consumer overrides through `[classify]` and gates/classify.sh proves the override reaches the classifier |
| D12 | the `frame-driven` kind profile (`instrument_word` emulator, `static_needs_env` ROMDIR, bbh's ten `tier.patterns`, the `[sweep]` literals) | bbh's DEFAULTS verbatim at the fidelity baseline (D20) | `config.py` KINDS | reference-calibrated | bbh `lib/py/bbh/config.py` at the baseline; F13 diffs a runner reading them against bbh. Followed the re-baseline to `10a82d2` on 2026-09-10 (the five `[sweep].placeholders` re-pointed M18: `m3b_merged26`, `don_m22`, `hui56`, `pyron41`, `m5_stock17`) — by this row's definition, not a new decision (R28) |
| D13 | the `self` kind profile (`gates_dir` gates, `lib_dir` lib/sh, registries under gates/, `static_needs_env` BBX_BBH_HOME, controls enforced) | as listed | `config.py` KINDS; `bbx.toml` | principled | BBX reading its own tree (R14) |
| D14 | quoted runtime of `bin/bbx selftest` | ~222 s (`real 221.56`, 12 gates, 26 controls, kept with `--log`, the bbx-2 close; was 219, 223, 227, 156, 117, 47 s with 7 gates) | `HANDOFF.md` | reference-calibrated | `/usr/bin/time -p bin/bbx selftest` with BBX_BBH_HOME set, 9 gates (the sweep runner's ground truth carries three deliberately slow gates), macOS, 2026-09-09 |
| D15 | the kind-blind `[sweep]` defaults (one lane `prereq`, release scope `release`, cadence `always`, timeout 600, no precondition, `input_env` BBX_INPUT, `log_dir_prefix` build/sweep_, no placeholders, no instruments, no scratch) | as listed | `lib/py/bbx/config.py` DEFAULTS | arbitrary | a sweep with no instrument named; the frame-driven profile carries bbh's literals (incl. VampireSaved's build dirs and MAME paths) so a bbh config resolves as in bbh (F14); `self` names `BBX_BBH_HOME` as its input |
| D16 | the kind-blind `[fingerprint]` defaults (`kind` file-sha1, `file_pattern` {set}.bin, no parent sets, no region rules, `region_default` other) and `[suite].registry` tests/expected/registry.tsv, `default_set` "" | as listed | `config.py` DEFAULTS | principled | the most generic artifact is one file whose identity is its SHA-1 (abstraction S2); the frame-driven profile is bbh's zip-members literals verbatim (F14f) |
| D5 | the platform floor | POSIX sh + Python 3; macOS, Linux, Windows via WSL | ruled R3 | principled (ruled) | every platform guard has a gate on each platform |
| D7 | where the gate finds the census files | `docs/census/` under the BBX root; env `BBX_CENSUS_DIR` overrides | `gates/census_recount.sh` | principled | the override exists so the SKIP path (an absent lineage tree) can be exercised on purpose without moving a repository |
| D6 | the environment every census command runs in | `PATH=/usr/bin:/bin:/usr/sbin:/sbin`, `LANG=LC_ALL=C.UTF-8`, `HOME`, `TMPDIR`, `PYTHONDONTWRITEBYTECODE=1`, nothing else | `lib/py/bbx/recount.py` `hermetic_env()` | principled | a count must not depend on which program answers to a name in the caller's shell (gotcha G9: ugrep vs BSD grep, six counts moved); bbh scrubs the replay family the same way `[BBH-35]` |
| D17 | where and how the recount obtains the tree it measures | a PLAIN local clone of the recorded commit under `${TMPDIR:-/tmp}` (`git clone --no-checkout` + `checkout <head>`; objects hardlinked on one filesystem, no `alternates`), removed after the run; `--in-place` runs in the working tree instead | `lib/py/bbx/recount.py` `make_clone` | principled | rulings R18 (the tree under measurement is never written) and R20 (plain, not `--shared`: a shared clone borrows the source's object store through an `alternates` file and a `git gc` there would break it mid-run). Measured 2026-09-09 on VampireSaved (`.git` 949 MB): plain clone + checkout 3.40 s, 490 MB as `du` counts a hardlinked pack (2 links, no extra disk); shared was 1.76 s, 363 MB. Never `git worktree add` (writes under the lineage's `.git`) |
| D18 | when the recount runs the rows a second time for drift | only when the repository's tip is not the recorded HEAD, and never under `--only` | `lib/py/bbx/recount.py` `main` | principled | drift is defined as tip ≠ recorded, so at the recorded HEAD there is nothing to measure; `--only` is the verifier's spot-check and a drift run there would double a partial count. Cost when it runs: VampireSaved's 127 rows again on a second clone — 42.8 s in all against 19.8 s without, measured 2026-09-09 |
| D19 | quoted runtime of `gates/fidelity_bbh.sh` | ~46 s (`real 46.10` on the plain clone; 37.49 in place; the header had said ~10 s + ~25 s) | the gate's header | reference-calibrated | `/usr/bin/time -p sh gates/fidelity_bbh.sh` with `BBX_BBH_HOME` set, F15 not run, macOS Darwin 25.6, Apple Silicon, 2026-09-09; re-measured whenever a pair is added (BBX-11) |
| D20 | the bbh commit the fidelity gate measures | `10a82d2` (was `f675710` until 2026-09-10 — `docs/rebaselines.md`); env `BBX_BBH_BASELINE` overrides; `BBX_FIDELITY_IN_PLACE=1` measures the working tree instead | `gates/fidelity_bbh.sh:37` and `gates/fidelity_bbh_s2.sh:45` (both default `10a82d2`); `gates/suite.sh:28` reads the same variable and its own fallback is still `f675710` — the reader the R28 re-baseline missed, measured at bbx-18 (G32), pending R43 | reference-calibrated | ruling R8 (the baseline is the commit, not the tree) made operational by R20 (a plain clone of it under `TMPDIR`); measured 2026-09-09: F13–F15 identical on the clone and in place, the clone clean after the run including ignored paths; a moved bbh tip is `NOTE: bbh-drift ahead=N`, never a verdict; a re-baseline changes this row with a dated line in `docs/rebaselines.md` |
| D21 | `[sweep].clone_per_slot` | `false`; a consumer sets `true` to give every `--jobs` slot (slot 0 included, the serial prereq lane too) its own plain clone of the consumer's HEAD under `${TMPDIR:-/tmp}/bbx-clones.XXXXXX/slotN`, removed at exit; the banner prints the commit and the working tree's porcelain | `lib/py/bbx/config.py` DEFAULTS, `bin/bbx-run-sweep` | arbitrary | ruling R19 (parallel work is a pull queue over pinned trees) — off by default because bbh's example and fidelity F14 run in place and a clone measures HEAD, not the working tree, which a consumer must choose knowingly; the second consumer that turns it on is its detector (BBX-24). Measured 2026-09-09: two slots on the gate's fixture, 2 clones, base untouched |
| D22 | where a kept run lives | nowhere unless `--log DIR` is given; the ritual uses `build/selftest_<stamp>` (gitignored) under the BBX root | `bin/bbx-run-static` `--log`, `HANDOFF.md` (the ritual) | arbitrary | a kept run is the readout's input and BBX-29's key (subject version + start time in `run.txt`); off by default so fidelity F13's printed output stays bbh's byte for byte, and `build/` because bbh's example logs there too (`log_dir_prefix`) |

The lineage's biased default (bbh's `DEFAULTS` naming VampireSaved's build
directories, `docs/gotchas.md` G4) is the reason every row here names its
class.
| D23 | the temporal family's thresholds in the `frame-driven` kind profile | `flicker_max` 2, `reconverge` 60, `flicker_max_total` 8 | `lib/py/bbx/config.py` KINDS, read by `lib/py/bbx/thresholds.py` and nowhere else | reference-calibrated | bbh `lib/py/bbh/thresholds.py` and `[thresholds]` at f675710, verbatim (its lineage measured them in VampireSaved session 7: single-frame flickers, full re-convergence); F17 diffs every comparator reading them against bbh. A kind with no temporal family carries no `thresholds` table and a temporal comparator under it is REFUSED (S2 step 1, 2026-09-10) |
| D24 | which direction is LOOSER, per threshold key (R25) | `flicker_max` larger, `flicker_max_total` larger, `reconverge` smaller | `lib/py/bbx/thresholds.py` LOOSER_WHEN | principled | follows from what each number bounds: a longer run or a larger inventory tolerated is looser; fewer identical frames demanded after a divergence is looser. A looser consumer value needs a `[thresholds].rulings` entry or every importer refuses (exit 3, one line); tighter needs nothing (BBX-13) |
| D25 | the expectation KINDS table, per kind profile | kind-blind `skip` / `sha1` (exact) / `pending`; frame-driven adds bbh's `masked` and `diverge` (temporal) | `lib/py/bbx/config.py` DEFAULTS and KINDS `[expectations].kinds`, read by `lib/py/bbx/expectations.py` and nowhere else | principled | E1: a kind is registered in one place so an unknown one is reported, never ignored `[BBH-50]`; R23 put the table in the profile so the family and the view come from the kind. The three kind-blind kinds are the ones with no comparator family or the exact one (S2 step 2, 2026-09-10) |
| D26 | `[suite].mask_default` and `mask_env` | kind-blind: mask_default EMPTY (a set without a mask file runs unmasked), mask_env `MASK_RANGES`; frame-driven: bbh's literal `043c-043d,4182-41a2,7f00-8000` | `lib/py/bbx/config.py`, read by `lib/sh/compare.sh` (`compare_mask_for`) through `bbx.expectations mask-default`, `BBX_MASK_DEFAULT` from a runner overriding | reference-calibrated (frame-driven) / principled (kind-blind) | bbh's literal is the lineage's board (a biased default, BBX-24) and stays under its profile's name; a mask is a frame-driven basis and no other kind has one yet, so the kind-blind value is none (2026-09-10) |
| D27 | `[suite].hash_cmd` | kind-blind `python3 -m bbx.sha1` (prints `<hex>  <file>`); frame-driven `shasum` | `lib/py/bbx/config.py`, read by `bin/bbx-run-suite` for the `.sha1` kind | principled (kind-blind) / reference-calibrated (frame-driven: bbh's) | R3's floor is sh + python3: `shasum` is not on every Linux, `sha1sum` not on macOS; the hex is the same whichever prints it, so F12's text is unchanged. R21's Linux run is the detector (S2 step 3, 2026-09-10) |
| D28 | `[suite].driver_home` — where a bare driver NAME resolves (R26) | kind-blind `$BBX_HOME/drivers` (absent until S3: a bare name FAILS naming the path); frame-driven `$BBX_BBH_HOME/drivers` | `lib/py/bbx/config.py`; `bin/bbx-run-suite` expands exactly those two tokens | principled | bbh stays the frame-driven consumer's driver home (CLAUDE.md §2), nothing is copied out of it; a path from the consumer root and `--driver` win; `BBX_BBH_HOME` unset under the frame-driven kind is a FAIL that names it |
| D29 | `[suite].runs_per_replay` | 2 (both layers) | `lib/py/bbx/config.py`; `bin/bbx-run-suite` | principled | BBX-14: every scenario runs more than once and any difference is NONDETERMINISTIC before any class is consulted; 2 is the smallest number that is more than once (bbh's value, `code (policy)` there) |
| D30 | the kept suite run's shape (`bbx-run-suite --log DIR`) | `results.tsv` — `scenario`, `kind`, `class`, `verdict` (the printed first line), `finding`; `run.txt` — subject, set, driver, HEAD, platform, counts, the suite's verdict; the finding vocabulary of `lib/py/bbx/finding.py` (`short` apart from `diverged`) | `bin/bbx-run-suite`, `lib/py/bbx/finding.py` | principled | BBX-4 and BBX-12: the printed text is bbh's and frozen (C4), so the distinction between a short observation and a divergence lives in the kept run, read by field name; `unclassified` for any line no rule names, never silently one of the others |
| D31 | `[provenance].register` and `exclude` | `PROVENANCE.toml` inside `[suite].expected_dir`; excluded basenames `PROVENANCE.toml`, `README.md`, `mask`, `MASK` (the register's own name always) | `lib/py/bbx/config.py`, read by `lib/py/bbx/provenance.py` and the readout's suite screen | principled | R24: the register sits beside the files a triage opens, inside the tree it describes, so one path names both; the mask records are bases, not expectations, and the register cannot vouch for itself. A consumer's `[provenance].exclude` (bbh's example keeps bbh's) wins over the default, the register's own basename excepted (S2 step 4, 2026-09-10) |
| D32 | what the readout reads beyond a static run | a kept SUITE run (`run.txt` carries `expset=`) gets the suite screen: findings apart, the register's histogram for its set, the classes PASSed on a pairing whose row is not `fixture` or `testimony`; a static run's `untracked_before` / `untracked_after` fields (written by `bbx-run-static --log`, printing nothing) on the tree line | `lib/py/bbx/readout.py`, `bin/bbx-run-static` | principled | RO1 (the expectations relied upon, by class) and BBX-4 for the suite; G17's mechanism for the static run — the printed working-tree block is bbh's and tracked-only (F13), the kept run is BBX's (S2 step 4, 2026-09-10) |
| D33 | the `document-set` kind profile | `instrument_word` extractor; no `static_needs_env` (portable); no `tier.patterns`; `[suite]` `scenario_ext` claims, `driver` docset, `rompath_env` = `input_env` = `DOCSET_PATH`, `hermetic_unset` the driver family `DOCSET_FORMS` / `DOCSET_NONDET` / `DOCSET_VIEW`; `[fingerprint]` file-sha1 over `{set}.tsv` (the whole-set key `dir_sha1` over the artifact's directory = documents + artifact, NOT recursive); kinds: the kind-blind three + `truth` (exact) `claims` (set) `covered` (set) `schema` (schema); no `[thresholds]` | `lib/py/bbx/config.py` KINDS | principled | R14's third profile (docs/plans/S3.md §3; R31–R34); the identity is computed from the files, never from what a document says of itself (§3.4); a temporal comparator under it is REFUSED (D23). Limit stated: a nested document tree is not identified (S3 step 1, 2026-09-10) |
| D34 | `[suite].scenario_ext` — the scenario file extension per kind (R32) | kind-blind `rpl`; document-set `claims`; command-line `cli` | `lib/py/bbx/config.py`; read by `bin/bbx-run-suite` (the loop) and `lib/sh/expectation_kinds.sh` through `bbx.expectations scenario-ext` | arbitrary (kind-blind) / principled (document-set, command-line) | `rpl` is bbh's literal kept so a bbh config resolves unchanged (F12, F16c diff the loop and the enumeration); `claims` names what the scenario IS for the kind (S3 step 1, 2026-09-10); `cli` added for the command-line kind (R35; S4 step 1, 2026-09-10) |
| D35 | the fixture's three bindable SENTENCE FORMS and the claim-set grammar | `has`: `<Name> has <field> <value>.`; `of-is`: `The <field> of <Name> is <value>.`; `row`: `\| <Name> \| <field> \| <value> \|`; a claim set is a TOML-subset file — `[claims]` `documents` / `forms` / `view`, then `[p<n>]` paraphrase rows (`document`, `line`, `name`, `field`, `literal`) and `[u<n>]` unbindable rows (`document`, `line`, `reason` ∈ `absence` / `no-source` / `no-form`) | `fixture/docset/mkdocset.py` (writes them); `lib/py/bbx/docset.py` (binds them; S3 step 2) | arbitrary | the FIXTURE's forms (SMS `docs/game/README.md:82-113`: the prose form is a contract with the checker); a real consumer declares its own forms in its claim sets and the driver refuses the rest, so the first real consumer is the biased-default detector (BBX-24, BBX-25) (S3 step 1, 2026-09-10) |
| D36 | the fixture's CHIRALITY predicates (BBX-15) | names distinct; no name, id or weight equals its own mirror; no id's or weight's mirror is another id or weight; weights distinct; ranks a permutation that is neither its own inverse nor the reversal | `fixture/docset/mkdocset.py` `predicates()`, asserted on the design and on the tree's artifact under `--check`, each named when it fails | principled | a mirror, order or orientation error in a reader cannot hide if no value equals its mirror and no mirror of a value is another value (VampireSaved "mirrored decode", BBX-15); `gates/docset_fixture.sh` control `symmetric-fixture` (S3 step 1, 2026-09-10) |
| D37 | the binding-STATUS vocabulary and the observation token (R31, R33) | statuses `BOUND` / `PARAPHRASE` / `UNBINDABLE` / `STALE` / `MISMATCH`; the log line `<index> <status>:<sha1-of-quoted>:<sha1-of-derived>`, `END <n>` with n = claims seen; the inventory and covered rows carry `document`, `line`, `form`, `status` as named TOML fields (R34, the maintainer's TSV caveat) | `fixture/docset/mkdocset.py` (the expected statuses in the inventory); `lib/py/bbx/docset.py` (the token, S3 step 2) | principled (ruled) | R33: closed so a status nobody named cannot read green; R31: the point stays an integer and `logfmt` stays bbh's (S3 step 1, 2026-09-10) |
| D38 | the QUOTED and DERIVED strings behind D37's `<sha1-of-quoted>:<sha1-of-derived>` | quoted = the document's line verbatim (the `text` view); derived = `<name>.<field>=<value>` from the artifact, `<name>.<field>=<absent>` when the record or the column is missing, `unbindable:<reason>` for a listed unbindable; SHA-1 over UTF-8 | `lib/py/bbx/docset.py` `quoted_text` / `derived_text` / `derived_unbindable` / `token` / `split_token` (the one splitter) — imported by `fixture/docset/mkdocset.py` to write the truth from the design | principled | one definition, so the generator's truth (from the design) and the driver's log (from the binding) agree by construction and differ only where the binding differs from the design; the token compares as a token (O1) and a triage re-derives both sides from the index through `bbx.docset map` (S3 step 2, 2026-09-10) |
| D39 | the lexical classes of D35's forms, and the artifact's record key | `<Name>` and `<field>`: one token without spaces or bars; `<value>`: a decimal or hexadecimal numeral `[0-9A-Fa-f]+`; the record key is the artifact's FIRST column | `lib/py/bbx/docset.py` FORMS, `read_artifact` | arbitrary | the fixture's: a numeral value keeps a markdown table's header row (`\| name \| field \| value \|`) from being a claim, and the first column is where the fixture keeps its names; a real consumer's forms and key are its own (BBX-24: the first real consumer is the detector); a deleted record is therefore a lost reference (STALE), never a shift (G21) (S3 step 2, 2026-09-10) |
| D40 | the unlisted-claim GUARDS (BBX-7) | a line matching no declared form and not listed in the claim set is REFUSED (exit 3, naming `<document>:<line>`) when it states an absence (`^(Nothing\|None\|No <word>) (lists\|records\|gives\|states\|has\|carries\|mentions)`) or carries a digit; any other line is not a claim | `lib/py/bbx/docset.py` ABSENCE_GUARD, NUMBER_GUARD | arbitrary (the regexes) / principled (that an unlisted absence or number is loud) | an absence nobody listed and a number nobody bound are the two ways a claim silently leaves R33's denominator; the regexes are the fixture's vocabulary and the first real consumer will find them too narrow or too wide — widening one is a tightening (more is refused), narrowing one is a ruling (BBX-13's shape) (S3 step 2, 2026-09-10) |
| D41 | the schema family's TYPE vocabulary (R34) | `int` `^-?[0-9]+$`; `hex` `^[0-9A-Fa-f]+$`; `str` any cell, the empty one included; `nonempty` any cell but the empty one; the rows line's ops `>=` and `=` | `lib/py/bbx/compare_schema.py` TYPES, OPS | principled (ruled) | R34: the smallest set that distinguishes the fixture's four columns; a fifth type or a third op is a ruling, and a type outside the vocabulary in a frozen file is `FAIL unknown schema type` (never read as `str`). The one consumer is the fixture's TSV until S4's JSON (BBX-25 unmet, stated by `gates/set_schema.sh`) (S3 step 3, 2026-09-10) |
| D42 | the document-set kind's NOTE keys | `coverage <covered>/<END n>`, `paraphrase <n>`, `unbindable <n>`, `stale <n>`, `mismatch <n>` (printed by `bbx.docset summary` from a log); `covered-grew <k>` (printed by `compare_set.py` after a shrink-only PASS that grew, R33); `verdict-lines-frozen <n>` (printed by `gates/set_schema.sh`) | `lib/py/bbx/docset.py` `cmd_summary`, `lib/py/bbx/compare_set.py`, `gates/set_schema.sh` | principled | BBX-18 (coverage is a number on the screen: `coverage` is the readout's key, RO1), R33 (coverage never fatal; growth a note, never a verdict), `docs/controls.md` (a NOTE-class number is `NOTE: <key> <value>` at column 0; every key but `coverage` is listed as a note). The suite carries them onto the screen in S3 step 4 (S3 step 3, 2026-09-10) |
| D43 | `[suite].log_summary` — the command that prints a run log's OWN NOTE-class numbers | kind-blind EMPTY (nothing printed); document-set `python3 -m bbx.docset summary` (D42's keys: `coverage <n>/<m>`, `paraphrase`, `unbindable`, `stale`, `mismatch`); command-line `python3 -m bbx.cli summary` (`exit <n>` or `exit crash`, `band-fields <n>`, `emitted-files <n>`; joined S4 step 2, 2026-09-10) | `lib/py/bbx/config.py` DEFAULTS and KINDS; run by `bin/bbx-run-suite` after a scenario's pairings under the kinds loop, the lines printed at column 0 and kept (D44); skipped when the scenario's schema failed | principled | RO1 and BBX-18 (coverage is a number on the screen), BBX-25 (the suite names no kind's module: the profile does); the frame-driven profile prints none, so F12's text is bbh's; a summary that fails is a suite FAIL naming it (S3 step 4, 2026-09-10) |
| D44 | the kept suite run's `notes.tsv` (beside D30's two files) | `scenario`, `key`, `value` — every `NOTE: <key> <value>` line the suite printed for a scenario: the log summary's (D43) and a comparator's (`covered-grew`, R33) | `bin/bbx-run-suite` `notes`; read by `lib/py/bbx/readout.py` — the suite screen's `coverage:` lines (one per scenario) and `note:` lines | principled | a number printed and not kept is a number the maintainer never saw (bbx-3's first fix); read by field name (BBX-12); the kept rows are PAIRINGS keyed (scenario, kind) since the kinds loop, and BBX-14 on the screen is met over pairings (S3 step 4, 2026-09-10) |
| D45 | the `command-line` kind profile | `instrument_word` tool; no `static_needs_env` (portable); no `tier.patterns`; `[suite]` `scenario_ext` cli, `driver` cli, `rompath_env` = `input_env` = `CLI_PATH`, `hermetic_unset` the driver family `CLI_NONDET` / `CLI_TIMEOUT` / `CLI_KEEP_ENV` (the search path is the suite's variable, D33's shape); `[fingerprint]` file-sha1 over `{set}.py` (the whole-set key `dir_sha1` over the tool's own directory, NOT recursive); kinds: the kind-blind three + `truth` (exact) `unordered` (set) `schema` (schema) `band` (tolerant-numeric); no `[thresholds]`; `log_summary` = `python3 -m bbx.cli summary` (joined with the driver at S4 step 2, as this row said it would; D43) | `lib/py/bbx/config.py` KINDS | principled | R14's fourth profile (docs/plans/S4.md §3 S1; R35–R40); the identity is computed from the tool's bytes, never from what it prints of itself (§3.4); a temporal comparator under it is REFUSED (D23). Limit stated: a tool that is a directory — a package, a script with siblings it imports — is identified by its one file only; such a consumer declares the `command` kind (S4 step 1, 2026-09-10) |
| D46 | the `.cli` scenario grammar (R35) | a TOML-subset file: `[scenario]` `args` (an array of strings, one per argv element — never a shell line), `stdin` (an array of lines, each newline-terminated) or `stdin_file` (relative to the scenario file's directory), `cwd` (relative to the sandbox; default `.`), `emits` (files the tool must produce in its working directory, each a `file` point), `bands` (the fields judged by the band kind), `fields = "json"` (stdout is ONE JSON object; absent: stdout is lines); `[env]` one key per variable the tool may see; nothing in it states an expected value; an unknown key is REFUSED by the driver (exit 3; S4 step 2) | `fixture/fakecli/mkfakecli.py` (writes them); `lib/py/bbx/cli.py` (reads them, S4 step 2) | principled (ruled) | R35: the scenario is an INVOCATION and nothing else (§3.4); SMS cliguard's rule applied to the scenario grammar; `stdin` as lines because the subset refuses escapes in a basic string (X19) (S4 step 1, 2026-09-10) |
| D47 | the command-line OBSERVATION token vocabulary (R31, R35) | `0 exit:<n>` first; `<i> line:<sha1>` per stdout line, or under `fields = "json"` `<i> field:<name>:<sha1>` per key in SORTED order (the hash over the value's CANONICAL JSON: sorted keys, no spaces, non-ASCII kept) and `field:<name>:band` for a band field (its value in the band view `<out>.bands`, `<i> <name>=<value>` / `END <n>`, written by the driver at step 2); `<i> err:<sha1>` per stderr line; `<i> file:<name>:<sha1-of-bytes>` per declared emitted file; `END <n>` with n = the last index; SHA-1 over UTF-8 without the newline; a name holds no space and no colon | `lib/py/bbx/cli.py` — the ONE writer of every token (the generator's truth logs now; the driver's logs at step 2, the S3 shape) | principled (ruled) | R31: the point stays an integer and `logfmt` stays bbh's; R35: the exit status is its own point, so a tool's refusal is a frozen scenario and a crash (exit 2, `END-CRASH`) is apart from it; the band value lives outside the log so the exact family never fails on a legitimate move (BBX-16) (S4 step 1, 2026-09-10) |
| D48 | the `band` expectation's shape (R36) | `[spec]` `class = "band"`, `baseset`, `measured` (how the band was measured, in words), `rulings` (optional inline table `<field> = "R<n>"`); `[b<i>]` `field`, `min`, `max` — inclusive INTEGERS in the field's own unit (the subset refuses floats: a fractional band is a consumer's question, stated NOT-ASSERTED) | `fixture/fakecli/mkfakecli.py` (writes it: `size` over seeds 1..9 of the design, `[236, 270]`, class `fixture`); `lib/py/bbx/compare_band.py` (reads it, S4 step 3) | principled (ruled) | R36: a band is what was measured, never a knob (VampireSaved's `+/- 30` kept through two re-centres; 0 tools there take a tolerance option); the `rulings` table is R25's shape on the frozen file so `--freeze` can refuse to widen without a ruling id (S4 step 1, 2026-09-10) |
| D49 | the JSON schema spec's shape and TYPE vocabulary (R40; R34's second consumer) | `[spec]` `class = "schema"`, `baseset`, `format = "json"`; `[k<i>]` `name`, `type`, the keys in SORTED order and the type one of `int` `number` `str` `bool` `null` `list` `object`; a list-valued key adds `items_op` (`>=` or `=`) and `items_n`; a nested object is one `object` key | `fixture/fakecli/mkfakecli.py` (writes it: five keys, `str str int int list`); `lib/py/bbx/compare_schema.py` (reads it, S4 step 3; D41's TSV vocabulary untouched) | principled (ruled) | R40: the smallest set that names every JSON value kind once; two formats, two vocabularies, one comparator; a type outside it is `FAIL unknown schema type`, never read as `str` (S4 step 1, 2026-09-10) |
| D50 | the command-line fixture's CHIRALITY predicates (BBX-15) | D36's ten on the tool's table, plus: the rank order is neither the name order nor its reverse; no name, id or weight is shared with `fixture/docset`'s design (read from `mkdocset.py` through the harness root — measured, never assumed); the join scenario's stdin lines are in neither sorted order | `fixture/fakecli/mkfakecli.py` `predicates()`, asserted on the design and on the TREE's tool (its `RECORDS` literal read back by `ast`) under `--check`, each named when it fails | principled | an order error cannot hide behind a sorted listing, and a reader that confused the two fixtures could not read green on either; `gates/cli_fixture.sh` control `symmetric-fixture` (S4 step 1, 2026-09-10) |
| D51 | `CLI_TIMEOUT` — seconds the command-line driver lets the tool run before killing it | 60 | `drivers/cli.sh` (the variable; a non-number or 0 REFUSED, exit 3); `lib/py/bbx/cli.py` `DEFAULT_TIMEOUT`, `run --timeout`; elapsed: the tool killed, no log written, exit 1 DISCARDED | arbitrary | D1's value and reason: no fixture scenario runs above 1 s (the `--sleep` control uses 1 and 5), 60 leaves room for a real tool without hiding a hang for a minute more; the second consumer is the detector (BBX-24). Scrubbed by the suite (D45): a caller sets it through a wrapper driver, never the environment (S4 step 2, 2026-09-10) |
| D52 | the SUBJECT's environment under the command-line driver | exactly D6's set with the sandbox as `HOME` and `TMPDIR` (`PATH` pinned, `LANG`/`LC_ALL` `C.UTF-8`, `PYTHONDONTWRITEBYTECODE=1`) PLUS the scenario's `[env]` table and nothing from the caller; the sandbox is also the working directory (the scenario's `cwd` under it), a fresh `mktemp -d` removed after the run when omitted; a `<set>.py` runs under the harness's own python3 (`sys.executable`), an executable `<set>` as itself; `[env]` may not name one of D6's six; `CLI_KEEP_ENV` REFUSED | `lib/py/bbx/cli.py` `hermetic_env()`, `load_scenario()`, `run()`; `drivers/cli.sh` | principled | D6 (G9: a measurement must not depend on the caller's shell) applied to a subject, [BBH-35] and [BBH-36] (the sandbox is the machine's home); what was fed is RECORDED in the sandbox — `argv.txt`, `stdin.bin`, `env.txt` — so a gate reads it back against the scenario (O5); `gates/cli_driver.sh` control `env-scrub` measures the premise on the tool called directly before asserting the scrub (S4 step 2, 2026-09-10) |
| D53 | what the command-line driver writes beyond D47's log | the CRASH log (D4 exit 2): no `0 exit:` line (a tool killed by a signal has no status), the stdout then stderr points it produced, `CRASH signal:<n>:<NAME>`, `END-CRASH <last index>` — never compared (logfmt.frames does not read it); the band view `<out>.bands`: `<i> <name>=<canonical value>` per declared band field the object carries, sorted, i = 1..k, `END <k>` (k the inventory's size, BBX-13's watch), written only when stdout was one JSON object and `bands` is declared; under `fields = "json"` a stdout that is not ONE JSON object is observed as LINES (a subject divergence the truth names, never a discard); a non-UTF-8 output is exit 1 DISCARDED | `lib/py/bbx/cli.py` `crash_log()`, `band_view()`, `run()` | principled | R35 (a crash apart from a refusal by construction: bbh's guarded grammar ends `END-CRASH`, [BBH-33]); BBX-16 (the band value lives in its own view); BBX-4 (a wrong shape is a divergence, a killed tool is a discard, two reports); `gates/cli_driver.sh` control `crash-vs-exit` (S4 step 2, 2026-09-10) |
| D54 | the command-line driver's JSON VIEW `<out>.json` | the canonical JSON text of the ONE object stdout held (`bbx.cli.canon`: sorted keys, no spaces, non-ASCII kept — the text the `field:` hashes are over, D47) plus a newline; written whenever the observation was read as fields (`fields = "json"`, exit 0, stdout one object), so a scenario with band fields has both views; removed with the log and the band view before every run | `lib/py/bbx/cli.py` `run()`; `drivers/cli.sh` (removes it); read by `lib/py/bbx/compare_schema.py`'s json format as its artifact — passed by the caller (`gates/json_schema.sh`; the suite's hand-over is S4 step 4's) | principled | the schema is judged on the OBJECT (D49) and the kept run's log holds hashes only, so the shape must live where the comparator can read it: a view beside the log, D53's shape, never a re-derivation from hashes and never a second run of the tool (the plan's silence on where the object lives, decided here; docs/plans/S4.md §3 "E1"). The subject file the kinds loop hands the schema family today (`bbx.fingerprint --path`) is the wrong artifact for this format — measured by the gate, `FAIL schema: the artifact is not one JSON object` (S4 step 3, 2026-09-10) |
| D55 | `compare_band --freeze`: what a re-freeze of a band may do without a ruling | `--freeze <frozen.band> <proposed.band>`: the frozen file is replaced by the proposed BANDS (its class and baseset kept, the proposed `measured` and `rulings` taken) when no band widens on either side; a band that widens needs its field in the PROPOSED file's `[spec].rulings` or the whole freeze is REFUSED (exit 3, the file unchanged); narrowing is free; a field in one file and not the other is REFUSED (the inventory never moves through a freeze); the written text is the generator's shape byte for byte | `lib/py/bbx/compare_band.py` `freeze()`, `render()` | principled | R36 (a band is loosened only with a measured mechanism named and a ruling: VampireSaved's `+/- 30` kept through two re-centres); BBX-13 (a growing tolerance inventory means stop and root-cause: the inventory is edited by hand under the register, never by the freeze); the proposed file carries the measurement and the ruling together, R25's shape on the frozen file (D48). What the freeze cannot check — that the ruling id exists and names a mechanism — is `gates/band.sh`'s NOT-ASSERTED (S4 step 3, 2026-09-10) |
| D56 | the set family's LINE row shape (the `unordered` kind) | a frozen row is `[l<i>]` `line` (the text) and `sha1` (the line's, `bbx.cli.sha1_text`); the shape is told from the claim shape by the row's fields, a file that mixes them is malformed, a file with no rows reads as the claim shape; the run's rows are the log's `line:<sha1>` points (D47) as a multiset, compared by sha1 both ways; a frozen row whose sha1 is not its line's is hand-editing (FAIL); a frozen duplicate is hand-editing (FAIL); a run duplicate is a surplus copy named by the count; the shrink-only mode is refused under this shape; the artifact and the scenario-file arguments are accepted and not read | `lib/py/bbx/compare_set.py` `load_shape()`, `line_rows()`; written by `fixture/fakecli/mkfakecli.py` | principled | R34's caveat (named fields say the shape, so no `rows =` key is needed); BBX-17 (multisets both ways, a duplicate a signal of hand-editing); BBX-25 met for the set family by its second consumer with `lib/sh/compare.sh`'s set branch unchanged; a tool whose unordered output legitimately repeats a line cannot be frozen under this shape — stated NOT-ASSERTED by `gates/json_schema.sh`, a consumer's question (S4 step 3, 2026-09-10) |
| D57 | the VIEW column of the kinds table — what the kinds loop hands a family as its artifact | a fourth field per kinds row: `-` none (`skip`, `pending`), `log` the run log itself (`sha1`, `masked`, `diverge`, `truth`, `unordered`), `subject` the subject file the identity was computed from (`bbx.fingerprint --path`: the document-set's `claims`, `covered`, `schema`), `json` the driver's JSON view `<log>.json` (the command-line `schema`, D54), `bands` the band view `<log>.bands` (`band`, D53); resolved by `view_path` in `bin/bbx-run-suite` and nowhere else, passed as `compare_check`'s artifact argument (the tolerant-numeric branch hands it to `compare_band`, which derives no path); a view the loop cannot resolve is a FAIL at the entrance, after the identity and before any scenario runs; a row without its four fields is refused by `bbx.expectations` (one line, exit 1) | `lib/py/bbx/config.py` DEFAULTS and KINDS `[expectations].kinds`; `lib/py/bbx/expectations.py` `table()`, `kinds`, `view`; `bin/bbx-run-suite` `view_of`, `view_path`; `lib/sh/compare.sh` `compare_band` | principled | R23 (the family and the VIEW are declared by the kind, never inside a spec line) and BBX-16 (every comparator declares which view it reads): before this row the loop handed every family the subject file, the wrong artifact for the json format (measured at bbx-15 by `gates/json_schema.sh`: `FAIL schema: the artifact is not one JSON object`) and the band family derived its own `<log>.bands` — two resolvers, one of them implicit. Vocabulary closed; a fifth view is a row amendment and a `view_path` case, never a family's private derivation. The plan's zero-line target for the loop (docs/plans/S4.md §3 E1) did not survive: 29 lines added, 5 removed, measured (`git diff --numstat`); D25's shape and D45's kinds row amended by this row (S4 step 4, 2026-09-10) |
