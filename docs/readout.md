# Readout — session 1, 2026-09-09

Shape: the one screen (CLAUDE.md §3.2). What was measured, what was assumed,
what was not looked at, and what this session's "done" does not assert.

## Verdict

**Session 1 delivered a plan, STOPPED, and the maintainer ruled.** Seven
deliverables are in commit one; all sixteen rulings are answered
(`DECISIONS.md`); the one approved edit to the constitution is its own
commit. No tool exists. Slice S1 may open.

## What it rests on

| claim | rests on | provenance class |
|---|---|---|
| the census counts (bbh 79 + VS 127 + SMS 94 = 300 count rows) | re-run by a different agent per file: 79/79 MATCH; VS 122 re-run → 121 MATCH + 1 corrected (an "all 0" gloss that failed its own command), and 5 rows added by the producer after verification, re-measured by the producer only; 94/94 MATCH | measured twice by different agents, except the 5 late VS rows (once) |
| the census items (280 + 740 + 228 = 1,248 rows) | 186 + 555 + 107 rows script-generated and re-generated byte-identical by the verifiers; hand-read rows spot-checked 15/15, 14/15, 11/15 — every miss a line-number citation, every one corrected by the producer and recorded | measured; citations corrected after verification |
| the headline numbers (87 / 555 / 66 / 311 / 4,808 / 32 / 121 / F1–F11) | the orchestrator's own run, recorded verbatim in `docs/census/README.md` | measured, three ways |
| the bins (402 keep / 339 generalize / 506 consumer / 1 drop) | one decision per row by a third agent; ids diffed identical to the census; drop-without-reason count printed: 0 | judgement, individually recorded |
| the abstraction, generality proof, fidelity plan | proposals citing `[BBH-N]` rules and census rows; no code | design, UNRATIFIED |
| the slice estimate (14 sessions) | calibrated on bbh's measured pace (10 slices, 19 commits, 3 days, 7 VS sessions) × an **arbitrary** factor 2 for the two new kinds | estimate — not a measurement |
| the corrected citation MJC-52 | grep of the sentence in VampireSaved; anchor `docs/project/gotchas.md:2913-2922` | measured |

## Controls that fired

This session wrote no gate, so no must-fire control exists yet. The protocol
itself was exercised in both directions: verifiers found 1 count-row defect,
1 generator offset defect, 5 wrong line-number citations and 1 regex false
positive across the three census files — all corrected by the producers and
all recorded — and the orchestrator's own "319 gate scripts" was found wrong
(311 gates + 8 library scripts) and corrected in five places. A verification
step that found nothing would have been the suspicious result.

## What this session does NOT assert

- That the abstraction is right. It is a proposal; the first evidence is F12
  diffing empty, and F12 does not exist.
- That the estimate is right. The factor 2 is arbitrary and says so.
- That any lineage number is *current* beyond the recorded HEADs
  (`f675710`, `5df1d8be`, `ecc5481`). A moved HEAD invalidates the census
  until the recount is re-run (R9).
- That bbh's own rules are correct — the census counts and locates them; it
  does not test them.
- Anything about F8 / F21 (MAME, ROMs): not run, not looked at.
- Anything about the content of VampireSaved's `STATE_HISTORY.md` (28,918
  lines), its 370 untracked build logs, its emulator submodules, or SMS's
  Lua probe bodies and traces — listed per file in each census's §D.
- That the two installed skills `romhacking-methodology` and `snes-romhacking`
  (plain copies, Aug 12) match any repository — not diffed; SMS and
  VampireSaved cite `[RH-N]` from the former (VS: 72 citations in 43 files).

## What was assumed

- The three local clones are the repositories the prompt names (checked by
  remote URL, not by directory name — G6).
- The maintainer's three rulings R6–R8 as answered in plan mode.
- That reading a rule's first sentence (≤ 140 chars) is enough to bin it —
  found false once (MJC-47) and handled by reading the source; not re-checked
  for the other 1,247 rows.

## Counts, separately (BBX-1)

| | |
|---|---|
| gates run | 0 (none exist) |
| PASS / SKIP / FAIL / TIMEOUT | 0 / 0 / 0 / 0 |
| controls declared / fired | 0 / 0 |
| fidelity rows run | 0 of F12–F21 planned |
| census count rows verified | 295 of 300 re-run by a verifier (294 MATCH at first pass, 1 corrected); 5 added after verification, measured once |
| census item rows | 1,248, all binned |
| rulings answered / open | 16 / 0 |
| incidents filed with price | 8 (`docs/gotchas.md`) |
| BBX rules re-anchored by an incident here | 0 of 30 formally; G7 and G8 are the first incidents under CLAUDE.md §1 (a filed count wrong by a third; a number attributed to a file never grepped) |

## Next

`HANDOFF.md` step 3: open slice S1 with the recount gate.

---

# Readout — slice S1, step 1: the census recount gate (2026-09-09)

## Verdict

`gates/census_recount.sh` → **PASS**, twice, byte-identical output:
`3 census files, 300 rows: 246 match, 0 mismatch, 54 not recountable, 1 matched with a non-zero exit; 2 controls fired` — 21 s.

## Counts, separately

| | |
|---|---|
| gates run / PASS / SKIP / FAIL / TIMEOUT | 1 / 1 / 0 / 0 / 0 |
| controls declared / fired | 2 / 2 (`wrong-head`: a shadow census at HEAD 0000000 → HEAD-MOVED; `moved-count`: row A1 191 vs 190 → MISMATCH) |
| the SKIP path exercised | yes: a census naming `/nonexistent/…` → `SKIP:` exit 0; a missing census file → `FAIL:` exit 1 |
| recountable rows, by file | bbh 79/79, VampireSaved 100/100, SMS 67/67 |
| NOT-RECOUNTABLE rows | 54 (VS 27, SMS 27; ids printed by the gate) — the census's uncovered claims, allowed to move only downward |
| census rows rewritten to reach this (document first, no count changed) | bbh 65 by script + 9 by hand; VS 22; SMS 34 |
| instrument findings | G9 (two greps), G10 (a never-matching pattern) |

## What it rests on

The recorded HEADs (`f675710`, `5df1d8be`, `ecc5481`), checked before any row runs; the hermetic environment (D6); the census grammar (`docs/census/README.md`); every count's own command.

## What this green does NOT assert

- Anything about §B item rows or §C generators: the recount re-runs §A counts only. The 1,248 item rows and the 848 generated rows were verified once, by the verifiers, through the interactive shell (G9 applies to any of them that used a recursive grep — none of the generators did; the §B spot-checks read files by line, not by grep).
- The 54 not-recountable rows. Their claims stand on the verifiers' one-time reading.
- That the counts are *current*: they are the counts at the recorded HEADs, and a moved HEAD turns the whole file red until it is re-measured.
- That the commands measure what their dimension says. The recount proves the command prints the number; whether the command is the right question is the reviewer's (G10 is the example).
- Portability: measured on macOS only so far (R3 wants Linux and WSL; the platform gates are later in S1).

---

# Readout — slice S1, step 2: the kernel, and BBX validating BBX (2026-09-09)

## Verdict

`BBX_BBH_HOME=~/Developer/blackbox-harness bin/bbx selftest` → **GREEN**, run twice, identical with durations normalised, 47 s:

```
PASS 7     SKIP 0     FAIL 0     MISSING 0
controls fired 9 / declared 9; gates with no declaration: 0; red: 0
ok: every instrument-free gate is registered
```

## Counts, separately

| | |
|---|---|
| gates PASS / SKIP / FAIL / TIMEOUT / MISSING | 7 / 0 / 0 / 0 / 0 (classify, config, tier, static_runner, controls; census_recount, fidelity_bbh) |
| controls declared / fired | 9 / 9 — every one prints its `CONTROL FIRED:` line and is checked by the runner |
| fidelity F13 | 9 pairs identical: synthetic repo plain + `--strict`; example portable, both tiers, NOT RUN, `--list`; tier `--list`, `--unregistered`; config `dump` |
| fidelity must-fire | `verdict-text`: one changed format string → 8 differing lines |
| F14 (sweep) / F15 (classifier over bbh's selftests) | not run — F14 not built; F15 built, opt-in, not yet run |
| new defaults registered with a class | D8–D14 |
| a defect caught by a control before the tool's first real use | 1 (the controls reader: a firing nobody declared read UNDECLARED; `gates/controls.sh` §2) |

## What it rests on

bbh at `f675710` (porcelain 4, recorded), found by `BBX_BBH_HOME`; lifted code cited line by line to its bbh origin; the diff mechanism inherited from bbh's F1–F11 with durations the only normalisation; BBX's own gates, each with a control that failed on purpose in this run.

## What this green does NOT assert

- Anything about the suite, the sweep, the comparators, expectations, or provenance registers: none exists in BBX yet (S1 step 3: the sweep runner and F14; S2: the comparators, F12, F16, F17).
- F15: the classifier agreement over bbh's real selftest logs has not been run.
- Portability beyond this host (macOS, bash 3.2, Python 3.9.6): the tomllib half of `gates/config.sh` is not asserted here (no tomllib on 3.9); Linux and WSL runs are still to come (R3).
- That the `frame-driven` profile is the right shape for kinds A and B: it is bbh's defaults under a name; the two new kinds are the test.
- That BBX's self-validation catches a convention error it shares with itself: the custodian for that is F13 (bbh), which passed, and the must-fire controls, which fired — nothing more (BBX-15).

---

# Readout — slice S1, step 3: the sweep runner, the fingerprint, F14 and F15 (2026-09-09)

## Verdict

`BBX_BBH_HOME=~/Developer/blackbox-harness bin/bbx selftest` → **GREEN**, 117 s:

```
PASS 9     SKIP 0     FAIL 0     MISSING 0
controls fired 12 / declared 12; gates with no declaration: 0; red: 0
ok: every instrument-free gate is registered
```

Run twice. The two runs differ in one block only: run one reported the working tree DIRTIED by three `docs/*.md` files, which the orchestrator was editing while the run was in progress; run two, with no concurrent edits, reported `ok: no tracked file changed`. The runner's working-tree check worked on its author.

## Counts, separately

| | |
|---|---|
| gates PASS / SKIP / FAIL / TIMEOUT / MISSING | 9 / 0 / 0 / 0 / 0 (+ sweep_runner, fingerprint since step 2) |
| controls declared / fired | 12 / 12 |
| fidelity F13 / F14 / F15 | 9 pairs / 12 pairs / 32 logs — all identical |
| fidelity deltas found | 1, and it is a finding about bbh's example consumer, not about BBX (G11): a sourced lib's fallback path is wrong and is masked by bbh's runner exporting its own location; recorded per `[BBH-82]`, both sides of that pair run with `BBH_HOME` exported |
| the mechanism of G11, measured | `env -u BBH_HOME sh tests/g_needs_fake.sh` → `…/Developer/drivers/fake.sh: No such file or directory`; with `BBH_HOME` set → PASS; the lib's fallback resolves to `/Users/koneko/Developer` |
| new defaults registered | D15 (sweep, kind-blind), D16 (fingerprint + suite keys) |
| S1 rows still open | the readout generator (RO1–RO3 as a tool), the platform gates (R3: Linux, WSL), F12 moved to S2 |

## What it rests on

bbh `f675710` (porcelain 4); every lifted file cites its bbh origin; F14's real run executes bbh's own example gates under each runner and compares only the runners' printed lines; F15 runs bbh's 32 selftests once and classifies each (exit, log) both ways.

## What this green does NOT assert

- Anything about a suite, a comparator, an expectation or a provenance register: S2.
- That BBX's sweep runner is right on any consumer but bbh's example and the synthetic repo of `gates/sweep_runner.sh`.
- That the `frame-driven` profile's literals (VampireSaved's build directories among them) are anything but bbh's — they are carried for fidelity and labelled so (D12, D15).
- Portability beyond macOS (R3) — no Linux or WSL run yet.
- bbh's example's correctness: G11 is bbh's to fix; BBX does not modify bbh.


---

# CLOSE — bbx-1 (2026-09-09)

**Green first, alone:** `PASS 9     SKIP 0     FAIL 0     MISSING 0` — `controls fired 12 / declared 12; gates with no declaration: 0; red: 0` — `ok: no tracked file changed during the run`.

**Nothing evaporates:** every number in the four readout sections above is reproduced by a gate (`bin/bbx selftest`) or a census row (`docs/census/`), except the slice estimate (an estimate, labelled) and the runtimes (measured once per close, D2, D14).

**Sweeps:** retraction — the corrected wordings live only in the ledgers; deferral — empty; defaults — 16 rows; controls — 9 of 9 gates declare. **Lineage untouched:** bbh 4 modified at f675710; VampireSaved 1 M + 370 ?? at 5df1d8be; SMS clean at ecc5481.

**Rulings this sitting:** R0–R17 (all answered). **Incidents filed with a price:** G1–G11. **Commits:** 8 including this close.

**What this close does NOT assert:** anything about S2's comparators; portability beyond macOS; that the ritual's sweeps are gated (they are hand-run until S1 step 4).

---

# bbx-2 open — the VampireSaved re-measure (2026-09-09)

## Verdict

The ritual's re-derivation, run first and alone, was **NOT GREEN**: `PASS 8     SKIP 0     FAIL 1     MISSING 0`, failed `census_recount` — `census=vampiresaved.md head=5df1d8be repo_head=0cdd9726c35b verdict=HEAD-MOVED rows=127`. bbh (79 rows) and SMS (94 rows) matched; both controls fired. VampireSaved had moved two commits since the bbx-1 close.

After the re-measure (`docs/census/vampiresaved.md` at `0cdd9726`), alone, no edits in flight → **GREEN**:

```
PASS 9     SKIP 0     FAIL 0     MISSING 0
controls fired 12 / declared 12; gates with no declaration: 0; red: 0
ok: no tracked file changed during the run
census=vampiresaved.md head=0cdd9726 porcelain=373 rows=127 match=100 mismatch=0 not_recountable=27 nonzero_exit=0
```

## Counts, separately

| | |
|---|---|
| commits VampireSaved moved | 2 (`git -C ~/Developer/Vampire_Saved/VampireSaved log --oneline 5df1d8be..0cdd9726 \| wc -l`) |
| files those commits touched | 2 (`STATE.md` +3/−1 at line 42; `tests/expect/mister_prg_window.txt`) |
| census counts that moved / recountable | 1 / 100 (A77, `wc -l < STATE.md`, 1509 → 1511) |
| §B line citations re-pointed | 13 (all into `STATE.md`, all +2; 10 `read` by hand, 3 `gen:G1` by re-run); old line diffed identical to new line: 13 / 13 |
| G1 re-run vs §B `gen:G1` rows | 555 / 555, diff empty |
| not-recountable rows | 27, the same 27 (allowed to move only downward: unchanged) |
| header numbers corrected | 3 (HEAD; 1502 → 1504 commits; 370 → 372 untracked, both new files under `build/`) |
| bins rows changed | 0 (`docs/bins/vampiresaved.md` keys by id) |
| gotchas filed | 1 (G12, price: the opening of bbx-2) |
| rulings raised | 1 (R18, open, recommendation filed, tool unchanged) |
| `census_recount` runtime | 20 s (header quotes ~21 s, D2) |

## What it rests on

The recount under the hermetic environment (D6) at the new HEAD, run first on a scratch copy of the census (so the measurement preceded every edit), then on the edited file; G1 re-run from the census's own §C text; the ten hand citations verified by `sed -n Lp` at `5df1d8be` against `sed -n (L+2)p` at `0cdd9726`, all identical; then the whole battery.

## What this green does NOT assert

- Anything about VampireSaved's two commits beyond the two files they touched; their content (an M18 release) was not read.
- That the census's 27 not-recountable rows are still true at `0cdd9726`: they are not run by construction (the number to bring down, unchanged).
- That the ten hand-read citations say what the rows claim: the check was old line = new line, not a re-read of the claim.
- That the ritual's step-8/9 sweeps are gated: still hand-run (S1 step 4).
- Anything R18 recommends: the recount is unchanged until the maintainer answers.

---

# bbx-2 step 1 — R18: the recount on a clone, the tests provably read-only (2026-09-09)

## Verdict

`BBX_BBH_HOME=~/Developer/blackbox-harness bin/bbx selftest`, alone → **GREEN**, 156 s:

```
PASS 9     SKIP 0     FAIL 0     MISSING 0
controls fired 14 / declared 14; gates with no declaration: 0; red: 0
ok: no tracked file changed during the run
NOTE  fidelity_bbh  bbh-untracked before=0 after=0
```

`gates/census_recount.sh` alone: `PASS: 3 census files, 300 rows: 244 match, 0 mismatch, 0 refused, 56 not recountable, 1 matched with a non-zero exit; 4 controls fired`, 32 s.

## What changed

The maintainer ruled R18: the census and the tests work on a clone or make explicit, and prove, that the tree is not changed. `lib/py/bbx/recount.py` now exports the recorded commit as a shared clone under `TMPDIR`, runs every row there, removes the clone, refuses a command that names the repository's absolute path, and reports a moved lineage tip as one `NOTE: drift …` line after re-running the rows on a clone of the tip. `gates/fidelity_bbh.sh` declares READ-ONLY on bbh's tree and proves it (tracked porcelain before = after, or FAIL). Census grammar rule 7; defaults D17–D19; D2 re-measured.

## Counts, separately

| | |
|---|---|
| controls declared / fired, `census_recount` | 4 / 4 (wrong-head → "HEAD unknown"; moved-count; names-the-tree → REFUSED; drift-detected → `ahead=1 rows_moved=1 ids=A2` on a shadow recorded at bbh's parent `b4852e5`) |
| drift on real data | the bbx-1 VampireSaved census (recorded `5df1d8be`) against the tip: `ahead=2 rows_moved=1 ids=A77` — the same one row the hand re-measure found; 42.8 s |
| shared clone of VampireSaved | 1.76 s, 363 MB (its `.git` is 949 MB; nothing written under it; `git worktree list` there unchanged) |
| census rows the clone exposed as host facts | 4: bbh A3 (4 → 0), SMS A4 (0 = 0, a row that could never fail), SMS A13 (5332 → 30 tracked), SMS A14 (324 → 37 tracked) — G13 |
| not-recountable rows, before → after | 54 → 56 (bbh 0 → 1, SMS 27 → 28): the two `host:` cells; the only upward move allowed, with G13 as the reason |
| recountable rows now measuring the commit, not the tree | 244 / 244 (every `ls` row is now a fact of the checkout, not of whatever the host had untracked) |
| bbh tree during fidelity | tracked porcelain 4 = 4; untracked 0 = 0 |
| lineage tips and porcelain at this run | bbh `f675710` / 4; VampireSaved `0cdd9726` / 377 (live: another session's work, not BBX's); SMS `ecc5481` / 0 |
| runtimes | census gate 21 → 32 s (D2); fidelity 37 s measured (D19; header had said ~10 s); selftest 117 → 156 s (D14), the sweep-runner gate 55 → 66 s under a host running VampireSaved's sweep at the same time |
| rulings | R18 in force; R19 (pull queue, N workers over N clones), R20 (fidelity on a clone of bbh) open |

## What it rests on

The four controls of the census gate, each failing (or, for drift, firing) for its stated reason on a shadow of bbh's census; the fidelity gate's before/after snapshot; the clone mechanism measured on the largest lineage; and the recount's summary line carrying `tree=clone tip= ahead= porcelain=` so the readout can tell a rotted census from a moved lineage.

## What this green does NOT assert

- That the fidelity gate is read-only on bbh's *untracked* files beyond the count (0 = 0 here; a gate that wrote and deleted a file inside the run would not show). Moving fidelity to a clone (R20) would make this structural.
- That `--in-place` is read-only: it is declared so and unproved; it exists for a verifier's spot-check on a tree that is already a clone.
- That the census's 56 not-recountable rows are true at their commits.
- Anything about the pull queue (R19): the sweep runner still batches with a barrier.
- Portability of the clone step beyond macOS (R3): `git clone --shared` is used on Linux the same way, not yet measured there.

---

# bbx-2 step 2 — R20: plain clones; the rulings-shape gate (2026-09-09)

## Verdict

`BBX_BBH_HOME=~/Developer/blackbox-harness bin/bbx selftest`, alone → **GREEN**, 227 s (host shared with another session's VampireSaved sweep):

```
PASS 10    SKIP 0     FAIL 0     MISSING 0
controls fired 18 / declared 18; gates with no declaration: 0; red: 0
ok: no tracked file changed during the run
NOTE  fidelity_bbh  bbh-source tip=f675710 porcelain=4 untouched-by-construction=clone
```

## What changed

R20 ruled plain clone: `gates/fidelity_bbh.sh` clones bbh at `f675710` (D20) under `TMPDIR` and measures there; after the run the clone must be clean of tracked, untracked and ignored entries (it is); bbh's tip past the baseline is a NOTE. The recount's clone is plain too (D17). New gate `gates/rulings_shape.sh` (portable): every ruling under the heading of its state, the queue and `DECISIONS.md` one registry both ways, the `Open rulings:` line exact — born of G14.

## Counts, separately

| | |
|---|---|
| gates PASS / SKIP / FAIL / MISSING | 10 / 0 / 0 / 0 (+ `rulings_shape`) |
| controls declared / fired | 18 / 18 (`rulings_shape` 4: answered-under-open, open-under-answered, no-decisions-row, row-without-entry) |
| rulings queue | 21 entries, 21 answered, 0 open, 21 DECISIONS rows, 0 errors |
| fidelity on the clone vs in place | F13–F15 identical; the only diff two `porcelain` lines (4 vs 0) |
| clone of bbh at `f675710` | plain: 0.32 s, 2.3 MB; clean after the run: 0 tracked, untracked, ignored |
| clone cost, VampireSaved, plain vs shared | 3.40 s / 490 MB (hardlinked pack) vs 1.76 s / 363 MB |
| runtimes, alone | census gate 44 s (D2; 57 s inside the loaded battery), fidelity 46 s (D19), rulings_shape 0.3 s |
| gotchas filed | G14 (answered under Open), G15 (a commit message ahead of its edits; amended) |
| rulings | R18, R19, R20 all answered; open: none |

## What it rests on

The measurement that preceded R20 (identical fidelity output on the clone and in place); the clone's `status --porcelain --ignored` empty after the run; `rulings_shape`'s four shadow files, each failing on the named id; the battery's registry and controls checks.

## What this green does NOT assert

- That the pull queue (R19) is built: the sweep runner still batches with a barrier.
- That any gate other than the recount and fidelity is clone-safe: BBX's own gates run in BBX's tree (a tracked-file check, not a clone), and `bbx.toml` does not yet carry a clone-per-slot option.
- The rulings grammar beyond what the gate reads: prose in an entry is not checked, only the answer line, the heading and the DECISIONS row.
- Runtimes as anything but this host under this load (D14's row says so).
- Portability beyond macOS (R3).

---

# bbx-2 step 3 — R19: the pull queue and clone-per-slot in the sweep runner (2026-09-09)

## Verdict

`BBX_BBH_HOME=~/Developer/blackbox-harness bin/bbx selftest`, alone → **GREEN**, 223 s (loaded host):

```
PASS 10    SKIP 0     FAIL 0     MISSING 0
controls fired 20 / declared 20; gates with no declaration: 0; red: 0
ok: no tracked file changed during the run
```

## What changed

`bin/bbx-run-sweep --jobs N` no longer batches: a FIFO holds one token per slot, a worker blocks reading a token, runs its gate, writes the token back (VampireSaved 14z-144, lifted; the loop stays in the main shell). `[sweep].clone_per_slot = true` (D21, off by default) gives every slot, the serial prereq lane included, a plain clone of the consumer's HEAD under `TMPDIR`, removed at exit; the banner names the commit and the working tree's porcelain. `gates/sweep_runner.sh` sections 15–16 measure both.

## Counts, separately

| | |
|---|---|
| queue, measured by the gates' own stamps | `--jobs 2`: the third 1-s gate started 2 s BEFORE the 4-s gate ended (a barrier or a line cannot); `--jobs 1`: 3 s AFTER (control `serial-order`) |
| clone-per-slot | 2 workers, 2 trees, neither the base, both at the fixture's HEAD; base tree untouched by two file-writing gates; clone dir removed; without clones the same gates left 2 files in the base (control `no-clone-dirties`) |
| controls declared / fired, `sweep_runner` | 4 / 4 (env-default-export, prereq-stop, serial-order, no-clone-dirties) |
| fidelity F14 after the runner change | every pair identical (--list 8 lines, --list all 9, --dry-run 32, real run 35, 8 fingerprint pairs) |
| runtimes | sweep-runner gate 90 s alone (sections 15–16 add ~15 s of deliberate sleeps); selftest 223 s (D14) |
| defaults | D21 added (`clone_per_slot`, arbitrary: off because a clone measures HEAD, not the working tree) |

## What it rests on

Stamps printed by the stub gates (`date +%s`), never the runner's own accounting; `pwd -P` and `git rev-parse` from inside the workers; the base tree's listing after the run; F14 against bbh's own runner at `--jobs 1`.

## What this green does NOT assert

- Speed-up on a real consumer: the 1.65× → 3.48× figures are VampireSaved's, quoted from its source, not re-measured here (no instrument-tier consumer exists in BBX yet).
- Clone-per-slot on a consumer with uncommitted work: the clone is HEAD; the banner says so, nothing refuses.
- A gate that escapes its clone by absolute path: not guarded (the recount's path guard is the census's, not the sweep's).
- That the queue's row order in `results.tsv` is stable: it is not, by design; `--resume` keys by name.
- Portability beyond macOS (R3): `mkfifo` and `exec 8<>` are POSIX, not yet run on Linux or WSL.

---

# bbx-2 step 4 — the readout generator: the screen below is GENERATED (2026-09-09)

## The screen, verbatim (`bin/bbx readout build/selftest_20260909T203339Z --against build/selftest_20260909T203000Z`)

```
== READOUT — self subject at /Users/koneko/Developer/generalized-blackbox-harness/BBX @ 8890e4e (porcelain 22) — started 2026-09-09T20:33:39Z on Darwin arm64 ==
VERDICT: GREEN   PASS 11  SKIP 0  FAIL 0  TIMEOUT 0  MISSING 0   (gates 11)
tree during the run: unchanged   harness: bbx @ 8890e4e
rests on:
  controls: fired 23 / declared 23; dead 0; undeclared firings 0; gates red 0
  each can fail: 11 of 11 gates proved a control fires on purpose
  expectations relied upon: none registered — the expectation register with its provenance classes is slice S2; until then no comparison against a frozen expectation is claimed
  coverage: census_recount: rows=300 recountable=244 not_recountable=56 (BBX-18: the census claims no command re-derives)
  BBX-14 (more than one run): met — 11 gates, 0 verdict differences against the run started 2026-09-09T20:30:01Z at the same HEAD
  last re-baseline: (none recorded)
what this green does NOT assert (declared by each gate's header):
  classify: that a verdict word printed by a gate outside the runner is read at all: the classifier reads exit status first, then the log; a PASS printed after a non-zero exit is FAIL by design
  config: the meaning of a consumer's keys: only that the layers resolve (consumer over kind profile over kind-blind default) and that dumps are stable
  tier: that a gate reaching an instrument through a path the source regex does not match is seen: the depth and the regex are the limit
  static_runner: the sweep runner or any gate that needs an instrument: this is the pre-commit chain only
  static_runner: runtimes as anything but this host under this load
  controls: that a control is RIGHT — only that a declared control fired and an undeclared one is red (docs/controls.md)
  sweep_runner: speed-up on a real consumer: the queue is measured on stub gates with sleeps, not on an instrument-tier suite
  sweep_runner: a gate that escapes its clone by an absolute path: clone-per-slot pins the cwd, nothing more
  sweep_runner: portability beyond macOS: mkfifo and exec 8<> are POSIX, not yet run on Linux or WSL
  fingerprint: the identity of any artifact that is not a single file: the kind-blind fingerprint is file-sha1 (D16)
  rulings_shape: the prose of a ruling: only its heading, its answer line and its DECISIONS row are read
  readout: that a declared blind spot is true or complete: the screen prints what the header says
  readout: the sweep runner's runs: only bbx-run-static --log is read
  census_recount: the truth of the not-recountable rows at their commits: they are named and counted, never run
  census_recount: that a hand-read citation says what its row claims: only that the line exists
  fidelity_bbh: anything about a suite, a comparator or an expectation: slice S2
  fidelity_bbh: bbh's example's correctness (G11 is bbh's to fix)
  fidelity_bbh: F15 unless BBX_FIDELITY_F15=1 was set for the run
  gates declaring no blind spot: 0   (a gate nobody has asked what its green leaves out)
```

## What changed

`bin/bbx-run-static --log DIR` keeps a run (results.tsv, every gate's log, controls.txt, run.txt with the subject's HEAD, porcelain, platform and the runner's own verdict) and prints nothing extra (F13 identical). `lib/py/bbx/readout.py` reads a kept run into the screen above: RO1's content line by line, RO2 from the gates' new `# NOT-ASSERTED:` header lines (all eleven gates declare; the screen counts any that do not), RO3 by construction. `gates/readout.sh` holds it to a fixture with three controls. The census gate prints its coverage as a NOTE the screen reads. R21 raised (no Linux/WSL host on this machine).

## Counts, separately

| | |
|---|---|
| kept runs at HEAD 8890e4e (porcelain 22, this step's edits) | 3: the first NOT GREEN (below), then two GREEN, 0 verdict differences between them |
| the first kept run's red | `rulings_shape` FAIL, 2 controls dead: the gate built its shadows from the queue's LAST entry, answered only until R21 was filed open the same hour; fixed to the last answered ruling (DECISIONS' last row); the screen reported it as `gates red 1: rulings_shape (RED)` and exited 1 |
| controls declared / fired | 23 / 23 (readout 3: verdict-follows-run, bbx-14-unmet, undeclared-blind-spot) |
| NOT-ASSERTED lines across the tree | 18 over 11 gates; gates declaring none: 0 |
| coverage on the screen | 300 rows, 244 recountable, 56 not (from the census gate's NOTE) |
| fidelity after the runner change | F13a–e identical (36, 37, 22, 24, 25, 10, 13, 2, 108 lines) |
| runtimes | readout gate 4 s; selftest 219 s (D14) |
| rulings | R21 open (platform runs); the shape gate caught its author's "Open rulings:" wording once (R3 named in passing) |

## What it rests on

The kept run's files, read and never re-derived; the gates' own headers for the blind spots; `--against` for BBX-14 (verdict equality per gate at one HEAD); the fixture gate's shadows (a FAIL row, a differing second run, a gate with no declaration).

## What this green does NOT assert

- Everything the screen itself lists above; plus: that the screen's wording is what the maintainer needs — RO3 is judged by its reader, not by a gate.
- The sweep runner's runs are not on the screen (only `bbx-run-static --log` is read).
- Platform runs (R21): none exist; every green here is Darwin arm64.
- The close ritual's sweeps (steps 8–9) are still hand-run.

---

# bbx-2 step 5 — the close sweeps as a gate; and CLOSE — bbx-2 (2026-09-10)

## The screen, verbatim (`bin/bbx readout build/selftest_20260909T221655Z --against build/selftest_20260909T221309Z`)

```
== READOUT — self subject at /Users/koneko/Developer/generalized-blackbox-harness/BBX @ 3247b80 (porcelain 6) — started 2026-09-09T22:16:56Z on Darwin arm64 ==
VERDICT: GREEN   PASS 12  SKIP 0  FAIL 0  TIMEOUT 0  MISSING 0   (gates 12)
tree during the run: unchanged   harness: bbx @ 3247b80
rests on:
  controls: fired 26 / declared 26; dead 0; undeclared firings 0; gates red 0
  each can fail: 12 of 12 gates proved a control fires on purpose
  expectations relied upon: none registered — the expectation register with its provenance classes is slice S2; until then no comparison against a frozen expectation is claimed
  coverage: census_recount: rows=300 recountable=244 not_recountable=56 (BBX-18: the census claims no command re-derives)
  BBX-14 (more than one run): met — 12 gates, 0 verdict differences against the run started 2026-09-09T22:13:10Z at the same HEAD
  last re-baseline: (none recorded)
what this green does NOT assert (declared by each gate's header):
  classify: that a verdict word printed by a gate outside the runner is read at all: the classifier reads exit status first, then the log; a PASS printed after a non-zero exit is FAIL by design
  config: the meaning of a consumer's keys: only that the layers resolve (consumer over kind profile over kind-blind default) and that dumps are stable
  tier: that a gate reaching an instrument through a path the source regex does not match is seen: the depth and the regex are the limit
  static_runner: the sweep runner or any gate that needs an instrument: this is the pre-commit chain only
  static_runner: runtimes as anything but this host under this load
  controls: that a control is RIGHT — only that a declared control fired and an undeclared one is red (docs/controls.md)
  sweep_runner: speed-up on a real consumer: the queue is measured on stub gates with sleeps, not on an instrument-tier suite
  sweep_runner: a gate that escapes its clone by an absolute path: clone-per-slot pins the cwd, nothing more
  sweep_runner: portability beyond macOS: mkfifo and exec 8<> are POSIX, not yet run on Linux or WSL
  fingerprint: the identity of any artifact that is not a single file: the kind-blind fingerprint is file-sha1 (D16)
  rulings_shape: the prose of a ruling: only its heading, its answer line and its DECISIONS row are read
  readout: that a declared blind spot is true or complete: the screen prints what the header says
  readout: the sweep runner's runs: only bbx-run-static --log is read
  close_sweeps: that every corrected claim has a register row: the register is written by hand at the correction (a claim nobody registered is not swept)
  close_sweeps: step 9 of the close (the lineage untouched): that is the recount's clone and the fidelity gate's proof, not this gate
  census_recount: the truth of the not-recountable rows at their commits: they are named and counted, never run
  census_recount: that a hand-read citation says what its row claims: only that the line exists
  fidelity_bbh: anything about a suite, a comparator or an expectation: slice S2
  fidelity_bbh: bbh's example's correctness (G11 is bbh's to fix)
  fidelity_bbh: F15 unless BBX_FIDELITY_F15=1 was set for the run
  gates declaring no blind spot: 0   (a gate nobody has asked what its green leaves out)
```

## Step 5, what changed

`gates/close_sweeps.sh` + `lib/py/bbx/close_sweeps.py` + `docs/retractions.tsv` (8 rows): the close ritual's step 8 as a check — a retracted wording anywhere but the ledgers its row allows, a deferral marker anywhere, a `D<n>` cited with no register row: FAIL; three planted-defect controls. Step 9 is by construction (R18/R20) and by the fidelity gate's proof. R21 carries the procedure for the Linux run (bundle BBX, clone bbh from GitHub, the census SKIPs there, run twice with `--log`). The sweep's first run on the real tree found three things its author had not thought of: DECISIONS' R12 row naming the retracted citation (a ledger, now allowed), bbh's bins citing a lineage D-id (lineage files excluded), and the gate's own control text (its two files never swept). The rulings-shape gate rejected R21's answer line written as "(open — …)": the grammar wants exactly "(open)", and got it.

# CLOSE — bbx-2 (2026-09-10)

**Green first, twice, kept:** `PASS 12    SKIP 0     FAIL 0     MISSING 0` — `controls fired 26 / declared 26; gates with no declaration: 0; red: 0` — `ok: no tracked file changed during the run` — BBX-14 met on the screen above (12 gates, 0 verdict differences between the two runs). Runtime 222 s (D14).

**Nothing evaporates:** every number in this sitting's sections is reproduced by a gate, a census row, or a kept run under `build/`; runtimes are measured once per step (D2, D14, D19) and say so; VampireSaved's 1.65×/3.48× figures are quoted from its source (R19) and labelled so.

**Sweeps:** gated — `close_sweeps` GREEN in both runs (8 retraction rows, 0 hits outside the ledgers; 0 deferrals; 22 defaults rows, 59 citations, 0 unresolved); every gate declares its controls (the runner: gates with no declaration 0); the queue's shape GREEN (`rulings_shape`: 22 entries, 21 answered, 1 open — R21). **Lineage untouched, by construction and by proof:** bbh recorded=f675710 tip=f675710 ahead=0 porcelain=4; vampiresaved recorded=0cdd9726 tip=e25e6f7 ahead=2 porcelain=385; sms recorded=ecc5481 tip=ecc5481 ahead=0 porcelain=0. The drift NOTE for VampireSaved is in the run's census log; the screen does not list it yet (bbx-3's first fix).

**Rulings this sitting:** R18, R19, R20 answered; R21 open (the platform run: the maintainer's host, procedure in the entry). **Incidents filed with a price:** G12–G15. **Commits:** 9 including this close (the close text first said 11, from memory; measured `git log --oneline de068f4..HEAD | wc -l` → 9, corrected in the commit that follows).

**What this close does NOT assert:** anything about a comparator, an expectation register or a suite (S2); any platform but Darwin arm64 (R21); the pull queue's speed-up on a real consumer; the close ritual's step 2 (nothing evaporates) is still read by a person; the screen's NOTE block beyond coverage.

# bbx-3 — the NOTE fix; S2 planned, STOPPED, ruled; S2 steps 1–3 built (2026-09-10)

## The screen, verbatim (`bin/bbx readout build/selftest_20260910T014436`; the second run for BBX-14 follows in the CLOSE section)

```
== READOUT — self subject at /Users/koneko/Developer/generalized-blackbox-harness/BBX @ 2124e54 (porcelain 38) — started 2026-09-09T23:44:37Z on Darwin arm64 ==
VERDICT: GREEN   PASS 18  SKIP 0  FAIL 0  TIMEOUT 0  MISSING 0   (gates 18)
tree during the run: unchanged   harness: bbx @ 2124e54
rests on:
  controls: fired 48 / declared 48; dead 0; undeclared firings 0; gates red 0
  each can fail: 18 of 18 gates proved a control fires on purpose
  expectations relied upon: none registered — the expectation register with its provenance classes is slice S2; until then no comparison against a frozen expectation is claimed
  coverage: census_recount: rows=300 recountable=244 not_recountable=56 (BBX-18: the census claims no command re-derives)
  note: census_recount: drift census=vampiresaved.md recorded=0cdd9726 tip=1cedf01bae67 ahead=6 rows_moved=17 ids=A21,A22,A23,A24,A25,A26,A32,A33,A62,A77,A80,A84,A87,A95,A120,A121,A127
  note: fidelity_bbh: bbh-source tip=f675710 porcelain=4 untouched-by-construction=clone
  note: fidelity_bbh_s2: bbh-source tip=f675710 porcelain=4 untouched-by-construction=clone
  note: suite: suite-runs 25 (each kept under --log)
  BBX-14 (more than one run): UNMET in this screen — one run only; pass --against a second kept run of the same HEAD
  last re-baseline: (none recorded)
what this green does NOT assert (declared by each gate's header):
  classify: that a verdict word printed by a gate outside the runner is read at all: the classifier reads exit status first, then the log; a PASS printed after a non-zero exit is FAIL by design
  config: the meaning of a consumer's keys: only that the layers resolve (consumer over kind profile over kind-blind default) and that dumps are stable
  tier: that a gate reaching an instrument through a path the source regex does not match is seen: the depth and the regex are the limit
  static_runner: the sweep runner or any gate that needs an instrument: this is the pre-commit chain only
  static_runner: runtimes as anything but this host under this load
  controls: that a control is RIGHT — only that a declared control fired and an undeclared one is red (docs/controls.md)
  sweep_runner: speed-up on a real consumer: the queue is measured on stub gates with sleeps, not on an instrument-tier suite
  sweep_runner: a gate that escapes its clone by an absolute path: clone-per-slot pins the cwd, nothing more
  sweep_runner: portability beyond macOS: mkfifo and exec 8<> are POSIX, not yet run on Linux or WSL
  fingerprint: the identity of any artifact that is not a single file: the kind-blind fingerprint is file-sha1 (D16)
  rulings_shape: the prose of a ruling: only its heading, its answer line and its DECISIONS row are read
  readout: that a declared blind spot is true or complete: the screen prints what the header says
  readout: the sweep runner's runs: only bbx-run-static --log is read
  close_sweeps: that every corrected claim has a register row: the register is written by hand at the correction (a claim nobody registered is not swept)
  close_sweeps: step 9 of the close (the lineage untouched): that is the recount's clone and the fidelity gate's proof, not this gate
  temporal: anything about a real log: every shape here is synthesized (fixture class); the classes' fitness for a consumer is that consumer's ratification
  temporal: the dispatcher's spec line and the suite's dispatch (S2 steps 2 and 3): the comparators are called directly
  temporal: that the thresholds are right for any subject: they are the frame-driven profile's (D23) and a consumer's override needs R25's ruling row (gates/thresholds.sh)
  thresholds: that a ruling id in [thresholds].rulings names a ruling that exists: any non-empty string is accepted here; the consumer's rulings-shape gate is where an id is checked
  thresholds: the values' fitness for any subject: 2 / 60 / 8 are bbh's ratified policy carried in the frame-driven profile (D23), not a measurement of anything here
  compare_dispatch: the suite's dispatch around this library (which file is read, how many runs, the .sha1 and .diverge kinds' own paths): S2 step 3
  compare_dispatch: anything about a real log or a real mask: every log is synthesized and the masks are strings the guard compares, never applied
  expectation_kinds: the content of any expectation file: only its extension and its stem are read here
  expectation_kinds: the suite's use of the dispositions (S2 step 3)
  census_recount: the truth of the not-recountable rows at their commits: they are named and counted, never run
  census_recount: that a hand-read citation says what its row claims: only that the line exists
  fidelity_bbh: anything about a suite, a comparator or an expectation: slice S2
  fidelity_bbh: bbh's example's correctness (G11 is bbh's to fix)
  fidelity_bbh: F15 unless BBX_FIDELITY_F15=1 was set for the run
  fidelity_bbh_s2: the kept suite run (--log) — bbh has none, so F12 diffs printed text only; the kept run's ground truth is gates/suite.sh
  fidelity_bbh_s2: anything about a real subject: F17's and F16b's inputs are synthesized, F16a's are the fake machine's (fixture class); the shapes are the lineage's paid-for cases and nothing else
  fidelity_bbh_s2: bbh's correctness: identical output on both sides is fidelity, not truth
  suite: any driver but the fake: a MAME or FBNeo driver is bbh's and untested here (bbh's own F8 rows)
  suite: the .sha1 kind's evidence: it is `self` class by construction (E4) and the register that says so is S2 step 4
  suite: the readout's reading of a kept suite run: the screen reads bbx-run-static runs only until step 4
  gates declaring no blind spot: 0   (a gate nobody has asked what its green leaves out)
```

## What changed

**Open.** The battery GREEN on the bbx-2 HEAD (12 gates, 26/26). The handoff's first fix: `lib/py/bbx/readout.py` lists every NOTE-class line (the VampireSaved drift was in the census log and not on the screen); `gates/readout.sh` gained the `note-follows-log` control, proved on a shadow generator with the feature stripped.

**S2 planned, STOPPED, ruled.** `docs/plans/S2.md`: the lift measured on the plain clone at `f675710` (11 files, 1,102 lines; 8 selftests; 62 frozen verdict lines; the example's expectation tree by kind), four rulings raised. The maintainer answered R23 (family and view by KIND), R25 (a loosened threshold needs a ruling id), R26 (a per-kind driver home, bbh's for the frame-driven kind) and objected to R24's TSV; R24 revised to a TOML register (measured against the subset parser) and left open.

**Step 1 — the temporal family.** `thresholds.py` (values from the kind profile, R25's refusal), `logfmt.py`, `compare_flicker/window/composite.py`, `check_diverge.py`, `propose_temporal.py` — bbh's text verbatim; `gates/temporal.sh` (4 controls), `gates/thresholds.sh` (3, one a shadow proposer that drifts); F17 in `gates/fidelity_bbh_s2.sh`.

**Step 2 — the dispatcher and the kinds.** `lib/sh/compare.sh` (`compare_temporal` = bbh's `masked_check`; `compare_check` by kind), `lib/sh/expectation_kinds.sh` reading the profile's `[expectations].kinds` table through `lib/py/bbx/expectations.py`; the mask default in the profile, none kind-blind; `gates/compare_dispatch.sh` (3 controls), `gates/expectation_kinds.sh` (3); F16.

**Step 3 — the suite.** `bin/bbx-run-suite` (bbh's loop, its text verbatim; `--log` keeps `results.tsv` with a FINDING column read by `lib/py/bbx/finding.py`, `short` apart from `diverged`; R25 at the entrance; R26's driver home; `bbx run-suite`, `bbx compare` in the dispatcher); `gates/suite.sh` (5 controls); F12.

**Step 4 (the register) waits on R24.** The screen's "expectations relied upon" line still says none registered, which is true: no register exists yet.

## Counts, separately

| | |
|---|---|
| gates | 18 (S1's 12 + temporal, thresholds, compare_dispatch, expectation_kinds, fidelity_bbh_s2, suite); registries complete both ways |
| controls declared / fired | 48 / 48 (27 after the NOTE fix + 21 new: temporal 4, thresholds 3, compare_dispatch 3, expectation_kinds 3, suite 5, fidelity_bbh_s2 3) |
| fidelity, all diff empty on the plain clone at `f675710` | F17 52 pairs; F16 35 (11 real-driver, 21 synthetic, 3 enumeration; 10 fake-driver runs); F12 17 suite pairs — 104 in `fidelity_bbh_s2` (69 s); F13–F15 unchanged in `fidelity_bbh` |
| verdict-text controls | three (f12, f16, f17): one changed string each, the diff non-empty |
| defaults rows added | D23–D30 (thresholds, looseness direction, kinds table, mask default, hash_cmd, driver_home, runs_per_replay, the kept suite run's shape) |
| kept battery runs this sitting | 5: open GREEN 12/26; after the NOTE fix GREEN 12/27; after step 1 GREEN 15/35; after step 2 **NOT GREEN** 16+1/42 (`close_sweeps`: D27 cited before its row, G17); after step 3 GREEN 18/48 (the screen above) |
| defects of this sitting's own gates, each caught by its first run | 5: the shadow literal appended after the entry point (thresholds); a failing comparator ending the subshell under `set -e` (fidelity); the kept-run variable unbound in the parent, exit 0 under the trap (suite, G18); derived configs written outside the example copy (suite); a bash process substitution in a POSIX gate (fidelity) |
| runtimes (this host, loaded) | temporal 2.7 s; thresholds 1.2 s; compare_dispatch 1.6 s; expectation_kinds 0.7 s; fidelity_bbh_s2 69 s; suite 58 s; the battery ~6 min |
| lineage tips | bbh f675710 (baseline, porcelain 4, clones only); VampireSaved moved twice during the sitting — 4, 5, then 6 commits past the census (17 rows moved), by design not re-measured |
| rulings | R23, R25, R26 in force; R21, R24 open |

## What it rests on

Every printed verdict line of the lifted code is bbh's, diffed over the same inputs on a plain clone of the baseline (F12, F16, F17); every new gate declares its controls and each fired; every new default has a register row; the kept run's finding column is read by field name; the clone is proved clean after every fidelity run.

## What this green does NOT assert

- Everything the screen lists above; plus: no expectation register exists, so no provenance class is claimed for anything (step 4, R24).
- The suite has one driver, bbh's fake, and one consumer, bbh's example: every class has PASSed only on a fixture-class pairing — the screen's "never on a real pairing" line is step 4's, and it will read that way.
- The looseness mechanism accepts any non-empty ruling id; whether it names a ruling is the consumer's shape gate's question.
- Platform: Darwin arm64 only (R21). The bytecode hazard for Linux clones is guarded (`PYTHONDONTWRITEBYTECODE`), not measured there.
- The mapped-state comparator (bbh `compare_fields.py`) is not lifted (BBX-25: one implementation of one subject).

# CLOSE — bbx-3 (2026-09-10)

## The screen, verbatim (`bin/bbx readout build/selftest_20260910T015043 --against build/selftest_20260910T014436`)

```
== READOUT — self subject at /Users/koneko/Developer/generalized-blackbox-harness/BBX @ 2124e54 (porcelain 39) — started 2026-09-09T23:50:43Z on Darwin arm64 ==
VERDICT: GREEN   PASS 18  SKIP 0  FAIL 0  TIMEOUT 0  MISSING 0   (gates 18)
tree during the run: unchanged   harness: bbx @ 2124e54
rests on:
  controls: fired 48 / declared 48; dead 0; undeclared firings 0; gates red 0
  each can fail: 18 of 18 gates proved a control fires on purpose
  expectations relied upon: none registered — the expectation register with its provenance classes is slice S2; until then no comparison against a frozen expectation is claimed
  coverage: census_recount: rows=300 recountable=244 not_recountable=56 (BBX-18: the census claims no command re-derives)
  note: census_recount: drift census=vampiresaved.md recorded=0cdd9726 tip=1cedf01bae67 ahead=6 rows_moved=17 ids=A21,A22,A23,A24,A25,A26,A32,A33,A62,A77,A80,A84,A87,A95,A120,A121,A127
  note: fidelity_bbh: bbh-source tip=f675710 porcelain=4 untouched-by-construction=clone
  note: fidelity_bbh_s2: bbh-source tip=f675710 porcelain=4 untouched-by-construction=clone
  note: suite: suite-runs 25 (each kept under --log)
  BBX-14 (more than one run): met — 18 gates, 0 verdict differences against the run started 2026-09-09T23:44:37Z at the same HEAD
  last re-baseline: (none recorded)
what this green does NOT assert (declared by each gate's header):
  classify: that a verdict word printed by a gate outside the runner is read at all: the classifier reads exit status first, then the log; a PASS printed after a non-zero exit is FAIL by design
  config: the meaning of a consumer's keys: only that the layers resolve (consumer over kind profile over kind-blind default) and that dumps are stable
  tier: that a gate reaching an instrument through a path the source regex does not match is seen: the depth and the regex are the limit
  static_runner: the sweep runner or any gate that needs an instrument: this is the pre-commit chain only
  static_runner: runtimes as anything but this host under this load
  controls: that a control is RIGHT — only that a declared control fired and an undeclared one is red (docs/controls.md)
  sweep_runner: speed-up on a real consumer: the queue is measured on stub gates with sleeps, not on an instrument-tier suite
  sweep_runner: a gate that escapes its clone by an absolute path: clone-per-slot pins the cwd, nothing more
  sweep_runner: portability beyond macOS: mkfifo and exec 8<> are POSIX, not yet run on Linux or WSL
  fingerprint: the identity of any artifact that is not a single file: the kind-blind fingerprint is file-sha1 (D16)
  rulings_shape: the prose of a ruling: only its heading, its answer line and its DECISIONS row are read
  readout: that a declared blind spot is true or complete: the screen prints what the header says
  readout: the sweep runner's runs: only bbx-run-static --log is read
  close_sweeps: that every corrected claim has a register row: the register is written by hand at the correction (a claim nobody registered is not swept)
  close_sweeps: step 9 of the close (the lineage untouched): that is the recount's clone and the fidelity gate's proof, not this gate
  temporal: anything about a real log: every shape here is synthesized (fixture class); the classes' fitness for a consumer is that consumer's ratification
  temporal: the dispatcher's spec line and the suite's dispatch (S2 steps 2 and 3): the comparators are called directly
  temporal: that the thresholds are right for any subject: they are the frame-driven profile's (D23) and a consumer's override needs R25's ruling row (gates/thresholds.sh)
  thresholds: that a ruling id in [thresholds].rulings names a ruling that exists: any non-empty string is accepted here; the consumer's rulings-shape gate is where an id is checked
  thresholds: the values' fitness for any subject: 2 / 60 / 8 are bbh's ratified policy carried in the frame-driven profile (D23), not a measurement of anything here
  compare_dispatch: the suite's dispatch around this library (which file is read, how many runs, the .sha1 and .diverge kinds' own paths): S2 step 3
  compare_dispatch: anything about a real log or a real mask: every log is synthesized and the masks are strings the guard compares, never applied
  expectation_kinds: the content of any expectation file: only its extension and its stem are read here
  expectation_kinds: the suite's use of the dispositions (S2 step 3)
  census_recount: the truth of the not-recountable rows at their commits: they are named and counted, never run
  census_recount: that a hand-read citation says what its row claims: only that the line exists
  fidelity_bbh: anything about a suite, a comparator or an expectation: slice S2
  fidelity_bbh: bbh's example's correctness (G11 is bbh's to fix)
  fidelity_bbh: F15 unless BBX_FIDELITY_F15=1 was set for the run
  fidelity_bbh_s2: the kept suite run (--log) — bbh has none, so F12 diffs printed text only; the kept run's ground truth is gates/suite.sh
  fidelity_bbh_s2: anything about a real subject: F17's and F16b's inputs are synthesized, F16a's are the fake machine's (fixture class); the shapes are the lineage's paid-for cases and nothing else
  fidelity_bbh_s2: bbh's correctness: identical output on both sides is fidelity, not truth
  suite: any driver but the fake: a MAME or FBNeo driver is bbh's and untested here (bbh's own F8 rows)
  suite: the .sha1 kind's evidence: it is `self` class by construction (E4) and the register that says so is S2 step 4
  suite: the readout's reading of a kept suite run: the screen reads bbx-run-static runs only until step 4
  gates declaring no blind spot: 0   (a gate nobody has asked what its green leaves out)
```

**Close measurements.** Green first, twice, kept: `PASS 18 SKIP 0 FAIL 0 MISSING 0`; `controls fired 48 / declared 48; gates with no declaration: 0; red: 0`; `ok: no tracked file changed during the run` in both; BBX-14 met (0 verdict differences at one HEAD). Runtimes: ~6 min per battery on a loaded host. **Sweeps:** `close_sweeps` GREEN (8 retraction rows, 0 hits outside the ledgers; 0 deferrals; 30 defaults rows, 86 citations, 0 unresolved); `rulings_shape` GREEN (27 entries, 25 answered, 2 open — R21, R24). **Lineage untouched, by construction and by proof:** bbh recorded=f675710 tip=f675710 ahead=0 porcelain=4 (every fidelity gate on a plain clone, clean after); vampiresaved recorded=0cdd9726 tip=1cedf01b ahead=6 (17 rows moved, not re-measured by design); sms recorded=ecc5481 tip=ecc5481 ahead=0.

**Retractions this sitting:** none. The maintainer's objection to R24's TSV is a declined recommendation kept in the queue, not a corrected claim.

**What this close does NOT assert:** anything about an expectation's provenance (no register: step 4, R24); any comparator class on a pairing that is not fixture-class; any driver but bbh's fake; any platform but Darwin arm64 (R21); the mapped-state comparator (not lifted, BBX-25); that the looseness mechanism's ruling ids name rulings (the consumer's shape gate's question).

# bbx-4 — S2 step 4: the expectation register; the suite screen; and the S2 slice readout (2026-09-10)

## The screen, verbatim (`bin/bbx readout build/selftest_20260910T083656 --against build/selftest_20260910T082952`)

```
== READOUT — self subject at /Users/koneko/Developer/generalized-blackbox-harness/BBX @ 9e376ff (porcelain 11) — started 2026-09-10T06:36:56Z on Darwin arm64 ==
VERDICT: GREEN   PASS 19  SKIP 0  FAIL 0  TIMEOUT 0  MISSING 0   (gates 19)
tree during the run: unchanged (untracked entries 2 -> 2)   harness: bbx @ 9e376ff
rests on:
  controls: fired 55 / declared 55; dead 0; undeclared firings 0; gates red 0
  each can fail: 19 of 19 gates proved a control fires on purpose
  expectations relied upon: none registered — the expectation register with its provenance classes is slice S2; until then no comparison against a frozen expectation is claimed
  coverage: census_recount: rows=300 recountable=244 not_recountable=56 (BBX-18: the census claims no command re-derives)
  note: census_recount: drift census=bbh.md recorded=f675710 tip=10a82d2dab53 ahead=1 rows_moved=1 ids=A2
  note: census_recount: drift census=vampiresaved.md recorded=0cdd9726 tip=195cd6ea686a ahead=10 rows_moved=35 ids=A21,A22,A23,A24,A25,A26,A27,A28,A32,A33,A35,A37,A45,A51,A59,A62,A63,A66,A67,A68,A77,A80,A84,A85,A86,A87,A88,A89,A90,A91,A93,A95,A120,A121,A127
  note: fidelity_bbh: bbh-drift baseline=f675710 tip=10a82d2 ahead=1
  note: fidelity_bbh: bbh-source tip=10a82d2 porcelain=0 untouched-by-construction=clone
  note: fidelity_bbh_s2: bbh-drift baseline=f675710 tip=10a82d2 ahead=1
  note: fidelity_bbh_s2: bbh-source tip=10a82d2 porcelain=0 untouched-by-construction=clone
  note: suite: suite-runs 25 (each kept under --log)
  BBX-14 (more than one run): met — 19 gates, 0 verdict differences against the run started 2026-09-10T06:29:53Z at the same HEAD
  last re-baseline: (none recorded)
what this green does NOT assert (declared by each gate's header):
  classify: that a verdict word printed by a gate outside the runner is read at all: the classifier reads exit status first, then the log; a PASS printed after a non-zero exit is FAIL by design
  config: the meaning of a consumer's keys: only that the layers resolve (consumer over kind profile over kind-blind default) and that dumps are stable
  tier: that a gate reaching an instrument through a path the source regex does not match is seen: the depth and the regex are the limit
  static_runner: the sweep runner or any gate that needs an instrument: this is the pre-commit chain only
  static_runner: runtimes as anything but this host under this load
  controls: that a control is RIGHT — only that a declared control fired and an undeclared one is red (docs/controls.md)
  sweep_runner: speed-up on a real consumer: the queue is measured on stub gates with sleeps, not on an instrument-tier suite
  sweep_runner: a gate that escapes its clone by an absolute path: clone-per-slot pins the cwd, nothing more
  sweep_runner: portability beyond macOS: mkfifo and exec 8<> are POSIX, not yet run on Linux or WSL
  fingerprint: the identity of any artifact that is not a single file: the kind-blind fingerprint is file-sha1 (D16)
  rulings_shape: the prose of a ruling: only its heading, its answer line and its DECISIONS row are read
  readout: that a declared blind spot is true or complete: the screen prints what the header says
  readout: the sweep runner's runs: only bbx-run-static --log and bbx-run-suite --log are read
  readout: that a register row's class is true of its file: the suite screen prints what the register says (gates/provenance.sh keeps it complete and inside the vocabulary)
  close_sweeps: that every corrected claim has a register row: the register is written by hand at the correction (a claim nobody registered is not swept)
  close_sweeps: step 9 of the close (the lineage untouched): that is the recount's clone and the fidelity gate's proof, not this gate
  temporal: anything about a real log: every shape here is synthesized (fixture class); the classes' fitness for a consumer is that consumer's ratification
  temporal: the dispatcher's spec line and the suite's dispatch (S2 steps 2 and 3): the comparators are called directly
  temporal: that the thresholds are right for any subject: they are the frame-driven profile's (D23) and a consumer's override needs R25's ruling row (gates/thresholds.sh)
  thresholds: that a ruling id in [thresholds].rulings names a ruling that exists: any non-empty string is accepted here; the consumer's rulings-shape gate is where an id is checked
  thresholds: the values' fitness for any subject: 2 / 60 / 8 are bbh's ratified policy carried in the frame-driven profile (D23), not a measurement of anything here
  compare_dispatch: the suite's dispatch around this library (which file is read, how many runs, the .sha1 and .diverge kinds' own paths): S2 step 3
  compare_dispatch: anything about a real log or a real mask: every log is synthesized and the masks are strings the guard compares, never applied
  expectation_kinds: the content of any expectation file: only its extension and its stem are read here
  expectation_kinds: the suite's use of the dispositions (S2 step 3)
  provenance: that a row's class is TRUE of its file: the register is written by hand at the freeze; only completeness and the vocabulary are checked
  provenance: bbh's example register (markdown, a consumer list of classes): it is bbh's and F19 (S6) reads it under R11
  census_recount: the truth of the not-recountable rows at their commits: they are named and counted, never run
  census_recount: that a hand-read citation says what its row claims: only that the line exists
  fidelity_bbh: anything about a suite, a comparator or an expectation: slice S2
  fidelity_bbh: bbh's example's correctness (G11 is bbh's to fix)
  fidelity_bbh: F15 unless BBX_FIDELITY_F15=1 was set for the run
  fidelity_bbh_s2: the kept suite run (--log) — bbh has none, so F12 diffs printed text only; the kept run's ground truth is gates/suite.sh
  fidelity_bbh_s2: anything about a real subject: F17's and F16b's inputs are synthesized, F16a's are the fake machine's (fixture class); the shapes are the lineage's paid-for cases and nothing else
  fidelity_bbh_s2: bbh's correctness: identical output on both sides is fidelity, not truth
  suite: any driver but the fake: a MAME or FBNeo driver is bbh's and untested here (bbh's own F8 rows)
  suite: the .sha1 kind's evidence: it is `self` class by construction (E4) and the register that says so is S2 step 4
  suite: the readout's reading of a kept suite run: the screen reads bbx-run-static runs only until step 4
  gates declaring no blind spot: 0   (a gate nobody has asked what its green leaves out)
```

## What changed (step 4, on R24)

`lib/py/bbx/provenance.py`: the register is `PROVENANCE.toml` inside the expectation tree — one bare table per row, the file a VALUE, every field named — complete both ways over every regular file under the tree (the excluded basenames and the register itself aside), every `class` EQUAL to one of R11's eight, no file named twice, a missing field refused by name, a duplicate table refused by the subset parser; `testimony` and `fixture` rows counted and NAMED as not evidence; `--histogram <set>`. `gates/provenance.sh` holds it with four controls. `lib/py/bbx/readout.py`: a kept SUITE run gets its own screen — the findings counted apart (BBX-4), the expectations relied upon as the register's histogram for the set, the comparator classes that PASSed on a pairing whose row is not `fixture` or `testimony`, BBX-14 against a second run; and the static screen's tree line shows the kept run's untracked entry count (G17's mechanism, `bin/bbx-run-static --log` recording it, printing nothing). `gates/readout.sh` gained three controls. D31, D32. bbh moved to `10a82d2` at the open (G11 fixed there; the four dirty files committed): both fidelity gates PASS against it by the D20 override; R28 raised to re-baseline.

## A kept suite run of bbh's example, read once (not gated: bbh's example carries no register, and BBX never writes one into it)

`bin/bbx run-suite --config bbh.toml --log …` on a copy of the example at `f675710`, driver bbh's fake through the frame-driven driver home; then `bin/bbx readout` on the kept run:

```
== READOUT (suite) — frame-driven subject at <a copy of bbh's example> @ - (porcelain 0) — set 'fake' -> expectation set 'build-a' — started 2026-09-10T06:29:46Z on Darwin arm64 ==
VERDICT: GREEN   PASS 5  SKIP 1  FAIL 0  OTHER 0   (scenarios 6; each run 2 times; driver fake.sh)
findings: pass 5, skip 1   (short = the observation ended before the comparison finished; diverged = the subject differed: BBX-4)
rests on:
  expectations relied upon: none registered — no expected/PROVENANCE.toml; the 7 expectation files of set 'build-a' carry no provenance class (E3)
  comparator classes in this run: composite, diverge, exact, flicker, window; PASSed on a real pairing: unknown — no register says which pairings are real
  BBX-14 (more than one run): each scenario ran 2 times inside this run (nondeterminism is a finding above); pass --against a second kept run for run-to-run
what this green does NOT assert:
  the correctness of any expectation beyond its class: a `self` expectation sees currency, never a regression against a reference (E4)
  anything about a SKIP scenario: it asserts nothing
  where any expectation's numbers came from: no register (E3)
```

This is the sentence S2 exists to put on a screen, in its honest first form: five classes ran and passed, and nothing yet says any of them passed on evidence about a real subject.

# S2 — the slice readout (CLAUDE.md §7; `docs/slices.md`'s row for S2)

1. **Gates green, controls fired on purpose.** The battery: 19 gates, 55 / 55 controls declared / fired (S2's 25: temporal 4, thresholds 3, compare_dispatch 3, expectation_kinds 3, suite 5, fidelity_bbh_s2 3, provenance 4; readout's 4 new of 7), twice at one HEAD, BBX-14 met (the screen above). Every control's firing is in its gate's log; every gate declares its blind spots.
2. **Fidelity exact.** F12 (17 pairs), F16 (35), F17 (52): the suite runner, the dispatcher with its guards and enumeration, the five temporal checkers and the proposer, over bbh's example on the fake driver and the lineage's synthetic shapes — every diff empty on a plain clone of `f675710` (`gates/fidelity_bbh_s2.sh`), and, measured once at the open, on a clone of `10a82d2`. Three verdict-text controls (one changed string each) make the diffs non-empty. The series continued F12, F16, F17; F13–F15 unchanged.
3. **Every frozen expectation's provenance class; every default in the register.** BBX freezes no expectation of its own in S2 (its `self` kind has no scenarios), so there is nothing to class — and the register tool, its gate and the screen are what would say so for a consumer that does. Defaults D23–D32, each with its class: reference-calibrated (D23 the thresholds, D26 and D27's frame-driven values), principled (D24, D25, D28–D32, the kind-blind values).
4. **What the green does not assert.** On the screen, per gate; and for the slice: no expectation's provenance is stated anywhere yet (no consumer of BBX has a register; bbh's example has bbh's page, F19 in S6); every PASS of every class is on a fixture-class pairing; one driver (bbh's fake), one consumer (bbh's example); the mapped-state comparator not lifted (BBX-25); Darwin arm64 only (R21); a ruling id in `[thresholds].rulings` is any non-empty string.
5. **Rules re-anchored or still inherited.** Still `[inherited]` in CLAUDE.md (S5 promotes); re-anchored in fact this slice: BBX-4 (the finding column, G18's neighbour), BBX-13 (R25's refusal), BBX-24 and BBX-26 (G17), BBX-1 (G18), BBX-17 (a duplicate row refused), BBX-3 (testimony never green), BBX-25 (`compare_fields.py` left in bbh).
6. **STATE, HANDOFF, gotchas, rulings, registries.** Rewritten at this close; G17, G18 filed with prices; R23–R27 answered, R28 raised; the registries complete both ways (the runner's anti-orphan line in every battery).

**What this slice measured that the plan predicted differently:** the fidelity rows live in one gate, not per row; `compare_fields.py` stays in bbh; the register is TOML, not TSV (R24); step 4 cost part of one sitting, not one (the plan's estimate for S2 was 2 sessions: it took bbx-3 and the first half of bbx-4).



# CLOSE — bbx-4 (2026-09-10)

**Close measurements.** Green first, twice, kept (`build/selftest_20260910T082952`, `build/selftest_20260910T083656`): `PASS 19 SKIP 0 FAIL 0 MISSING 0`; `controls fired 55 / declared 55; gates with no declaration: 0; red: 0`; `ok: no tracked file changed during the run` in both, untracked entries 2 -> 2; BBX-14 met (0 verdict differences at one HEAD). **Sweeps:** `close_sweeps` GREEN; `rulings_shape` GREEN (29 entries, 27 answered, 2 open — R21, R28). **Lineage untouched, by construction and by proof:** bbh recorded=f675710 tip=10a82d2 ahead=1 porcelain=0 (clones only; both fidelity gates PASS on the tip by the D20 override — R28); vampiresaved recorded=0cdd9726 tip=195cd6ea ahead=10 (35 rows moved, not re-measured by design); sms recorded=ecc5481 tip=ecc5481 ahead=0.

**Retractions this sitting:** none. **Incidents reviewed for learnings (R27, step 10):** no incident reached a gate this sitting; one defect of the suite screen (the real pairings counted by kind, not by file) was caught by the shadow test before the code entered the tree — the learning is the practice itself, drafting and testing outside the tree while a battery runs, now the handoff's rule; no mechanism needed.

**What this close does NOT assert:** any expectation's provenance for any real consumer (bbh's example has no BBX register and never will from here); any class PASSed on a pairing that is not fixture-class; any driver but bbh's fake; any platform but Darwin arm64 (R21); the mapped-state comparator (BBX-25); that a ruling id in `[thresholds].rulings` names a ruling; the re-baseline (R28: recommended, not done).

# bbx-5 — the re-baseline to `10a82d2` as a procedure; R29 raised, R30 built (2026-09-10)

## The screen, verbatim (`bin/bbx readout build/selftest_20260910T092412 --against build/selftest_20260910T091729`)

```
== READOUT — self subject at /Users/koneko/Developer/generalized-blackbox-harness/BBX @ f303454 (porcelain 14) — started 2026-09-10T07:24:13Z on Darwin arm64 ==
VERDICT: GREEN   PASS 19  SKIP 0  FAIL 0  TIMEOUT 0  MISSING 0   (gates 19)
tree during the run: unchanged (untracked entries 0 -> 0)   harness: bbx @ f303454
rests on:
  controls: fired 56 / declared 56; dead 0; undeclared firings 0; gates red 0
  each can fail: 19 of 19 gates proved a control fires on purpose
  expectations relied upon: none registered — the expectation register with its provenance classes is slice S2; until then no comparison against a frozen expectation is claimed
  coverage: census_recount: rows=300 recountable=244 not_recountable=56 (BBX-18: the census claims no command re-derives)
  note: census_recount: drift census=bbh.md recorded=10a82d2 tip=447e5d26bc23 ahead=1 rows_moved=1 ids=A2
  note: census_recount: drift census=vampiresaved.md recorded=0cdd9726 tip=47a78a6a0028 ahead=13 rows_moved=35 ids=A21,A22,A23,A24,A25,A26,A27,A28,A32,A33,A35,A37,A45,A51,A59,A62,A63,A66,A67,A68,A77,A80,A84,A85,A86,A87,A88,A89,A90,A91,A93,A95,A120,A121,A127
  note: fidelity_bbh: bbh-drift baseline=10a82d2 tip=447e5d2 ahead=1
  note: fidelity_bbh: bbh-source tip=447e5d2 porcelain=0 untouched-by-construction=clone
  note: fidelity_bbh_s2: bbh-drift baseline=10a82d2 tip=447e5d2 ahead=1
  note: fidelity_bbh_s2: bbh-source tip=447e5d2 porcelain=0 untouched-by-construction=clone
  note: suite: suite-runs 25 (each kept under --log)
  BBX-14 (more than one run): met — 19 gates, 0 verdict differences against the run started 2026-09-10T07:17:29Z at the same HEAD
  last re-baseline: 2026-09-10 (bbx-5, R28): bbh `f675710` → `10a82d2` — bbh's commit "config: the M18 re-point that sat dirty since 14z-144; the example lib's root one level too high; the extraction counts dated" (7 files; G11 fixed there; the four dirty files of R8 committed). Measured before the move by the D20 override: F12, F13, F14, F14f, F16, F17 identical on a clone of `10a82d2`. Moved with it: D20; D12's five `[sweep].placeholders` (by definition); the census `docs/census/bbh.md` (A2 19 → 20). No verdict text changed on either side.
what this green does NOT assert (declared by each gate's header):
  classify: that a verdict word printed by a gate outside the runner is read at all: the classifier reads exit status first, then the log; a PASS printed after a non-zero exit is FAIL by design
  config: the meaning of a consumer's keys: only that the layers resolve (consumer over kind profile over kind-blind default) and that dumps are stable
  tier: that a gate reaching an instrument through a path the source regex does not match is seen: the depth and the regex are the limit
  static_runner: the sweep runner or any gate that needs an instrument: this is the pre-commit chain only
  static_runner: runtimes as anything but this host under this load
  controls: that a control is RIGHT — only that a declared control fired and an undeclared one is red (docs/controls.md)
  sweep_runner: speed-up on a real consumer: the queue is measured on stub gates with sleeps, not on an instrument-tier suite
  sweep_runner: a gate that escapes its clone by an absolute path: clone-per-slot pins the cwd, nothing more
  sweep_runner: portability beyond macOS: mkfifo and exec 8<> are POSIX, not yet run on Linux or WSL
  fingerprint: the identity of any artifact that is not a single file: the kind-blind fingerprint is file-sha1 (D16)
  rulings_shape: the prose of a ruling: only its heading, its answer line and its DECISIONS row are read
  readout: that a declared blind spot is true or complete: the screen prints what the header says
  readout: the sweep runner's runs: only bbx-run-static --log and bbx-run-suite --log are read
  readout: that a register row's class is true of its file: the suite screen prints what the register says (gates/provenance.sh keeps it complete and inside the vocabulary)
  close_sweeps: that every corrected claim has a register row: the register is written by hand at the correction (a claim nobody registered is not swept)
  close_sweeps: step 9 of the close (the lineage untouched): that is the recount's clone and the fidelity gate's proof, not this gate
  temporal: anything about a real log: every shape here is synthesized (fixture class); the classes' fitness for a consumer is that consumer's ratification
  temporal: the dispatcher's spec line and the suite's dispatch (S2 steps 2 and 3): the comparators are called directly
  temporal: that the thresholds are right for any subject: they are the frame-driven profile's (D23) and a consumer's override needs R25's ruling row (gates/thresholds.sh)
  thresholds: that a ruling id in [thresholds].rulings names a ruling that exists: any non-empty string is accepted here; the consumer's rulings-shape gate is where an id is checked
  thresholds: the values' fitness for any subject: 2 / 60 / 8 are bbh's ratified policy carried in the frame-driven profile (D23), not a measurement of anything here
  compare_dispatch: the suite's dispatch around this library (which file is read, how many runs, the .sha1 and .diverge kinds' own paths): S2 step 3
  compare_dispatch: anything about a real log or a real mask: every log is synthesized and the masks are strings the guard compares, never applied
  expectation_kinds: the content of any expectation file: only its extension and its stem are read here
  expectation_kinds: the suite's use of the dispositions (S2 step 3)
  provenance: that a row's class is TRUE of its file: the register is written by hand at the freeze; only completeness and the vocabulary are checked
  provenance: bbh's example register (markdown, a consumer list of classes): it is bbh's and F19 (S6) reads it under R11
  census_recount: the truth of the not-recountable rows at their commits: they are named and counted, never run
  census_recount: that a hand-read citation says what its row claims: only that the line exists
  fidelity_bbh: anything about a suite, a comparator or an expectation: slice S2
  fidelity_bbh: bbh's example's correctness (G11 is bbh's to fix)
  fidelity_bbh: F15 unless BBX_FIDELITY_F15=1 was set for the run
  fidelity_bbh_s2: the kept suite run (--log) — bbh has none, so F12 diffs printed text only; the kept run's ground truth is gates/suite.sh
  fidelity_bbh_s2: anything about a real subject: F17's and F16b's inputs are synthesized, F16a's are the fake machine's (fixture class); the shapes are the lineage's paid-for cases and nothing else
  fidelity_bbh_s2: bbh's correctness: identical output on both sides is fidelity, not truth
  suite: any driver but the fake: a MAME or FBNeo driver is bbh's and untested here (bbh's own F8 rows)
  suite: the .sha1 kind's evidence: it is `self` class by construction (E4) and the register that says so is S2 step 4
  suite: the readout's reading of a kept suite run: the screen reads bbx-run-static runs only until step 4
  gates declaring no blind spot: 0   (a gate nobody has asked what its green leaves out)
```

## What changed

**The re-baseline (R28, reclassified by the maintainer: a procedure under R8/R20, not a ruling).** Measured first by the D20 override: F12, F13, F14, F14f, F16, F17 identical on a clone of `10a82d2`. Then moved in one commit: both fidelity gates' default (`BBX_BBH_BASELINE:-10a82d2`); D20; D12's five `[sweep].placeholders` in the frame-driven profile (by the row's definition: bbh's defaults verbatim at the baseline); the dated line in `docs/rebaselines.md` (printed at the head of every fidelity run); the census `docs/census/bbh.md` re-measured (A2 19 → 20, every other row and citation unchanged; the recount at the new HEAD: 79 rows, 78 match, 1 not recountable); HANDOFF's dirty-tree hazard retired (bbh is clean); G11 closed as fixed in bbh. bbh moved once more during the sitting (`447e5d2`: its README names the renamed remote, BBH-frame-based): a NOTE on the screen, nothing BBX measures.

**Two findings raised from bbh's work on its own must-fire contract, the maintainer's.** R30, ruled and built: the gate header is the LEADING COMMENT BLOCK — every `#` line after the shebang up to the first non-comment line; a bare `#` no longer ends it (bbh measured 264 of its 315 gates with a bare separator within five lines, colliding with its gate index). BBX had two readers of that block; now one (`controls.py` `header_lines`, imported by `readout.py`); `gates/controls.sh` proves a declaration after a bare `#` is read and one after the first code line is not (the control `body-is-not-header`); over the 19 gates the old and the new reader agree: 55 declarations. R29, raised and open: executable controls — the runner invokes a gate with `CONTROL=<name>`, the gate perturbs its REAL input and runs to its verdict, and the runner requires the gate's own FAIL, because a self-reported FIRED can be printed by a control that tests nothing (bbh's 14z-144 case). Recommended for slice S6 with the cadence left to the tier.

## Counts, separately

| | |
|---|---|
| gates / controls | 19 / 56 (controls.sh gained `body-is-not-header`) |
| fidelity on the new baseline | F13a–e, F14, F14f (`fidelity_bbh`), F12 17 + F16 35 + F17 52 (`fidelity_bbh_s2`): every diff empty on the plain clone at `10a82d2` |
| the census at `10a82d2` | bbh.md 79 rows: 78 match, 0 mismatch, 1 not recountable; A2 moved 19 → 20 |
| kept battery runs this sitting | 4: the open GREEN 19/55 (bbh at `10a82d2`, a NOTE); after the re-baseline GREEN 19/55; the two close runs above |
| rulings | R28 answered and reclassified; R30 answered and built; R29 open; R21 open |
| bbh's tip at the close | `447e5d2`, 1 past the baseline (README only); porcelain 0; remote renamed BBH-frame-based (the old URL redirects) |

## What it rests on

The two fidelity gates on a plain clone of the new baseline, clean after; the recount on a clone of the new census HEAD; the controls gate's two new cases and its control; every other gate unchanged and green twice.

## What this green does NOT assert

- Everything the screen lists; plus: R29 is a contract on the page's future, not a mechanism — no control here is yet driven by the runner on the gate's real path; every FIRED is still the gate's self-report.
- bbh at `447e5d2` is measured by nothing but a NOTE; the procedure follows it when a sitting needs to (R28).
- Platform: Darwin arm64 only (R21).

# CLOSE — bbx-5 (2026-09-10)

**Close measurements.** Green first, twice, kept (`build/selftest_20260910T091729`, `build/selftest_20260910T092412`): `PASS 19 SKIP 0 FAIL 0 MISSING 0`; `controls fired 56 / declared 56; gates with no declaration: 0; red: 0`; `ok: no tracked file changed during the run` in both; BBX-14 met (0 verdict differences at one HEAD). **Sweeps:** `close_sweeps` GREEN; `rulings_shape` GREEN (31 entries, 29 answered, 2 open — R21, R29; it caught its author once more: "R10 work" in the open line read as an open id, reworded). **Lineage untouched, by construction and by proof:** bbh baseline=10a82d2 tip=447e5d2 ahead=1 porcelain=0 (clones only, clean after); vampiresaved recorded=0cdd9726 (past the census, not re-measured by design); sms recorded=ecc5481 tip=ecc5481 ahead=0.

**Retractions this sitting:** none (R28's entry keeps its first recommendation as history; its answer reclassifies it). **Incidents reviewed for learnings (R27, step 10):** none reached a gate; the shape gate's catch was a wording, fixed in the same minute; no mechanism.

**What this close does NOT assert:** anything R29 would prove (no executable control exists); bbh's tip beyond the baseline; any platform but Darwin arm64; and everything S2's slice readout listed.


# bbx-6 — S3 planned and STOPPED; G19, the generated sentence that outlived its slice (2026-09-10)

## The screen, verbatim (`bin/bbx readout build/selftest_20260910T082723 --against build/selftest_20260910T082039`)

```
== READOUT — self subject at /Users/koneko/Developer/generalized-blackbox-harness/BBX @ 415c88b (porcelain 7) — started 2026-09-10T08:27:23Z on Darwin arm64 ==
VERDICT: GREEN   PASS 19  SKIP 0  FAIL 0  TIMEOUT 0  MISSING 0   (gates 19)
tree during the run: unchanged (untracked entries 1 -> 1)   harness: bbx @ 415c88b
rests on:
  controls: fired 56 / declared 56; dead 0; undeclared firings 0; gates red 0
  each can fail: 19 of 19 gates proved a control fires on purpose
  expectations relied upon: none registered — a static run compares against no frozen expectation; a kept suite run (bbx-run-suite --log) carries its register's histogram (D32)
  coverage: census_recount: rows=300 recountable=244 not_recountable=56 (BBX-18: the census claims no command re-derives)
  note: census_recount: drift census=bbh.md recorded=10a82d2 tip=447e5d26bc23 ahead=1 rows_moved=1 ids=A2
  note: census_recount: drift census=vampiresaved.md recorded=0cdd9726 tip=dbf183aa13e7 ahead=15 rows_moved=36 ids=A21,A22,A23,A24,A25,A26,A27,A28,A32,A33,A35,A37,A45,A51,A59,A62,A63,A66,A67,A68,A77,A80,A84,A85,A86,A87,A88,A89,A90,A91,A93,A95,A111,A120,A121,A127
  note: fidelity_bbh: bbh-drift baseline=10a82d2 tip=447e5d2 ahead=1
  note: fidelity_bbh: bbh-source tip=447e5d2 porcelain=0 untouched-by-construction=clone
  note: fidelity_bbh_s2: bbh-drift baseline=10a82d2 tip=447e5d2 ahead=1
  note: fidelity_bbh_s2: bbh-source tip=447e5d2 porcelain=0 untouched-by-construction=clone
  note: suite: suite-runs 25 (each kept under --log)
  BBX-14 (more than one run): met — 19 gates, 0 verdict differences against the run started 2026-09-10T08:20:40Z at the same HEAD
  last re-baseline: 2026-09-10 (bbx-5, R28): bbh `f675710` → `10a82d2` — bbh's commit "config: the M18 re-point that sat dirty since 14z-144; the example lib's root one level too high; the extraction counts dated" (7 files; G11 fixed there; the four dirty files of R8 committed). Measured before the move by the D20 override: F12, F13, F14, F14f, F16, F17 identical on a clone of `10a82d2`. Moved with it: D20; D12's five `[sweep].placeholders` (by definition); the census `docs/census/bbh.md` (A2 19 → 20). No verdict text changed on either side.
what this green does NOT assert (declared by each gate's header):
  classify: that a verdict word printed by a gate outside the runner is read at all: the classifier reads exit status first, then the log; a PASS printed after a non-zero exit is FAIL by design
  config: the meaning of a consumer's keys: only that the layers resolve (consumer over kind profile over kind-blind default) and that dumps are stable
  tier: that a gate reaching an instrument through a path the source regex does not match is seen: the depth and the regex are the limit
  static_runner: the sweep runner or any gate that needs an instrument: this is the pre-commit chain only
  static_runner: runtimes as anything but this host under this load
  controls: that a control is RIGHT — only that a declared control fired and an undeclared one is red (docs/controls.md)
  sweep_runner: speed-up on a real consumer: the queue is measured on stub gates with sleeps, not on an instrument-tier suite
  sweep_runner: a gate that escapes its clone by an absolute path: clone-per-slot pins the cwd, nothing more
  sweep_runner: portability beyond macOS: mkfifo and exec 8<> are POSIX, not yet run on Linux or WSL
  fingerprint: the identity of any artifact that is not a single file: the kind-blind fingerprint is file-sha1 (D16)
  rulings_shape: the prose of a ruling: only its heading, its answer line and its DECISIONS row are read
  readout: that a declared blind spot is true or complete: the screen prints what the header says
  readout: the sweep runner's runs: only bbx-run-static --log and bbx-run-suite --log are read
  readout: that a register row's class is true of its file: the suite screen prints what the register says (gates/provenance.sh keeps it complete and inside the vocabulary)
  close_sweeps: that every corrected claim has a register row: the register is written by hand at the correction (a claim nobody registered is not swept)
  close_sweeps: step 9 of the close (the lineage untouched): that is the recount's clone and the fidelity gate's proof, not this gate
  temporal: anything about a real log: every shape here is synthesized (fixture class); the classes' fitness for a consumer is that consumer's ratification
  temporal: the dispatcher's spec line and the suite's dispatch (S2 steps 2 and 3): the comparators are called directly
  temporal: that the thresholds are right for any subject: they are the frame-driven profile's (D23) and a consumer's override needs R25's ruling row (gates/thresholds.sh)
  thresholds: that a ruling id in [thresholds].rulings names a ruling that exists: any non-empty string is accepted here; the consumer's rulings-shape gate is where an id is checked
  thresholds: the values' fitness for any subject: 2 / 60 / 8 are bbh's ratified policy carried in the frame-driven profile (D23), not a measurement of anything here
  compare_dispatch: the suite's dispatch around this library (which file is read, how many runs, the .sha1 and .diverge kinds' own paths): S2 step 3
  compare_dispatch: anything about a real log or a real mask: every log is synthesized and the masks are strings the guard compares, never applied
  expectation_kinds: the content of any expectation file: only its extension and its stem are read here
  expectation_kinds: the suite's use of the dispositions (S2 step 3)
  provenance: that a row's class is TRUE of its file: the register is written by hand at the freeze; only completeness and the vocabulary are checked
  provenance: bbh's example register (markdown, a consumer list of classes): it is bbh's and F19 (S6) reads it under R11
  census_recount: the truth of the not-recountable rows at their commits: they are named and counted, never run
  census_recount: that a hand-read citation says what its row claims: only that the line exists
  fidelity_bbh: anything about a suite, a comparator or an expectation: slice S2
  fidelity_bbh: bbh's example's correctness (G11 is bbh's to fix)
  fidelity_bbh: F15 unless BBX_FIDELITY_F15=1 was set for the run
  fidelity_bbh_s2: the kept suite run (--log) — bbh has none, so F12 diffs printed text only; the kept run's ground truth is gates/suite.sh
  fidelity_bbh_s2: anything about a real subject: F17's and F16b's inputs are synthesized, F16a's are the fake machine's (fixture class); the shapes are the lineage's paid-for cases and nothing else
  fidelity_bbh_s2: bbh's correctness: identical output on both sides is fidelity, not truth
  suite: any driver but the fake: a MAME or FBNeo driver is bbh's and untested here (bbh's own F8 rows)
  suite: the .sha1 kind's evidence: it is `self` class by construction (E4) and the register that says so is S2 step 4
  suite: the readout's reading of a kept suite run: the screen reads bbx-run-static runs only until step 4
  gates declaring no blind spot: 0   (a gate nobody has asked what its green leaves out)
```

## What changed

**The open.** GREEN on `415c88b` (19 gates, 56 / 56 controls, tree unchanged): the bbx-5 close re-derived, the two expected drift NOTEs present (VampireSaved 14 past the census, bbh one past the baseline). Read off that screen: the static run's sentence `expectations relied upon: none registered — the expectation register with its provenance classes is slice S2; until then no comparison against a frozen expectation is claimed` — true at bbx-2, false since bbx-4, printed on the bbx-4 and bbx-5 close screens in this file, unnoticed, because `gates/readout.sh` held only the prefix `none registered`. Fixed: the generated sentence now says what is true by construction (a static run compares against no frozen expectation; a kept suite run carries its register's histogram, D32) and the gate holds the full clause; the module docstring's twin sentence fixed with it. G19, paid 0, the first re-anchor of BBX-10 (rot class 4, a stale reference — inside a GENERATED artifact, the one place a reader trusts most). Its learning (R27) is a mechanism, filed for S6: a rot gate over generated and printed text that refuses "until slice N" sentences outside a history ledger.

**S3 planned, STOPPED.** `docs/plans/S3.md`, the first plan for a kind with no ancestor to diff against. Measured on plain clones at the census commits (never a working tree): SMS `tools/checkdocs.py` at `ecc5481` — 1,500 lines, 37 hand checks, 17 table validators each re-run at a wrong base, 5 sentence forms bound by `docaddrs.py` (557 lines), 55 `says(` quotations, 4 control families; census rows A35–A41 re-derived by `bin/bbx recount docs/census/sms.md --only A35,…,A41` on the clone, 7 of 7 match. VampireSaved `tools/checkdocs_rom.py` at `0cdd9726` — 815 lines, 15 checks, 14 PARAPHRASE mentions, its gate 205 lines with 5 must-fire controls, its covered set 31 rows compared as a multiset (measured once by the commands in the plan; not census rows). bbh's `example/` as the fixture consumer's model (fakesys 314 lines, 6 replays, 4 registry rows). BBX's own plug-in points at `415c88b`: the suite's `*.rpl` literal and one-kind-per-scenario precedence, `compare.sh`'s single family branch, `logfmt.frames()` reading an integer point, `dir_sha1` non-recursive, D28's driver home "absent until S3". The design, contract by contract (§3); the fixture (§4: designed counts, measured when built); fourteen controls in five gates (§5); the readout's coverage line from a suite run (§6); the defaults to register (§7); five steps (§8); what the plan does not decide (§9). Four rulings raised, R31–R34, each with its measured context, recommendation, declined alternatives and cost of being wrong.

**Caught by the document gates on the way, as designed:** `rulings_shape` — DECISIONS' open line still listed R21 alone (updated to R21, R31–R34); `close_sweeps` — G19's first wording named the three deferral tokens in prose (reworded). Both gates green to a file with their exits tested (G16's lesson).

## Counts, separately

| | |
|---|---|
| gates / controls | 19 / 56/56 (unchanged: no gate added; the readout gate's 7 controls fire on the corrected sentence) |
| fidelity | F12 17 + F16 35 + F17 52, F13a–e, F14, F14f: every diff empty on the plain clone at `10a82d2` (both close runs) |
| the S3 plan's measurements | SMS: 7 census rows re-derived on the clone (7 match); 4 counts measured by the commands in the plan. VampireSaved: 6 counts measured by the commands in the plan (not census rows). bbh: 3 |
| rulings | 35 entries: 30 answered, 5 open (R21, R31–R34) |
| kept battery runs this sitting | 3: the open GREEN 19/56 on `415c88b`; the two close runs above |
| S3 tools written | 0 |

## What it rests on

The three kept runs; the recount on a plain clone of SMS at `ecc5481`; the two ancestors read through `git show` at their census commits; the readout gate's seven controls on the corrected sentence; the two document gates green after the queue and the ledger changed.

## What this green does NOT assert

- Everything the screen lists; plus: anything about the document-set kind — no fixture, no driver, no comparator of the `set` or `schema` family exists; every count in `docs/plans/S3.md` §4 is a DESIGN number until the fixture is built and measured.
- That R31–R34 are right: they are recommendations with their alternatives declined; the maintainer rules.
- The VampireSaved measurements in the plan (15 checks, 14 mentions, 205 lines, 5 controls, 31 rows) are measured once by the commands beside them, not gated and not census rows.
- bbh at `447e5d2` is measured by nothing but a NOTE (R28 when needed); platform: Darwin arm64 only (R21).

# CLOSE — bbx-6 (2026-09-10)

**Close measurements.** Green first, twice, kept (`build/selftest_20260910T082039`, `build/selftest_20260910T082723`): `PASS 19 SKIP 0 FAIL 0 MISSING 0`; `controls fired 56 / declared 56; gates with no declaration: 0; red: 0`; `ok: no tracked file changed during the run` in both; BBX-14 met (0 verdict differences at one HEAD). **Sweeps:** `close_sweeps` GREEN (deferrals 0, retraction hits 0, every `D<n>` cited has a row); `rulings_shape` GREEN (35 entries, 30 answered, 5 open — R21, R31–R34). **Lineage untouched, by construction and by proof:** bbh baseline=10a82d2 tip=447e5d2 ahead=1 porcelain=0 (clones only, clean after); vampiresaved recorded=0cdd9726 tip=dbf183a (past the census, not re-measured by design); sms recorded=ecc5481 tip=ecc5481 ahead=0 (the plan's recount ran on its clone).

**Retractions this sitting:** none (G19 corrects a generated sentence, not a claim of this file's; the old wording stays quoted in the gotcha and in the bbx-4/bbx-5 screens above as history). **Incidents reviewed for learnings (R27, step 10):** G19 → a mechanism for S6 (a rot gate over generated text), written in the entry; the two document-gate catches → no learning beyond "the gates work" (both fired on the author, as they did at bbx-5).

**What this close does NOT assert:** anything about the document-set kind (planned, not built); that the four rulings will be answered as recommended; bbh's tip beyond the baseline; any platform but Darwin arm64; and everything S2's slice readout listed.

# bbx-7 step 1 — the document-set profile, the scenario extension, the fixture and its generator (2026-09-10)

**The open.** GREEN on `d9b363b` (19 gates, 56 / 56 controls, tree unchanged, the two drift NOTEs): the bbx-6 close re-derived. R34's TOML revision validated by the maintainer at the open.

## What changed

The `document-set` kind profile in `lib/py/bbx/config.py` (D33): portable, no instrument, the scenario a claim set, the identity file-sha1 over the artifact with the whole-set key over its directory (documents and artifact together), seven expectation kinds (the kind-blind three plus `truth` exact, `claims` set, `covered` set, `schema` schema), no temporal family. `[suite].scenario_ext` (D34, R32): kind-blind `rpl`, document-set `claims`, read by `bin/bbx-run-suite`'s loop and by `lib/sh/expectation_kinds.sh` through a new `bbx.expectations scenario-ext`. The fixture generator `fixture/docset/mkdocset.py` (`--check` regenerates under TMPDIR and diffs; ten chirality predicates named, asserted on the design and on the tree's artifact; D35, D36, D37) and what it writes — BBX's second consumer, `fixture/docset/` with its own `bbx.toml`. `gates/docset_fixture.sh`, portable tier.

**Built in a shadow tree first**, as the handoff prescribes; the shadow caught one defect before the tree saw it: the enumeration helper reused `_ee_ext` for each file's own extension and clobbered the scenario extension read a line earlier, so every enumeration matched nothing and returned 0 — the kinds gate's three controls read DEAD and the fixture gate's `unknown-kind-under-profile` refused to fire. One rename; no mechanism (R27: the practice is the learning, as at bbx-4). Then in the shadow: `fidelity_bbh_s2` PASS (F12, F16, F17: 104 pairings identical after the loop change), `suite` PASS (5 controls).

## Counts, separately (measured by `gates/docset_fixture.sh` and the generator's NOTE)

| | |
|---|---|
| the fixture's NOTE | `records=12 documents=3 claims=20 wrong=1 paraphrase=1 unbindable=2 scenarios=3 expectations=9` — the plan's §4 design numbers, now measured |
| files under `subject/`, `claims/`, `expected/` | 18 (+ `bbx.toml`, `mkdocset.py`) |
| kinds under the profile | 7; no temporal family; a `.masked` beside a `.claims` enumerates UNKNOWN-KIND (control) |
| the registry row | the fingerprint tool's own whole-set key over `subject/` resolves to set `fixture` |
| gates / controls in the tree after step 1 | 20 / 59 (the new gate's 3), to be confirmed by the close battery |
| shadow fidelity | 104 pairings identical; suite gate 5 controls |

## What it rests on

The two gates run in the tree to a file with their exits tested (`docset_fixture` 3 controls, `expectation_kinds` 3); the shadow's fidelity and suite gates; the generator's own `--check` on the tree.

## What this green does NOT assert

- Anything about a claim's binding: no driver exists (step 2). The `.truth` kind is not generated yet (it needs the driver's token grammar), so the register has 10 rows for 9 expectations and the registry.
- The suite over the fixture (step 4). The `set` and `schema` comparators (step 3).
- A nested document tree's identity (`dir_sha1` is not recursive, D33).
- That the fixture's forms are anybody's but the fixture's (D35, arbitrary: the first real consumer is the detector).

## The screen, verbatim (`bin/bbx readout build/selftest_20260910T100609 --against build/selftest_20260910T100051`)

```
== READOUT — self subject at /Users/koneko/Developer/generalized-blackbox-harness/BBX @ d9b363b (porcelain 11) — started 2026-09-10T10:06:10Z on Darwin arm64 ==
VERDICT: GREEN   PASS 20  SKIP 0  FAIL 0  TIMEOUT 0  MISSING 0   (gates 20)
tree during the run: unchanged (untracked entries 2 -> 2)   harness: bbx @ d9b363b
rests on:
  controls: fired 59 / declared 59; dead 0; undeclared firings 0; gates red 0
  each can fail: 20 of 20 gates proved a control fires on purpose
  expectations relied upon: none registered — a static run compares against no frozen expectation; a kept suite run (bbx-run-suite --log) carries its register's histogram (D32)
  coverage: census_recount: rows=300 recountable=244 not_recountable=56 (BBX-18: the census claims no command re-derives)
  note: docset_fixture: docset-fixture records=12 documents=3 claims=20 wrong=1 paraphrase=1 unbindable=2 scenarios=3 expectations=9
  note: census_recount: drift census=bbh.md recorded=10a82d2 tip=447e5d26bc23 ahead=1 rows_moved=1 ids=A2
  note: census_recount: drift census=vampiresaved.md recorded=0cdd9726 tip=930396df0519 ahead=18 rows_moved=37 ids=A21,A22,A23,A24,A25,A26,A27,A28,A32,A33,A35,A37,A45,A51,A59,A62,A63,A66,A67,A68,A76,A77,A80,A84,A85,A86,A87,A88,A89,A90,A91,A93,A95,A111,A120,A121,A127
  note: fidelity_bbh: bbh-drift baseline=10a82d2 tip=447e5d2 ahead=1
  note: fidelity_bbh: bbh-source tip=447e5d2 porcelain=0 untouched-by-construction=clone
  note: fidelity_bbh_s2: bbh-drift baseline=10a82d2 tip=447e5d2 ahead=1
  note: fidelity_bbh_s2: bbh-source tip=447e5d2 porcelain=0 untouched-by-construction=clone
  note: suite: suite-runs 25 (each kept under --log)
  BBX-14 (more than one run): met — 20 gates, 0 verdict differences against the run started 2026-09-10T10:00:51Z at the same HEAD
  last re-baseline: 2026-09-10 (bbx-5, R28): bbh `f675710` → `10a82d2` — bbh's commit "config: the M18 re-point that sat dirty since 14z-144; the example lib's root one level too high; the extraction counts dated" (7 files; G11 fixed there; the four dirty files of R8 committed). Measured before the move by the D20 override: F12, F13, F14, F14f, F16, F17 identical on a clone of `10a82d2`. Moved with it: D20; D12's five `[sweep].placeholders` (by definition); the census `docs/census/bbh.md` (A2 19 → 20). No verdict text changed on either side.
what this green does NOT assert (declared by each gate's header):
  classify: that a verdict word printed by a gate outside the runner is read at all: the classifier reads exit status first, then the log; a PASS printed after a non-zero exit is FAIL by design
  config: the meaning of a consumer's keys: only that the layers resolve (consumer over kind profile over kind-blind default) and that dumps are stable
  tier: that a gate reaching an instrument through a path the source regex does not match is seen: the depth and the regex are the limit
  static_runner: the sweep runner or any gate that needs an instrument: this is the pre-commit chain only
  static_runner: runtimes as anything but this host under this load
  controls: that a control is RIGHT — only that a declared control fired and an undeclared one is red (docs/controls.md)
  sweep_runner: speed-up on a real consumer: the queue is measured on stub gates with sleeps, not on an instrument-tier suite
  sweep_runner: a gate that escapes its clone by an absolute path: clone-per-slot pins the cwd, nothing more
  sweep_runner: portability beyond macOS: mkfifo and exec 8<> are POSIX, not yet run on Linux or WSL
  fingerprint: the identity of any artifact that is not a single file: the kind-blind fingerprint is file-sha1 (D16)
  rulings_shape: the prose of a ruling: only its heading, its answer line and its DECISIONS row are read
  readout: that a declared blind spot is true or complete: the screen prints what the header says
  readout: the sweep runner's runs: only bbx-run-static --log and bbx-run-suite --log are read
  readout: that a register row's class is true of its file: the suite screen prints what the register says (gates/provenance.sh keeps it complete and inside the vocabulary)
  close_sweeps: that every corrected claim has a register row: the register is written by hand at the correction (a claim nobody registered is not swept)
  close_sweeps: step 9 of the close (the lineage untouched): that is the recount's clone and the fidelity gate's proof, not this gate
  temporal: anything about a real log: every shape here is synthesized (fixture class); the classes' fitness for a consumer is that consumer's ratification
  temporal: the dispatcher's spec line and the suite's dispatch (S2 steps 2 and 3): the comparators are called directly
  temporal: that the thresholds are right for any subject: they are the frame-driven profile's (D23) and a consumer's override needs R25's ruling row (gates/thresholds.sh)
  thresholds: that a ruling id in [thresholds].rulings names a ruling that exists: any non-empty string is accepted here; the consumer's rulings-shape gate is where an id is checked
  thresholds: the values' fitness for any subject: 2 / 60 / 8 are bbh's ratified policy carried in the frame-driven profile (D23), not a measurement of anything here
  compare_dispatch: the suite's dispatch around this library (which file is read, how many runs, the .sha1 and .diverge kinds' own paths): S2 step 3
  compare_dispatch: anything about a real log or a real mask: every log is synthesized and the masks are strings the guard compares, never applied
  expectation_kinds: the content of any expectation file: only its extension and its stem are read here
  expectation_kinds: the suite's use of the dispositions (S2 step 3)
  provenance: that a row's class is TRUE of its file: the register is written by hand at the freeze; only completeness and the vocabulary are checked
  provenance: bbh's example register (markdown, a consumer list of classes): it is bbh's and F19 (S6) reads it under R11
  docset_fixture: that any claim in the fixture is BOUND, MISMATCH or anything else: no driver exists yet (S3 step 2); only that the files are the generator's and the design is chiral
  docset_fixture: the `.truth` kind: not generated until the driver's token grammar exists (step 2)
  docset_fixture: the suite over the fixture: step 4
  census_recount: the truth of the not-recountable rows at their commits: they are named and counted, never run
  census_recount: that a hand-read citation says what its row claims: only that the line exists
  fidelity_bbh: anything about a suite, a comparator or an expectation: slice S2
  fidelity_bbh: bbh's example's correctness (G11 is bbh's to fix)
  fidelity_bbh: F15 unless BBX_FIDELITY_F15=1 was set for the run
  fidelity_bbh_s2: the kept suite run (--log) — bbh has none, so F12 diffs printed text only; the kept run's ground truth is gates/suite.sh
  fidelity_bbh_s2: anything about a real subject: F17's and F16b's inputs are synthesized, F16a's are the fake machine's (fixture class); the shapes are the lineage's paid-for cases and nothing else
  fidelity_bbh_s2: bbh's correctness: identical output on both sides is fidelity, not truth
  suite: any driver but the fake: a MAME or FBNeo driver is bbh's and untested here (bbh's own F8 rows)
  suite: the .sha1 kind's evidence: it is `self` class by construction (E4) and the register that says so is S2 step 4
  suite: the readout's reading of a kept suite run: the screen reads bbx-run-static runs only until step 4
  gates declaring no blind spot: 0   (a gate nobody has asked what its green leaves out)
```

# CLOSE — bbx-7 (2026-09-10)

**Close measurements.** Green first, twice, kept (`build/selftest_20260910T100051`, `build/selftest_20260910T100609`): `PASS 20 SKIP 0 FAIL 0 MISSING 0`; `controls fired 59 / declared 59; gates with no declaration: 0; red: 0`; `ok: no tracked file changed during the run` in both; BBX-14 met (0 verdict differences at one HEAD). **Sweeps:** `close_sweeps` GREEN (deferrals 0, retraction hits 0, defaults rows 37, every `D<n>` cited has a row); `rulings_shape` GREEN (35 entries, 34 answered, 1 open — R21). **Lineage untouched, by construction and by proof:** bbh baseline=10a82d2 tip=447e5d2 ahead=1 porcelain=0 (clones only, clean after); vampiresaved recorded=0cdd9726 tip=930396d (past the census, not re-measured by design); sms recorded=ecc5481 tip=ecc5481 ahead=0.

**Retractions this sitting:** none. **Incidents reviewed for learnings (R27, step 10):** one, caught by the shadow before the tree — the `_ee_ext` clash (the step-1 section above): no mechanism, the practice is the learning, as at bbx-4; the tree's gates never saw it, so no gotcha entry (the incident and its price, one shadow run, are in the readout and the history).

**What this close does NOT assert:** anything about a claim's binding, the `set` / `schema` / `exact` comparators, the driver, the `.truth` kind or the suite over the fixture (steps 2–4); bbh's tip beyond the baseline; any platform but Darwin arm64 (R21); and everything S2's slice readout listed.

# bbx-8 step 2 — the document-set extractor, the driver, the truth kind and the driver gate (2026-09-10)

**The open.** GREEN on `e398abd` (20 gates, 59 / 59 controls, tree unchanged; the two drift NOTEs: bbh one ahead, VampireSaved 19 ahead): the bbx-7 close re-derived. No ruling was pending but R21.

## What changed

`lib/py/bbx/docset.py`: the extractor. It QUOTES a claim from a document (the line, verbatim — the `text` view), DERIVES the fact the line names from the artifact (`<name>.<field>=<value>` — the `artifact` view, keyed by the first column, D39), BINDS the two into one of the five closed statuses (D37: BOUND, MISMATCH, PARAPHRASE against the listed literal, UNBINDABLE with its listed reason, STALE when the record or column is gone or a listed line no longer exists), and writes the point-indexed log the harness already reads: `<index> <status>:<sha1-quoted>:<sha1-derived>` then `END <n>` (R31; `logfmt.py` untouched, F17 still green). The two strings and the token are defined there and nowhere else (D38); `split_token` is the one splitter. A line that matches no declared form and is not listed, but states an absence or carries a digit, is REFUSED naming `<document>:<line>` (D40, BBX-7). Every run begins with a self-test on synthetic lines against a synthetic record: the true line BOUND, the mirrored value MISMATCH (BBX-15), the loose line not found — a form that stopped matching is named before any document is read. `bbx.docset map` prints the index → (document, line, form, status, quoted, derived) table a triage needs; `bbx.docset summary` prints the coverage NOTEs the suite will carry (R33).

`drivers/docset.sh` (bbh's four arguments; `DOCSET_PATH` a `;` search path made absolute component by component; the out file removed before the run; exit 0 only with an `END` line; 3 REFUSED on a form no extractor implements, a view, `DOCSET_VIEW`, `DOCSET_FORMS`, and the whole frame-driven and guard families; 1 DISCARDED on an unreadable artifact, document or claim set, or a failed self-test; `DOCSET_NONDET=1` salts every token with the clock). `drivers/README.md`: the contract cited from bbh, the exit vocabulary, one driver row. `fixture/docset/mkdocset.py` now writes the `truth` kind — `expected/fixture/<s>.truth` (`exact fixture`) and `expected/fixture/logs/<s>.log` — from the DESIGN's statuses with `docset.py`'s strings, never by running the driver; `--check` covers them (the plan's separate `--truth` flag was not built: a truth every generation writes is a truth `--check` verifies). The register gains six rows, all `fixture` (16 rows: 12 expectations, 3 truth logs, the registry). `gates/docset_driver.sh`, portable, 7 controls; `gates/portable.txt` 17 rows.

**Built in a shadow tree first.** The shadow caught one defect before the tree saw it (G20): the `extractor-shadow` control's first run read DEAD because the self-test's own true line, with its regex broken, tripped the number guard and the driver printed a REFUSED line about a synthetic sentence instead of naming the form. The self-test now catches the refusal on its true line. And the gate's first run corrected the plan (G21): deleting the artifact's first row makes the two claims about that record STALE, not every bound claim MISMATCH — a keyed view is not a positional one. The plan was corrected in its own commit (`d230fc7`, BBX-19; retraction X9) before the gate asserted the measured shape.

## Counts, separately (measured by `gates/docset_driver.sh` and the generator's NOTE)

| | |
|---|---|
| the fixture's NOTE | `records=12 documents=3 claims=20 wrong=1 paraphrase=1 unbindable=2 scenarios=3 expectations=12 truth_logs=3` |
| the driver's logs against the design's truth | `01_all` END 23, `02_weights` END 7, `03_rows` END 7 — each byte-identical to `expected/fixture/logs/<s>.log`, twice |
| `01_all`'s summary | `coverage 20/23`, `paraphrase 1`, `unbindable 2`, `stale 0`, `mismatch 1` |
| statuses seen on the tree | BOUND, PARAPHRASE, UNBINDABLE, MISMATCH; STALE only under the edited-document and shifted-artifact controls |
| the controls, each on a copy, the perturbation proven applied | edited-document (index 4 STALE, `4c4`); shifted-artifact (indices 1 and 10 STALE, 21 unchanged); wrong-claim (index 12 the one MISMATCH; the truth rewritten BOUND there differs at line 12); paraphrase (index 3 PARAPHRASE; the literal 37 → 36 turns index 3 MISMATCH only); unlisted-unbindable (2 counted in END 23; the absence row removed → REFUSED `overview.md:9`, the number row removed → REFUSED `overview.md:11`); extractor-shadow (exit 1 naming `of-is`, no log); refused-form-and-family (form `regex`, `MASK_RANGES`, `POKES`, `DOCSET_VIEW` → exit 3, no log; an EMPTY `MASK_RANGES`, what the suite exports for a maskless kind, is not a refusal) |
| the gate's runtime | ~3 s (measured once, not gated) |
| gates / controls in the tree after step 2 | 21 / 66/66 (the new gate's 7), confirmed by the close battery below |

## What it rests on

The seven gates run in the tree to a file with their exits tested (`docset_driver` 7 controls, `docset_fixture` 3, `provenance`, `expectation_kinds` 3, `controls`, `readout`, `close_sweeps` 3); the generator's own `--check` on the tree; the two close batteries below.

## What this green does NOT assert

- The verdict text of the `exact`, `set` and `schema` comparators (step 3): the gate compares the log to the truth with `cmp` and `diff` and names the index itself.
- The suite over the fixture — the identity refusal, the loop over the kinds, `--freeze`, the coverage NOTE on the screen (step 4). Until then `NOTE: coverage 20/23` is printed by a gate, not by a suite run.
- Prose, reasoning and causal claims in a document: only sentences in a declared form and the listed rows are claims. The truth of the artifact itself: a document that agrees with a wrong artifact reads BOUND.
- That D39's lexical classes and D40's guards are anybody's but the fixture's (arbitrary: the first real consumer is the detector).
- A nested document tree or a second artifact per set (`DOCSET_VIEW` is refused).
- Executable controls (R29, S6): every `CONTROL FIRED` here is the gate's self-report.

## The screen, verbatim (`bin/bbx readout build/selftest_20260910T110956Z --against build/selftest_20260910T110237Z`)

```
== READOUT — self subject at /Users/koneko/Developer/generalized-blackbox-harness/BBX @ d230fc7 (porcelain 11) — started 2026-09-10T11:09:57Z on Darwin arm64 ==
VERDICT: GREEN   PASS 21  SKIP 0  FAIL 0  TIMEOUT 0  MISSING 0   (gates 21)
tree during the run: unchanged (untracked entries 6 -> 6)   harness: bbx @ d230fc7
rests on:
  controls: fired 66 / declared 66; dead 0; undeclared firings 0; gates red 0
  each can fail: 21 of 21 gates proved a control fires on purpose
  expectations relied upon: none registered — a static run compares against no frozen expectation; a kept suite run (bbx-run-suite --log) carries its register's histogram (D32)
  coverage: docset_driver: 20/23
  coverage: census_recount: rows=300 recountable=244 not_recountable=56 (BBX-18: the census claims no command re-derives)
  note: docset_fixture: docset-fixture records=12 documents=3 claims=20 wrong=1 paraphrase=1 unbindable=2 scenarios=3 expectations=12 truth_logs=3
  note: docset_driver: paraphrase 1
  note: docset_driver: unbindable 2
  note: docset_driver: stale 0
  note: docset_driver: mismatch 1
  note: census_recount: drift census=bbh.md recorded=10a82d2 tip=447e5d26bc23 ahead=1 rows_moved=1 ids=A2
  note: census_recount: drift census=vampiresaved.md recorded=0cdd9726 tip=3a6d8dc8250f ahead=20 rows_moved=37 ids=A21,A22,A23,A24,A25,A26,A27,A28,A32,A33,A35,A37,A45,A51,A59,A62,A63,A66,A67,A68,A76,A77,A80,A84,A85,A86,A87,A88,A89,A90,A91,A93,A95,A111,A120,A121,A127
  note: fidelity_bbh: bbh-drift baseline=10a82d2 tip=447e5d2 ahead=1
  note: fidelity_bbh: bbh-source tip=447e5d2 porcelain=0 untouched-by-construction=clone
  note: fidelity_bbh_s2: bbh-drift baseline=10a82d2 tip=447e5d2 ahead=1
  note: fidelity_bbh_s2: bbh-source tip=447e5d2 porcelain=0 untouched-by-construction=clone
  note: suite: suite-runs 25 (each kept under --log)
  BBX-14 (more than one run): met — 21 gates, 0 verdict differences against the run started 2026-09-10T11:02:37Z at the same HEAD
  last re-baseline: 2026-09-10 (bbx-5, R28): bbh `f675710` → `10a82d2` — bbh's commit "config: the M18 re-point that sat dirty since 14z-144; the example lib's root one level too high; the extraction counts dated" (7 files; G11 fixed there; the four dirty files of R8 committed). Measured before the move by the D20 override: F12, F13, F14, F14f, F16, F17 identical on a clone of `10a82d2`. Moved with it: D20; D12's five `[sweep].placeholders` (by definition); the census `docs/census/bbh.md` (A2 19 → 20). No verdict text changed on either side.
what this green does NOT assert (declared by each gate's header):
  classify: that a verdict word printed by a gate outside the runner is read at all: the classifier reads exit status first, then the log; a PASS printed after a non-zero exit is FAIL by design
  config: the meaning of a consumer's keys: only that the layers resolve (consumer over kind profile over kind-blind default) and that dumps are stable
  tier: that a gate reaching an instrument through a path the source regex does not match is seen: the depth and the regex are the limit
  static_runner: the sweep runner or any gate that needs an instrument: this is the pre-commit chain only
  static_runner: runtimes as anything but this host under this load
  controls: that a control is RIGHT — only that a declared control fired and an undeclared one is red (docs/controls.md)
  sweep_runner: speed-up on a real consumer: the queue is measured on stub gates with sleeps, not on an instrument-tier suite
  sweep_runner: a gate that escapes its clone by an absolute path: clone-per-slot pins the cwd, nothing more
  sweep_runner: portability beyond macOS: mkfifo and exec 8<> are POSIX, not yet run on Linux or WSL
  fingerprint: the identity of any artifact that is not a single file: the kind-blind fingerprint is file-sha1 (D16)
  rulings_shape: the prose of a ruling: only its heading, its answer line and its DECISIONS row are read
  readout: that a declared blind spot is true or complete: the screen prints what the header says
  readout: the sweep runner's runs: only bbx-run-static --log and bbx-run-suite --log are read
  readout: that a register row's class is true of its file: the suite screen prints what the register says (gates/provenance.sh keeps it complete and inside the vocabulary)
  close_sweeps: that every corrected claim has a register row: the register is written by hand at the correction (a claim nobody registered is not swept)
  close_sweeps: step 9 of the close (the lineage untouched): that is the recount's clone and the fidelity gate's proof, not this gate
  temporal: anything about a real log: every shape here is synthesized (fixture class); the classes' fitness for a consumer is that consumer's ratification
  temporal: the dispatcher's spec line and the suite's dispatch (S2 steps 2 and 3): the comparators are called directly
  temporal: that the thresholds are right for any subject: they are the frame-driven profile's (D23) and a consumer's override needs R25's ruling row (gates/thresholds.sh)
  thresholds: that a ruling id in [thresholds].rulings names a ruling that exists: any non-empty string is accepted here; the consumer's rulings-shape gate is where an id is checked
  thresholds: the values' fitness for any subject: 2 / 60 / 8 are bbh's ratified policy carried in the frame-driven profile (D23), not a measurement of anything here
  compare_dispatch: the suite's dispatch around this library (which file is read, how many runs, the .sha1 and .diverge kinds' own paths): S2 step 3
  compare_dispatch: anything about a real log or a real mask: every log is synthesized and the masks are strings the guard compares, never applied
  expectation_kinds: the content of any expectation file: only its extension and its stem are read here
  expectation_kinds: the suite's use of the dispositions (S2 step 3)
  provenance: that a row's class is TRUE of its file: the register is written by hand at the freeze; only completeness and the vocabulary are checked
  provenance: bbh's example register (markdown, a consumer list of classes): it is bbh's and F19 (S6) reads it under R11
  docset_fixture: that any claim in the fixture is BOUND, MISMATCH or anything else: no driver exists yet (S3 step 2); only that the files are the generator's and the design is chiral
  docset_fixture: the `.truth` kind: not generated until the driver's token grammar exists (step 2)
  docset_fixture: the suite over the fixture: step 4
  docset_driver: the verdict text of the exact, set and schema comparators (S3 step 3): the truth is compared here with cmp and diff
  docset_driver: the suite over the fixture (identity, the kinds loop, the coverage NOTE on the screen): S3 step 4
  docset_driver: prose, reasoning and causal claims in a document: only sentences in a declared form and the listed rows are claims
  docset_driver: the truth of the artifact itself: a document that agrees with a wrong artifact reads BOUND
  docset_driver: a nested document tree or a second artifact per set (DOCSET_VIEW is refused): S4 or a consumer's question
  census_recount: the truth of the not-recountable rows at their commits: they are named and counted, never run
  census_recount: that a hand-read citation says what its row claims: only that the line exists
  fidelity_bbh: anything about a suite, a comparator or an expectation: slice S2
  fidelity_bbh: bbh's example's correctness (G11 is bbh's to fix)
  fidelity_bbh: F15 unless BBX_FIDELITY_F15=1 was set for the run
  fidelity_bbh_s2: the kept suite run (--log) — bbh has none, so F12 diffs printed text only; the kept run's ground truth is gates/suite.sh
  fidelity_bbh_s2: anything about a real subject: F17's and F16b's inputs are synthesized, F16a's are the fake machine's (fixture class); the shapes are the lineage's paid-for cases and nothing else
  fidelity_bbh_s2: bbh's correctness: identical output on both sides is fidelity, not truth
  suite: any driver but the fake: a MAME or FBNeo driver is bbh's and untested here (bbh's own F8 rows)
  suite: the .sha1 kind's evidence: it is `self` class by construction (E4) and the register that says so is S2 step 4
  suite: the readout's reading of a kept suite run: the screen reads bbx-run-static runs only until step 4
  gates declaring no blind spot: 0   (a gate nobody has asked what its green leaves out)
```

# CLOSE — bbx-8 (2026-09-10)

**Close measurements.** Green first, twice, kept (`build/selftest_20260910T110237Z`, `build/selftest_20260910T110956Z`): `PASS 21 SKIP 0 FAIL 0 MISSING 0`; `controls fired 66 / declared 66; gates with no declaration: 0; red: 0`; `ok: no tracked file changed during the run` in both; BBX-14 met (0 verdict differences at one HEAD). **Sweeps:** `close_sweeps` GREEN (files 91, deferrals 0, retraction hits 0 over 9 retractions, defaults rows 40, 213 `D<n>` citations resolved); `rulings_shape` GREEN (35 entries, 34 answered, 1 open — R21). **Lineage untouched, by construction and by proof:** bbh baseline=10a82d2 tip=447e5d2 ahead=1 porcelain=0 (clones only, clean after); vampiresaved recorded=0cdd9726 tip=8eece1ea ahead=19 (past the census, not re-measured by design); sms recorded=ecc5481 tip=ecc5481 ahead=0.

**Retractions this sitting:** X9 (the plan's shifted-artifact prediction; the plan corrected in its own commit `d230fc7`, BBX-19, before the gate asserted the measured shape). **Incidents reviewed for learnings (R27, step 10):** two — G20 (the self-test's blind spot, found by the `extractor-shadow` control in the shadow tree, paid 1 gate run: a trap, the control is the mechanism) and G21 (a positional prediction from a keyed view, paid 0: a mechanism candidate for S4's positional view, nothing built). One hazard filed in HANDOFF for step 4: `--freeze` and the `.truth` kind name the same `logs/<s>.log`.

**What this close does NOT assert:** the `exact` / `set` / `schema` comparators' verdict text and the suite over the fixture (steps 3–4: the truth is compared by `cmp` inside a gate); a real document set (every expectation is `fixture` class, the forms and guards are the fixture's); bbh's tip beyond the baseline; any platform but Darwin arm64 (R21); executable controls (R29, S6: every FIRED is a self-report); and everything S2's slice readout listed.

# bbx-9 step 3 — the three comparator families of the document-set kind, and the truth logs that were never in the repository (2026-09-10)

**The open.** GREEN on `6a80ec7` (`build/selftest_20260910_140052`: 21 gates, 66 / 66 controls, tree unchanged; the two drift NOTEs: bbh one ahead, VampireSaved 21 ahead): the bbx-8 close re-derived. No ruling was pending but R21.

## What changed

**The first finding was not step 3's.** The shadow tree, built with `git archive HEAD` as the handoff prescribes, had no `fixture/docset/expected/fixture/logs/`: the three truth logs the bbx-8 close counted ("register 16 rows") sat under the `.gitignore` rule `*.log` and were never staged. In the shadow `gates/docset_fixture.sh` went red (`--check`: 3 files DIFFER) and `gates/docset_driver.sh` went red on every truth comparison — the first time either ran on what the repository actually holds. A clone, R21's Linux run included, would have opened red. Gotcha G22, paid one shadow run. Fixed: `!fixture/**/*.log`, the three logs added; and the mechanism (R27): `bbx.provenance` now requires every file a register row names to be one git TRACKS when the expectation tree is inside a work tree, reports the check as *not run* outside one (never as passed), and `gates/provenance.sh` proves it with the control `row-untracked` (a registered, present, ignored file FAILs naming it); `gates/docset_fixture.sh` now runs the tool over the fixture's REAL register (16 rows, complete, every file tracked) instead of leaving it to `--check`'s diff alone.

**Step 3, as planned (§8.3), built in the shadow and moved into the tree once green there.** Three comparators under the one dispatcher (R23), each printing ONE verdict line in BBX's own text — no ancestor prints these, so the family's gate is the freeze (C4):

- `lib/py/bbx/compare_exact.py` — the truth log against the run log BY INDEX: `PASS exact (<n> indices, every token the truth's)`; `FAIL exact: index <i> differs (truth <status>, run <status>; quoted same|differs, derived same|differs)` when both tokens split as document-set tokens (the one splitter), so a triage reads which half moved; `FAIL-SHORT exact: the run ends at index <m>, the truth at <n> (…)` — `short`, apart from `diverged` (BBX-4, C3); a gap, an END that disagrees with its indices, an index the truth lacks and a duplicated index each FAIL naming the index; an empty truth is `nothing compared` (bbh's #54 lesson); a missing truth log is bbh's `NO-BASE-LOG`.
- `lib/py/bbx/compare_set.py` — a frozen MULTISET of `(document, line, form, status)` rows (named TOML fields, R34's caveat) against the run's rows, which are the log's statuses joined by index with the claim map — `bbx.docset run_rows`, which PROVES the map describes this log through the quoted-hash half of every token (D38) and refuses otherwise. Two consumers inside the family (BBX-25): `claims` (mode `inventory`, both ways: `FAIL set-inventory: <a> frozen row(s) not in the run (first …); <b> run row(s) not frozen (first …)`) and `covered` (mode `shrink-only`, R33: a lost row `FAIL set-covered: no longer covers <row> (<k> lost)`; growth `PASS set-covered (<n> frozen rows still covered; the run covers <k> more)` plus `NOTE: covered-grew <k>`; a frozen row whose status is not BOUND or PARAPHRASE refused). A duplicate row on either side FAILs naming it — the binder never produces one, so a duplicate is a hand edit (BBX-17).
- `lib/py/bbx/compare_schema.py` — the artifact's SHAPE before any value (R34, BBX-16): header names in order, one type per column from `int | hex | str | nonempty` (D41), the rows line (`>=` or `=`); the FIRST violation named — `column <i> is '<got>' (frozen '<want>')`, `row <r> has <m> columns (frozen <n>)` (the maintainer's fifteenth control), `row <r> column '<name>' = '<value>' is not <type>`, `rows <m> (frozen <op> <n>)`. A blank line inside the artifact is a row with one empty column and fails on shape; nothing is skipped silently.
- `lib/sh/compare.sh` — `compare_check <expdir> <name> <kind> <spec> <runmask> <log> [<scenario> <artifact>]`: the temporal branch is untouched (F16 green); `exact)` reads the expectation file's `[spec].baseset` and pairs `<root>/<baseset>/logs/<name>.log` (the temporal family's own pairing); `set)` needs the two trailing arguments; `schema)` the artifact. A kind outside the profile in force is still `FAIL kind '<k>' has no comparator family …` — a `.masked` under `document-set` and a `.truth` under `frame-driven` both measured.
- `lib/py/bbx/docset.py` — two verbs: `rows` (the join above) and `resolve <set>`, the ONE resolver over `DOCSET_PATH` (`;`-separated, each component made absolute, an absent absolute component skipped, a relative one that resolves nowhere an error); `drivers/docset.sh` now calls it instead of carrying its own loop, so the schema comparator and the driver read the same artifact by construction. `bin/bbx compare exact|set|schema` reaches the tools.
- `lib/py/bbx/finding.py` — one rule generalised: `FAIL unknown ` (bbh has exactly one such line, `masked_compare.sh:127` at `10a82d2`, measured by `git grep`) so `FAIL unknown exact class`, `set mode`, `schema type`, `schema rows op` all read `unknown-class`; no new word (§8.3's prediction held: `diverged`, `short`, `no-base-log`, `unknown-class`, `pass` cover every line).
- `gates/set_schema.sh` (portable, 5 controls, ~7 s): every kind of every scenario PASSes through the dispatcher (12 pairings); the refusals; the five controls below; and every verdict line the gate saw is fed to `finding.py` — a line reading `unclassified` fails the gate (`NOTE: verdict-lines-frozen 38`).
- `docs/defaults.md` D41 (the schema type vocabulary, principled/ruled), D42 (the kind's NOTE keys). The plan's §5 and §8.3 had placed the third profile's unknown-kind control in `gates/expectation_kinds.sh`; it was built in `gates/docset_fixture.sh` at bbx-7 — the plan corrected in its own commit `08c9fef` (BBX-19; retraction X10) and nothing was added to `expectation_kinds.sh`.

**The tree's classifier caught what the shadow had not (G23, paid two close batteries):** the gate's section-5 banner quoted `unclassified` in backticks inside double quotes; the shell ran the word, printed `command not found`, and the gate went on to PASS with exit 0. The shadow had run it bare and tested the exit — green. `bin/bbx selftest` read the log through the classifier's shell-error clause (lineage G18) and both first close batteries were NOT GREEN on that one gate (`PASS 21 FAIL 1`, all 72 controls fired: `build/selftest_20260910T121903Z`, `…T122414Z`). The quote fixed; the shadow practice now runs every touched gate through `bin/bbx classify` (HANDOFF). BBX-1 re-anchored a second time.

**The shadow caught two things before the tree saw either:** G22 above, and a verdict label — the set family's duplicate line printed `set-shrink-only` where its own docstring and the gate's frozen text said `set-covered` (the kind's word, not the mode's); the gate's exact-string check refused it on the shadow's first run. Neither reached a tracked file; the second is in this readout and the history, not a gotcha (as at bbx-7).

## Counts, separately (measured by `gates/set_schema.sh`, `gates/provenance.sh`, `gates/docset_fixture.sh`)

| | |
|---|---|
| pairings PASSing through `compare_check` on the tree's fixture | 12: `01_all` exact 23 indices / inventory 23 rows / covered 20 / schema 4 columns 12 rows; `02_weights` 7 / 7 / 6 / schema; `03_rows` 7 / 7 / 7 / schema |
| verdict lines frozen and classified | 38 distinct, 0 `unclassified` |
| inventory-both-ways | a frozen row removed → `1 run row(s) not frozen (first overview.md:7 has BOUND)`; a row added → `1 frozen row(s) not in the run (first ranks.md:99 row BOUND)`; both at once → both directions in one line |
| covered-shrink-only | the paraphrase's literal 37 → 36 in a copied artifact → `no longer covers overview.md:5 paraphrase PARAPHRASE (1 frozen row(s) lost)`; a duplicated frozen row → `appears 2 times (a duplicate is hand-editing, BBX-17)`; 19 frozen of 20 covered → PASS + `NOTE: covered-grew 1`; a frozen row with status MISMATCH → refused |
| schema-before-values | `weight` → `mass` in the header → `column 3 is 'mass' (frozen 'weight')`; a cell dropped from row 2 → `row 2 has 3 columns (frozen 4)`; `x` in row 1's weight → `row 1 column 'weight' = 'x' is not int`; a frozen `= 11` against 12 rows → `rows 12 (frozen = 11)`; Dorin 57 → 56 → schema PASS, `FAIL exact: index 4 differs (truth BOUND, run MISMATCH; quoted same, derived differs)` |
| short-apart-from-diverged | cut at index 20 → `FAIL-SHORT … ends at index 20, the truth at 23` (finding `short`); index 5's status changed → `index 5 differs (… quoted same, derived same)` (`diverged`); a gap, END 23 over 22 indices, index 24, index 5 twice — each named; the edited document (G-control of step 2) → `index 4 differs (truth BOUND, run STALE; quoted differs, derived differs)` |
| verdict-text (shadow-tool) | `PASS set-inventory (` → `[` in a shadow `compare_set.py`: the frozen-text check refuses the line |
| row-untracked (`gates/provenance.sh`) | a registered, present, `*.log`-ignored file → `FAIL: 1 provenance row(s) naming a file git does not track …`, named; outside a work tree the check prints `not run`, never `ok` |
| the fixture's register through the tool | 16 rows, complete both ways, every named file tracked by git |
| runtimes (this host, loaded; measured once in the shadow, not gated) | set_schema 7.0 s; provenance 1.0 s; docset_fixture 0.6 s; docset_driver 2.7 s |
| gates / controls in the tree after step 3 | 22 / 72 / 72 (the new gate's 5, provenance's 1), confirmed by the close batteries below — and by the two red ones before the G23 fix, whose controls line was already 72 / 72 |

## What it rests on

The touched gates run in the shadow (a git tree built from HEAD) to a file with their exits tested — `set_schema` 5 controls, `provenance` 5, `docset_fixture` 3, `docset_driver` 7, `compare_dispatch` 3, `expectation_kinds` 3, `controls`, `readout`, `close_sweeps` 3 — then the two close batteries below in the tree.

## What this green does NOT assert

- The suite over the fixture (step 4): the loop over the kinds, the kept-run rows `(scenario, kind)`, `NOT-EVALUATED (schema failed)` on a schema FAIL, `--freeze` for the shrink-only kind, the coverage NOTEs on the screen. Until then every verdict line above is printed by a gate calling `compare_check`, not by a suite run.
- Anything about a real document set: every input is the fixture's (fixture class) or a perturbed copy of it; the forms, guards and lexical classes are the fixture's (D35, D39, D40).
- The schema family on a second format or consumer: `tsv` is its one format and the fixture's artifact its one consumer until S4's JSON (BBX-25 unmet, stated in the gate's header).
- That a register row's class is true of its file; that a tracked file is the RIGHT file (tracked-ness is presence in a clone, nothing more).
- Executable controls (R29, S6): every `CONTROL FIRED` here is the gate's self-report.

## The screen, verbatim (`bin/bbx readout build/selftest_20260910T123545Z --against build/selftest_20260910T123032Z`)

```
== READOUT — self subject at /Users/koneko/Developer/generalized-blackbox-harness/BBX @ 08c9fef (porcelain 21) — started 2026-09-10T12:35:46Z on Darwin arm64 ==
VERDICT: GREEN   PASS 22  SKIP 0  FAIL 0  TIMEOUT 0  MISSING 0   (gates 22)
tree during the run: unchanged (untracked entries 4 -> 4)   harness: bbx @ 08c9fef
rests on:
  controls: fired 72 / declared 72; dead 0; undeclared firings 0; gates red 0
  each can fail: 22 of 22 gates proved a control fires on purpose
  expectations relied upon: none registered — a static run compares against no frozen expectation; a kept suite run (bbx-run-suite --log) carries its register's histogram (D32)
  coverage: docset_driver: 20/23
  coverage: census_recount: rows=300 recountable=244 not_recountable=56 (BBX-18: the census claims no command re-derives)
  note: docset_fixture: docset-fixture records=12 documents=3 claims=20 wrong=1 paraphrase=1 unbindable=2 scenarios=3 expectations=12 truth_logs=3
  note: docset_driver: paraphrase 1
  note: docset_driver: unbindable 2
  note: docset_driver: stale 0
  note: docset_driver: mismatch 1
  note: set_schema: verdict-lines-frozen 38
  note: census_recount: drift census=bbh.md recorded=10a82d2 tip=447e5d26bc23 ahead=1 rows_moved=1 ids=A2
  note: census_recount: drift census=vampiresaved.md recorded=0cdd9726 tip=e57939ed41be ahead=22 rows_moved=40 ids=A21,A22,A23,A24,A25,A26,A27,A28,A32,A33,A35,A37,A45,A48,A51,A59,A62,A63,A64,A66,A67,A68,A69,A76,A77,A80,A84,A85,A86,A87,A88,A89,A90,A91,A93,A95,A111,A120,A121,A127
  note: fidelity_bbh: bbh-drift baseline=10a82d2 tip=447e5d2 ahead=1
  note: fidelity_bbh: bbh-source tip=447e5d2 porcelain=0 untouched-by-construction=clone
  note: fidelity_bbh_s2: bbh-drift baseline=10a82d2 tip=447e5d2 ahead=1
  note: fidelity_bbh_s2: bbh-source tip=447e5d2 porcelain=0 untouched-by-construction=clone
  note: suite: suite-runs 25 (each kept under --log)
  BBX-14 (more than one run): met — 22 gates, 0 verdict differences against the run started 2026-09-10T12:30:32Z at the same HEAD
  last re-baseline: 2026-09-10 (bbx-5, R28): bbh `f675710` → `10a82d2` — bbh's commit "config: the M18 re-point that sat dirty since 14z-144; the example lib's root one level too high; the extraction counts dated" (7 files; G11 fixed there; the four dirty files of R8 committed). Measured before the move by the D20 override: F12, F13, F14, F14f, F16, F17 identical on a clone of `10a82d2`. Moved with it: D20; D12's five `[sweep].placeholders` (by definition); the census `docs/census/bbh.md` (A2 19 → 20). No verdict text changed on either side.
what this green does NOT assert (declared by each gate's header):
  classify: that a verdict word printed by a gate outside the runner is read at all: the classifier reads exit status first, then the log; a PASS printed after a non-zero exit is FAIL by design
  config: the meaning of a consumer's keys: only that the layers resolve (consumer over kind profile over kind-blind default) and that dumps are stable
  tier: that a gate reaching an instrument through a path the source regex does not match is seen: the depth and the regex are the limit
  static_runner: the sweep runner or any gate that needs an instrument: this is the pre-commit chain only
  static_runner: runtimes as anything but this host under this load
  controls: that a control is RIGHT — only that a declared control fired and an undeclared one is red (docs/controls.md)
  sweep_runner: speed-up on a real consumer: the queue is measured on stub gates with sleeps, not on an instrument-tier suite
  sweep_runner: a gate that escapes its clone by an absolute path: clone-per-slot pins the cwd, nothing more
  sweep_runner: portability beyond macOS: mkfifo and exec 8<> are POSIX, not yet run on Linux or WSL
  fingerprint: the identity of any artifact that is not a single file: the kind-blind fingerprint is file-sha1 (D16)
  rulings_shape: the prose of a ruling: only its heading, its answer line and its DECISIONS row are read
  readout: that a declared blind spot is true or complete: the screen prints what the header says
  readout: the sweep runner's runs: only bbx-run-static --log and bbx-run-suite --log are read
  readout: that a register row's class is true of its file: the suite screen prints what the register says (gates/provenance.sh keeps it complete and inside the vocabulary)
  close_sweeps: that every corrected claim has a register row: the register is written by hand at the correction (a claim nobody registered is not swept)
  close_sweeps: step 9 of the close (the lineage untouched): that is the recount's clone and the fidelity gate's proof, not this gate
  temporal: anything about a real log: every shape here is synthesized (fixture class); the classes' fitness for a consumer is that consumer's ratification
  temporal: the dispatcher's spec line and the suite's dispatch (S2 steps 2 and 3): the comparators are called directly
  temporal: that the thresholds are right for any subject: they are the frame-driven profile's (D23) and a consumer's override needs R25's ruling row (gates/thresholds.sh)
  thresholds: that a ruling id in [thresholds].rulings names a ruling that exists: any non-empty string is accepted here; the consumer's rulings-shape gate is where an id is checked
  thresholds: the values' fitness for any subject: 2 / 60 / 8 are bbh's ratified policy carried in the frame-driven profile (D23), not a measurement of anything here
  compare_dispatch: the suite's dispatch around this library (which file is read, how many runs, the .sha1 and .diverge kinds' own paths): S2 step 3
  compare_dispatch: anything about a real log or a real mask: every log is synthesized and the masks are strings the guard compares, never applied
  expectation_kinds: the content of any expectation file: only its extension and its stem are read here
  expectation_kinds: the suite's use of the dispositions (S2 step 3)
  provenance: that a row's class is TRUE of its file: the register is written by hand at the freeze; only completeness and the vocabulary are checked
  provenance: bbh's example register (markdown, a consumer list of classes): it is bbh's and F19 (S6) reads it under R11
  docset_fixture: that any claim in the fixture is BOUND, MISMATCH or anything else (gates/docset_driver.sh and gates/set_schema.sh): only that the files are the generator's, the design is chiral, and the register is complete and tracked
  docset_fixture: the suite over the fixture: step 4
  docset_driver: the verdict text of the exact, set and schema comparators (S3 step 3): the truth is compared here with cmp and diff
  docset_driver: the suite over the fixture (identity, the kinds loop, the coverage NOTE on the screen): S3 step 4
  docset_driver: prose, reasoning and causal claims in a document: only sentences in a declared form and the listed rows are claims
  docset_driver: the truth of the artifact itself: a document that agrees with a wrong artifact reads BOUND
  docset_driver: a nested document tree or a second artifact per set (DOCSET_VIEW is refused): S4 or a consumer's question
  set_schema: the suite's loop over the kinds, the kept-run rows (scenario, kind), NOT-EVALUATED on a schema FAIL and --freeze for the shrink-only kind: S3 step 4
  set_schema: anything about a real document set: every input is the fixture's (fixture class) or a perturbed copy of it
  set_schema: the schema family on a second format or a second consumer (S4's JSON): tsv is its one format and the fixture's artifact its one consumer (BBX-25 unmet, stated)
  census_recount: the truth of the not-recountable rows at their commits: they are named and counted, never run
  census_recount: that a hand-read citation says what its row claims: only that the line exists
  fidelity_bbh: anything about a suite, a comparator or an expectation: slice S2
  fidelity_bbh: bbh's example's correctness (G11 is bbh's to fix)
  fidelity_bbh: F15 unless BBX_FIDELITY_F15=1 was set for the run
  fidelity_bbh_s2: the kept suite run (--log) — bbh has none, so F12 diffs printed text only; the kept run's ground truth is gates/suite.sh
  fidelity_bbh_s2: anything about a real subject: F17's and F16b's inputs are synthesized, F16a's are the fake machine's (fixture class); the shapes are the lineage's paid-for cases and nothing else
  fidelity_bbh_s2: bbh's correctness: identical output on both sides is fidelity, not truth
  suite: any driver but the fake: a MAME or FBNeo driver is bbh's and untested here (bbh's own F8 rows)
  suite: the .sha1 kind's evidence: it is `self` class by construction (E4) and the register that says so is S2 step 4
  suite: the readout's reading of a kept suite run: the screen reads bbx-run-static runs only until step 4
  gates declaring no blind spot: 0   (a gate nobody has asked what its green leaves out)
```

# CLOSE — bbx-9 (2026-09-10)

**Close measurements.** Green first, twice, kept (`build/selftest_20260910T123032Z`, `build/selftest_20260910T123545Z`): `PASS 22 SKIP 0 FAIL 0 MISSING 0`; `controls fired 72 / declared 72; gates with no declaration: 0; red: 0`; `ok: no tracked file changed during the run` in both; BBX-14 met (0 verdict differences at one HEAD). The two batteries before the G23 fix (`build/selftest_20260910T121903Z`, `…T122414Z`) were NOT GREEN — `PASS 21 FAIL 1`, `set_schema` "exit 0 after a shell error", controls already 72 / 72 — and are kept as the incident's evidence. **Sweeps:** `close_sweeps` GREEN and `rulings_shape` GREEN inside both batteries, re-run once more on the final tree after these ledgers were written (below). **Lineage untouched, by construction and by proof:** bbh baseline=10a82d2 tip=447e5d2 ahead=1 porcelain=0 (clones only, clean after); VampireSaved past the census (not re-measured by design); sms recorded=ecc5481 tip=ecc5481 ahead=0 — the tips as the recount summaries printed them are in the kept runs' `census_recount.log`.

**Retractions this sitting:** X10 (the plan's control placement; the plan corrected in its own commit `08c9fef`, BBX-19). **Incidents reviewed for learnings (R27, step 10):** three — G22 (the untracked truth logs, paid 1 shadow run: a mechanism built, `bbx.provenance` tracked-ness with its control, and the shadow-as-git-tree practice written down), the set family's duplicate label (paid 0, caught by the gate's frozen text in the shadow: the mechanism is the freeze), G23 (the backticked word, paid 2 close batteries: a practice — the shadow runs gates through `bin/bbx classify` — the mechanism exists and fired).

**What this close does NOT assert:** the suite over the fixture (step 4: no suite run has printed one of the new verdict lines or a coverage number on the screen; `coverage: docset_driver: 20/23` is still a gate's NOTE); a real document set (every expectation is `fixture` class, the forms and guards the fixture's); the schema family beyond one format and one consumer (BBX-25 unmet); that a tracked file is the right file; bbh's tip beyond the baseline; any platform but Darwin arm64 (R21); executable controls (R29, S6); and everything S2's slice readout listed.

# bbx-10 step 4 — the suite's kinds loop: the document-set kind runs end to end and its coverage reaches the screen (2026-09-10)

**The open.** GREEN on `46cbe6f` (`build/selftest_20260910_144737`: 22 gates, 72 / 72 controls, porcelain 0, tree unchanged; the two drift NOTEs: bbh one ahead at `447e5d2`, VampireSaved 22 ahead): the bbx-9 close re-derived. R21 the only open ruling.

## What changed

**Measured before anything was built (§1):** the plan's §5 row said a caller's `DOCSET_NONDET=1` reads NONDETERMINISTIC through the suite. It does not: the document-set profile lists the driver family in `hermetic_unset` (D33), so the suite scrubs it — the two runs were byte-identical and the suite stayed on its verdicts — while the driver called directly under it wrote two logs differing at the first token. The plan corrected in its own commit `35c8007` (BBX-19; retraction X11) before the gate asserted the corrected shape; the control now goes through a wrapper driver passed with `--driver`, and the scrub is proven in the same control. D33 stands: a salted token must never reach a freeze.

**Step 4, as planned (§8.4), built in a shadow tree first and moved into the tree once every touched gate was green there through `bin/bbx classify`:**

- `bin/bbx-run-suite` — the KINDS LOOP. Which loop a run gets is read off the kinds table, never configured: a table that carries the temporal family (the frame-driven profile's `masked` / `diverge`) gets bbh's loop unchanged — one kind per scenario, `masked` > `diverge` > `sha1`, the printed text bbh's — and F12 (17 suite pairs among the 104 fidelity pairings) is the control that nothing moved for bbh; any other table gets the kinds loop: every EVAL kind whose file exists is its own pairing, the `schema` family FIRST (the artifact's shape before any value, R34), then the table's order, the self-frozen `.sha1` last (E4). The printed shape is `<scenario> <kind> <verdict>` with the scenario named once; run-level lines (SKIP, PENDING, RUN-FAIL, NONDETERMINISTIC, NO-EXPECTATION) print as bbh prints them, kept with kind `-`. On a schema FAIL the scenario's value kinds print the suite's own `NOT-EVALUATED (schema failed)` — finding `pending`, counted as failures, no NOTE kept for the scenario (a coverage of a wrong shape is a number about nothing). The comparators that read the artifact get it through `compare_check`'s two trailing arguments: the scenario file and the SUBJECT FILE the fingerprint resolved (`bbx.fingerprint --path`, new: the file the identity was computed from — proven equal to the driver's own resolver on the fixture). After a scenario's pairings `[suite].log_summary` (D43) prints the log's own numbers at column 0 — the document-set profile's `bbx.docset summary`, kind-blind none, so the frame-driven text is untouched — and every `NOTE:` line the scenario printed is kept in `notes.tsv` (D44). `--freeze` under the kinds loop rewrites the shrink-only kind from the run (`bbx.compare_set --freeze`: the `[spec]` kept, one table per covered row in extraction order — on the fixture the rewritten file equals the generator's byte for byte) and leaves every other authored kind alone (`authored .<kind> expectation — not self-frozen`); the `.sha1` self-freeze runs ONLY for a scenario with no authored kind, so `logs/<name>.log` — where an exact kind's truth log lives — is never overwritten beside a `.truth`: the bbx-9 HANDOFF hazard closed by construction, and proven in both directions.
- `lib/py/bbx/finding.py` — `NOT-EVALUATED` → `pending` (E2's disposition) and `authored ` as the prefix (bbh's `.masked` line is one of them); no new word.
- `lib/py/bbx/readout.py` — the suite screen counts scenarios and pairings apart, prints `coverage:` per scenario and every other key as a note from `notes.tsv`, meets BBX-14 over pairings `(scenario, kind)`, and lists the DRIVER's `NOT-ASSERTED` lines under "what this green does NOT assert" (RO2 — bbh's fake driver declares none, and the screen says so). `gates/readout.sh`'s three frozen lines moved with it.
- `gates/docset_suite.sh` (portable, 6 controls, 17 suite runs, ~55 s): the tree's fixture GREEN with all 29 printed lines frozen and the 12 verdict lines classified `pass`; the kept run's 12 rows keyed `(scenario, kind)` in the loop's order, the 15 notes, `loop=kinds`; the second run byte-identical and the readout over both exit 0 with the eight screen lines checked; the six controls below; the `.sha1` beside the authored kinds as a fifth pairing; NO-EXPECTATION; the fixture's every file checksummed before and after (untouched).
- Headers: the four blind-spot lines that said "S3 step 4" now name `gates/docset_suite.sh`; `gates/suite.sh`'s two that had named "S2 step 4" since bbx-4 (G24) name `gates/readout.sh` and `gates/provenance.sh`. `docs/defaults.md` D43, D44. `gates/portable.txt` +1.

**One thing the plan's §6 words differently from the screen:** §6 says `comparator classes: exact / set / schema`; the screen's class column carries the spec's `class` field — `exact, multiset, schema` — because `set` is the family and `multiset` its one class (as `masked` is the kind and `flicker` the class). Not a retraction: the sentence names families and the screen names classes; recorded here so the next reader is not surprised.

## Counts, separately (measured by `gates/docset_suite.sh`; the shadow's runs quoted where the gate does not print the number)

| | |
|---|---|
| the tree's fixture through the suite | GREEN; 12 pairings: `01_all` schema 4 columns 12 rows / exact 23 indices / inventory 23 rows / covered 20; `02_weights` schema / 7 / 7 / 6; `03_rows` schema / 7 / 7 / 7; coverage `20/23`, `6/7`, `7/7`; paraphrase 1, unbindable 2, mismatch 1 + 1 (the planted wrong claim in two scenarios) |
| printed lines frozen / verdict lines classified `pass` / kept rows / notes rows | 29 / 12 / 12 / 15 |
| twice (BBX-14) | the second run's `results.tsv` and `notes.tsv` byte-identical; the readout `--against`: `met — 12 pairings, 0 verdict differences`, exit 0 |
| identity-before-any-claim | `weight` → `mass` in a copied `overview.md` → `UNREGISTERED build: whole-set …`, exit 1, `verdict=UNREGISTERED`, `results.tsv` the header alone; restored → set `fixture`, SUITE GREEN |
| nondeterministic-before-any-class | a wrapper driver exporting `DOCSET_NONDET=1` → NONDETERMINISTIC on all 3 scenarios, 0 PASS lines, kept kind `-` finding `nondeterministic`, RED; the caller's `DOCSET_NONDET=1` → SUITE GREEN (scrubbed, D33); the driver directly under it → two logs differing at char 50 of line 1 |
| covered-shrink-only | a row added (`weights.md:5 of-is BOUND`) → `FAIL set-covered: no longer covers weights.md:5 of-is BOUND (1 frozen row(s) lost)`, `diverged`, RED; `c6` removed → `PASS set-covered (5 frozen rows still covered; the run covers 1 more)` + `NOTE: covered-grew 1` kept; `--freeze` → `frozen set-covered (6 rows)`, the file equal to the generator's byte for byte, the other two covered files and all three truth logs untouched, no `.sha1` written; the next plain run GREEN, 0 growth notes |
| wrong-truth | the green run keeps `02_weights mismatch 1` and its exact pairing PASSes; index 3 of the truth log rewritten `BOUND` → `FAIL exact: index 3 differs (truth BOUND, run MISMATCH; quoted same, derived same)`, `diverged`, RED, while schema / claims / covered PASS |
| schema-before-any-value | `weight` → `mass` in a copied `01_all.schema` → `FAIL schema: column 3 is 'weight' (frozen 'mass')`; truth, claims, covered each `NOT-EVALUATED (schema failed)` (`pending`); `pass=8 fail=4`; 0 notes kept for `01_all`, 5 for `02_weights`; SUITE RED |
| freeze-keeps-the-truth-log | a `MARKER` line appended to a copied `logs/03_rows.log`: `--freeze` with the four kinds present → `authored .schema … not self-frozen`, `frozen set-covered (7 rows)`, the marker still the last line, no `.sha1`; the four kinds removed → `03_rows sha1 frozen <sha>`, the log's last line `END 7` (overwritten), `03_rows.sha1` written |
| the self-frozen kind beside the authored kinds | a `.sha1` → a fifth pairing `sha1 PASS`, 13 rows; `--freeze` with both → `authored kinds present — .sha1 not self-frozen (logs/03_rows.log is theirs)`; nothing of any kind → `NO-EXPECTATION …`, `no-expectation` |
| F12 (bbh's loop, the control that nothing moved) | `gates/fidelity_bbh_s2.sh` PASS: 104 pairings identical on the plain clone at `10a82d2`; `gates/suite.sh` PASS (57 checks, 5 controls) |
| runtimes (this host, loaded; measured once, not gated) | docset_suite 55.0 s (17 suite runs); the shadow's other touched gates: readout, set_schema, docset_driver, docset_fixture, expectation_kinds, compare_dispatch, close_sweeps, rulings_shape, controls, static_runner, config, provenance, fingerprint, classify, tier — all PASS through the classifier |
| gates / controls in the tree after step 4 | 23 / 78 / 78 (the new gate's 6), confirmed by the close batteries below |

## What it rests on

Every touched gate run in the shadow (a git tree built from HEAD) to a file and read through `bin/bbx classify` — the two static ones with `BBX_BBH_HOME` — then the two close batteries below in the tree. The kinds loop's first green was read from its printed values (the 29 lines above) before the gate froze them (BBX-2).

## What this green does NOT assert

- Anything about a real document set: every input is the fixture's (fixture class) or a perturbed copy of it; the forms, guards and lexical classes are the fixture's (D35, D39, D40). `PASSed on a real pairing: none` is the screen's own sentence.
- A second consumer of the kinds loop: one profile drives it (S4's command-line kind is the second; BBX-25 unmet, stated in the gate's header). The schema family still has one format and one consumer.
- The readout screen beyond the lines the gate checks (the verdict, the pairings, the coverage and note lines, BBX-14, the driver's blind spots): the rest is `gates/readout.sh`'s.
- That a register row's class is true of its file; that a tracked file is the right file.
- The slice's own accounting (step 5): families per kind, controls declared / fired per gate, provenance classes, defaults rows, the first shared-vs-kind-specific file census, and what the slice's green does not assert — the readout that lets the maintainer rule S3 done (§7).
- Executable controls (R29, S6): every `CONTROL FIRED` here is the gate's self-report.

## The screen, verbatim (`bin/bbx readout build/selftest_20260910T131501Z --against build/selftest_20260910T130844Z`)

```
== READOUT — self subject at /Users/koneko/Developer/generalized-blackbox-harness/BBX @ 35c8007 (porcelain 14) — started 2026-09-10T13:15:01Z on Darwin arm64 ==
VERDICT: GREEN   PASS 23  SKIP 0  FAIL 0  TIMEOUT 0  MISSING 0   (gates 23)
tree during the run: unchanged (untracked entries 1 -> 1)   harness: bbx @ 35c8007
rests on:
  controls: fired 78 / declared 78; dead 0; undeclared firings 0; gates red 0
  each can fail: 23 of 23 gates proved a control fires on purpose
  expectations relied upon: none registered — a static run compares against no frozen expectation; a kept suite run (bbx-run-suite --log) carries its register's histogram (D32)
  coverage: docset_driver: 20/23
  coverage: census_recount: rows=300 recountable=244 not_recountable=56 (BBX-18: the census claims no command re-derives)
  note: docset_fixture: docset-fixture records=12 documents=3 claims=20 wrong=1 paraphrase=1 unbindable=2 scenarios=3 expectations=12 truth_logs=3
  note: docset_driver: paraphrase 1
  note: docset_driver: unbindable 2
  note: docset_driver: stale 0
  note: docset_driver: mismatch 1
  note: set_schema: verdict-lines-frozen 38
  note: docset_suite: suite-runs 17 (each kept under --log)
  note: census_recount: drift census=bbh.md recorded=10a82d2 tip=447e5d26bc23 ahead=1 rows_moved=1 ids=A2
  note: census_recount: drift census=vampiresaved.md recorded=0cdd9726 tip=77f54e929962 ahead=23 rows_moved=40 ids=A21,A22,A23,A24,A25,A26,A27,A28,A32,A33,A35,A37,A45,A48,A51,A59,A62,A63,A64,A66,A67,A68,A69,A76,A77,A80,A84,A85,A86,A87,A88,A89,A90,A91,A93,A95,A111,A120,A121,A127
  note: fidelity_bbh: bbh-drift baseline=10a82d2 tip=447e5d2 ahead=1
  note: fidelity_bbh: bbh-source tip=447e5d2 porcelain=0 untouched-by-construction=clone
  note: fidelity_bbh_s2: bbh-drift baseline=10a82d2 tip=447e5d2 ahead=1
  note: fidelity_bbh_s2: bbh-source tip=447e5d2 porcelain=0 untouched-by-construction=clone
  note: suite: suite-runs 25 (each kept under --log)
  BBX-14 (more than one run): met — 23 gates, 0 verdict differences against the run started 2026-09-10T13:08:44Z at the same HEAD
  last re-baseline: 2026-09-10 (bbx-5, R28): bbh `f675710` → `10a82d2` — bbh's commit "config: the M18 re-point that sat dirty since 14z-144; the example lib's root one level too high; the extraction counts dated" (7 files; G11 fixed there; the four dirty files of R8 committed). Measured before the move by the D20 override: F12, F13, F14, F14f, F16, F17 identical on a clone of `10a82d2`. Moved with it: D20; D12's five `[sweep].placeholders` (by definition); the census `docs/census/bbh.md` (A2 19 → 20). No verdict text changed on either side.
what this green does NOT assert (declared by each gate's header):
  classify: that a verdict word printed by a gate outside the runner is read at all: the classifier reads exit status first, then the log; a PASS printed after a non-zero exit is FAIL by design
  config: the meaning of a consumer's keys: only that the layers resolve (consumer over kind profile over kind-blind default) and that dumps are stable
  tier: that a gate reaching an instrument through a path the source regex does not match is seen: the depth and the regex are the limit
  static_runner: the sweep runner or any gate that needs an instrument: this is the pre-commit chain only
  static_runner: runtimes as anything but this host under this load
  controls: that a control is RIGHT — only that a declared control fired and an undeclared one is red (docs/controls.md)
  sweep_runner: speed-up on a real consumer: the queue is measured on stub gates with sleeps, not on an instrument-tier suite
  sweep_runner: a gate that escapes its clone by an absolute path: clone-per-slot pins the cwd, nothing more
  sweep_runner: portability beyond macOS: mkfifo and exec 8<> are POSIX, not yet run on Linux or WSL
  fingerprint: the identity of any artifact that is not a single file: the kind-blind fingerprint is file-sha1 (D16)
  rulings_shape: the prose of a ruling: only its heading, its answer line and its DECISIONS row are read
  readout: that a declared blind spot is true or complete: the screen prints what the header says
  readout: the sweep runner's runs: only bbx-run-static --log and bbx-run-suite --log are read
  readout: that a register row's class is true of its file: the suite screen prints what the register says (gates/provenance.sh keeps it complete and inside the vocabulary)
  close_sweeps: that every corrected claim has a register row: the register is written by hand at the correction (a claim nobody registered is not swept)
  close_sweeps: step 9 of the close (the lineage untouched): that is the recount's clone and the fidelity gate's proof, not this gate
  temporal: anything about a real log: every shape here is synthesized (fixture class); the classes' fitness for a consumer is that consumer's ratification
  temporal: the dispatcher's spec line and the suite's dispatch (S2 steps 2 and 3): the comparators are called directly
  temporal: that the thresholds are right for any subject: they are the frame-driven profile's (D23) and a consumer's override needs R25's ruling row (gates/thresholds.sh)
  thresholds: that a ruling id in [thresholds].rulings names a ruling that exists: any non-empty string is accepted here; the consumer's rulings-shape gate is where an id is checked
  thresholds: the values' fitness for any subject: 2 / 60 / 8 are bbh's ratified policy carried in the frame-driven profile (D23), not a measurement of anything here
  compare_dispatch: the suite's dispatch around this library (which file is read, how many runs, the .sha1 and .diverge kinds' own paths): S2 step 3
  compare_dispatch: anything about a real log or a real mask: every log is synthesized and the masks are strings the guard compares, never applied
  expectation_kinds: the content of any expectation file: only its extension and its stem are read here
  expectation_kinds: the suite's use of the dispositions (S2 step 3)
  provenance: that a row's class is TRUE of its file: the register is written by hand at the freeze; only completeness and the vocabulary are checked
  provenance: bbh's example register (markdown, a consumer list of classes): it is bbh's and F19 (S6) reads it under R11
  docset_fixture: that any claim in the fixture is BOUND, MISMATCH or anything else (gates/docset_driver.sh and gates/set_schema.sh): only that the files are the generator's, the design is chiral, and the register is complete and tracked
  docset_fixture: the suite over the fixture: gates/docset_suite.sh
  docset_driver: the verdict text of the exact, set and schema comparators (S3 step 3): the truth is compared here with cmp and diff
  docset_driver: the suite over the fixture (identity, the kinds loop, the coverage NOTE on the screen): gates/docset_suite.sh
  docset_driver: prose, reasoning and causal claims in a document: only sentences in a declared form and the listed rows are claims
  docset_driver: the truth of the artifact itself: a document that agrees with a wrong artifact reads BOUND
  docset_driver: a nested document tree or a second artifact per set (DOCSET_VIEW is refused): S4 or a consumer's question
  set_schema: the suite's loop over the kinds, the kept-run rows (scenario, kind), NOT-EVALUATED on a schema FAIL and --freeze for the shrink-only kind: gates/docset_suite.sh
  set_schema: anything about a real document set: every input is the fixture's (fixture class) or a perturbed copy of it
  set_schema: the schema family on a second format or a second consumer (S4's JSON): tsv is its one format and the fixture's artifact its one consumer (BBX-25 unmet, stated)
  docset_suite: anything about a real document set: every input is the fixture's (fixture class) or a perturbed copy of it; the forms, guards and lexical classes are the fixture's (D35, D39, D40)
  docset_suite: bbh's precedence loop and its printed text: gates/fidelity_bbh_s2.sh (F12) and gates/suite.sh
  docset_suite: the readout screen beyond the lines checked here (the verdict, the pairings, the coverage and note lines, BBX-14, the driver's blind spots): gates/readout.sh
  docset_suite: a second consumer of the kinds loop (S4's command-line kind): one profile drives it here (BBX-25 unmet, stated)
  census_recount: the truth of the not-recountable rows at their commits: they are named and counted, never run
  census_recount: that a hand-read citation says what its row claims: only that the line exists
  fidelity_bbh: anything about a suite, a comparator or an expectation: slice S2
  fidelity_bbh: bbh's example's correctness (G11 is bbh's to fix)
  fidelity_bbh: F15 unless BBX_FIDELITY_F15=1 was set for the run
  fidelity_bbh_s2: the kept suite run (--log) — bbh has none, so F12 diffs printed text only; the kept run's ground truth is gates/suite.sh
  fidelity_bbh_s2: anything about a real subject: F17's and F16b's inputs are synthesized, F16a's are the fake machine's (fixture class); the shapes are the lineage's paid-for cases and nothing else
  fidelity_bbh_s2: bbh's correctness: identical output on both sides is fidelity, not truth
  suite: any driver but the fake: a MAME or FBNeo driver is bbh's and untested here (bbh's own F8 rows)
  suite: the .sha1 kind's evidence: it is `self` class by construction (E4) and the register that says so is gates/provenance.sh's
  suite: the readout's reading of a kept suite run: gates/readout.sh reads the suite screen, gates/docset_suite.sh its coverage lines
  gates declaring no blind spot: 0   (a gate nobody has asked what its green leaves out)
```

# CLOSE — bbx-10 (2026-09-10)

**Close measurements.** Green first, twice, kept (`build/selftest_20260910T130844Z`, `build/selftest_20260910T131501Z`): `PASS 23 SKIP 0 FAIL 0 MISSING 0` (23 gates); `controls fired 78 / declared 78`; `ok: no tracked file changed during the run` in both; BBX-14 met (23 gates, 0 verdict differences at one HEAD). **Sweeps:** `close_sweeps` and `rulings_shape` inside both batteries, re-run once more on the final tree after these ledgers were written (the commit message quotes the two verdicts). **Lineage untouched, by construction and by proof:** bbh baseline=10a82d2 tip=447e5d2 ahead=1 porcelain=0 (clones only, clean after); VampireSaved past the census (not re-measured by design); the tips as the recount summaries printed them are in the kept runs' `census_recount.log`.

**Retractions this sitting:** X11 (the plan's §5 nondeterminism row; the plan corrected in its own commit `35c8007`, BBX-19). **Incidents reviewed for learnings (R27, step 10):** G24 (two blind-spot lines naming "S2 step 4" as their future since bbx-4; paid 0; G19's mechanism, now with two instances, and a practice: a blind-spot line names the gate that will close it); the new gate's two defects caught in the shadow (paid 1 shadow run; the shadow-through-the-classifier practice is the mechanism and it fired).

**What this close does NOT assert:** a real document set (every expectation is `fixture` class; the screen says `PASSed on a real pairing: none`); a second consumer of the kinds loop or of the schema family (BBX-25 unmet, stated in `gates/docset_suite.sh`); the slice's own accounting and the file census (step 5); bbh's tip beyond the baseline; any platform but Darwin arm64 (R21); executable controls (R29, S6); and everything S2's slice readout listed.

# bbx-11 step 5 — the S3 slice readout: families per kind, controls, provenance, defaults, the first file census, and §7 answered one by one (2026-09-10)

## The screen, verbatim (`bin/bbx readout build/selftest_20260910T142107Z --against build/selftest_20260910T141218Z`)

```
== READOUT — self subject at /Users/koneko/Developer/generalized-blackbox-harness/BBX @ bf971c3 (porcelain 7) — started 2026-09-10T14:21:08Z on Darwin arm64 ==
VERDICT: GREEN   PASS 23  SKIP 0  FAIL 0  TIMEOUT 0  MISSING 0   (gates 23)
tree during the run: unchanged (untracked entries 1 -> 1)   harness: bbx @ bf971c3
rests on:
  controls: fired 78 / declared 78; dead 0; undeclared firings 0; gates red 0
  each can fail: 23 of 23 gates proved a control fires on purpose
  expectations relied upon: none registered — a static run compares against no frozen expectation; a kept suite run (bbx-run-suite --log) carries its register's histogram (D32)
  coverage: docset_driver: 20/23
  coverage: census_recount: rows=300 recountable=244 not_recountable=56 (BBX-18: the census claims no command re-derives)
  note: docset_fixture: docset-fixture records=12 documents=3 claims=20 wrong=1 paraphrase=1 unbindable=2 scenarios=3 expectations=12 truth_logs=3
  note: docset_driver: paraphrase 1
  note: docset_driver: unbindable 2
  note: docset_driver: stale 0
  note: docset_driver: mismatch 1
  note: set_schema: verdict-lines-frozen 38
  note: docset_suite: suite-runs 17 (each kept under --log)
  note: census_recount: drift census=bbh.md recorded=10a82d2 tip=447e5d26bc23 ahead=1 rows_moved=1 ids=A2
  note: census_recount: drift census=vampiresaved.md recorded=0cdd9726 tip=77f54e929962 ahead=23 rows_moved=40 ids=A21,A22,A23,A24,A25,A26,A27,A28,A32,A33,A35,A37,A45,A48,A51,A59,A62,A63,A64,A66,A67,A68,A69,A76,A77,A80,A84,A85,A86,A87,A88,A89,A90,A91,A93,A95,A111,A120,A121,A127
  note: fidelity_bbh: bbh-drift baseline=10a82d2 tip=447e5d2 ahead=1
  note: fidelity_bbh: bbh-source tip=447e5d2 porcelain=0 untouched-by-construction=clone
  note: fidelity_bbh_s2: bbh-drift baseline=10a82d2 tip=447e5d2 ahead=1
  note: fidelity_bbh_s2: bbh-source tip=447e5d2 porcelain=0 untouched-by-construction=clone
  note: suite: suite-runs 25 (each kept under --log)
  BBX-14 (more than one run): met — 23 gates, 0 verdict differences against the run started 2026-09-10T14:12:19Z at the same HEAD
  last re-baseline: 2026-09-10 (bbx-5, R28): bbh `f675710` → `10a82d2` — bbh's commit "config: the M18 re-point that sat dirty since 14z-144; the example lib's root one level too high; the extraction counts dated" (7 files; G11 fixed there; the four dirty files of R8 committed). Measured before the move by the D20 override: F12, F13, F14, F14f, F16, F17 identical on a clone of `10a82d2`. Moved with it: D20; D12's five `[sweep].placeholders` (by definition); the census `docs/census/bbh.md` (A2 19 → 20). No verdict text changed on either side.
what this green does NOT assert (declared by each gate's header):
  classify: that a verdict word printed by a gate outside the runner is read at all: the classifier reads exit status first, then the log; a PASS printed after a non-zero exit is FAIL by design
  config: the meaning of a consumer's keys: only that the layers resolve (consumer over kind profile over kind-blind default) and that dumps are stable
  tier: that a gate reaching an instrument through a path the source regex does not match is seen: the depth and the regex are the limit
  static_runner: the sweep runner or any gate that needs an instrument: this is the pre-commit chain only
  static_runner: runtimes as anything but this host under this load
  controls: that a control is RIGHT — only that a declared control fired and an undeclared one is red (docs/controls.md)
  sweep_runner: speed-up on a real consumer: the queue is measured on stub gates with sleeps, not on an instrument-tier suite
  sweep_runner: a gate that escapes its clone by an absolute path: clone-per-slot pins the cwd, nothing more
  sweep_runner: portability beyond macOS: mkfifo and exec 8<> are POSIX, not yet run on Linux or WSL
  fingerprint: the identity of any artifact that is not a single file: the kind-blind fingerprint is file-sha1 (D16)
  rulings_shape: the prose of a ruling: only its heading, its answer line and its DECISIONS row are read
  readout: that a declared blind spot is true or complete: the screen prints what the header says
  readout: the sweep runner's runs: only bbx-run-static --log and bbx-run-suite --log are read
  readout: that a register row's class is true of its file: the suite screen prints what the register says (gates/provenance.sh keeps it complete and inside the vocabulary)
  close_sweeps: that every corrected claim has a register row: the register is written by hand at the correction (a claim nobody registered is not swept)
  close_sweeps: step 9 of the close (the lineage untouched): that is the recount's clone and the fidelity gate's proof, not this gate
  temporal: anything about a real log: every shape here is synthesized (fixture class); the classes' fitness for a consumer is that consumer's ratification
  temporal: the dispatcher's spec line and the suite's dispatch (S2 steps 2 and 3): the comparators are called directly
  temporal: that the thresholds are right for any subject: they are the frame-driven profile's (D23) and a consumer's override needs R25's ruling row (gates/thresholds.sh)
  thresholds: that a ruling id in [thresholds].rulings names a ruling that exists: any non-empty string is accepted here; the consumer's rulings-shape gate is where an id is checked
  thresholds: the values' fitness for any subject: 2 / 60 / 8 are bbh's ratified policy carried in the frame-driven profile (D23), not a measurement of anything here
  compare_dispatch: the suite's dispatch around this library (which file is read, how many runs, the .sha1 and .diverge kinds' own paths): S2 step 3
  compare_dispatch: anything about a real log or a real mask: every log is synthesized and the masks are strings the guard compares, never applied
  expectation_kinds: the content of any expectation file: only its extension and its stem are read here
  expectation_kinds: the suite's use of the dispositions (S2 step 3)
  provenance: that a row's class is TRUE of its file: the register is written by hand at the freeze; only completeness and the vocabulary are checked
  provenance: bbh's example register (markdown, a consumer list of classes): it is bbh's and F19 (S6) reads it under R11
  docset_fixture: that any claim in the fixture is BOUND, MISMATCH or anything else (gates/docset_driver.sh and gates/set_schema.sh): only that the files are the generator's, the design is chiral, and the register is complete and tracked
  docset_fixture: the suite over the fixture: gates/docset_suite.sh
  docset_driver: the verdict text of the exact, set and schema comparators (S3 step 3): the truth is compared here with cmp and diff
  docset_driver: the suite over the fixture (identity, the kinds loop, the coverage NOTE on the screen): gates/docset_suite.sh
  docset_driver: prose, reasoning and causal claims in a document: only sentences in a declared form and the listed rows are claims
  docset_driver: the truth of the artifact itself: a document that agrees with a wrong artifact reads BOUND
  docset_driver: a nested document tree or a second artifact per set (DOCSET_VIEW is refused): S4 or a consumer's question
  set_schema: the suite's loop over the kinds, the kept-run rows (scenario, kind), NOT-EVALUATED on a schema FAIL and --freeze for the shrink-only kind: gates/docset_suite.sh
  set_schema: anything about a real document set: every input is the fixture's (fixture class) or a perturbed copy of it
  set_schema: the schema family on a second format or a second consumer (S4's JSON): tsv is its one format and the fixture's artifact its one consumer (BBX-25 unmet, stated)
  docset_suite: anything about a real document set: every input is the fixture's (fixture class) or a perturbed copy of it; the forms, guards and lexical classes are the fixture's (D35, D39, D40)
  docset_suite: bbh's precedence loop and its printed text: gates/fidelity_bbh_s2.sh (F12) and gates/suite.sh
  docset_suite: the readout screen beyond the lines checked here (the verdict, the pairings, the coverage and note lines, BBX-14, the driver's blind spots): gates/readout.sh
  docset_suite: a second consumer of the kinds loop (S4's command-line kind): one profile drives it here (BBX-25 unmet, stated)
  census_recount: the truth of the not-recountable rows at their commits: they are named and counted, never run
  census_recount: that a hand-read citation says what its row claims: only that the line exists
  fidelity_bbh: anything about a suite, a comparator or an expectation: slice S2
  fidelity_bbh: bbh's example's correctness (G11 is bbh's to fix)
  fidelity_bbh: F15 unless BBX_FIDELITY_F15=1 was set for the run
  fidelity_bbh_s2: the kept suite run (--log) — bbh has none, so F12 diffs printed text only; the kept run's ground truth is gates/suite.sh
  fidelity_bbh_s2: anything about a real subject: F17's and F16b's inputs are synthesized, F16a's are the fake machine's (fixture class); the shapes are the lineage's paid-for cases and nothing else
  fidelity_bbh_s2: bbh's correctness: identical output on both sides is fidelity, not truth
  suite: any driver but the fake: a MAME or FBNeo driver is bbh's and untested here (bbh's own F8 rows)
  suite: the .sha1 kind's evidence: it is `self` class by construction (E4) and the register that says so is gates/provenance.sh's
  suite: the readout's reading of a kept suite run: gates/readout.sh reads the suite screen, gates/docset_suite.sh its coverage lines
  gates declaring no blind spot: 0   (a gate nobody has asked what its green leaves out)
```

## Open

The battery GREEN on the bbx-10 HEAD `88278e9` before anything was touched (`build/selftest_20260910T134014Z`): `PASS 23 SKIP 0 FAIL 0 MISSING 0`, `controls fired 78 / declared 78`, `ok: no tracked file changed during the run`; the two drift NOTEs the handoff predicted (bbh `447e5d2` one past the baseline; VampireSaved 23 past the census) and nothing else moved. The bbx-10 close re-derived, as the ritual asks.

## What changed

**No tool was written** (`docs/plans/S3.md` §8.5; the handoff's "no tool is written in step 5"). The sitting produced: this readout; `docs/census/bbx_files.md`, the first shared-vs-kind-specific FILE CENSUS, measured at runtime in a shadow tree by a scratchpad generator that is reproduced verbatim in the census's §C and run by hand — **measured once, not gated**: the gate is S4's plan to decide; the plan's §6 screen line corrected in its own commit before this readout quoted it (BBX-19; X12: `fixture 15`, not `fixture 10`); G25 (the instrument's first version ended every header, caught by `gates/docset_suite.sh`, paid 1 shadow gate run). `docs/plans/S3.md` §5's "Fourteen controls, in five gates" was the design; the measure is 21 controls in 4 gates (the fifth gate was X10's).

## The slice readout (`docs/slices.md` "What a slice's readout must carry"; CLAUDE.md §7)

**1. Gates, counted separately** — the close screen above: PASS 23, SKIP 0, FAIL 0, TIMEOUT 0, MISSING 0; both registries complete both ways (the runner reports MISSING and unregistered; 0 and 0). The four gates S3 added: `docset_fixture` (bbx-7), `docset_driver` (bbx-8), `set_schema` (bbx-9), `docset_suite` (bbx-10); the kernel gates S3 changed: `provenance` (row-untracked, bbx-9), `readout` (the suite screen's coverage, note and driver lines, bbx-10), `expectation_kinds` and `compare_dispatch` (the third profile, bbx-7/bbx-9).

**2. Controls declared / fired, per gate** (`build/selftest_20260910T134014Z/controls.txt`, the kept run's own lines; every `CONTROL FIRED:` line with its evidence is in that run's `<gate>.log`):

| gate | declared | fired | | gate | declared | fired |
|---|---|---|---|---|---|---|
| classify | 1 | 1 | | expectation_kinds | 3 | 3 |
| config | 1 | 1 | | provenance | 5 | 5 |
| tier | 1 | 1 | | **docset_fixture** | **3** | **3** |
| static_runner | 2 | 2 | | **docset_driver** | **7** | **7** |
| controls | 2 | 2 | | **set_schema** | **5** | **5** |
| sweep_runner | 4 | 4 | | **docset_suite** | **6** | **6** |
| fingerprint | 1 | 1 | | census_recount | 4 | 4 |
| rulings_shape | 4 | 4 | | fidelity_bbh | 1 | 1 |
| readout | 7 | 7 | | fidelity_bbh_s2 | 3 | 3 |
| close_sweeps | 3 | 3 | | suite | 5 | 5 |
| temporal | 4 | 4 | | | | |
| thresholds | 3 | 3 | | **total** | **78** | **78** |
| compare_dispatch | 3 | 3 | | **the S3 gates (bold)** | **21** | **21** |

Every control is the gate's self-report (`CONTROL FIRED:` against its header's `MUST-FIRE:`); executable controls are R29, slice S6. The plan's §5 design said fourteen; the four gates declare twenty-one, each named in its header with what must fail and why that proves the gate can fail.

**3. Fidelity** (`build/selftest_20260910T134014Z/fidelity_bbh_s2.log`, `fidelity_bbh.log`; the plain clone of bbh at `10a82d2`, R20; the clone clean after): F12+F16+F17 `104 pairs, 0 differ` (F17 52, F16 35, F12 17); F13a–e and F14/F14f 21 pairs identical; F15 not run (opt-in). The document-set kind has no ancestor to diff against (`docs/fidelity.md` "What fidelity does NOT prove"): its fidelity obligation is that bbh's verdicts did not move while it was built — F12 is that control, unchanged through steps 1–4 (the kinds loop reads the table and gives a temporal-family table bbh's loop verbatim).

**4. Every frozen expectation's provenance class** — the document-set fixture's register `fixture/docset/expected/PROVENANCE.toml`, 16 rows, written by the generator: `fixture` 15 (the 12 expectation files — 3 scenarios × `.truth` `.claims` `.covered` `.schema` — and the 3 truth logs) and `registry` 1 (`registry.tsv`); complete both ways, inside R11's vocabulary and every file git-tracked (`gates/docset_fixture.sh` runs `bbx.provenance` over the real register; `gates/provenance.sh` proves the three refusals). The suite screen over the tree's fixture (a kept `bbx-run-suite --log`, this sitting, not gated — the gate checks eight of its lines) says it in one sentence: `expectations relied upon: fixture 15 (register expected/PROVENANCE.toml; 12 files in the set); fixture 15: evidence about no real subject` and `comparator classes in this run: exact, multiset, schema; PASSed on a real pairing: none`. The frame-driven kind's example carries no register (bbh's; F19 is S6) and the screen says `none registered` for it.

**5. Every default the slice introduced, in the register** (`docs/defaults.md` D33–D44, 12 rows, each written before first use — `gates/close_sweeps.sh` fails a cited id with no row): `principled` 6 (D33 the profile, D36 chirality, D38 the two strings, D42 the NOTE keys, D43 `log_summary`, D44 `notes.tsv`); `principled (ruled)` 2 (D37 the status vocabulary R31/R33, D41 the schema types R34); `arbitrary` 2 (D35 the fixture's three sentence forms, D39 the lexical classes and record key); split 2 (D34 `scenario_ext`: arbitrary kind-blind `rpl` / principled `claims`; D40 the guards: arbitrary regexes / principled that an unlisted absence is loud). The four arbitrary halves are the fixture's vocabulary and name their detector: the first real document-set consumer (BBX-24's biased default).

**6. Comparator families per kind** (`python3 -m bbx.expectations kinds` under each profile's config; `docs/census/bbx_files.md` A3, A4): document-set **3** — `truth` exact (by index), `claims` and `covered` set (multiset both ways; shrink-only), `schema` — the design target A = 3 of `docs/generality.md` met; frame-driven **1** — `masked` and `diverge`, temporal; the kind-blind `sha1` (exact, disposition N/A) under both. B = 4 (the tolerant-numeric family) and the second consumer of `schema` are S4's.

**7. The first shared-vs-kind-specific FILE CENSUS** (`docs/census/bbx_files.md`: the method, every row, the generator; measured once, not gated). Runtime traces of what each of the 23 gates executes, in a shadow tree built from HEAD; a gate's kind derived from its trace (F: a temporal-family module or bbh's tree; D: a document-set module or driver; K: neither), never from its vocabulary. Universe: 37 tracked files under `lib/`, `bin/`, `drivers/`.

| files executed by … | count | the rows |
|---|---|---|
| gates of BOTH kinds — the abstraction, proved by two instances | **11** | `bin/bbx-run-suite`, `lib/sh/compare.sh`, `lib/sh/config.sh`, `lib/sh/expectation_kinds.sh`, `config.py`, `expectations.py`, `finding.py`, `fingerprint.py`, `logfmt.py`, `toml_subset.py`, `__init__.py` |
| frame-driven gates only (no kernel gate) | **7** | the temporal family: `compare_flicker` `compare_window` `compare_composite` `check_diverge` `propose_temporal` `thresholds`; `_mklog` (the synthetic-log helper) |
| frame-driven AND kernel gates, never a document-set gate | **5** | `bin/bbx-run-static`, `bin/bbx-run-sweep`, `tier.py`, `lib/sh/classify.sh`, `lib/sh/registry.sh` — the static and sweep runners: the document-set fixture has no gate battery of its own to run them over |
| document-set gates only (no kernel gate) | **7** | the document-set family: `docset.py`, `compare_exact` `compare_set` `compare_schema`, `drivers/docset.sh`; `bin/bbx` (the dispatcher — ONE gate, `set_schema`, through `bbx compare`); `sha1.py` (one gate, the `.sha1`-beside-authored-kinds pairing) |
| document-set AND kernel gates, never a frame-driven gate | **3** | `controls.py`, `provenance.py`, `readout.py` — `docset_suite` reads a kept run through the readout; the frame-driven gates diff printed text (F12: bbh has no kept run) and `gates/readout.sh` reads a synthetic frame-shaped run |
| kernel gates only | **4** | `bin/bbx-classify`, `close_sweeps.py`, `recount.py`, `rulings_shape.py` |
| no gate | **0** | — |

Gates by derived kind: K 13, F 6 (`compare_dispatch`, `temporal`, `thresholds`, `fidelity_bbh`, `fidelity_bbh_s2`, `suite`), D 4 (`docset_fixture`, `docset_driver`, `set_schema`, `docset_suite`), both 0. Files executed by exactly one gate: 6 (`bin/bbx`, `bin/bbx-classify`, `close_sweeps.py`, `recount.py`, `rulings_shape.py`, `sha1.py`); by 15 gates or more: 3 (`config.py`, `toml_subset.py`, `__init__.py`). Two runs of the generator at one HEAD: every gate's file set byte-identical (23 of 23, `cmp`), the analysis identical — BBX-14 met for the census (its §D). What the census reads: the 11 shared files are the contracts (the suite loop, the dispatcher library, the config layers, the kinds table, the finding vocabulary, the identity, the log format, the register format) and every one is executed by both kinds' gates — BBX-25 met for the S3 abstraction by measurement, not by the plan's list; the 7 + 7 kind-specific files are exactly the two comparator families with their driver and log helper, as the design meant. The two "+kernel" rows are the census's finding for S4: `bin/bbx` is executed by one gate, and the static and sweep runners by no document-set gate — facts about the gates' reach, stated, not verdicts.

**8. Rules re-anchored this slice, by incident, with its price** — none PROMOTED (CLAUDE.md §4 still marks all 30 `[inherited]`; edits need R16; the promotion is S5). Re-anchored in fact during S3: G20 → BBX-5 (1 gate run, shadow); G21 → §1, BBX-16 (0: the gate's first run measured it); G22 → BBX-10, §1 (1 shadow run); G23 → BBX-1 (2 close batteries, ~12 min); G24 → BBX-10 (0); G25 → BBX-5, BBX-16, R30 (1 shadow gate run, 86 s). Six incidents, four caught by a gate or the shadow before a number was read off them.

## What the slice's green does NOT assert

- Anything about a REAL document set: every expectation is `fixture` class (the suite screen's own sentence: `PASSed on a real pairing: none`); the sentence forms, guards and lexical classes are the fixture's (D35, D39, D40) and their detector is the first real consumer.
- A second consumer of the kinds loop, of the `schema` family (one format, TSV) or of the set family: BBX-25 unmet for each, stated in `gates/docset_suite.sh` and `gates/set_schema.sh`; S4's command-line kind is the second consumer by design.
- That the file census is a BBX-25 verdict: it is a measurement of what BBX's gates execute on this host, once; the proof's end is S4's third kind (`docs/plans/S3.md` §6), and the census is not yet a gate (S4 decides).
- Executable controls: every `CONTROL FIRED` is the gate's self-report (R29, S6).
- Any platform but Darwin arm64 (R21); bbh's tip `447e5d2` beyond the baseline (R28 when needed).
- The tolerant-numeric family, a JSON schema, a nested document tree, a second artifact per set (`DOCSET_VIEW` refused), the adapter contract (D7): S4 or a consumer's question (`docs/plans/S3.md` §9).
- The prose, reasoning and causal claims of any document, and the truth of the artifact itself (the driver's two `NOT-ASSERTED` lines, on the suite screen).
- The registers' TRUTH: that a `fixture` row's file is what its `describes` says, and that a default's class is right — completeness and vocabulary are checked, the classes are the generator's and the register's authors'.

## CLAUDE.md §7 — the six DONE conditions, answered one by one

| # | condition | evidence, this sitting | met? |
|---|---|---|---|
| 1 | gates green AND must-fire controls demonstrably failed on purpose, both counts in the readout | the screen: PASS 23 / 0 / 0 / 0 / 0; controls fired 78 / declared 78 (the S3 gates 21 / 21); `each can fail: 23 of 23` | yes (self-reported controls; R29 stated) |
| 2 | fidelity exact — the lineage tool and BBX over the same inputs, verdict text diffs empty, the series from F12 | F12+F16+F17 104 pairs 0 differ, F13/F14 21 identical, on the plain clone at `10a82d2`; the document-set kind has no ancestor, and bbh's loop is proven unmoved by F12 | yes |
| 3 | every frozen expectation carries a provenance class; every new default is in the register | 16 register rows, all classed, complete both ways, all tracked; D33–D44, cited nowhere before their rows (`close_sweeps`) | yes |
| 4 | the maintainer readout says what the green does not assert | the screen's `NOT-ASSERTED` block (every gate declares; 0 declaring none); the suite screen's driver lines; the section above | yes |
| 5 | rules re-anchored or still marked inherited — never silently promoted | all 30 still `[inherited]` in CLAUDE.md; six incidents re-anchor in fact (point 8); promotion is S5 | yes |
| 6 | STATE, HANDOFF, gotchas, rulings reflect it; registries complete both ways | rewritten this close; G25 filed with its price; R21 the one open ruling; MISSING 0, unregistered 0 in both runs | yes |

**"Fully validated" is a load rating (§7):** the six hold for the document-set kind AS A FIXTURE. Nothing above is claimed for a real document set, and the second non-frame kind that would make the abstraction a proof rather than one instance is S4's. The maintainer rules S3 closed or not on this table. **Ruled DONE by the maintainer, 2026-09-10** (after the bbx-12 close, on the bbx-11 table: "From what I see, S3 looks done").

## Counts, separately

| | |
|---|---|
| gates / controls (open run, `build/selftest_20260910T134014Z`) | 23 / 78 / 78 |
| the S3 gates' controls (measured; the plan's design said fourteen) | 21 in 4 gates: 3 + 7 + 5 + 6 |
| comparator families per kind (EVAL kinds) | document-set 3, frame-driven 1 |
| the fixture register | 16 rows: fixture 15, registry 1 |
| the fixture (`docset_fixture`'s NOTE) | records 12, documents 3, claims 20, wrong 1, paraphrase 1, unbindable 2, scenarios 3, expectations 12, truth logs 3 |
| the suite over the fixture (kept once, not gated: the gate freezes the 29 lines) | 12 pairings PASS; coverage 20/23, 6/7, 7/7 |
| defaults D33–D44 | 12: principled 6, principled (ruled) 2, arbitrary 2, split 2 |
| fidelity | 104 + 21 pairs identical; F15 not run |
| the file census (measured once, not gated) | 37 files; shared 11, frame-only 7 (+5 with kernel), docset-only 7 (+3 with kernel), kernel-only 4, unreached 0; gates K 13 / F 6 / D 4 |
| the census's runs | the first run's `docset_suite` red on the instrument (G25) and discarded; the gate re-run PASS 83 s; a second full run with the corrected generator: 23 PASS, every file set identical to the first (BBX-14) |
| retractions | X12 (the plan's `fixture 10`; own commit) |
| runtimes (this host, loaded; measured once) | the open battery ~9 min (sweep_runner 94 s, docset_suite 87 s, fidelity_bbh_s2 ~100 s); the shadow battery ~9 min |

## What it rests on

Two batteries in the tree (the open one and the close's two), every number above read off a kept run's own files or off a command quoted beside it; the file census off two shadow runs whose generator is reproduced in the census and whose per-gate file sets are compared byte for byte.

# CLOSE — bbx-11 (2026-09-10)

**Close measurements.** Green first, twice, kept (`build/selftest_20260910T141218Z`, `build/selftest_20260910T142107Z`): `PASS 23 SKIP 0 FAIL 0 MISSING 0` (23 gates); `controls fired 78 / declared 78`; `ok: no tracked file changed during the run` in both; BBX-14 met (23 gates, 0 verdict differences at one HEAD — the screen above). **Sweeps:** `close_sweeps` and `rulings_shape` inside both batteries, re-run once more on the final tree after these ledgers were written (the commit message quotes the two verdicts). **Lineage untouched, by construction and by proof:** bbh baseline=10a82d2 tip=447e5d2 ahead=1 porcelain=0 (clones only, clean after); VampireSaved past the census (not re-measured by design); the tips as the recount summaries printed them are in the kept runs' `census_recount.log`.

**Nothing evaporates:** every number in this sitting's section is read off a kept run under `build/` or off a command quoted beside it, except the file census, which is labelled **measured once, not gated** everywhere it appears and carries its generator and both runs in `docs/census/bbx_files.md`.

**Retractions this sitting:** X12 (the plan's §6 screen line, `fixture 10`; the plan corrected in its own commit `bf971c3`, BBX-19). **Incidents reviewed for learnings (R27, step 10):** G25 (the census instrument ended every header; paid 1 shadow gate run, 86 s; the learning — prove an instrument on the gates before reading a number off it, BBX-5; the header is one view shared by three readers, BBX-16 — and the mechanism, a file-census gate, is S4's plan). **Rulings:** none raised or answered; R21 open. **S3 is laid before the maintainer for the DONE ruling** (the §7 table above).

**What this close does NOT assert:** a real document set (every expectation is `fixture` class; the suite screen says `PASSed on a real pairing: none`); a second consumer of the kinds loop or of the schema family (BBX-25 unmet, stated in the gates' headers); that the file census is a BBX-25 verdict or a gate; bbh's tip beyond the baseline; any platform but Darwin arm64 (R21); executable controls (R29, S6); and everything the slice readout above lists.

# bbx-12 — the S4 plan: the command-line kind and the adapters, measured and STOPPED at R35–R40 while S3's DONE ruling is pending (2026-09-10)

## The screen, verbatim (`bin/bbx readout build/selftest_20260910T151653Z --against build/selftest_20260910T151030Z`)

```
== READOUT — self subject at /Users/koneko/Developer/generalized-blackbox-harness/BBX @ 13cc141 (porcelain 0) — started 2026-09-10T15:16:53Z on Darwin arm64 ==
VERDICT: GREEN   PASS 23  SKIP 0  FAIL 0  TIMEOUT 0  MISSING 0   (gates 23)
tree during the run: unchanged (untracked entries 0 -> 0)   harness: bbx @ 13cc141
rests on:
  controls: fired 78 / declared 78; dead 0; undeclared firings 0; gates red 0
  each can fail: 23 of 23 gates proved a control fires on purpose
  expectations relied upon: none registered — a static run compares against no frozen expectation; a kept suite run (bbx-run-suite --log) carries its register's histogram (D32)
  coverage: docset_driver: 20/23
  coverage: census_recount: rows=300 recountable=244 not_recountable=56 (BBX-18: the census claims no command re-derives)
  note: docset_fixture: docset-fixture records=12 documents=3 claims=20 wrong=1 paraphrase=1 unbindable=2 scenarios=3 expectations=12 truth_logs=3
  note: docset_driver: paraphrase 1
  note: docset_driver: unbindable 2
  note: docset_driver: stale 0
  note: docset_driver: mismatch 1
  note: set_schema: verdict-lines-frozen 38
  note: docset_suite: suite-runs 17 (each kept under --log)
  note: census_recount: drift census=bbh.md recorded=10a82d2 tip=447e5d26bc23 ahead=1 rows_moved=1 ids=A2
  note: census_recount: drift census=vampiresaved.md recorded=0cdd9726 tip=77f54e929962 ahead=23 rows_moved=40 ids=A21,A22,A23,A24,A25,A26,A27,A28,A32,A33,A35,A37,A45,A48,A51,A59,A62,A63,A64,A66,A67,A68,A69,A76,A77,A80,A84,A85,A86,A87,A88,A89,A90,A91,A93,A95,A111,A120,A121,A127
  note: fidelity_bbh: bbh-drift baseline=10a82d2 tip=447e5d2 ahead=1
  note: fidelity_bbh: bbh-source tip=447e5d2 porcelain=0 untouched-by-construction=clone
  note: fidelity_bbh_s2: bbh-drift baseline=10a82d2 tip=447e5d2 ahead=1
  note: fidelity_bbh_s2: bbh-source tip=447e5d2 porcelain=0 untouched-by-construction=clone
  note: suite: suite-runs 25 (each kept under --log)
  BBX-14 (more than one run): met — 23 gates, 0 verdict differences against the run started 2026-09-10T15:10:30Z at the same HEAD
  last re-baseline: 2026-09-10 (bbx-5, R28): bbh `f675710` → `10a82d2` — bbh's commit "config: the M18 re-point that sat dirty since 14z-144; the example lib's root one level too high; the extraction counts dated" (7 files; G11 fixed there; the four dirty files of R8 committed). Measured before the move by the D20 override: F12, F13, F14, F14f, F16, F17 identical on a clone of `10a82d2`. Moved with it: D20; D12's five `[sweep].placeholders` (by definition); the census `docs/census/bbh.md` (A2 19 → 20). No verdict text changed on either side.
what this green does NOT assert (declared by each gate's header):
  classify: that a verdict word printed by a gate outside the runner is read at all: the classifier reads exit status first, then the log; a PASS printed after a non-zero exit is FAIL by design
  config: the meaning of a consumer's keys: only that the layers resolve (consumer over kind profile over kind-blind default) and that dumps are stable
  tier: that a gate reaching an instrument through a path the source regex does not match is seen: the depth and the regex are the limit
  static_runner: the sweep runner or any gate that needs an instrument: this is the pre-commit chain only
  static_runner: runtimes as anything but this host under this load
  controls: that a control is RIGHT — only that a declared control fired and an undeclared one is red (docs/controls.md)
  sweep_runner: speed-up on a real consumer: the queue is measured on stub gates with sleeps, not on an instrument-tier suite
  sweep_runner: a gate that escapes its clone by an absolute path: clone-per-slot pins the cwd, nothing more
  sweep_runner: portability beyond macOS: mkfifo and exec 8<> are POSIX, not yet run on Linux or WSL
  fingerprint: the identity of any artifact that is not a single file: the kind-blind fingerprint is file-sha1 (D16)
  rulings_shape: the prose of a ruling: only its heading, its answer line and its DECISIONS row are read
  readout: that a declared blind spot is true or complete: the screen prints what the header says
  readout: the sweep runner's runs: only bbx-run-static --log and bbx-run-suite --log are read
  readout: that a register row's class is true of its file: the suite screen prints what the register says (gates/provenance.sh keeps it complete and inside the vocabulary)
  close_sweeps: that every corrected claim has a register row: the register is written by hand at the correction (a claim nobody registered is not swept)
  close_sweeps: step 9 of the close (the lineage untouched): that is the recount's clone and the fidelity gate's proof, not this gate
  temporal: anything about a real log: every shape here is synthesized (fixture class); the classes' fitness for a consumer is that consumer's ratification
  temporal: the dispatcher's spec line and the suite's dispatch (S2 steps 2 and 3): the comparators are called directly
  temporal: that the thresholds are right for any subject: they are the frame-driven profile's (D23) and a consumer's override needs R25's ruling row (gates/thresholds.sh)
  thresholds: that a ruling id in [thresholds].rulings names a ruling that exists: any non-empty string is accepted here; the consumer's rulings-shape gate is where an id is checked
  thresholds: the values' fitness for any subject: 2 / 60 / 8 are bbh's ratified policy carried in the frame-driven profile (D23), not a measurement of anything here
  compare_dispatch: the suite's dispatch around this library (which file is read, how many runs, the .sha1 and .diverge kinds' own paths): S2 step 3
  compare_dispatch: anything about a real log or a real mask: every log is synthesized and the masks are strings the guard compares, never applied
  expectation_kinds: the content of any expectation file: only its extension and its stem are read here
  expectation_kinds: the suite's use of the dispositions (S2 step 3)
  provenance: that a row's class is TRUE of its file: the register is written by hand at the freeze; only completeness and the vocabulary are checked
  provenance: bbh's example register (markdown, a consumer list of classes): it is bbh's and F19 (S6) reads it under R11
  docset_fixture: that any claim in the fixture is BOUND, MISMATCH or anything else (gates/docset_driver.sh and gates/set_schema.sh): only that the files are the generator's, the design is chiral, and the register is complete and tracked
  docset_fixture: the suite over the fixture: gates/docset_suite.sh
  docset_driver: the verdict text of the exact, set and schema comparators (S3 step 3): the truth is compared here with cmp and diff
  docset_driver: the suite over the fixture (identity, the kinds loop, the coverage NOTE on the screen): gates/docset_suite.sh
  docset_driver: prose, reasoning and causal claims in a document: only sentences in a declared form and the listed rows are claims
  docset_driver: the truth of the artifact itself: a document that agrees with a wrong artifact reads BOUND
  docset_driver: a nested document tree or a second artifact per set (DOCSET_VIEW is refused): S4 or a consumer's question
  set_schema: the suite's loop over the kinds, the kept-run rows (scenario, kind), NOT-EVALUATED on a schema FAIL and --freeze for the shrink-only kind: gates/docset_suite.sh
  set_schema: anything about a real document set: every input is the fixture's (fixture class) or a perturbed copy of it
  set_schema: the schema family on a second format or a second consumer (S4's JSON): tsv is its one format and the fixture's artifact its one consumer (BBX-25 unmet, stated)
  docset_suite: anything about a real document set: every input is the fixture's (fixture class) or a perturbed copy of it; the forms, guards and lexical classes are the fixture's (D35, D39, D40)
  docset_suite: bbh's precedence loop and its printed text: gates/fidelity_bbh_s2.sh (F12) and gates/suite.sh
  docset_suite: the readout screen beyond the lines checked here (the verdict, the pairings, the coverage and note lines, BBX-14, the driver's blind spots): gates/readout.sh
  docset_suite: a second consumer of the kinds loop (S4's command-line kind): one profile drives it here (BBX-25 unmet, stated)
  census_recount: the truth of the not-recountable rows at their commits: they are named and counted, never run
  census_recount: that a hand-read citation says what its row claims: only that the line exists
  fidelity_bbh: anything about a suite, a comparator or an expectation: slice S2
  fidelity_bbh: bbh's example's correctness (G11 is bbh's to fix)
  fidelity_bbh: F15 unless BBX_FIDELITY_F15=1 was set for the run
  fidelity_bbh_s2: the kept suite run (--log) — bbh has none, so F12 diffs printed text only; the kept run's ground truth is gates/suite.sh
  fidelity_bbh_s2: anything about a real subject: F17's and F16b's inputs are synthesized, F16a's are the fake machine's (fixture class); the shapes are the lineage's paid-for cases and nothing else
  fidelity_bbh_s2: bbh's correctness: identical output on both sides is fidelity, not truth
  suite: any driver but the fake: a MAME or FBNeo driver is bbh's and untested here (bbh's own F8 rows)
  suite: the .sha1 kind's evidence: it is `self` class by construction (E4) and the register that says so is gates/provenance.sh's
  suite: the readout's reading of a kept suite run: gates/readout.sh reads the suite screen, gates/docset_suite.sh its coverage lines
  gates declaring no blind spot: 0   (a gate nobody has asked what its green leaves out)
```

## Open

The battery GREEN on the bbx-11 HEAD `ce3191d` before anything was touched (`build/selftest_20260910T145700Z`): `PASS 23 SKIP 0 FAIL 0 MISSING 0`, `controls fired 78 / declared 78`, `tree during the run: unchanged`; the two drift NOTEs the handoff predicted (bbh `447e5d2` one past the baseline; VampireSaved 23 past the census, 40 rows moved) and nothing else moved. The bbx-11 close re-derived, as the ritual asks.

## What changed

**No tool was written.** The maintainer's instruction for the sitting was "do what you can until I can give you my ruling on S3 DONE"; what does not depend on that ruling is the next STOP, so the sitting produced **`docs/plans/S4.md`** — the command-line kind and the adapters, in the S3 plan's shape (the ancestors measured, the design contract by contract, the fixture, the controls, the defaults, the steps, what it does not decide, the rulings) — and queued **R35–R40** in `docs/rulings.md`, each with its recommendation and the declined alternatives. The plan's header says it was written while S3's ruling is pending: if S3 is ruled not done, the condition named is the next sitting's first task (BBX-26) and the plan waits. The S4 row of `docs/slices.md` points at the plan; DECISIONS.md's and STATE.md's open-rulings lines carry R35–R40 (`gates/rulings_shape.sh` caught the mismatch before the commit — `open-line-mismatch listed=R21 actual=R21,R35,…,R40` — and reads `entries=41 open=7 answered=34` after it). Committed as `13cc141` before the close batteries.

**What the plan measured** (every count with its command, in the plan's §2; the lineage read at the census commits through `git show` / `git grep`, never a working tree — R18, R20):

| ancestor | what | count |
|---|---|---|
| bbh `10a82d2` | `example/fakesys/fakesys.py` lines; its `FAKE_*` knobs; its argv options | 314; 5; 1 |
| bbh `10a82d2` | `drivers/fake.sh` lines; the refusal / sandbox / out-file removal / crash exit 2 / missing-END exit 1 | 80; lines 30 / 54 / 59 / 69 / 74 |
| bbh `10a82d2` | `bin/bbh-run-suite` lines; the `HERMETIC` scrub | 196; lines 68, 101 |
| SMS `ecc5481` | `tools/cliguard.py` lines; files naming it; self-test cases (must fire / must stay silent); unittest or pytest users | 108; 6; 5 / 7; 0 |
| VampireSaved `0cdd9726` | files under `tools/` and `tests/` naming a band; tools taking a tolerance option; the sim anchor's band; unittest / pytest / node-test users | 9; 0; `+/- 30` kept through two re-centres (`tests/test_mister_sim_anchor.sh` 58, 74, 89); 0 / 0 / 0 |
| this host | `pytest` `bats` `shellcheck` `docker` `podman`; `node` `ruby` `perl` `make`; Python | absent; present; 3.9.6 (`unittest` stdlib) |
| BBX `ce3191d` | the kinds loop; `compare.sh` families; `compare_schema.py`'s one format; the R25 mechanism; the `command` fingerprint kind; `bin/bbx`; distinct finding words | lines 118–121; 58–62; 58; 64–73; 197–201; 38 lines; 14 |

**The design in one paragraph:** the `command-line` profile (portable; the driver family `CLI_PATH CLI_NONDET CLI_TIMEOUT CLI_KEEP_ENV`); the `.cli` scenario as a TOML-subset file whose unknown key is REFUSED (SMS cliguard's rule on the scenario grammar); the observation `0 exit:<n>` first, then `line` / `field` / `err` / `file` points and a band VIEW file beside the log (BBX-16); `drivers/cli.sh` over `lib/py/bbx/cli.py` — `env -i` with D6's hermetic set, the sandbox as `HOME` and `TMPDIR`, four exits with a tool's non-zero exit an OBSERVATION and death by a signal the guard (exit 2, `END-CRASH`); the kinds `truth` (exact, its second consumer), `unordered` (set, second consumer), `schema` (json, second format), `band` (the tolerant-numeric family: an inclusive MEASURED band, `--freeze` refusing to widen without a ruling id, `NOTE: band-fields` on the screen — VampireSaved's "root-cause rather than widen" as a mechanism); the adapters `drivers/unittest.sh` and `drivers/gates.sh` over the one core (R37: the two frameworks on R3's floor); BBX's runners as subjects through `fixture/selfgates/` with the `command` identity (R14, R38); the file census lifted into a generated, `--check`ed document and a shrink-only static gate as the first sweep row, plus a portable gate on the instrument with G25 as a must-fire mode (R39). The design target for the kinds loop is ZERO changed lines (BBX-25 met for the loop by its second consumer, measured at S4's close). Eighteen controls in seven gates designed; every count in the fixture's table is a design number, measured when built.

**One inconsistency found, not corrected:** `docs/generality.md`'s kind-B row says `--crash-at n` → "exit 1, discarded" while abstraction D4 and bins B-X19 say exit 2 with the observation as the bug report. It is R35's question; the page is corrected in its own commit with a retraction row when R35 is answered (BBX-19), never before the ruling.

## Counts, separately

| | |
|---|---|
| gates / controls (open run `build/selftest_20260910T145700Z`; close runs `build/selftest_20260910T151030Z`, `build/selftest_20260910T151653Z`) | 23 / 78 / 78, three times |
| rulings queued / answered this sitting | 6 (R35–R40) / 0; open 7 (with R21) |
| documents written / tools written | 1 plan (`docs/plans/S4.md`, 474 lines) / 0 |
| retractions | none (X12 stays the last) |
| runtimes (this host, loaded; measured once) | the open battery 413 s; the close batteries 383 s and 386 s |

## What this close does NOT assert

- **S3 DONE:** that is the maintainer's ruling on the bbx-11 table, still pending; nothing in this sitting changed S3's tree, its gates or its readout.
- Anything about S4 beyond a plan: no profile, driver, comparator, fixture, adapter or census gate exists; the counts in the plan's §4 and §5 are DESIGN numbers and the fixture's chirality, the band's width and the adapters' shapes are unmeasured until built.
- That the six rulings are right: they are recommendations with their declined alternatives; the maintainer rules.
- That the lineage's numbers hold on today's tips: every count was read at the census commits (the drift NOTEs say the tips moved).
- Any platform but Darwin arm64 (R21); bbh's tip beyond the baseline (R28 when needed); executable controls (R29, S6); and everything the bbx-11 slice readout lists.

## What it rests on

Three batteries in the tree this sitting (the open one and the close's two), every number above read off a kept run's own files or off a command quoted in the plan beside it.

# CLOSE — bbx-12 (2026-09-10)

**Close measurements.** Green first, twice, kept (`build/selftest_20260910T151030Z`, `build/selftest_20260910T151653Z`): `PASS 23    SKIP 0     FAIL 0     MISSING 0` / `PASS 23    SKIP 0     FAIL 0     MISSING 0`; `controls fired 78 / declared 78` / `controls fired 78 / declared 78`; `tree during the run: unchanged` in both; BBX-14 met (met — 23 gates, 0 verdict differences against the run started 2026-09-10T15:10:30Z at the same HEAD). **Sweeps:** `close_sweeps` and `rulings_shape` inside both batteries, re-run once more on the final tree after these ledgers were written (the commit message quotes the two verdicts). **Lineage untouched, by construction and by proof:** bbh-drift baseline=10a82d2 tip=447e5d2 ahead=1; bbh-source tip=447e5d2 porcelain=0 untouched-by-construction=clone (clones only, clean after); VampireSaved past the census (not re-measured by design); the tips as the recount summaries printed them are in the kept runs' `census_recount.log`.

**Nothing evaporates:** every number in this sitting's section is read off a kept run under `build/` or off a command quoted in `docs/plans/S4.md` §2 beside it; the runtimes are labelled measured once.

**Retractions this sitting:** none. **Incidents reviewed for learnings (R27, step 10):** one event, caught before the commit — `gates/rulings_shape.sh` red on DECISIONS.md's open-rulings line after six rulings were queued (paid: one gate run, 1 s); the gate is G14's mechanism doing what it was built for, and the learning is already its rule (the line is held to the queue both ways); no gotcha filed, the question asked and answered here. **Rulings:** R35–R40 raised; none answered; R21 open. **S3 stays laid before the maintainer for the DONE ruling** (the bbx-11 table); **S4 is STOPPED at R35–R40.**

**What this close does NOT assert:** S3's DONE (pending); any S4 tool (none exists); the rightness of the six recommendations; any platform but Darwin arm64; executable controls (R29, S6); and everything the section above lists.
