# DECISIONS — rulings in force

Shape: living page; states what is ruled now. Its history twin is
`DECISIONS_HISTORY.md` (how each ruling came to be; append-only). The queue
with every recommendation and declined alternative is `docs/rulings.md`.
CLAUDE.md §8 lists the rulings as they stood at birth and is historical; the
table below is what is in force.

| id | ruling | ruled by | date |
|---|---|---|---|
| R0 | The project is **BBX — Black Box harness eXpanded**; rule prefix `BBX`; repository directory `BBX`. | maintainer | 2026-09-09 |
| R1 | Licence GPL-3, matching bbh; `LICENSE` is bbh's file byte for byte. | maintainer | 2026-09-09 |
| R2 | The generality proof's two non-frame kinds are a document set (no executable) and a command-line tool; with R14 and R15 added by the maintainer. | maintainer | 2026-09-09 |
| R3 | Floor: POSIX sh + Python 3. Fully portable and OS-agnostic: macOS, Linux, and Windows through WSL/Ubuntu treated as Linux. Every platform guard is gated on each platform; fragmentation is held by BBX validating BBX (R14) per platform. | maintainer | 2026-09-09 |
| R4 | BBX carries its own skill, generated by the H10 machinery, citing `[BBH-N]` by ID; `[BBH-` is the one lineage prefix not forbidden in it. | maintainer | 2026-09-09 |
| R5 | Nothing is excluded by kind; a subject that cannot be driven deterministically is admitted with BBX-14 reported unmet in every readout. | maintainer | 2026-09-09 |
| R6 | The repository root is `BBX/`. | maintainer | 2026-09-09 |
| R7 | Session 1 ends in one commit; no remote, no push. *("no remote, no push" superseded by R22, 2026-09-10; the one close commit per sitting stands.)* | maintainer | 2026-09-09 |
| R22 | **A GitHub remote:** `github.com/DefinitelyFrenchName/BBX` (private until ruled otherwise), `origin`; the close commit and any post-close correction are pushed. Supersedes R7's "no remote, no push". | maintainer | 2026-09-10 |
| R23 | **Comparator family and view are declared by the expectation KIND** (the registered file extension, in the kind profile's kinds table), never inside the spec line; bbh's `<class> <baseset> <args>` line is the temporal family's, verbatim; a new family is a new kind. | maintainer | 2026-09-10 |
| R25 | **A loosened threshold needs a ruling id:** a consumer value looser than its kind profile's (larger `flicker_max` / `flicker_max_total`, smaller `reconverge`) needs a `[thresholds].rulings` entry naming the key and a ruling, or every comparator, the proposer and the suite refuse to run (exit 3, one line); tighter needs nothing. | maintainer | 2026-09-10 |
| R26 | **A bare driver name resolves in a per-kind driver home** (`[suite].driver_home`): kind-blind `$BBX_HOME/drivers` (absent until S3), frame-driven `$BBX_BBH_HOME/drivers` — bbh stays the frame-driven consumer's driver home; a path from the consumer root and `--driver` win; nothing is copied out of bbh. | maintainer | 2026-09-10 |
| R24 | **The expectation register is TOML** (`PROVENANCE.toml` beside the expectation tree): one bare table per row, the file a VALUE, every field named (`file`, `describes`, `class`, `refreeze`, `notes`), the class EQUAL to one of R11's eight; complete both ways; `testimony` never green; `fixture` counted as not evidence about a real subject. | maintainer | 2026-09-10 |
| R27 | **The close reviews the sitting's incidents for learnings** (ritual step 10, before the commit): a mechanism becomes the next sitting's first small fix or a ruling, a trap becomes a HANDOFF hazard, and "no learning" is written in the gotcha so the question is seen to have been asked. | maintainer | 2026-09-10 |
| R28 | **A re-baseline is a procedure, not a ruling:** bbh's fidelity baseline follows bbh's tip when the fidelity rows pass on it, through a dated line in `docs/rebaselines.md`, D20, D12 by definition and the census re-measured — never a queue entry. Done for `f675710` → `10a82d2` on 2026-09-10. | maintainer | 2026-09-10 |
| R30 | **The gate header is the leading comment block** — every `#` line after the shebang up to the first non-comment line; a bare `#` does not end it; one reader (`lib/py/bbx/controls.py` `header_lines`). Raised from bbh's R10 work (264 of 315 gates there carry a bare `#` within five lines). | maintainer | 2026-09-10 |
| R29 | **Executable controls:** a declared control is also a MODE — the runner invokes the gate with `CONTROL=<name>`, the gate perturbs its REAL input and runs to its verdict, and the runner requires the gate's own FAIL (`CONTROL LIES` otherwise; a mode not honoured is `REFUSED`, read as dead). The R10 lines stay. Contract on the page now; built in S6; cadence per tier (portable every battery, static under release until measured). | maintainer | 2026-09-10 |
| R31 | **The observation point of a non-frame kind is an INTEGER index** into the scenario's own inventory; `logfmt` stays bbh's (one reader, F17); the token is one space-free field split in one place; the scenario file is the map from index to `(document, line, form)`. | maintainer | 2026-09-10 |
| R32 | **The scenario contract per kind:** `[suite].scenario_ext` is a kind-profile key (kind-blind `rpl`, document-set `claims`); under a profile with more than one EVAL kind every expectation file of a scenario is its own pairing, verdict line and kept-run row `(scenario, kind)`; under `frame-driven` bbh's one-kind precedence stays verbatim (F12 the control); the driver-variable family is the profile's `hermetic_unset`. | maintainer | 2026-09-10 |
| R33 | **The document-set coverage contract:** binding statuses closed to `BOUND / PARAPHRASE / UNBINDABLE / STALE / MISMATCH`; the denominator is the log's `END <n>` (bound + declared unbindable), never a ruled constant; coverage a NOTE, never fatal; the covered set frozen SHRINK-ONLY (a lost row FAILS, a duplicate FAILS, growth PASSes with a note and re-freezes); PARAPHRASE checked against the literal fact, never counted BOUND. Reworked only if experience proves it insufficient. | maintainer | 2026-09-10 |
| R34 | **The schema and set specs:** `schema … tsv` with `column <name> <type>` (`int / hex / str / nonempty`) and a `rows` line, checked BEFORE any value kind (value kinds read NOT-EVALUATED on a schema FAIL); `multiset … inventory / shrink-only`; `exact` by index; each family's own gate freezes its verdict text with a shadow-tool control. Caveat recorded: TSV breaks silently — so the frozen rows of the `claims`, `covered` and `schema` kinds are TOML-subset tables with named fields (the R24 shape), the fixture's artifact stays a TSV as the SUBJECT the schema guards, `results.tsv` (D30) the remaining TSV. | maintainer | 2026-09-10 |
| R35 | **The command-line scenario and observation contract:** the `.cli` scenario is a TOML-subset file (`args` one string per argv element, `stdin` / `stdin_file`, `cwd`, `emits`, `bands`, `fields = "json"`, an `[env]` table); an unknown key is REFUSED (exit 3); the observation is `0 exit:<n>` first, then `line` / `field` / `err` / `file` points and `END <n>`; a band field carries a constant marker in the log and its value in the `.bands` view (O4); a tool's non-zero exit is an OBSERVATION, death by a signal the guard (exit 2, `END-CRASH`, the partial output kept). `docs/generality.md`'s crash row corrected (X13). | maintainer | 2026-09-10 |
| R36 | **The tolerant-numeric contract:** the kind `band`, family `tolerant-numeric` — `[spec] class = "band", baseset, measured, rulings = { <field> = "R<n>" }` and `[b<i>] field, min, max`, the band inclusive and MEASURED on a named reference carrying R11's class; PASS inside, FAIL naming the field, the value and the band; `NOTE: band-fields <n>` on the screen (BBX-13); `--freeze` refuses to WIDEN a frozen band with no rulings entry (exit 3, the file unchanged), narrower is free. Never a tolerance option. | maintainer | 2026-09-10 |
| R37 | **The two adapter frameworks:** Python `unittest` (`drivers/unittest.sh`) and BBX's own gate battery through `bin/bbx run-static --log` (`drivers/gates.sh`), both over the one `bbx.cli` core; the framework's verdict is an OBSERVATION token, never a BBX verdict; `Ran 0 tests` is DISCARDED (exit 1, BBX-7). pytest, bats and node's runner declined (absent or off R3's floor). | maintainer | 2026-09-10 |
| R38 | **The self subject's identity:** the `command` fingerprint kind — program key the tree hash of `bin`, `lib`, `drivers` at HEAD, whole-set key the same plus `gates`; a moved key is "unregistered build fingerprint" until a REVIEWED refreeze at the close, never a step of the runner (`[BBH-67]`, §3.4). | maintainer | 2026-09-10 |
| R39 | **The file census as a gate:** `lib/py/bbx/file_census.py` and `bin/bbx file-census [--check]` regenerate `docs/census/bbx_files.md` §A/§B (generated, never hand-edited, BBX-21); a frozen kind-set per file (`expected/file_census.toml`, class `self`) compared SHRINK-ONLY — a file reached by no gate FAILs always, a lost kind FAILs naming it, a gained kind is a NOTE that re-freezes; `gates/file_census.sh` static, the first `gates/sweep.tsv` row at release cadence; `gates/file_census_tool.sh` portable, the instrument on a synthetic tree with G25's shebang insertion as a must-fire mode. | maintainer | 2026-09-10 |
| R40 | **The JSON schema format:** `[spec] class = "schema", format = "json"`, `[k<i>] name, type` with keys in SORTED order and types `int / number / str / bool / null / list / object`, `items <op> <n>` for a list; a type outside it `FAIL unknown schema type`; a nested object one `object` key (a key path is a consumer's question, NOT-ASSERTED); the TSV format and D41 unchanged. Caveat, the maintainer's words: "no caveat on R40 but a caveat on the use of TSV elsewhere" — applied: every file S4 introduces is a TOML-subset file (the `.cli` scenario, the `unordered`, `schema`, `band` expectations, `expected/file_census.toml`); the kernel's pre-existing TSVs (`results.tsv` D30, `notes.tsv` D44, `registry.tsv`, `gates/sweep.tsv`) are not multiplied, and the gates adapter reads `results.tsv` by column name only, never the printed rows. | maintainer | 2026-09-10 |
| R41 | **Two writers of the `band-fields` NOTE key, both kept:** the comparator's `NOTE: band-fields <n>` after a band PASS (R36) and the log summary's (D43) both reach the screen, and `04_band` prints the number twice by design. Measured before the ruling: on a band FAIL only the summary's line survives; over a scenario with no band the comparator never runs and the summary prints `0`; and the two counts cannot disagree under `drivers/cli.sh`, which writes the log's band tokens and the band view from one list in one run — the duplicate is one number by two routes, not a cross-check. The contributor recommended the summary alone; declined. Nothing printed changes; `gates/cli_suite.sh` freezes the duplicate as an intended screen and its NOT-ASSERTED line becomes the residual one (no check compares the two routes). | maintainer | 2026-09-11 |
| R42 | **The fixture's deliberate crash dies by SIGKILL, not `os.abort()`:** `--crash-at <n>` killed the process with SIGABRT, and macOS wrote a crash report to the maintainer's home for every one — two per battery, 25 on this host across two days, against zero before the control was built. The report escapes the sandbox the driver promises to remove and no line of the contract declared it; nothing was broken, but a real Python crash hid among deliberate ones. Measured both ways before the ruling: abort leaves one report, SIGKILL none, and the driver's crash path is identical (exit 2, the same three lines kept, the logs differing only in `CRASH signal:6:SIGABRT` against `CRASH signal:9:SIGKILL`). Signal 6's naming stays under test in `bbx.cli`'s self-test, which spawns nothing. Declined: declaring it only; keeping the abort at release cadence; deleting the reports; disabling host crash reporting. | maintainer | 2026-09-11 |
| R8 | bbh fidelity baseline is HEAD `f675710`; its 4 uncommitted modifications are a recorded finding, never touched. | maintainer | 2026-09-09 |
| R9 | The census recount gate is the first gate of slice S1 and the first self-validation gate (R14). | maintainer | 2026-09-09 |
| R10 | Must-fire controls are a machine-readable contract (abstraction G3): declared in one header grammar, a controls registry complete both ways, a dead control refuses the verdict, the readout counts fired / declared. | maintainer | 2026-09-09 |
| R11 | One closed provenance vocabulary: expectations `reference > corroborator > self > derived > hash-lock > registry > fixture > testimony`; defaults `principled / reference-calibrated / arbitrary`; one class per row, riders in a notes column. | maintainer | 2026-09-09 |
| R12 | CLAUDE.md BBX-5's citation corrected `MFI-52` → `MJC-52`, one approved edit, its own commit. | maintainer | 2026-09-09 |
| R13 | SETUP-FAIL is a reported sub-outcome of FAIL; the four verdict words stay bbh's. | maintainer | 2026-09-09 |
| R14 | **Self-validation:** BBX must be able to validate itself using itself without degrading its rules; paired with the must-fire controls and the bbh fidelity rows as the lineage-independent custodians (BBX-15). | maintainer | 2026-09-09 |
| R15 | **Adapters:** external testing tools and frameworks can be interfaced or integrated as drivers under the four-argument contract (abstraction D7), so any deterministic test suite can put BBX in front of it as a guardrail. | maintainer | 2026-09-09 |
| R16 | **Any edit to `CLAUDE.md` requires maintainer approval** before it is made; a contributor files the proposed wording in `docs/rulings.md` and waits. | maintainer | 2026-09-09 |
| R17 | **The session ritual** (`HANDOFF.md`): open by HANDOFF → STATE → rulings and the battery run alone; close by green-first, nothing-evaporates, the readout section, STATE rolled into `STATE_HISTORY.md` under the key, HANDOFF rewritten, decisions and gotchas filed, the retraction / deferral / defaults / controls sweeps shown, the lineage checked untouched, one close commit. Sessions keyed `bbx-N`, one per sitting, never renamed. Adapted from VampireSaved VSP-17/18/162; NEXT_SESSION folded into HANDOFF, the three-group STATE window replaced by the twin's paragraphs. | maintainer | 2026-09-09 |
| R18 | **Safety of the measured tree:** the census and the tests work on a clone of the recorded commit or, where a clone is impossible, declare explicitly that the tree must not be changed and prove it. Clones are not mandatory; they are safe and safety is valued; any equivalent implementation is acceptable. A lineage's movement is reported, never fatal to BBX. | maintainer | 2026-09-09 |
| R19 | **Parallel work is a pull queue:** N workers pull from a queue, each on its own pinned tree (clone per slot), batching is the fallback never the design. The FIFO token queue (VampireSaved 14z-144) is BBX's default implementation and recommendation; the principle is the ruling, the implementation is replaceable. | maintainer | 2026-09-09 |
| R20 | **Fidelity runs on a plain clone of bbh at `f675710`** under `TMPDIR`, never in bbh's working tree; the recount's clone is plain too (no `alternates` dependence on a live object store). Measured before ruling: identical F13–F15 output, +0.2 s, under 3 MB. | maintainer | 2026-09-09 |
| M4 | A default true of one subject kind lives in that kind's profile, never in DEFAULTS (config layers: consumer → kind profile → kind-blind). | contributor (method) | 2026-09-09 |
| M1 | Every lineage rule is binned individually; a `drop` row with no reason is an error. | contributor (method) | 2026-09-09 |
| M2 | A census row's provenance is `gen:<generator>` or `read`; every count has its command; a filed count is labelled `filed:` with its source. | contributor (method) | 2026-09-09 |
| M3 | Every load-bearing census number is produced by one agent and re-derived by a different one; unresolved mismatches are reported, never averaged. | contributor (method), from FIRST_PROMPT.md | 2026-09-09 |

Open rulings: R21 (how the platform runs are obtained; the queue explains) and R43 (which bbh commit `gates/suite.sh` clones, and where the one baseline default lives — raised bbx-18 from G32), and R44 (what the self subject's moving identity costs at every close, and whether the whole-set key keeps `gates`; raised bbx-18), and R45 (how the orphan direction of the registry rule gets a verdict when the runner that reports it is the lineage's; raised bbx-18), and R46 (where the harness's own identity key is computed, now that two writers of it exist and a gate cross-checks them; raised bbx-19 from G42), and R47 (what detects a harness file no gate executes BETWEEN releases, now that the file census is release-scoped; raised bbx-19 from the census cadence ruled for it). New ones are raised in `docs/rulings.md` under `## Open` and moved under an `## Answered` heading when answered (`gates/rulings_shape.sh`).
