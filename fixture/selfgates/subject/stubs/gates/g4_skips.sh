#!/bin/sh
# g4_skips.sh — a STUB whose input is absent: it prints a SKIP marker and exits 0, and SKIP IS NOT PASS
# (BBX-1). Written by mkselfgates.py from the design.
echo "SKIP: the designed absent input (asserts nothing)"
exit 0
